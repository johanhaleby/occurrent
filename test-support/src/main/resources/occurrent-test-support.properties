# Filtered at build time from integration-tests.mongo.version in the root pom, so the fallback in
# ReplicaSetReadyMongoDBContainer cannot drift from the version the rest of the build tests against.
mongo.version=${integration-tests.mongo.version}
