schema_version: 2
epic: cdx33d
revision: 12
updated_at: 2026-08-15 12:05:00+00:00
updated_by: 'orchestrator: EPIC CLOSED as CAPPED-WITH-REMEDIATION, 5 of 5 units DONE and swept, both watchers RETIRED,
  heartbeat retired at this closeout, no unresolved pending actions. The single fixpoint round (cap 1) returned 3 prose majors
  at the cap, all fixed as D4 under Johan''s ruling with no further round; the library code verified clean on every error
  and empty branch. Verification of record: main 05763ca is 24/24 green (and 5f753a0 27/27 before it). Held docs heads for
  release day: preview dcab6d3, fencing b571eb6, release-notes 62bed8c. The 0.33.0 tag and every docs merge stay Johan''s
  manual acts'
baseline:
  repo: johanhaleby/occurrent
  branch: main
authority: 'Meta-plan approved by Johan 2026-08-15 (ExitPlanMode). Rulings by AskUserQuestion the same morning: fixpoint cap
  1 (budget; stopping rule carried forward: clean = convergence, above-minor = CAPPED routed to Johan and reported as capped,
  after clean or cap only a confirmed BLOCKER reopens, rest is 0.34); D1 vehicle subagent; fencing Cluster section narrow
  fix now with the wholesale port staying Johan''s release-day call. Finding-1 severity is principle-settled as BLOCKER (AGENTS.md
  no-severity-ladder rule plus safety-work-finishes-in-its-release; derivation in the plan), ratified by plan approval. The
  0.33.0 tag stays HELD; the tag and every docs-repo merge are Johan''s manual acts. Mode Attended (Johan messaging 2026-08-15
  morning). Standing grants per .context/orchestrator-policy.yml. Conventions: invariant-first briefs, adversarial pass per
  correctness unit, CAS merges with marker-stripped squash bodies, suppressed blocks read at every head, CI-over-local-suites,
  [ci skip] only for genuinely prose-only diffs, foreground waits only, johan-writing gates on every prose surface, asserted
  anchors on scripted memory edits'
pending_actions:
- id: act-001
  type: workflow-dispatch
  target: 'D1 planning subagent (Opus/high, read-only, plan_approval_venue orchestrator): reactor registration-read refusal
    design per the meta-plan''s five settle-points (failed-vs-empty distinction, capture paths, test rewrite + regression
    shape, ripple sweep, blocking parity both directions)'
  expected: 'a unit plan handed back; gate through the orchestrator with Johan ruling taste questions while present; implementation
    dispatched separately after the gate. RESOLVED: 2026-08-15: registration-grade plan returned, correcting three brief premises
    (falsified claim is changelog:96, blocker confined to the cold-primitive path for out-of-tree wrapped models, stopped
    path cannot throw synchronously under the monitor) and finding the empty-capture silent twin on both stacks plus the third
    held branch docs/0.33.0-release-notes missing the refusal paragraph. Gate: Q1 ruled by Johan (empty IN SCOPE, both stacks''
    pin paths), Q2 raw propagation and Q3 handle-carries-failure decided by derivation, changelog:27 flag refuted by direct
    read (already names StartPositionAlreadyPinnedException). Implementation dispatched as the same agent resumed'
  status: VERIFIED
  attempts: 1
  safe_to_repeat: true
  issued_at: '2026-08-15T05:25:00Z'
- id: act-002
  type: workflow-dispatch
  target: 'D2 subagent (Sonnet/high): changelog.md:38 delete-safety proof completion and :67/:93/:106 IntrospectableSubscriptions
    rename, one PR off d05b3d9'
  expected: 'PR delivered with DELIVERY_RESULT; orchestrator verifies and merges under standing authority. RESOLVED: 2026-08-15:
    PR 794 delivered and merged as f073e39 (CAS ee49921, marker-free squash, Copilot clean, 0 threads). Deviation ratified
    on orchestrator-verified evidence: lines 67/93/106 sit in the released 0.32.0 section and the occurrent-0.32.0 tag shipped
    IntrospectableSubscriptionModel, so those entries are historically accurate and Codex finding 6''s unreleased-entries
    premise was wrong; only the delete-safety proof was fixed. Decline recorded for CREG'
  status: VERIFIED
  attempts: 1
  safe_to_repeat: true
  issued_at: '2026-08-15T05:25:00Z'
- id: act-004
  type: workflow-dispatch
  target: 'D1 implementation (same agent resumed, context preserved, Opus/high): B1/B2/B3 on the reactor cold path, empty-capture
    refusal on both stacks per Q1, raw propagation per Q2, handle carries failure per Q3, tests X1-X10 plus blocking twins
    with mutations, ripple incl. changelog:96 and :27 in-place amendments, ADR 89 paragraph, three held docs branches incl.
    0.33.0-release-notes refusal-paragraph catch-up'
  expected: 'PR delivered to done off f073e39 with DELIVERY_RESULT pinned to the exact head, docs branches pushed; orchestrator
    runs the adversarial pass, then merges. REDISPATCHED: the resumed planning agent could not obtain a writable tree (classifier
    denies git worktree add in its session; the cross-worktree edit hook blocks edits outside its pinned cwd; EnterWorktree/ExitWorktree
    refuse from a cwd-pinned subagent) and twice STOPPED rather than work around, incl. refusing the orchestrator-created
    worktree as permission laundering, the right failure mode both times. Implementation respawned 2026-08-15 as a fresh
    isolation-worktree Opus agent with the complete ratified plan inlined and the base pinned to f073e39 by explicit fetch-and-checkout.
    RESOLVED: 2026-08-15: delivered to done at 9305dd2 (28/28 green, five Copilot rounds with suppressed blocks read, 13
    mutations killed, two named unkillables) with five flagged deviations incl. four refusal exemptions added during review;
    all three docs branches pushed (preview 36335b3, fencing 27ae3bf, release-notes fde6a82, heads verified by ls-remote).
    Merge waits on the adversarial pass'
  status: VERIFIED
  attempts: 3
  safe_to_repeat: true
  issued_at: '2026-08-15T06:20:00Z'
- id: act-005
  type: workflow-dispatch
  target: 'D1 adversarial pass on final head 9305dd2 (Opus, fresh context): attack the four refusal exemptions for constructible
    event loss, the removed resolveStartAt fallback, concurrency interleavings, the blocking exists-read timing, prose truth
    on every changed surface incl. the three docs refs, tests-as-claims'
  expected: 'verdict per angle; merge waits on it (ADR 89 paragraph is an amendment extension, no new decision, so no separate
    wording confirm owed unless the pass raises one). RESOLVED: 2026-08-15: verdict 1 CONFIRMED MAJOR (the delegating path,
    the only one in-tree reactor models take, still loses events on an empty read for a stopped registration via the StartAt.now
    fallback at resolveStartAt:388, and the new prose ratifies the hole) + 4 minors (StartAt.now justification false, exemption-d
    absent from user prose, three-vs-two left-alone contradiction, glued markdown paragraph on two refs); the four cold-path
    exemptions, concurrency angles, blocking exists-read timing and the removed fallback all verified clean. Fix round routed
    to the implementer with the empty==error-everywhere derivation from Q1 (error already refuses on those paths, blocking
    already refuses null unconditionally so parity was broken, and only unconditional refusal removes the check-then-park
    race)'
  status: VERIFIED
  attempts: 1
  safe_to_repeat: true
  issued_at: '2026-08-15T08:15:00Z'
- id: act-006
  type: workflow-dispatch
  target: 'Fixpoint round, THE ONLY ONE (cap 1): fresh-context Opus review of the complete cdx33d increment (library d05b3d9..5f753a0,
    three docs branch deltas) with error-path enumeration against release-contract absolutes, the four exemptions, the empty-save
    addition, and whole-0.33.0-state cross-surface consistency'
  expected: 'CLEAN (nothing above minor) = convergence; any above-minor = CAPPED, loop ends, remainder routed to Johan, reported
    as capped never convergence. RESOLVED: 2026-08-15: verdict 3 CONFIRMED MAJOR, all prose (the unscoped no-loss absolute
    contradicted by the StartAt.now exemption the same delta added, on all three docs refs plus changelog:98; the release-notes
    branch missing the one-arg stoppedByDefault correction and the pin-scoping correction the other branches carry) + 5 minors;
    library code verified CLEAN on every error and empty branch, main matrix 27/27 on 5f753a0. CAP REACHED, loop ended. Johan
    ruled fix-3-majors-plus-2-doc-minors-close-capped by AskUserQuestion; remediation runs as D4 with NO further round, the
    three code-adjacent minors banked in the register as 0.34 notes'
  status: VERIFIED
  attempts: 1
  safe_to_repeat: true
  issued_at: '2026-08-15T10:15:00Z'
- id: act-003
  type: workflow-dispatch
  target: 'D3 subagent (Sonnet/high): findings 2-4 plus docs halves of 5-6 on both held docs branches (preview 9b7c378, fencing
    13e128f), scratchpad worktrees, jekyll build, origin-ref verification'
  expected: 'both branches pushed with DELIVERY_RESULT; orchestrator verifies heads and greps on origin refs. RESOLVED: 2026-08-15:
    delivered and verified, preview c7894e3, fencing 04153cb, all five fixes present, forbidden phrases gone from 0.33 surfaces'
  status: VERIFIED
  attempts: 1
  safe_to_repeat: true
  issued_at: '2026-08-15T05:25:00Z'
units:
  D1:
    issue: null
    goal: 'BLOCKER fix (sweep finding 1): a reactor registration whose globalCheckpoint() read fails must refuse visibly
      instead of falling forward to a later read at start (capturePositionNow swallow + resolveStartAt switchIfEmpty substitution
      skips the events between registration and start; the stopped-registration test locks the loss in as intended). Invariant:
      the position pinned at start is the position captured at registration, or registration/start fails visibly; no path
      pins a later read; a legitimately-empty capture keeps its documented meaning. Blocking stack is the settled reference
      shape (forget-and-rethrow)'
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: []
    session: null
    deliverables:
    - check: pr_merged
      repo: johanhaleby/occurrent
      ref: '795'
      met: true
      evidence: 'MERGED 2026-08-15 as squash 5f753a0, CAS-pinned to 1886950 (28/28 by job conclusion verified, final Copilot
        round of nine clean incl. suppressed blocks, MERGEABLE/CLEAN, three accepted-fixed threads resolved, one reasoned-decline
        thread open per policy, squash body marker-free). Adversarial pass round: 1 MAJOR (delegating-path empty read still
        lost events, prose ratified it) + 4 minors, ALL fixed in the addendum under the empty==error derivation from Q1;
        17/17 mutations killed, two named review-only unkillables. Docs ripple pushed and verified on origin refs: preview
        3cba4f2, fencing 89e8def, release-notes d0e9b01 (the last also gaining the previously-missing pin-refusal coverage).
        Three judgment calls ratified: empty-save refusal (closes the last silent-now route), no ERROR log where the exception
        reaches the caller, the number-771-window handle report declined as out of scope. Main matrix on 5f753a0 watched'
  D2:
    issue: null
    goal: 'Library release prose (findings 5 and 6, library halves): changelog.md:38 delete-safety proof completed with the
      forStandalone-to-Cluster migration case, and the stale IntrospectableSubscriptionModel name at :67/:93/:106 renamed
      to IntrospectableSubscriptions'
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: []
    session: null
    deliverables:
    - check: pr_merged
      repo: johanhaleby/occurrent
      ref: '794'
      met: true
      evidence: 'MERGED 2026-08-15 as squash f073e39, CAS-pinned to ee49921 (md-only diff, no checks by paths-ignore design,
        Copilot review clean with no suppressed block, 0 unresolved threads, squash body marker-free). Fix 1 verified against
        the javadoc and migration guide; fix 2 correctly declined as historically-accurate released-section entries'
  D3:
    issue: null
    goal: 'Held docs branches (findings 2, 3 narrow, 4, and the docs halves of 5 and 6): news behave-exactly-as-before join
      claim, fencing Cluster-cannot-fence section replaced with the shipped hash-tag design, fencing one-argument stoppedByDefault
      guarantee corrected from the preview wording, preview delete-safety proof completed, preview triple-duplicated introspection
      paragraph deduplicated with the current type name'
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: []
    session: null
    deliverables:
    - check: branch_pushed
      repo: occurrent-org/occurrent-org.github.io
      ref: docs/0.33.0-preview
      met: true
      evidence: 'Pushed 9b7c378 -> c7894e3, verified on the origin ref: join claim corrected at news:36 (narrowed step window
        stated), delete-safety proof completed at docs.md:3623 with the forStandalone migration case, introspection paragraph
        deduplicated at :7366 with the current IntrospectableSubscriptions name. The one remaining behave-exactly-as-before
        string is the 0.30 news post''s correct historical claim about stream-only stores, out of scope. Jekyll exit 0 per
        worker, gates run on diffs'
    - check: branch_pushed
      repo: occurrent-org/occurrent-org.github.io
      ref: docs/checkpoint-fencing
      met: true
      evidence: 'Pushed 13e128f -> 04153cb, verified on the origin ref: Cluster section replaced (supports-the-fence with
        hash-tag design, per-id qualification folded into the adjacent paragraph per the narrow-fix ruling, flagged adaptation
        ratified), one-argument stoppedByDefault guarantee corrected from preview''s wording. Fencing news post checked, no
        join claim present'
  D4:
    issue: null
    goal: 'Capped-close remediation, ruled by Johan: scope the no-loss absolute to model-default registrations (changelog:98
      plus the guarantee paragraphs on all three docs refs), port the two blocking corrections the release-notes branch never
      received (one-arg stoppedByDefault guarantee, unconditional pin claim), extend migration-guide section 8 to the new
      IllegalStateException refusals, describe the empty-save refusal in the class javadoc and docs. No further review round;
      fixes verified mechanically against the named falsifying facts'
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: []
    session: null
    deliverables:
    - check: pr_merged
      repo: johanhaleby/occurrent
      ref: '796'
      met: true
      evidence: 'MERGED 2026-08-15 as squash 05763ca, CAS-pinned to a150daa (final Copilot round of three clean incl. no
        suppressed block, matrix green on the code-bearing head by job conclusions, marker-free squash, the one open thread
        replied-and-resolved by the orchestrator). All five ruled fixes verified; the empty-save docs mention correctly SKIPPED
        because the docs reactive CheckpointStorage section still shows the pre-0.33 two-argument save, verified by the orchestrator
        and recorded as a release-day checklist item rather than silently patched around'
    - check: branch_pushed
      repo: occurrent-org/occurrent-org.github.io
      ref: docs/0.33.0-preview
      met: true
      evidence: 'Pushed 3cba4f2 -> dcab6d3, F1 scoping verified on the origin ref (old absolute gone)'
    - check: branch_pushed
      repo: occurrent-org/occurrent-org.github.io
      ref: docs/checkpoint-fencing
      met: true
      evidence: 'Pushed 89e8def -> b571eb6, F1 scoping verified on the origin ref'
    - check: branch_pushed
      repo: occurrent-org/occurrent-org.github.io
      ref: docs/0.33.0-release-notes
      met: true
      evidence: 'Pushed d0e9b01 -> 62bed8c, F1 scoping plus the two ported blocking corrections (F2 one-arg guarantee, F3
        pin scoping) verified on the origin ref'
  CREG:
    issue: null
    goal: 'Register and tracker dispositions: the blocker recorded with the reviewer rank, orchestrator verification and
      principle derivation; routing markers where tracker items exist or are created; the fencing wholesale-port decision
      recorded unchanged; ORCHESTRATOR.md register updated'
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on:
    - D1
    - D2
    - D3
    session: null
    deliverables:
    - check: branch_pushed
      repo: johanhaleby/occurrent
      ref: null
      met: true
      evidence: 'DONE 2026-08-15: tracker scan from the 00:00Z safety bound returned only the three fleet PRs, no third-party
        arrivals, no new issues to route. Dispositions recorded in ORCHESTRATOR.md register: the blocker with reviewer rank,
        orchestrator verification and the principle derivation; finding-6 rename half declined on the released-0.32.0-section
        evidence; the number-771-window decline on the PR 795 thread; the three code-adjacent fixpoint minors banked as 0.34
        register notes; the fencing wholesale-port decision unchanged; the stale reactive CheckpointStorage docs section added
        to the release-day checklist'
watchers:
- id: watcher-001
  kind: github-work-item-monitor
  repository: johanhaleby/occurrent
  external_id: bj71didql
  status: RETIRED
  retirement:
    kind: no_unmet_pr_advanceable_deliverables
  last_verified_at: '2026-08-15T05:25:00Z'
  last_verified_revision: 1
- id: watcher-002
  kind: github-work-item-monitor
  repository: johanhaleby/occurrent
  external_id: b2r13r30w
  status: RETIRED
  retirement:
    kind: no_unmet_pr_advanceable_deliverables
  last_verified_at: '2026-08-15T10:55:00Z'
  last_verified_revision: 10
