schema_version: 1
epic: cdx33
revision: 54
updated_at: 2026-08-11T19:56:20Z
updated_by: "orchestrator session faee3625 (U12 MERGED as 743391bc6, all 15 library units done; UDOCS2 dispatched as the last unit)"
baseline: {repo: johanhaleby/occurrent, branch: main}
authority: "REOPENED 2026-08-11 after closeout. The tag is HELD again for U11 and U12. standing grants per .context/orchestrator-policy.yml (library repo only; docs-repo merges are Johan's). Design gates: U1 and U3 plan-first, UADR closed. Release gate now: U1 (739), U2 done, U3, U5 done, U6, U8 done, UDOCS. Finding 19 is MOOT because PR 739 deletes the file U9 fixed, so U9 merged work is deliberately erased. The PR 728 hold is spent: Option E merged as 6743d3e6c3c14a962e85e06e90396acfd684456f"
pending_actions:
  - id: act-001
    type: dispatch-chip
    target: "Wave A chips U1/U2/U3/U5/U8/U9/U10 (task ids in updated_by)"
    expected: "chips visible to Johan; sessions start on his click; spawn-result pending-lists suggest U1/U2/U3/U5 already started"
    status: VERIFIED
    attempts: 1
    safe_to_repeat: false
    issued_at: 2026-08-11T09:13:22Z
  - id: act-002
    type: dispatch-chip
    target: "U7 and UADR dispatched as in-session worktree subagents, not chips (nearest allowed type; ids in updated_by)"
    expected: "each delivers a PR and a DELIVERY_RESULT completion notification"
    status: VERIFIED
    attempts: 1
    safe_to_repeat: false
    issued_at: 2026-08-11T09:13:22Z
  - id: act-003
    type: dispatch-chip
    target: "U13 dispatched as an in-session subagent (nearest allowed type; agent id in updated_by)"
    expected: "one PR correcting FlowStateImpl's retention bound, the changelog's sealed-type entry and migration section 10, plus a DELIVERY_RESULT"
    status: PENDING
    attempts: 1
    safe_to_repeat: false
    issued_at: 2026-08-11T19:40:00Z
units:
  U13:
    issue: null
    goal: "Documentation truth for two defects in this epic's own output: #754 (FlowStateImpl.java:52 still states the superseded retention bound historyWindow + stepWindow + 1) and #755 (the reopened-hierarchy startup refusal now applies to subscriptions as well as sagas, which is a breaking change against 0.32.0 that the changelog denies and migration section 10 frames as saga-only)"
    phase: RUNNING
    health: PROGRESSING
    swept: false
    depends_on: []
    session: null
    deliverables:
      - {check: pr_merged, repo: johanhaleby/occurrent, ref: null, met: false, evidence: null}
  U1:
    issue: null
    goal: "Applied-position ordering (finding 1, blocker): direction decided at its plan gate (document early-return window / refuse recording for async Mongo projections / committed-prefix design), docs-truth floor either way, ADR 0111 amendment"
    phase: DONE
    health: PROGRESSING
    swept: false
    depends_on: []
    session: local_4640fd9a-5acf-4678-863e-c4b79e9e7460
    deliverables:
      - {check: pr_merged, repo: johanhaleby/occurrent, ref: "739", met: true, evidence: "MERGED as squash 0f3980c20a42ad3389ebc6e14a1962120e79eede, CAS-pinned to 355f198fa8b351a5b4b05c872781584d65b37f6f. Matrix completed/success on that exact head by run list. 3 of 5 threads resolved, 2 left as reasoned declines the policy permits, both correctly scoped out (docs repo, and ORCHESTRATOR.md which is the orchestrator file and on the worker must-not-touch list) with concrete replies naming the owners. Verified on origin/main: no reference to the withdrawn API anywhere in dsl, framework, subscription or eventstore; ADR 0122 present; ADR 0111 carries a withdrawal blockquote AFTER its original Status text rather than a silent rewrite; all three sibling changelog bullets (U2, U7, U10) survived and both applied-position entries are gone. Final diff 26 files, 176 insertions, 2572 deletions, 14 files deleted. Branch deleted on merge, no stranded push"}
      - {check: manual_approval, ref: "U1 plan gate (D3 direction), Johan", met: true, evidence: "SATISFIED 2026-08-11T10:38:40Z: Johan approved /Users/johan/.claude/plans/whimsical-cooking-hopper.md after four planning rounds plus a requested second opinion. Direction: WITHDRAW the applied-position feature from 0.33.0 (13 files deleted, 10 unwired), new ADR with the completed-prefix refutation and verified Axon/Marten/Emmett prior art, ADR 0111 withdrawn via amendment blockquotes, and an epic-grade 0.34.0 issue for the append-identity design with its three named open points. D3 resolved as none-of-the-above: withdraw-then-redesign"}
    blocking_on: []
    next_action: sweep the worktree, branch and session
    last_meaningful_progress_at: 2026-08-11T12:09:39Z
    observed: {}
    assessment: "Bigger than the meta-plan contingency and cleaner, nothing unsound ships at all. Grounds: no shipped configuration was sound (change streams and push feeds violate the position-order precondition, the in-memory store publishes outside its lock), WriteResult carries no position so the documented scenario was unreachable from the stream API, and AGENTS.md rules against docs-only for a correctness gap and against unsettled public abstractions. Finding 19 becomes moot, U9 work knowingly erased. #361 linkage and docs PR 52 retargeting via worker DELIVERY_RESULT plus UDOCS"
  U2:
    issue: null
    goal: "Manual-start pin refusal (finding 12, blocker): compare stored vs captured on ifAbsent refusal, fail loud on difference, later-registrant-starts-first test, check manual-start ADR amendment need"
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: []
    session: local_cff3efc3-ade2-406e-8aac-a3f25e144bce
    deliverables:
      - {check: pr_merged, repo: johanhaleby/occurrent, ref: "728", met: true, evidence: "MERGED as squash 6743d3e6c3c14a962e85e06e90396acfd684456f, CAS-pinned to 1de6225f6db43ed524510ae82dbe86d300608fa9 with the post-merge head equal to the pin. Matrix verified by RUN LIST as completed/success on that exact head as a push event, 27 of 27, 0 pending, 0 unresolved threads, Copilot reviewed that head with no new findings. Verified on origin/main: the pin call sits inside subscribe at line 166, the diagnostic is Level.INFO not WARNING, and both decisive tests are present (an_earlier_registrant_always_wins_the_pin_even_when_a_later_registrant_starts_first and a_subscription_registered_on_a_running_model_still_pins_its_registration_position_before_starting_live). Branch deleted on merge, no stranded push, worktree clean at the pin and swept"}
    blocking_on: []
    next_action: none, merged and swept
    last_meaningful_progress_at: 2026-08-11T11:43:07Z
    observed: {}
    assessment: "OPTION E APPROVED 2026-08-11T11:08:42Z. Pin moves from start to REGISTRATION so the earliest registrant wins and every refusal is safe to accept (worst case a duplicate, never a skip, and at-least-once with dedup by event id is the contract). Reduces the reversal window from routine (capture-to-write spans until someone starts, minutes to hours) to pathological (milliseconds against a registration gap of minutes). Two worker findings reshaped it: the class public javadoc has promised pin-at-registration since 0.32.0 while the code did the opposite (ADR 86 Decision text matched the code), and the reactor twin at ReactorDurableSubscriptionModel:298-299 still carries the same read-then-save race #669 fixed here, filed as #738 leave-for-later. Orchestrator additions: scope or prove the new live-path write (today the live path never pins), and the ADR 86 amendment must record that pinning at registration RESTORES what deferring subscribe displaced from DurableSubscriptionModel, which is the real argument. Costs accepted: a new Changes bullet for 0.32.0-released behavior, no recipe, deliberate reactor divergence, INFO-level diagnostic on a differing accepted position"
  U3:
    issue: null
    goal: "StepCondition semantics AND evaluation (findings 2, 3 in full per D4, 4, ADR 0120 drift): fired-window reactions with initiating() reachable, compiled per-step matcher state, determinism contract, duplicate-children rule. Hard constraint: reads 0.32.0-written saga documents (seed-by-hand proof)"
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: []
    session: "subagent a83373d1dceeb9f7a on branch cdx33/u3b-stepcondition (chip session local_2e5a90d0 ABANDONED, prompt-blocked)"
    deliverables:
      - {check: pr_merged, repo: johanhaleby/occurrent, ref: "742", met: true, evidence: "MERGED as squash dc6faa6a7be9d459570d8a3215cbb36b400feb0a, CAS-pinned to 4f25ba6226a0ab6b71e1dfbc65932eac23c4fab4. Matrix completed/success on that exact head by RUN LIST, 26 of 27 checks green with the last shard (mongodb-native java-21) still running and its java-25 sibling passed, merged under the recorded pending_matrix_jobs allow_one_equivalent_sibling grant. 0 unresolved threads, both Copilot findings fixed rather than declined. Verified on origin/main with absolute grep: loweredFromJoin present (3 hits), the join regression test present, previousStepEntryIndex persisted (6 hits), and the 195-line SpringMongoSagaStateStoreFlowConditionRoundTripTest present. Branch deleted on merge, no stranded push, worktree clean at the pin and swept"}
      - {check: manual_approval, ref: "U3 plan gate, Johan", met: true, evidence: "SATISFIED 2026-08-11T12:06:55Z via structured ask in the orchestrator session rather than in U3 own session, since the gate had been parked over two hours. Three rulings: (1) finding 3 counters DEFERRED and paired with the retention bound as a pre-tag follow-up, accepting the worker argument against D4 as literally written, so this PR lands findings 2 and 4 plus the determinism contract, the ADR 0120 corrections and the retention documentation; (2) finding 4 rejects a shared EventMatcher or equal child in allOf, which also catches allOf(event(A,2), event(A,3)), with anyOf left permissive and that asymmetry recorded in the ADR; (3) the orchestrator granted the SpringMongoSagaStateStore edit, which is a file-ownership call rather than Johan, with the hand-seeded pre-0.33.0 document test required per the #724 precedent"}
    blocking_on: []
    next_action: none, merged and swept; #741 carries the deferred counters-plus-retention follow-up
    last_meaningful_progress_at: 2026-08-11T15:33:45Z
    observed: {}
    assessment: "REPLACED 2026-08-11. The original chip session parked on its own AskUserQuestion and could never receive the answers: a session blocked on an interactive prompt has an in-flight turn only a human in that session can finish, so the relayed ruling queued behind it forever, and the tool result had said queued rather than sent. Salvage: its revision-2 plan survived on disk at the session scratchpad (PLAN-u3.md, written after a fresh-context review that broke its own counter design), so the replacement subagent got the plan path plus the three rulings instead of a fresh investigation. Deliberately on branch u3b so the abandoned session cannot collide if anyone later answers its prompt. Scope per the rulings: findings 2 and 4 plus the determinism contract and ADR 0120 corrections; counters deferred"
  U4:
    issue: null
    goal: "Kotlin StepCondition variance (finding 10): on(condition) takes out E, compile test"
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: [U3]
    session: "worktree subagent ae3fe6d4ee9905efe"
    deliverables:
      - {check: pr_merged, repo: johanhaleby/occurrent, ref: "744", met: true, evidence: "MERGED as squash 408187c763f8d42a50a825cd0d342af5211128a0, CAS-pinned to af9e1506834f49f2bc6680ecdf098c0df5cf09d9. Matrix completed/success on that exact head by run list, 26 of 26, 0 pending, 0 unresolved threads, and Copilot reviewed that head with NO findings. Verified on origin/main: StepCondition<out E> present and the new 56-line StepConditionKotlinVarianceTest.kt present. The worker proved its compile test real by reverting the out projection, confirming the exact expected compile error, then restoring from a saved copy rather than git restore. It also confirmed there is exactly ONE StepCondition-taking on overload, correcting the review prose which implied two. No changelog clause owed: the existing entry says Kotlin gets matching on(condition, then) sugar, which this fix makes more true rather than false, so the deliberate skip was right and needed no reconciliation after 742 merged. Branch deleted, no stranded push, worktree clean at the pin and swept"}
    blocking_on: []
    next_action: none, merged and swept
    last_meaningful_progress_at: 2026-08-11T15:40:49Z
    observed: {}
    assessment: "Dependency cleared by evidence rather than by waiting: PR 742 touches 15 files in dsl/saga-dsl and NONE of them is SagaFlowExtensions.kt, so U4 can run in parallel. Two collision guards in the brief: its compile test goes in a NEW Kotlin test file because 742 is editing DocumentedFlowSagaKotlinTest.kt, and it adds no changelog clause if that would collide with 742 edit of the same unreleased entry, leaving the orchestrator to reconcile after both merge. Adding out to a type parameter position is source-compatible, so no migration machinery is owed"
  U5:
    issue: null
    goal: "Migration tooling truth (findings 8, 11, 9-guide-half): recipe stubs delegate any() and reject stronger, empty writeVersion, TimerName effect-record constructor matchers, guide corrections, runtime-behavior test via DurableSubscriptionModel"
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: []
    session: local_d940740e-389c-4b2e-8c63-7529654a31a6
    deliverables:
      - {check: pr_merged, repo: johanhaleby/occurrent, ref: "731", met: true, evidence: "MERGED as squash cb6f012db2cd225906ea386098d337aa0d66893b, CAS-pinned to 4bd873fbe9489be9d247a3a7537a2ad13a186d26. Matrix verified by RUN LIST as completed/success on that exact head as a PUSH event (the earlier head had no matrix at all and a dispatched run that failed then cancelled, neither visible in the rollup). 0 unresolved threads. Verified on main: the generated stubs now delegate any() to the two-arg save and answer empty writeVersion on both stacks, and DurableSubscriptionModelMigratedCheckpointStorageTest is present. U7 and U10 changelog bullets survived the rebase (checked via git archive after a zsh :c modifier made an earlier grep return a false zero). Branch deleted on merge, no stranded push"}
    blocking_on: []
    next_action: none, merged and swept
    last_meaningful_progress_at: 2026-08-11T11:28:00Z
    observed: {}
    assessment: "Gating: official migration converts compile break into runtime failure today. Owns doc/migration/upgrading-to-0.33.0.md exclusively until U6"
  U6:
    issue: null
    goal: "Spring fencing wiring (findings 15, 16): per D5 fail startup on ambiguous strategy beans (back default off any interface-typed bean), starter validates conditional-write capability or explicit unfenced mode; guide caveat lands after U5"
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: [U5]
    session: "worktree subagent a73c222882b9549d1 (chip task_4ff34e51 dismissed unstarted)"
    deliverables:
      - {check: pr_merged, repo: johanhaleby/occurrent, ref: "745", met: true, evidence: "MERGED as squash b8b6d71c4669f6aa9c5906c9ea13d8f42fec705a, CAS-pinned to 412bd0c86b3ff2995bb7e9fa0d1eea4b6a71ad49. Matrix completed/success on that exact head by RUN LIST, 27 of 27, 0 pending, 0 unresolved threads with all six Copilot findings FIXED rather than declined. Mergeable read UNKNOWN first and was re-read to MERGEABLE CLEAN before pinning. Verified on origin/main: the guard is invoked from OccurrentBlockingAnnotationBeanPostProcessor, evaluatesWriteConditions is on both stacks with the six storages advertising true, and the fence-checkpoints property exists. 35 files, three commits. Branch deleted, no stranded push"}
      - {check: manual_approval, ref: "U6 plan gate (capability-discovery mechanism under D1-kept contract), Johan", met: true, evidence: "SATISFIED 2026-08-11T13:34:39Z via structured ask in the orchestrator session (Johan mobile). OPTION A: add one default method evaluatesWriteConditions() returning false to CheckpointStorage on BOTH stacks, so unable-by-default means startup failure rather than delivery failure. Decisive evidence from the worker: CheckpointStorageFixture.evaluatesWriteConditions() already exists in the TCK and its javadoc states the gap is that nothing on CheckpointStorage reports it back, so this fills a documented contract gap rather than speculating. Consistent with D1, which kept the ADR 116 break and anticipated exactly this small addition. Orchestrator granted the ownership extension (subscription/api both stacks, six shipped storages needing a true override, ~14 mock stubs, and the TCK fixture javadoc sentence that is now false). Also approved: @Fallback as the mechanism, widening occurrentCompetingDurableSubscriptionModel to the interface, a SmartInitializingSingleton validator, two named exceptions, and the fence-checkpoints opt-out property"}
    blocking_on: []
    next_action: none, merged and swept; its branch also had to be released from the orchestrator session worktree, which U6 had checked out
    last_meaningful_progress_at: 2026-08-11T16:43:10Z
    observed: {}
    assessment: "Delivered both findings plus a bug nobody had named: a custom strategy used to leave the starter default acquiring leases BESIDE it, so one subscription had two lease owners, fixed by widening occurrentCompetingDurableSubscriptionModel to the interface. Six Copilot threads all fixed, the important one being that the startup guard was outrunnable by OccurrentBlockingAnnotationBeanPostProcessor; the fix is structural rather than an Order annotation, the check is the first statement of the registering callback, reproduced before fixing and proven by deletion. Two SmartInitializingSingletons have no usable relative order, which is now a recorded rule"
  U7:
    issue: null
    goal: "Redis sentinels (finding 17): non-negative validation in notOlderThan ONLY if no shipped/documented version source can be negative, else adapter-boundary structured result"
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: []
    session: "subagent a32867d69034d2062"
    deliverables:
      - {check: pr_merged, repo: johanhaleby/occurrent, ref: "729", met: true, evidence: "MERGED as squash 6ad6bc084327c405db253a761d3055a61b7a30e3, CAS-pinned to 68213aa4bbead38473ef4e70538af0adf30dbeea and the post-merge head equals that pin. Gate on a fresh read at merge time: 0 failing, 0 pending, 0 unresolved threads. The code head 411cb7dce ran the full matrix 27 of 27; the merged head differed by one changelog line only. Non-negative validation confirmed present on origin/main at CheckpointWriteCondition.java:51-60. Branch already deleted on merge, no stranded commit, worktree clean at the merged pin and swept"}
    blocking_on: []
    next_action: none, merged and swept
    last_meaningful_progress_at: 2026-08-11T09:53:13Z
    observed: {}
    assessment: "Took the PREFERRED non-negative validation in CheckpointWriteCondition, and the conditional evidence holds: the only production version source is CompetingConsumerCheckpointWriteVersionSource, reaching only the two Mongo lease strategies, whose version comes from MongoListenerLockService seeding 0 and incrementing by 1, with the default fencingToken returning empty; no ADR or migration text permits a negative, no other call sites exist, and the Redis adapter comment already asserted the invariant. Copilot then correctly required the refinement be folded into the existing unreleased changelog bullet rather than omitted, which reverses this unit brief no-changelog instruction; the convention (AGENTS.md unreleased-capability rule) is what governs and the orchestrator confirmed the fold is minimal"
  U8:
    issue: null
    goal: "Filter AND short-circuit (finding 18): restore ordered short-circuit on in-memory query path keeping the reparse win, regression test vs 0.32.0 semantics, check #647 changelog entry"
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: []
    session: local_2ba1226c-7d6e-4d21-a68a-35987af498f1
    deliverables:
      - {check: pr_merged, repo: johanhaleby/occurrent, ref: "727", met: true, evidence: "MERGED as squash 4a3cee367f0ee8f7cff311f9ef187fe7a962e4ad, CAS-pinned and post-merge verified. Matrix 26 of 26 on the exact merged head, 0 unresolved threads. Orchestrator verified the corrected benchmark text states plainly that per-leaf laziness INSIDE a data run was not restored and that FilterMatcherTest asserts the call counts directly, since a throughput benchmark cannot prove zero reads"}
    blocking_on: []
    next_action: none, merged and swept
    last_meaningful_progress_at: 2026-08-11T10:23:26Z
    observed: {}
    assessment: "Fix direction chosen: contiguous-run batching rather than a lazy memoized reader. That does fix the reported regression (a preceding type operand short-circuits before any payload read) and keeps #647 reparse win, but it is weaker than per-leaf laziness within one data-leaf run, which is a real semantic difference from 0.32.0 allMatch only when a reader can fail per path. Verify at review that the claim written in code and PR text matches what shipped"
  U9:
    issue: null
    goal: "Reactive wait deadline (finding 19): mirror blocking twin's wait-local deadline in ReactiveMongoAppliedProjectionPositionStore, outage test for returns-false-never-throws within deadline"
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: []
    session: local_62371645-451f-4569-bfbc-6bacbb68d4ca
    deliverables:
      - {check: pr_merged, repo: johanhaleby/occurrent, ref: "730", met: true, evidence: "MERGED as squash 33dc5a940469c4043116e05f4345ded7d1e434c8, CAS-pinned and post-merge verified. Matrix completed successfully on the exact head eb55793fd (verified via run list, not just the rollup), 0 unresolved threads. Wait-local deadline confirmed present on origin/main: reads retry on the same Retry but bounded to the wait deadline, answering empty rather than throwing"}
    blocking_on: []
    next_action: none, merged and swept
    last_meaningful_progress_at: 2026-08-11T10:23:26Z
    observed: {}
    assessment: "Gating. Boundary: rebases after U1's plan gate if waitUntilApplied semantics move"
  U10:
    issue: null
    goal: "Lifecycle cleanup (findings 13, 14): opt-out marker only after delegate subscribe succeeds; terminal cleanup for failed catch-up respecting removal-as-cancellation semantics. 14 is the scope-cut line"
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: []
    session: local_e22bccfe-56f9-4717-a309-aa2beb8a3e12
    deliverables:
      - {check: pr_merged, repo: johanhaleby/occurrent, ref: "733", met: true, evidence: "MERGED as squash 1e4dfbeb5c8944c0c4745963e3dcf3e8fa3bb770, CAS-pinned to 38629623afbeda8b1a6464abc2f2713af8941cb3. Matrix completed success on the exact head (run-list verified, not rollup-only). ONE thread left open as a reasoned decline per policy: Copilot correctly noted the failure-cleanup removal is keyed by subscriptionId with no per-attempt identity, and the worker showed the identical shape exists at four pre-existing sites (both catch-up models success paths and the DCB model), so fixing only the new site would not close the race; recorded as item 4 on #737. Both halves shipped: finding 13 marker-after-subscribe, finding 14 terminal cleanup, with the released-behavior changelog bullet present"}
    blocking_on: []
    next_action: none, merged and swept
    last_meaningful_progress_at: 2026-08-11T10:34:23Z
    observed: {}
    assessment: "Non-gating but ships with release unless stalled; finding 14 fix needs a Changes changelog entry (released behavior)"
  UADR:
    issue: null
    goal: "ADR status drift (finding 6 residue: 0110, 0116 status lines and stale claims)"
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: []
    session: "subagent a8343ed82d7f98335"
    deliverables:
      - {check: pr_merged, repo: johanhaleby/occurrent, ref: "726", met: true, evidence: "MERGED as squash 985c362df65e331c2610386f638a28588e6b58fe at 2026-08-11T09:31:01Z, CAS-pinned to c868a625f8d7044e4f940cc9591012088b0646d7 and the post-merge head equals that pin. Gate at merge time: 0 failing checks, 0 pending, 0 unresolved threads, two Copilot reviews with the second on the merged head reporting no new comments. Corrected text verified present on origin/main. Two heads: 56f3e4f2e content-complete, c868a625f rewrapping one 140-char line the orchestrator caught in review"}
      - {check: manual_approval, ref: "design gate: Johan wording confirm before merge", met: true, evidence: "SATISFIED 2026-08-11T09:29:12Z: Johan answered the structured ask with merge-once-the-rewrap-lands, having read the orchestrator summary of both ADR status corrections. The gate is CLOSED, so no per-PR re-ask"}
    blocking_on: []
    next_action: none, merged and swept
    last_meaningful_progress_at: 2026-08-11T09:32:18Z
    observed: {}
    assessment: "Two false ADR status lines corrected: 0110 claimed nothing under Decision existed in code (implemented by #638) and cited the removed of(Object) lookup, 0116 claimed #665 stayed open for implementation (the fence shipped whole into 0.33.0). Worker self-caught a type error Copilot flagged, applying a findIn signature to ReplayAware, which is a view-dsl interface and no SubscriptionModelCapability. Orchestrator caught the introduced 140-char line; the file wraps at 120. Worktree, branches and stale refs swept, no stranded commit"
  UDOCS:
    issue: null
    goal: "Docs repo ripple: sweep held branches (52, 55, 61, 54 news post, others as touched), rebuild docs/0.33.0-preview, update occurrent-org.github.io#57. Trigger: every GATING library unit with a docs surface merged; stalled non-gating units defer their ripple alongside"
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: [U1, U3, U5, U6, U8, U9]
    session: "subagent afbe9f24d44e68160 (no worktree isolation, it edits a second repository through scripted Bash)"
    deliverables:
      - {check: branch_pushed, repo: occurrent-org/occurrent-org.github.io, ref: "docs/0.33.0-preview and five held branches", met: true, evidence: "DELIVERED. Docs PRs 52 and 63 CLOSED (verified CLOSED via gh): 52 documented the withdrawn applied-position feature end to end, 63 renamed its header and without an async counterpart would have left two headings both saying synchronous plus a broken published anchor. Branches updated and pushed, none merged: 49 (e05dab2, the pre-existing kramdown blank line, placed here because 49 merges first so later rebases inherit it), 54 (b8b9a7a), 55 (ac19258, all four U6 facts), 61 (122fb73, all three U3 semantics verified by the orchestrator in its added lines), the unopened release-note branch (81aabfd), and the preview (0a2274f, rebuilt by 24 asserted exact-text replacements, never a merge). Preview built clean and grep-verified: withdrawn API 0 hits across eight patterns, new fencing and step-condition facts present, anchors resolve once each. Issue 57 body rewritten as the authoritative order. Merges remain Johan"}
    blocking_on: []
    next_action: none, delivered; the nine remaining docs PRs are held until the occurrent-0.33.0 tag exists
    last_meaningful_progress_at: 2026-08-11T17:22:49Z
    observed: {}
    assessment: "Found three things nobody had listed. A FIFTH stale claim in PR 54 news post said two strategy beans now start with an unfenced timer poller, which U6 reversed, so the release notes would have published the opposite of shipped behaviour. PR 63 never mechanically depended on 52 (all three hunks change text already on main), so issue 57 dependency was editorial. And the preview already carried the duplicated historyWindow paragraph issue 57 warns about, two adjacent copies with no conflict. It also corrected the orchestrator own wording: the custom-strategy bug is that such a strategy NEVER REACHED the subscription model, not that there were two lease owners. Left to Johan in issue 57: whether to drop the now-redundant unopened release-note branch, and that PR 58 will look small after rebasing onto 54 since they duplicate 11 identical rename lines"
  UREG:
    issue: null
    goal: "Deferred/declined register at closeout: issues for finding 21 (measurement), finding 5 (wrapper contract); decline record for finding 20; ORCHESTRATOR.md register update"
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: []
    session: null
    deliverables:
      - {check: manual_approval, ref: "orchestrator inline work verified at closeout", met: true, evidence: "COMPLETE 2026-08-11T16:45:31Z. Filed and routed with confirmed marker comments: #746 (finding 5) and #747 (finding 21). Finding 20 DECLINED and recorded in ORCHESTRATOR.md as do-not-re-raise. #741 accepted as the pre-tag follow-up for finding 3, #743 deferred, and #734 and #735 CLOSED as moot after PR 739 deleted the code they described. Every one of the review 21 findings now has a routed outcome and the tracker carries a visible routing comment for each"}
    blocking_on: []
    next_action: none, register complete; the epic closes once UDOCS lands and main matrix is verified
    last_meaningful_progress_at: 2026-08-11T16:45:31Z
    observed: {}
    assessment: "Inline orchestrator work; issues carry the fleet marker and origin link"
  U11:
    issue: 741
    goal: "Persisted per-step matcher state plus a bound on the active step's retained events, the other half of design-review finding 3, with the false 0.31.0 no-unbounded-growth changelog claim corrected"
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: []
    session: "worktree subagent af8b678b3c64ae940"
    deliverables:
      - {check: pr_merged, repo: johanhaleby/occurrent, ref: "749", met: true, evidence: "MERGED as squash d03d4e59b4b60b8d39c65c4e5292e71a4f45b553, CAS-pinned to 9334aea4fd7601d6e625665637473f0c24a7c90b. Matrix completed/success on that exact head by RUN LIST, 27 of 27, MERGEABLE/CLEAN, 0 unresolved threads with all eight carrying a reply (verified resolved=8 withOwnerReply=8, none silent). Verified on origin/main: stepWindow present (10 hits), predicateId present (14 hits), the corrected bound historyWindow + 2*stepWindow + 1 stated, the carry-over eviction regression test present, ADR 0123 merged at 215 lines. Worktree was untracked-dirty from the scratch file the orchestrator told it to create; its 47 files were enumerated, the three narrative ones preserved to the session scratchpad, then removed. Branch deleted, no stranded push"}
      - {check: manual_approval, ref: "U11 plan gate, Johan via the orchestrator", met: true, evidence: "SATISFIED 2026-08-11T17:44:27Z. Johan chose the FULL plan (counters plus the retention bound) over the smaller stepWindow-only escape hatch the worker offered, accepting a persisted-shape change pre-tag because after the tag it becomes a second breaking change, and accepting the fail-loud trade when a bounded step leaf declaration changes mid-flight. Johan also chose the knob name stepWindow. The worker found a THIRD broken design: the per-leaf fingerprint of (type, has-predicate, count) that the orchestrator handed it does NOT close known failure mode 1, because two leaves agreeing on all three produce an identical entry, so swapping them crosses their counters and fires the wrong branch. Its fix, approved: raw uncapped saturating counters with the threshold OUT of the key (removing failure mode 2 as a class), plus build-time eligibility requiring same-entry leaves to be equal EventMatchers. Orchestrator answered the other five questions: threshold out of key approved, stepWindow as a NEW knob not a redefinition of historyWindow, the false 0.31.0 growth claim corrected FORWARD rather than by editing a past changelog section, one Kotlin parity function approved, and IllegalStateException with a precise message rather than a new type per the ADR 104/105 precedent"}
    blocking_on: []
    next_action: none, merged and swept
    last_meaningful_progress_at: 2026-08-11T19:02:54Z
    observed: {}
    assessment: "FOURTH broken design in this area and the first that is the orchestrator own rather than inherited. Johan answered do what is best long-term per AGENTS.md, so the ruling was DERIVED: existing structure is not a constraint to design around, an easier solution is not acceptable when the gap is correctness, and step conditions are UNRELEASED so the API shape is still free. Therefore give the predicate an identity rather than narrowing eligibility to survive its lack of one. Keep event(type, count, predicate) so an uncapped caller pays nothing, add an identifier overload that reaches the fingerprint, and refuse at build() a capped step with an unidentified predicated leaf, naming the overload as the remedy. Residual stated in the ADR: a caller who changes a predicate body while keeping its identifier still gets silent reuse, which is a broken caller promise of the same kind a schemaVersion carries, categorically different from the library assuming two predicates are the same"
  U12:
    issue: 743
    goal: "A saga declaring a supertype event type subscribes on a CloudEvent type string no concrete event carries, so it silently receives nothing; reproduce first, then fix at the layer the flow DSL, on(Class, ..) and the core builder actually share"
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: []
    session: "worktree subagent a80581aec529a818c"
    deliverables:
      - {check: pr_merged, repo: johanhaleby/occurrent, ref: "752", met: true, evidence: "MERGED as squash 743391bc6045e8350caca3e63b3bad39046af8ad, CAS-pinned to 37b4fa990ef58cd54675e083cf931b0b9a3909fc. Matrix completed/success on that exact head by RUN LIST, 27 of 27, MERGEABLE/CLEAN, 0 unresolved threads with all THREE carrying a reply (verified). Verified on origin/main: the shared EventTypeExpansion lives in common/filter internal and states the invariant, SubscriptionAnnotations consumes it (3 references), the ONLY remaining getPermittedSubclasses walk in main sources is the shared helper so the duplicate is gone, the AGENTS.md bullet is present, ADR 0124 merged, and migration sections 9 and 10 each appear once. Branch deleted, no stranded push, worktree clean at the pin and swept"}
      - {check: manual_approval, ref: "U12 plan gate, Johan via the orchestrator", met: true, evidence: "SATISFIED 2026-08-11T17:44:27Z by the orchestrator, since the remaining question (whether to add Saga#filter parity) was a decline-to-widen rather than a scope grant. REPRODUCED on all three routes with controls in the same harness proving the empty results are the defect: core, flow and window-condition sagas all issue nothing, and the core case is worst because startsOn(supertype) never creates an instance at all. The shared layer is the saga eventTypes() rather than the type mapper, so blast radius stays in dsl/saga-dsl. Decisive argument: every layer BELOW the filter is already supertype aware deliberately (TypeDispatch documents it, isInstance in the execution support, the flow impl and ReceivedEvents), and only the filter compares type strings for equality, so this is an internal inconsistency rather than a caller error. Second finding: the projection DSL knew about the constraint and mitigated it with a javadoc note plus a filter() hatch, and the saga DSL copied the derivation without either. Approved fix: expand sealed hierarchies transitively, keep the declared type so a collapsing custom mapper still works, refuse at build() what cannot be expanded, one shared helper for both builders. Saga#filter() deliberately NOT in this PR"}
    blocking_on: [{what: "third variant of the same defect, from Copilot re-review: foundAll == false is suppressed when the declared root is instantiable, so for an instantiable sealed root with a reopened branch the descendants below it are missing from the filter while dispatch still accepts them by assignability. Ruled fix plus a standing instruction to state the invariant positively (the derived filter must name every event type dispatch would accept) and test it as a table over hierarchy shapes rather than adding a case per bug", since: 2026-08-11T19:29:26Z, recheck_after: "on worker push"}]
    next_action: none, merged and swept
    last_meaningful_progress_at: 2026-08-11T19:56:20Z
    observed: {}
    assessment: "Delivered with two things worth recording. It could NOT deliver the recipe review marker and said so explicitly rather than omitting it: OpenRewrite carries Flag.Sealed but does not populate it for the type behind a class literal, so the marker flagged correctly sealed hierarchies, its own test caught that, and it removed it rather than ship advice pointing at correct code, writing the reason into ADR 0124 and the guide. And its independent local review found a hole the orchestrator reasoning had missed: a non-sealed CONCRETE class in a sealed hierarchy was collected and its branch called complete, so its own subclasses vanished from the filter silently, which means expansion could MISS types rather than only add disjuncts. The walk now requires each level to be sealed or final. It also caught that getConcreteEventTypes feeds a handler assignability check as well as the filter, so it widened only the filter, avoiding a startup regression for a handler with a narrower parameter than a listed sealed parent"
  UDOCS2:
    issue: null
    goal: "Second docs pass for the work adopted after the first closeout: stepWindow and predicateId from U11, and the supertype expansion plus the widened subscription filter from U12. UDOCS ran before either unit existed, so the held docs set documents neither"
    phase: RUNNING
    health: PROGRESSING
    swept: false
    depends_on: [U11, U12]
    session: "subagent a5e8b17545ab295c1 (edits a second repository through scripted Bash)"
    deliverables:
      - {check: branch_pushed, repo: occurrent-org/occurrent-org.github.io, ref: null, met: false, evidence: null}
    blocking_on: []
    next_action: await DELIVERY_RESULT; at merge-readiness verify the preview greps show all four topics present and the withdrawn API still at zero
    last_meaningful_progress_at: 2026-08-11T19:56:20Z
    observed: {}
    assessment: "EXISTS BECAUSE THE EPIC REOPENED. UDOCS delivered against the 13-unit scope and is DONE for that, but U11 and U12 landed public behaviour afterwards, so the held docs set is stale again: the saga chapter needs stepWindow and predicateId, and the supertype expansion plus the subscription-filter widening need documenting where sagas and subscriptions are described. Recording it as its own unit rather than reopening UDOCS keeps the first pass honest about what it covered. This is the #741 class of obligation, an epic cannot close while a deliverable it created is outstanding, so the closeout must check this unit rather than trusting that docs were done once"
