schema_version: 2
epic: cdx33e
revision: 18
updated_at: 2026-08-16 16:55:00+00:00
updated_by: 'orchestrator: E11 dispatched (Opus, round-three feedback with skill rules recorded first); E8 at its ninth
  head processing suppressed-block findings, one more round of grace before a calibration nudge'
baseline:
  repo: johanhaleby/occurrent
  branch: main
authority: 'Meta-plan approved by Johan 2026-08-15 evening (ExitPlanMode). Rulings by AskUserQuestion: exit is the mechanical
  cross-branch enumeration ONLY, no adversarial fixpoint round (the dominant class is cross-branch omission, which a diff-scoped
  round structurally misses); tag sequencing is merge-E1-first-then-tag (the javadoc ships in the artifact). The sweep''s
  no-blocker verdict stands beside the release-readiness declaration. Docs branches stay push-only, never merged; the tag
  and docs merges are Johan''s manual acts. Mode Attended. Standing grants per .context/orchestrator-policy.yml; all recorded
  conventions apply (johan-writing gates, claim-class sweeps, CAS merges, suppressed blocks at every head, foreground waits
  only, asserted anchors)'
pending_actions:
- id: act-001
  type: workflow-dispatch
  target: 'E1 subagent (Sonnet, isolation worktree): split the dynamic-refusal timing claim by wrapped-model type in the
    ReactorDurableSubscriptionModel class javadoc (one PR off 05763ca) and on all three docs tips, plus F4''s fencing parity
    scoping folded into the fencing edit'
  expected: 'PR delivered with DELIVERY_RESULT and docs branches pushed; orchestrator verifies on exact refs, merges the
    PR under standing authority. RESOLVED: 2026-08-15: PR 797 merged as 06f1b5e, both owned docs branches verified'
  status: VERIFIED
  attempts: 1
  safe_to_repeat: true
  issued_at: '2026-08-15T18:30:00Z'
- id: act-002
  type: workflow-dispatch
  target: 'E2 subagent (Sonnet): release-notes news-post catch-up (join sentence, source-breaking enumeration incl. the CheckpointStorage
    break) plus the full divergence enumeration of the release-notes news post and docs.md against preview, every divergence
    dispositioned deliberate-or-defect, defects fixed, ambiguity routed to Johan'
  expected: 'release-notes branch pushed with DELIVERY_RESULT incl. the enumeration table; orchestrator verifies on the origin
    ref. RESOLVED: 2026-08-15: delivered and verified at cb383b5; enumeration complete with 4 defects fixed (2 beyond the
    ruled set), the deliberate list recorded, the dead-anchor AMBIGUOUS cluster routed to Johan by ask'
  status: VERIFIED
  attempts: 1
  safe_to_repeat: true
  issued_at: '2026-08-15T18:30:00Z'
units:
  E1:
    issue: null
    goal: 'Sweep F1 + F4: the dynamic-StartAt refusal timing claim split by wrapped-model type (delegating path throws from
      subscribe at registration, cold path reports on the resumeSubscription handle) in the class javadoc and on all three
      docs tips; the fencing Cluster opener scoped to slot-alignable ids'
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: []
    session: null
    deliverables:
    - check: pr_merged
      repo: johanhaleby/occurrent
      ref: '797'
      met: true
      evidence: 'MERGED 2026-08-15 as squash 06f1b5e, CAS-pinned to b40cc42 (24/24 by job conclusion, final Copilot round
        clean, marker-free squash, the accepted-fixed thread resolved). Copilot round 1 caught a second layer of the same
        timing imprecision (running-vs-stopped on the cold path), split too. Main matrix on 06f1b5e watched'
    - check: branch_pushed
      repo: occurrent-org/occurrent-org.github.io
      ref: docs/0.33.0-preview
      met: true
      evidence: 'Pushed dcab6d3 -> 3293762, F1 split verified on the origin ref'
    - check: branch_pushed
      repo: occurrent-org/occurrent-org.github.io
      ref: docs/checkpoint-fencing
      met: true
      evidence: 'Pushed b571eb6 -> 5fcf235, F1 split plus F4 slot-alignable scoping verified on the origin ref'
  E2:
    issue: null
    goal: 'Sweep F2 + F3 + the class completion: port the preview news-post corrections the release-notes post never received
      (join behavior change, the CheckpointStorage source break in the enumeration), then enumerate every divergence between
      the release-notes branch''s news post and docs.md and the preview branch''s, dispositioning each as deliberate or defect,
      fixing defects, routing ambiguity. Owns the release-notes branch exclusively this epic, incl. applying F1''s split
      sentence there (E1 handles the other two branches), so the two units never push the same ref'
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: []
    session: null
    deliverables:
    - check: branch_pushed
      repo: occurrent-org/occurrent-org.github.io
      ref: docs/0.33.0-release-notes
      met: true
      evidence: 'Pushed 62bed8c -> cb383b5, verified on the origin ref: all four old claims gone, corrections present (join
        window, CheckpointStorage in the source-break enumeration with a self-contained recipe bullet, findIn renames, the
        third registration-position overclaim instance scoped). The enumeration delivered in full: 2 additional defects found
        and fixed beyond the ruled two, deliberate list recorded, and an AMBIGUOUS cluster routed to Johan: the branch''s
        own news post links to two DEAD ANCHORS (#replay-batching, #saga-step-conditions) whose sections exist only on preview,
        plus further preview-only 0.33 sections and stale-but-compiling timer examples'
  E4:
    issue: null
    goal: 'Adopted post-closeout unit (Johan''s word, 2026-08-16): rewrite the preview branch''s stale reactive CheckpointStorage
      section to the 0.33 interface at 06f1b5e, structured like the branch''s corrected blocking section, executing release-day
      checklist item 4 for the branch that wins at release day. Docs-only, no monitor re-arm owed (no PR deliverable)'
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: []
    session: null
    deliverables:
    - check: branch_pushed
      repo: occurrent-org/occurrent-org.github.io
      ref: docs/0.33.0-preview
      met: true
      evidence: 'Pushed 3293762 -> 71a8f3a, verified on the origin ref: the reactive CheckpointStorage section now lists
        the 0.33 interface (three-argument save, writeVersion, both capability members) mirroring the blocking section minus
        exists which the reactor interface lacks, old two-argument-only listing gone, every claim source-cited incl. the
        empty-save refusal and the blocking-only fencing check attribution. Jekyll exit 0 per worker'
  E5:
    issue: null
    goal: 'Adopted unit, decided by derivation from Johan''s docs-must-reflect-code principle (his explicit direction to
      decide rather than route): the fencing docs branch is superseded by preview and is NOT merged at release; therefore
      the release-notes post must carry the fencing announcement. Port the summary mention and highlight from the fencing
      branch''s post copy, claim-checked against 06f1b5e so the older snapshot''s corrected-away claims do not return; anchors
      resolved against preview''s docs.md'
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on: []
    session: null
    deliverables:
    - check: branch_pushed
      repo: occurrent-org/occurrent-org.github.io
      ref: docs/0.33.0-release-notes
      met: true
      evidence: 'Pushed cb383b5 -> 6b4148f, verified on the origin ref: the Checkpoint fencing Highlights section present,
        no stale Cluster claim (the ported paragraphs never mentioned Redis, grep-confirmed), anchor resolves against preview
        71a8f3a. Worker corrected the brief''s premise with evidence (summary and behavior bullets already announced fencing,
        only the displaced Highlights section was missing) and removed the redundant stand-in bullet. It also caught a colon-reveal
        the literal grep misses when a backtick follows the colon, a gate-pattern gap worth noting'
  E6:
    issue: null
    goal: 'Adopted unit (Johan''s docs feedback, 2026-08-16): 20 itemized prose fixes plus full simplify passes over the
      Flow DSL and Checkpoint Fencing sections and both news posts, human-for-human standard, with the fencing auto-configuration
      truth corrected (the starter auto-contributes the fallback strategy, verified at OccurrentMongoAutoConfiguration 191-199)
      and pre-0.33 bug history cut per the new AGENTS.md rule. Voice learnings recorded to johan-writing first (a902090)
      so the worker writes under the updated gates. Preview was first brought current with docs main by clean merge (b1db97e)
      at Johan''s flag; release-notes docs.md alignment to preview follows after E6 lands'
    phase: DONE
    health: PROGRESSING
    swept: false
    depends_on: []
    session: null
    deliverables:
    - check: branch_pushed
      repo: occurrent-org/occurrent-org.github.io
      ref: docs/0.33.0-preview
      met: true
      evidence: 'Pushed b1db97e -> 3c92140 (4 commits), verified on the origin ref: all 20 quoted sentences 0 hits, replacements
        and both new snippets present in rendered output, two duplicated paragraphs removed, the fencing auto-configuration
        truth corrected to auto-contributes-with-override. Worker flagged 23 Reach-for hits and a bare window outside its
        two sections, adopted as E7'
    - check: branch_pushed
      repo: occurrent-org/occurrent-org.github.io
      ref: docs/0.33.0-release-notes
      met: true
      evidence: 'Pushed 6b4148f -> 6ffe405: the two news-post items fixed identically to preview''s post'
  E7:
    issue: null
    goal: 'Completion sweep per the corrections-complete-everywhere rule: the newly banned patterns (Reach for, bare window,
      a-real intensifier, tree leaves, bold fragment labels, command-pair remedies) swept across the whole preview docs.md
      beyond E6''s two sections; then release-notes docs.md and shared includes made byte-identical to preview so the release-day
      second merge cannot conflict or resurrect stale text'
    phase: DONE
    health: PROGRESSING
    swept: false
    depends_on: []
    session: null
    deliverables:
    - check: branch_pushed
      repo: occurrent-org/occurrent-org.github.io
      ref: docs/0.33.0-preview
      met: true
      evidence: 'Pushed 3c92140 -> cd41668, verified on the origin ref: 0 Reach-for hits file-wide, 7 intensifier fixes
        with 2 genuine literals correctly kept, both bare windows unpacked, 3 fragment labels sentence-formed, verb-leaves
        correctly untouched'
    - check: branch_pushed
      repo: occurrent-org/occurrent-org.github.io
      ref: docs/0.33.0-release-notes
      met: true
      evidence: 'Pushed 6ffe405 -> 1f32c7d: docs.md, three decider includes and main.js byte-identical to preview cd41668
        (verified by empty cross-branch diff), news post untouched, jekyll exit 0. The release-day second merge can no longer
        conflict on docs content'
  E8:
    issue: null
    goal: 'Recipe completeness before the tag, ruled by Johan (fix both pre-tag): number-736''s generated-stub any() delegation
      recursion (StackOverflowError at runtime on the no-own-save inheritance shape, no compile-time warning, falsifying
      the docs'' permanent-answer claim) and its alreadyDeclared supertype gap, plus number-732''s review-comment marker
      for Object-typed capability-lookup callers. Rewrite module only, invariant: generated code never re-enters itself
      on any inheritance shape'
    phase: RUNNING
    health: PROGRESSING
    swept: false
    depends_on: []
    session: null
    deliverables:
    - check: pr_merged
      repo: johanhaleby/occurrent
      ref: null
      met: false
      evidence: null
  E9:
    issue: null
    goal: 'Round-two Flow DSL feedback (11 items): the Step Conditions material restructured for cold-reader order (counted
      conditions and stepWindow introduced in their own subsection before any rule referencing them), the ten quoted sentences
      dissolved or rewritten, transitionTo re-entry semantics verified in code before writing, and the predicate-name truth
      stated plainly (needed only under stepWindow, count irrelevant, harmless without). After it lands, release-notes docs
      content must be RE-ALIGNED to preview (E7''s alignment predates this rewrite)'
    phase: DONE
    health: PROGRESSING
    swept: false
    depends_on: []
    session: null
    deliverables:
    - check: branch_pushed
      repo: occurrent-org/occurrent-org.github.io
      ref: docs/0.33.0-preview
      met: true
      evidence: 'Pushed cd41668 -> 32464a1, verified on the origin ref: all ten quoted sentences gone, the Counted Conditions
        and stepWindow subsection present (TOC :138, heading :5983, cross-reference :6562), concept-order table delivered
        with introduction preceding first use for every load-bearing term, every behavioral claim code-cited incl. re-entry-resets
        and the fingerprint-omits-count fact, one exposed contradiction in the five-things list fixed along the way'
    - check: branch_pushed
      repo: occurrent-org/occurrent-org.github.io
      ref: docs/0.33.0-release-notes
      met: true
      evidence: 'Re-aligned by the orchestrator at 8d87245: docs.md byte-identical to preview 32464a1 (0-line diff verified
        before commit). Johan''s primary checkout also fast-forwarded to 32464a1 so his local jekyll shows the restructure'
  E10:
    issue: null
    goal: 'Approved from the onlyIf-vs-condition investigation: ADR 120 gains one amendment paragraph recording the signature
      asymmetry as design fact (the guard''s BiPredicate sees retained history, a condition leaf''s Predicate sees one event
      by construction, which is what makes counts recount-safe and is the practical choosing criterion); the docs gain a
      when-to-use-which passage after the collecting-payment example with the same branch written both ways, onlyIf introduced
      at first appearance, cold-reader order respected; release-notes re-aligned after'
    phase: DONE
    health: PROGRESSING
    swept: false
    depends_on: []
    session: null
    deliverables:
    - check: pr_merged
      repo: johanhaleby/occurrent
      ref: '799'
      met: true
      evidence: 'MERGED 2026-08-16 as squash d49ae19, CAS-pinned to 49a6dad. Copilot round 1 caught three overclaims in the
        draft rationale (per-delivery re-derivation contradicting ADR 123''s carried counts, signature-enforces-purity, elapsed-time-is-guard-only),
        all fixed against the actual javadoc contracts; the orchestrator closed the worker''s re-review gap (final head reviewed
        clean on re-request), replied to and resolved the thread, squash body marker-free'
    - check: branch_pushed
      repo: occurrent-org/occurrent-org.github.io
      ref: docs/0.33.0-preview
      met: true
      evidence: 'Pushed 32464a1 -> db24b2d, verified by ls-remote: onlyIf introduced at its true first appearance (:5876,
        two prose mentions preceded the assumed spot), the written-both-ways passage with Java/Kotlin tabs, the four-difference
        list and the rule of thumb, forward links only for not-yet-introduced mechanisms'
    - check: branch_pushed
      repo: occurrent-org/occurrent-org.github.io
      ref: docs/0.33.0-release-notes
      met: true
      evidence: 'Re-aligned at b6362e4, docs.md byte-identical to preview db24b2d'
  E11:
    issue: null
    goal: 'Round-three docs feedback (6 items plus the apply-broadly mandate): example-anchored mechanism introduction for
      the Counted Conditions opener per Johan''s supplied shape, the unstated-norm appeal killed, the ReceivedEvents non-sequitur
      given a real transition or boundary, count and none() introduced before use, directional snippet references, the visibility
      rules (reaction vs guard vs timeout scope) given their own structural home, then the five disease patterns swept across
      the section. Five new rules recorded in johan-writing (a11fcc7) BEFORE dispatch so the worker writes under them. AGENTS.md
      deliberately NOT extended: the writing skill already binds every docs surface via the global rule, duplication is bloat'
    phase: RUNNING
    health: PROGRESSING
    swept: false
    depends_on: []
    session: null
    deliverables:
    - check: branch_pushed
      repo: occurrent-org/occurrent-org.github.io
      ref: docs/0.33.0-preview
      met: false
      evidence: null
    - check: branch_pushed
      repo: occurrent-org/occurrent-org.github.io
      ref: docs/0.33.0-release-notes
      met: false
      evidence: null
  CREG:
    issue: null
    goal: 'Register: the sweep''s no-blocker verdict recorded beside the release-readiness declaration; the four dispositions;
      the enumeration''s deliberate-divergence list recorded as the release-notes branch''s known shape'
    phase: DONE
    health: PROGRESSING
    swept: true
    depends_on:
    - E1
    - E2
    session: null
    deliverables:
    - check: branch_pushed
      repo: johanhaleby/occurrent
      ref: null
      met: true
      evidence: 'DONE 2026-08-15: tracker scan clean (only PR 797, fleet-created); the fifth sweep''s no-blocker verdict,
        the four dispositions, E2''s two extra defect fixes, the deliberate-divergence list, and Johan''s preview-docs-wins
        ruling all recorded in the ORCHESTRATOR.md closeout block and release-day checklist'
watchers:
- id: watcher-002
  kind: github-work-item-monitor
  repository: johanhaleby/occurrent
  external_id: bteh21ltq
  status: READY
  retirement:
    kind: no_unmet_pr_advanceable_deliverables
  last_verified_at: '2026-08-16T11:30:00Z'
  last_verified_revision: 12
- id: watcher-001
  kind: github-work-item-monitor
  repository: johanhaleby/occurrent
  external_id: b8vfgsmkn
  status: RETIRED
  retirement:
    kind: no_unmet_pr_advanceable_deliverables
  last_verified_at: '2026-08-15T18:30:00Z'
  last_verified_revision: 1
