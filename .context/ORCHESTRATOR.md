# Occurrent Orchestrator Memory

## Hot restart

- Verified at: session worktree `rel34-epic-registration-8c2066`, 2026-08-22T12:18:12Z. **0.33.0 IS RELEASED. 0.34.0 IS NOT CUT.** FIVE rel34 units merged: U8 `0ef8eefce` (#834), U1 `a45a17a08` (PR 900), U11's ADR 134 `e9360c959`, U9 `a86909f5d` (#753), U7 `29afaed32` (#866 and #917). TWO withdrawn as not reproducible: U12 (#903, PR 908 closed unmerged) and U18 (#909).
- Workspace baseline: session worktree clean, all rel34 memory committed to main with `[ci skip]`. Docs repo `occurrent-org/occurrent-org.github.io` has SEVEN held PRs (68, 69, 70, 71, 72, 73, 74) plus an EIGHTH branch `docs/saga-step-conditions-added-tag` that is a superseded no-op to DELETE not merge, plus U15's new branch when it opens. `preview/0.34.0` is STALE against PR 69 and PR 74 and must be rebuilt after the 69-versus-74 conflict is ruled.
- Active epic / definition of done: **rel34, registered 2026-08-22T07:05:33Z** (tracker-scan lower bound). The 0.34.0 RELEASE epic for milestone `0.34.0` (number 3, existing, not created). Plan `/Users/johan/.claude/plans/rel34-0-34-0-release.md`, state `.context/epics/rel34.yml`, journal `.context/orchestrator/rel34/` (local only). PLAN PENDING Johan's approval at registration. Done when: every milestone item is merged or re-milestoned by Johan's ruling; #896 merged (DONE) and the brk epic closed; the held docs branches merged by Johan on release day with `preview/0.34.0` showing the merged result and `docs/append-identity`'s news post taken, its stale PushObserver highlight fixed first; #740 closed as shipped and #361 as its duplicate; the tag cut with the Sonatype and tag-link checks green; memory checkpointed. **r34 (the readiness epic) is COMPLETE and superseded by rel34; do not reconcile r34.yml further.**
- **Gate rulings 2026-08-16, do not re-ask:** broker IN with #419 committed (epic brk, own session); #740 IN as marquee (epic ayi, own session, closes #361 at ship); join removal IN plan-first (#806, U14); #71 closed superseded by #392; #725+#721 pulled in as U15 (design round; implementation registers as its own epic if the plan sizes it epic-scale), and U15 DELIVERED that design as ADR 127 in PR 819, which did size it epic-scale, so the implementation epic is still to register. Deferred at the gate, accepted OUT: #397, #404, #540, #418, #72, the pre-2026 backlog. #718/#747 are community-in-flight (Shubh2-0, PR 801): watched, never assigned.
- Milestone `0.34.0` is number 3: 35 issues assigned (the r34 set, #740/#361, broker family #388/#412-#417/#419/#421, #806). Stale milestones 1 and 2 closed. **Milestone `0.35.0` created as number 4 by the 2026-08-22 triage sweep.**

- **TRIAGE SWEEP 2026-08-22, Johan's rulings, do not re-ask.** Scope: the 20 open unmilestoned issues created since 2026-08-10 plus the four undecided milestone items #876/#871/#721/#725. (Johan's count of 21 was one high, verified against `gh` twice.) Johan pulled far more into 0.34.0 than the fleet deferrals had assumed, so several recorded deferrals below are now SUPERSEDED and must not be cited as current.
  - **0.34.0 open set after the sweep, 21 issues:** #361, #388, #421, #721, #725, #740, #753, #818, #834, #837, #857, #866, #867, #871, #876, #877, #878, #879, #884, #891, #893.
  - **0.35.0 open set, 5 issues:** #781, #842, #853, #864, #870.
  - **Backlog, deliberately unmilestoned:** #718 (epic-scale, Shubh2-0's PR 801 in flight), #747 (measurement, no demonstrated defect), #887 (tooling, Johan schedules it).
  - **SUPERSEDED deferrals, all now 0.34.0.** Every one carries a routing comment stating what it supersedes. ayi dec-0008's "next release" for #877, #878 and #879. ayi dec-0015's deferral of #891, which Johan pulled forward because it is a hole in #740 on the reactor stack. #857's follow-up-hardening note. The 2026-08-17 ruling routing #837 into the #725 implementation epic, now a STANDALONE 0.34.0 fix because sdi has not started and the silent `@Transactional` bypass is shipped. #867's decline for PR 861. #866's own-design-pass deferral, and the pass now happens in 0.34.0. #753's ADR 0124 and ADR 0126 deferral. brk's #884 flake deferral.
  - **#721 and #725 STAY in 0.34.0 by Johan's explicit ruling**, so the sdi epic (ADR 127 implementation, epic-scale, still unregistered) is now ON the 0.34.0 critical path rather than after it. This is the single largest schedule risk the sweep created and it was Johan's deliberate choice, not a derivation.
  - **#753 decided BY THE ORCHESTRATOR on Johan's conditional delegation** ("include it in 0.34.0 if it's serious, otherwise 0.35.0"). Ruled 0.34.0. Derivation, verified against the code rather than r34's recorded assessment: reachability is genuinely low (records and Kotlin data classes are implicitly final, so the repo convention never hits it), but the failure is silent event loss upstream of the handler with no log, warning or exception, and the fix is a refusal at registration. Deferring therefore does not avoid the breaking change, it only adds another release of undetectable loss before the same break arrives. Johan may reverse this.
  - **Shipped-code defects confirmed present in the `occurrent-0.33.0` tag** (verified by `git ls-tree`, not inferred): #818 (`SagaRunner`), #837 (registrars), #866 (`RetryExecution`), #876 (`updateEvent` in both Mongo stores), #834 (`rewrite/.../subscription-mode-0_32.yml`), #753, #842. #834 was NOT in Johan's first read and is a shipped defect: the recipe can rewrite an unrelated YAML document's `enabled` key to an invalid mode value.
  - **Housekeeping:** #781's stale `in-progress` label removed (its branch `johan/dreamy-keller-da2a83` is gone from origin). 19 routing comments posted under the `<!-- orchestrator-routing -->` marker, all gated and read back. Visibility-repair list EMPTY.
  - **Not dispatched.** This sweep was triage only, by instruction. Nothing is claimed, no unit registered, no epic opened. The 0.34.0 set now holds 12 issues with no owning epic (#753, #818, #834, #837, #857, #866, #867, #871, #876, #877, #878, #879, #891 less the ayi/brk ones already closed out), so SOMETHING has to own them before the tag.
- Authority: `.context/orchestrator-policy.yml`. No session-scoped deviations.
- Active units: `.context/epics/rel34.yml` revision 31, 23 units. IN THE GATE: 901 (U2, re-verify running on its new preservePosition code), 902 (U3, needs ONE Copilot review on head 094d29757, everything substantive is unreviewed), 914 (U4, re-verify running after a falsification), 919 (U10, adversarial pass running, no third Copilot round by ruling). DISPATCHED: U16 working, U15 chip UNCLICKED. UNDISPATCHED: U5 (#891), U13 (#906), U14 (#905), R1 to R4, C1.
- Next action: **R3 GATES ON brk's FENCE-REMOVAL PR for #922, a CONFIRMED PRODUCTION SILENT STALL in code already on main.** amqp-client re-issues basic.consume before running recovery listeners, so the first redelivery after a recovery arrives under the old channel generation, the fence drops it unacked, and at prefetch 1 the bridge stops consuming until closed. Reproduced deterministically. Until that fix lands it is a FLEET-WIDE CI TAX: RabbitMqCloudEventBridgeConnectionRecoveryTest fails intermittently on any PR that merges main, and the correct action is RERUN (no landed fix exists to rebase onto), unlike #884 where merging main WAS the fix.
- Monitors: RETIRED 2026-08-17T~19:00Z. Both stopped (TaskStop b3ae1qein and bs9p96l3j) in the sweep that merged 841, the last PR-advanceable deliverable, per the lifetime rule. ayi runs its own monitors.
- H2 DONE: PR 807 merged as `4f52632` 2026-08-16T17:1xZ (CAS pin `55455c7`). Copilot's one inline finding (migration-guide intro phrased as construction process) fixed on the branch and replied to before merge. The shared docs checkout at `occurrent-org.github.io` carries U4's live branch, so orchestrator docs fixes go through the GitHub contents API on fresh branches, never that checkout (docs PR 67 was built that way; note gh api needs `-F content=@file`, `-f` sends the literal string).
- Verification: **MERGE GATE, four facts plus a fifth.** (1) `gh run list --commit <head>` shows "Java CI with Maven" `completed/success`, OR every file changed since the last such head is markdown (`paths-ignore: '**/*.md'`). NEVER read `statusCheckRollup` for readiness and NEVER count contexts (26 after a code push, 3 after markdown; counting fails in the DANGEROUS direction if the matrix shrinks). (2) `reviewDecision`. (3) unresolved threads by `isResolved==false` ALONE. (4) **READ EVERY REVIEW BODY AND ITS SUPPRESSED BLOCK** and the headline verdict: a yellow or blue verdict with zero threads is not a clean review. (5) a fresh-context adversarial pass on every correctness-bearing unit, whose CLAIM must name the absent, empty and zero cases explicitly or they go unexercised. **SCORE SO FAR: six of six gated PRs carried a real defect invisible to facts 1 to 3.** The v7 monitor's flag is renamed `nopending` here and is NOT a merge signal.
- Tracker: github (block below)
- Mode: ATTENDED (Johan's word at rel34 registration 2026-08-22T07:05:33Z)
- Journal and status page (brk): `.context/orchestrator/brk/decisions.jsonl` (initialized 2026-08-20, tracked, writer claude:brk-orchestrator, generation 1, empty since the epic predates the journal machinery, prior rulings live in the epic entries below), status projection written per tick, fleet page regenerated with viewer-data.py and served by the fleet hub at http://127.0.0.1:8642/occurrent-zealous-williamson-0ad901/ (viewer v26, one hub on 8642 for every repository, never a second server)
- Hygiene 2026-08-17T~21:4xZ: 27 completed fleet and pre-r34 sessions archived on Johan's cleanup order, eight stale apirev branches deleted, merged unit worktrees removed. Live: this orchestrator plus the four resume workers (U7, U18, U12, U19)

- Cross-epic file contention with ayi (recorded 2026-08-22): ayi's U11 edits BlockingHandover, ReactiveHandover and both CatchupThenPushSubscriptionModels on both stacks at the same time as brk's fixpoint addendum (#893). Protocol agreed with the ayi orchestrator: brk messages ayi the branch and diff scope when the addendum PR opens and again before its merge, and holds the merge behind U11's if ayi asks. ayi asked (their dec-0021), brk agreed (dec-0014): the addendum is two PRs, A outside the shared files merges independently, B on the shared files branches from PR 892's squash and is written against U11's identity-token design, dropping the reactor ownership guard where U11 removed the per-id marker. ayi's PR 892 build break after brk's 889 and 894 was ayi's own mid-implementation state, not a brk regression. PR 892 merged as 50bd66ecd on 2026-08-22; the ayi orchestrator session then left the host's session list, so the pre-merge heads-up for brk's PR 896 went as a comment on that PR instead of a message.


## Shared skill edits, announced per SKILL.md

Read this if you are a live orchestrator. You are running whatever copy of the orchestrator skill you
loaded at invocation, and this file is the only way an edit reaches you before your next checkpoint.

- `549596e4e` 2026-08-22 08:42, Johan. Adds a literal chip title check before every spawn. If you
  loaded the skill before 08:42 you do not have it, and rel34 shipped a mis-titled chip at 12:53
  because of exactly that. Compose a chip title from the fleet rule first and check the tool's
  length hint second, never the other way round.
- `ca083e38d` 2026-08-22 12:57. A rollup with nothing pending is not a finished CI run. The
  fleet monitor's `nopending` flag is not a merge signal. sdi and brk were running without this for
  hours because whoever made the edit told nobody, which is the defect the announce rule now names.
  Attribution in this block is unreliable and should not be relied on. Every commit in the skill repo is
  authored `Johan Haleby` whichever session wrote it, so cite the commit and let the reader diff it.
- `cc615fad9` 12:43 and `7f14d4513` 15:02, Johan. Both change `epic-state.py` only. No re-read is
  needed for either, the script runs from disk, but note the validator now checks the dependency
  graph so an epic file that passed this morning can fail this afternoon.
- `e0c3eea93` 2026-08-22 15:04, Johan. Bucket C now holds your OWN questions to the same bar as
  relayed ones. Read the conventions document BEFORE asking, and surface a question through a channel
  that renders (`AskUserQuestion`) with a recommendation and a three sentence preamble. rel34 had been
  asking in plain prose all day.
- `396b084cd` and `d1b7f0b88` 2026-08-22, authorised by Johan. Three additions. Re-read
  the skill at every checkpoint, reading only what changed since the session started rather than the
  whole file. Announce your own skill edits here, which is what this block is. And refresh
  `status.json` at every checkpoint and read it back, because `stale_after` is a promise nothing else
  keeps. rel34's own page sat at epic revision 5 while the epic was at 43.

## Tracker

- Binding: github
- Skill to invoke: none, gh CLI directly
- Unclaimed target: open issue in johanhaleby/occurrent carrying the fleet marker
- Unit target: issue in johanhaleby/occurrent, milestone 0.34.0
- Decided: Johan, standing practice since apirev (2026-08-06), reaffirmed at r34 registration 2026-08-16

## Epic history and standing decisions

- **Thirteenth epic cdx33b (codex review round 2) plus the unrecorded cdx33c/cdx33d fixpoint rounds, ENDED by the 0.33.0 release 2026-08-16.** Registered 2026-08-12, nine chips dispatched; the fixpoint chain ran 2026-08-13 to -16 as PRs 789-800 without memory checkpoints. Residue: issues #786/#788/#791 (cdx33c markers) and #803-#805 (cdx33d), all routed into r34. The 0.33.0 tag supersedes cdx33b's release gate; its epic file stays at revision 4 as history.

- **Twelfth epic cdx33, COMPLETE 2026-08-11, same day as registration (09:05:38Z to ~18:00Z).** The codex design-review remediation before 0.33.0. Meta-plan `/Users/johan/.claude/plans/unified-bubbling-koala.md`; state `.context/epics/cdx33.yml` revision 39, 13 units. The review's 21 findings all have a routed outcome: 13 FIXED on main, 1 moot by withdrawal (finding 19, whose file was deleted, so U9's merged work is deliberately erased), 1 accepted as the pre-tag follow-up (#741), 2 deferred with issues (#746 finding 5, #747 finding 21), 1 DECLINED (finding 20, the ADR 0110 documented limitation), and finding 3 split between a shipped contract half and #741. Merged: U-ADR 726, U7 729, U8 727, U9 730, U10 733, U5 731, U2 728, U1 739, U3 742, U4 744, U6 745.
- **cdx33's four biggest outcomes.** (1) The applied-position feature was WITHDRAWN whole from 0.33.0 (PR 739, ADR 0122): `waitUntilApplied` read a monotonic maximum as a completed prefix, which needs position-ordered delivery no Occurrent source provides, and `WriteResult` carries no position so the documented scenario was unreachable. ADR 0111 is withdrawn, #361 reopened, #740 tracks the append-identity successor. (2) Manual-start pinning moved from start to REGISTRATION (PR 728, ADR 86 amendment), so the earliest registrant always wins and a refusal is always safe; this also made the code finally match a public javadoc promise that had been false since 0.32.0. (3) Step-condition reactions now read the window that fulfilled them, with the deprecated `join` deliberately still reading full history (PR 742). (4) Ambiguous `CompetingConsumerStrategy` beans now FAIL STARTUP instead of silently disabling fencing, `CheckpointStorage.evaluatesWriteConditions()` exists on both stacks, and a custom strategy finally reaches the subscription model at all, which it previously did not (PR 745).
- **Do not relitigate from cdx33:** the ADR 116 contract break stays (Johan, on AGENTS.md long-term grounds); `capability`/`hasCapability` stay; counters for finding 3 are deferred to #741 and NOT abandoned; finding 20 is declined; the `join` full-history exemption is REVERSED by Johan 2026-08-12 (ADR 0125, U14/PR 759): a lowered `join`'s reaction reads its own window at every step, so do not cite the old sugar-preserves-semantics ruling; ADR 0122's refutation of the completed-prefix premise.

- **0.32.0 release readiness arc, COMPLETE 2026-08-06.** Meta-plan `/Users/johan/.claude/plans/i-want-you-to-sparkling-catmull.md`, approved by Johan 2026-08-06. All units merged (11 library PRs, 2 docs PRs), 12 held docs PRs review-clean, all unit sessions archived and swept. U13 found NO lifecycle race: the flaky reading was a Surefire double-report rendering issue, fixed by #576, so a red `subscription-rest` shard is real evidence again.
- Standing merge authority granted 2026-08-06 (orchestrator squash-merges each green unit PR with resolved review threads, then hygiene-sweeps session, branches, worktree); same terms apply to sibling epics per `.context/orchestrator-policy.yml`.
- Do-not-relitigate decisions: no `requireNonNull` in `ReactorCatchupSubscriptionModel.ownerOf` (#570/#257 visible-disagreement precedent, thread deliberately left open); the docs' pre-existing "delivering event" phrasing stays; docs #31's colon-before-snippet stays (page-wide convention); the removal of `ManualStartSubscriptionModel`'s exemption from `SubscriptionModelLifeCycle.stop()`'s reopen rule is RATIFIED, the rule now covers every model on both stacks with no exemption.
- Scope decisions, next-release, do not re-raise: #565 (later its own epic, ccpause), #540, #468-residual, #439, JDBC #392 family, broker #388 family, #361 (its own epic vgpr, whose feature was later WITHDRAWN, so #361 is reopened and now tracked by #740).
- U14 (#577) and #575 (via #576) closed the arc; U12 readiness gate satisfied.
- **Johan's manual release checklist, remaining items only (0.32.0 already released):** verify tag-pinned docs links resolve; merge held docs PRs occurrent-org.github.io#49 and #52 at the next release (`addedTags` conflicts in `js/_partials/main.js` are keep-both); Sonatype Central Usage Center check against all three limits about 24h after the release; confirm Dependabot alert #27 auto-closed after the #473 merge.
- **Fifth epic registered 2026-08-08 (afternoon): ccpause, the waiting-consumer pause fix (#565).** Single-unit epic; unit state in `.context/epics/ccpause.yml`; plan approved by Johan after a two-round fresh-context review (round 2 APPROVE) at `/Users/johan/.claude/plans/https-github-com-johanhaleby-occurrent-i-lucky-canyon.md`, which carries the full evidence map (the five mishandling sites, the ADR 105 has-started/is-running split, the `SubscriptionModelLifeCycle:116-118` contract amendment as a mandatory ask at the unit's plan gate, the saga-timer composition hazard as a hard constraint, the dependent-module verification set). Unit U1 chip `task_d649cf9f` spawned (plan-first, /johan-plan, Opus/high, downshift at the plan gate); #565 claimed (in-progress + comment). Work-item monitor: `bc0cymzzm` stopped at U1 completion per the lifetime rule, re-armed as `b2vrbsyzm` when Johan adopted #656 as U3 (structured ask, widening the 565-only scope; U3 chip `task_c5308960`, Sonnet/high). U1 merged as PR 655 (squash `f8e310db8`, one reasoned-decline thread left open per policy), #565 closed, session archived and swept. EPIC CLOSED 2026-08-08 ~21:25Z at state revision 9: U1 (PR 655, squash `f8e310db8`, one reasoned-decline thread open per policy), U3 (#656 adopted by structured ask, PR 657, squash `a60cd1af8`), U2 held docs PR occurrent-org.github.io#53 review-clean and frozen until release (no docs monitor by design). All sessions archived, worktrees and branches swept, both library monitors stopped under the lifetime rule, tracker scans clean. Main's matrix on `a60cd1af8` completed 26/26 green (verified 2026-08-08 ~21:45Z); combined verification passed. **Release-day addition: merge held docs PR 53 alongside PR 49.** Race finding #651 went to Johan's own ccrace epic (separate orchestrator session, hands-off). Milestone skipped. Standing authority per `.context/orchestrator-policy.yml`. Session title for this epic: `[ccpause] Orchestrator, waiting-consumer pause`. Coexists with vgpr and archrev-B11; their epic files, monitors, sessions and branches are off-limits to this epic's sweeps.
- Next action (2026-08-08, ccpause): CLOSED, main green on the final squash, no action; held docs PR 53 waits for release day.
- **Sixth epic ccrace, COMPLETE 2026-08-09 ~12:45Z, one day after registration.** Registered from Johan's /orchestrator invocation on #651 (the competing-consumer register-vs-refresh race), widened by structured asks into the full lease-hardening arc. Seven units, all DONE and swept at `.context/epics/ccrace.yml` revision 30, zero drift, no monitors running: U1 #651 per-consumer locks (PR 658, ADR 113), U2 #659 one-clock lease expiry (PR 664, ADR 114), U3 #660 honest fencing javadoc (PR 666, ADR 115), U4 wrapper `synchronized` removal (PR 663), U5 the 0.33.0 docs held PR occurrent-org.github.io#54, U6 #665 fencing design pass (ADR 116 final after four Johan-driven iterations, PRs 667/670/671/673, one-replacement rewrite), U7 lock-document survival shipped INTO 0.33.0 (PR 672). Final main matrix 26/26 green on `f9249f5c3`. **Do not re-litigate:** ADR 113 (per-consumer lock array, listeners outside the lock, refresh does not hand back), ADR 114, ADR 115, ADR 116 as merged (write condition on `CheckpointStorage` itself with the break taken, recipe plus migration guide owed, models stamp their own writes via `CheckpointWriteVersionSource`, token never travels through subscription models, `any()`/`notOlderThan` semantics). **The fence must NOT merge before the 0.33.0 tag exists** (same-release-as-groundwork recreates the mixed-version refusal ADR 116 prevents). **Fencing implementation epic registration input** lives in the ccrace epic file's U6 section: ADR 116's closing-paragraph split, the two-release boundary satisfied by U7 (fence may ship 0.34.0), #668 and #669 adopted as its units (routing comments on both issues), issue #665 stays open and claimed as its anchor; register with `/orchestrator https://github.com/johanhaleby/occurrent/issues/665` in a fresh session, cleanest after the 0.33.0 release. **Release-day additions:** merge held docs PR 54 alongside 49/52/53, fix PR 54's placeholder news date (2026-08-24), `addedTags` conflicts keep-both. Worktree `worktree-mobile-setup-916a9c` left in place detached (three sessions reused it, host behavior worth watching).
- Next action (2026-08-09, ccrace): nothing runs; Johan cuts 0.33.0 (his manual act) with the release-day docs set above. That orchestrator session retired with its checkpoint; the fencing epic registered as ccfence the same morning (next bullet), before the release, with every merge tag-gated.
- **Seventh epic registered 2026-08-09T09:20Z: ccfence, the ADR 116 checkpoint-fencing implementation (#665, plus #668 and #669 routed in by Johan during ccrace).** Unit state in `.context/epics/ccfence.yml` (12 units F1-F11 plus F2b); plan `/Users/johan/.claude/plans/ccfence-adr116-implementation.md`, approved by Johan via structured ask 2026-08-09 after a fresh-context Opus review (13 findings folded in, three of them must-fix: the conformance-suite red-main trap, the 20-implementer compile surface, the Redis infinite-retry hang). Two registration decisions by Johan: the third `CheckpointWriteCondition` case (`ifAbsent`) folds into F2 (the review proved `notOlderThan` cannot close #669, it accepts writes when nothing is stored), and reactor `writeVersion` is `Mono<Long>` empty-means-none. SUPERSEDED GATE: registration tag-gated everything on `occurrent-0.33.0`, then Johan decided by structured ask (the ADR 116 deploy-window hazard quoted to him) that survival and fence ship TOGETHER in 0.33.0, so the tag gate dissolved, Johan holds the 0.33.0 release until the fence units land, and new unit F0 rewrites ADR 116's two-release decision (one-coherent-decision style; F0's prose PR merges only after Johan confirms wording, design-gate rule). Wave order (F0, F1, F2 parallel, chips task_91560d5d / task_42b4ec6d / task_eab370e0 spawned 2026-08-09 ~09:42Z) -> (F2b, F3, F4, F5 after F2) -> F6 -> F7 (merge-gated on F1/F3/F4/F5/F6) -> F8 -> (F9, F10), F11 after F7; F8 closes #665, F9 #668, F10 #669; F11 is now part of the 0.33.0 docs set and must coordinate with ccrace's held docs PR 54 (the news post needs fence content). Release readiness is declared at F8 plus F11 with F9/F10 status stated either way. Work-item monitor `bciv33mhs` armed (v7, repo-wide, MONITOR-READY at 0 open PRs, arming sweep clean); tag-watch `blo36oc8r` stopped as obsolete. Sweep-loop ceiling: 40 ticks or 200k output tokens. Session title: `[ccfence] Orchestrator, ADR 116 fencing implementation`; orchestrator session id `local_fab707d0-ec0d-44d1-8a21-a4c321100134` (derived from the scratchpad path, verify on first worker message). Registration timestamp is the tracker-scan lower bound. ccrace's file, sessions and branches are off-limits to this epic's sweeps.
- **ccfence EPIC CLOSED 2026-08-10 ~01:30Z at state revision 38, 15 units, all DONE and swept, final combined matrix 27/27 green on `831d7d21f`.** The checkpoint fence (ADR 116, rewritten by F0 for the one-release decision, touched up by F12) shipped whole into the unreleased 0.33.0: F1 strategy token (PR 675), F2 write-condition break (676), F2b recipe (677), F3 Mongo (680, probe-verified), F4 Redis (678, retry exclusion mutation-proved), F5 model stamping (679), F6 refusal-never-retried (682, four mutation kills), F7 starter wiring (683), F8 e2e proof (687, closed #665), F10 pin race (685, closed #669), F9 lease-regain resume (690, ADR 0117, closed #668), F12 saga gate + dead parameter (688, closed #684/#686), F13 documented ifAbsent edge (689, closed #681 after Johan's investigate-first reversal). Docs: fence section + news paragraph in held PR occurrent-org.github.io#55 (stacked on #54); "leaf" wording fix merged to docs main (#56) at Johan's request, banned in the johan-writing skill. Both monitors stopped under the lifetime rule; tracker scan clean from the registration cursor. Fleet spend ~5.4M subagent tokens of the 5.5M ceiling (raised twice by structured ask from 600k output-token initial). Do-not-relitigate: ADR 116 as merged including the one-release decision and three-site starter wiring; ADR 0117 (resume rides the checkpoint, fallback is the delegate's own position); #681 stays a documented edge (the BEFORE-based fix is on the issue if a real caller surfaces).
- **0.33.0 release-day checklist (Johan's manual act):** cut the release, then follow `occurrent-org/occurrent-org.github.io#57`, which records the docs merge order and its checks in the docs tracker (49, 52, 53, then 54, then 55; 54's `addedTags` entries point at anchors 49 and 52 define, so 54 must follow them; 55 is stacked on 54's branch so it either goes after a rebase or subsumes 54; placeholder news date 2026-08-24; the read-side-only news intro predates the fence; a pre-existing kramdown blank-line defect at `## Synchronous Subscriptions`). Also: Sonatype Central Usage Center check ~24h after the release against all three limits. Verified 2026-08-10 while writing #57: the docs-repo `.context` claim that 49/52/53 conflict on `addedTags` was wrong, only 54 touches `js/_partials/main.js`, and the fence section had no `addedTags` label until this session added `checkpoint-fencing-blocking` to 55's branch.
- Next action (2026-08-10, ccfence): none, the epic is closed and this orchestrator session retires with this checkpoint; the next epic registers in a fresh session.
- **Eighth epic rv33, COMPLETE 2026-08-10 ~00:05Z, same day as registration (2026-08-09T19:27:04Z).** The review-verdicts remediation before 0.33.0: plan `/Users/johan/.claude/plans/plan-to-fix-this-serialized-storm.md`, state `.context/epics/rv33.yml` (revision 16, 8 units, all DONE and swept). U1 release-docs truth (PR 694 + docs PR 55 branch commit `ef786ac7b`, merged after Johan's wording confirm, two reasoned-decline Copilot threads on the Redis Cluster phrasing left open by design), U2 waitUntilApplied deadline fix (PR 700), U3 reactor replay bridge + rename to `ReactiveReplayAwareMaterializedView` (PR 697, name Johan-approved at the plan gate), U4 reactor CheckpointStorageConformance suite + fresh-key any() case (PR 698), U5 ADR-fidelity pass over ADRs 94/110/111/113/115/116 (PR 695, design-gate merged after Johan's confirm, closed #696), U6 lease-refresh retry cap (PR 701, closed #691, changelog entry for the released-surface change), U7 benchmark coverage (PR 702, closed #692), U8 extractWriteVersion null tolerance (PR 699, closed #693). U1-U5 ran as chip sessions, U6-U8 as worktree-isolated subagents after Johan went mobile (Sonnet/Sonnet/Haiku). Final matrix green at `f4cd7dd99`; release-docs greps zero hits on main and on docs PR 55's branch; tracker scans clean on both repos. **U9 (post-closeout adoption):** Johan adopted #703 at the closeout ask (U2's post-merge Copilot finding on `readOnceBoundedBy`), fixed as PR 704 by a Haiku subagent (swallow only past the deadline, propagation test, temporary-revert verified), squash `28281a3d5`, #703 closed. Also: PR 700's thread carries my too-strong "designed behaviour" reply corrected in a follow-up. Brief-boundary lesson recorded in lessons.md (rename ripples vs file ownership). Both monitors/background waits stopped. Fleet spend roughly 520k subagent tokens plus five chip sessions, well under the 2M ceiling.
- **Ninth epic registered 2026-08-10T10:35:24Z: stepcond, matcher-based flow saga step conditions (#707), TARGET 0.33.0 (Johan holds the release until it lands).** State `.context/epics/stepcond.yml` (2 units, U1 library, U2 docs); plan `/Users/johan/.claude/plans/https-github-com-johanhaleby-occurrent-i-whimsical-glade.md`, approved by Johan via ExitPlanMode after a fresh-context adversarial review (verdict REVISE, all 8 findings folded in) and four structured-ask decisions plus a class-literal shortcut he added at the approval gate. Registration timestamp above is the tracker-scan lower bound. Session title: `[stepcond] Orchestrator, matcher step conditions`. **Coexists with rv33, which is still live (U12-U17): rv33's epic file, units, sessions, branches and monitors are OFF-LIMITS to this epic's sweeps.** Do-not-relitigate (settled by the approved plan): unified `event` leaf naming, `allOf`/`anyOf` composites (DcbCriteria precedent), `join` DEPRECATED and lowered to sugar, monotone conditions only (no `not()`, no absence, the step timeout owns absence), no new `ActionKind` (rollback safety), guarded classic `on<T>` branches NOT lowered to window leaves, no OpenRewrite recipe for the deprecation. **Correction carried into the issue: `join` shipped in 0.31.0, not 0.32.0 as the issue body claims.** The ADR reverses ADR 63's "no dynamic N-of-M joins" non-goal, partially, runtime-varying counts stay out. **ADR number corrected 2026-08-10T10:52Z: NOT 118.** Registration read max+1 from main and got 118, then rv33's U12 merged 0118 to main and its U16 claimed 0119 on an unmerged branch, so stepcond takes 0120 and the worker was told to re-audit across ALL remote branches before writing. The cross-branch audit is the only view that sees a number claimed on an unmerged branch, which is precisely the collision mode ORCHESTRATOR.md already warned about, and a working-tree or main-only check misses it every time.
- stepcond mechanics: work-item monitor `bvw0xv0om` (STOPPED at U1 completion) (replaced `bb7q18gam` at 11:14Z, still repo-wide but post-filtering DELTA lines on branches matching `^rv33/`, since three of the first four wakes were rv33 traffic; openings and closures are still always reported, so discovery is intact, which is the point the skill makes about never narrowing the poll itself), first armed 2026-08-10T10:40Z (v7 pattern, repo-wide, MONITOR-READY at 0 open PRs, so the arming sweep had nothing to act on); U1 chip `task_abdd6422`; #707 claimed (in-progress label plus the routing comment); orchestrator session `local_dc08d709`. Sweep-loop ceiling: 30 ticks or 400k output tokens. No docs-repo monitor by design, U2's branch is held until release.
- Visibility-repair list: (empty, #707's routing comment verified present 2026-08-10T10:35Z).
- **stepcond EPIC CLOSED 2026-08-10T14:10Z at state revision 15, 7 units, all DONE and swept (reopened twice after its first closeout, for the guarded examples and then the docs audit), final main matrix 26/26 success on `573316d4b`.** U1 matcher step conditions (PR 712, squash `9a26411be`, ADR 0120, closed #707), U2 held docs PR occurrent-org.github.io#61 plus a separate unopened release-note branch `docs/707-step-conditions-release-note` on PR 54's tip, U3 three guarded composition examples (PR 713, squash `ee91f9919`) adopted by Johan after U2 reported that only one example was guarded, plus its docs half extending PR 61 in place, U4 a two-line semicolon recast (PR 714, squash `573316d4b`) fixing a writing-gate miss U3 shipped and the docs worker surfaced. Tracker scans clean in both repositories, no monitors running, every worker worktree and branch removed. **Release-day addition: docs PR 61 joins the 0.33.0 merge order in occurrent-org.github.io#57, and the release-note branch `docs/707-step-conditions-release-note` must be merged or cherry-picked alongside PR 54. That branch CONFLICTS with PR 55 on `_posts/news/2026-08-24-0_33_0.md`, both edit the Smaller additions list; resolution is keep-both, all three bullets plus PR 55's separate fencing section. Recorded on issue 57 at 2026-08-10T14:33Z.** Do-not-relitigate: the ADR 0120 decisions as merged (monotone conditions only, no negation and no absence because the step timeout owns it, unified `event` leaf, `allOf`/`anyOf`, `join` deprecated and lowered to sugar, no new `ActionKind`, guarded classic branches deliberately not lowered to window leaves, no OpenRewrite recipe). One item deliberately left for Johan: U1's session `local_abfbd577` is unarchived, because the archive tool prompts and it was not worth interrupting him.
- **stepcond docs set, final state 2026-08-10T15:46Z.** Held for the release: docs PR 61 (step conditions, head `9f0a77d`) and the unopened branch `docs/707-step-conditions-release-note` on PR 54's tip. Held for AFTER the release: docs PR 62 (saga chapter cleanup, head `6109648`), last in the order because it touches the same chapter. `docs/0.33.0-preview` head `081897e` carries the finished combined state for Johan to read. Final content decisions by Johan: a worked branch now precedes the abstract step definition (reusing the guarded `on<PaymentReserved>` snippet, later copy removed so nothing duplicates), `idempotent` stays, `pinned` recast in the auction sentence, and the second `pinned` (documentation links pinned to a version tag) deliberately left because it sits outside the saga chapter. **Two release-day traps, both on docs issue 57, both found by trial merge rather than by reading diffs:** every held branch must be REBASED before merging or it reintroduces the removed either-branches-or-join claim (six also reintroduce wording main already cleaned), and PR 61 plus PR 62 both rewrite the `historyWindow` paragraph in a way git DUPLICATES silently with no conflict, so confirm that paragraph appears exactly once after merging 62. A third mechanism worth remembering: a plain `git merge` into the preview branch produced a criss-cross result that duplicated content with no conflict, so preview updates are applied as exact-text replacements and verified, never merged blind.
- **rv33 FINAL, 2026-08-10: 17 units, all DONE and swept at state revision 48, zero drift, final matrix 4 of 4 green at `701f8bd42`.** U1 to U9 were the review-verdicts remediation (see the bullet below). U10 to U17 came from Johan's questions after that closeout and reshaped the subscription capability API. **The naming arc:** six interfaces named `*SubscriptionModel` or `*MaterializedView` turned out to be capability mixins that never implemented those types, found by a mechanical audit over 581 public types (head noun is another public type it does not extend). They became `RepositionableSubscriptions`, `ReplayAwareSubscriptions`, `IntrospectableSubscriptions`, `SubscriptionModelWrapper`, `ReplayAware` and `ReactiveReplayAware` (U10, PR 705). U12 (PR 708) added the `SubscriptionModelCapability` root so the lookups take a real type instead of `Object`, and renamed `of` to `findIn` because the method searches a wrapper chain and can come back empty while `of` promises construction. U16 (PR 710) added instance-side `capability(..)` and `hasCapability(..)`, which as a side effect collapsed the three static `findIn` helpers into one-line delegates, so the chain walk now exists in exactly one place. U14 (PR 711) renamed `AppliedPositionStore` to `AppliedProjectionPositionStore` to match how `SagaStateStore` and `SnapshotStore` name their domain. ADRs 118 and 119 record the design, including why the event store's `EventStoreCapability` is an enum while this is an interface, and reserving `SubscriptionModelFeature` for any future declared-capability enum. **Do not re-open:** the `of(Object)` to `findIn(SubscriptionModelCapability)` shape, the decision that `hasCapability` earns its place beside `capability` (a boolean question should not force a caller to mention the value), the DSL class `org.occurrent.dsl.subscription.blocking.Subscriptions` keeping its name, and the mixins keeping their own names rather than becoming `*Capability`.
- **rv33 lessons that cost real time.** Three of eight workers backgrounded a Maven run and stalled waiting for a result nothing would deliver, despite the brief forbidding it, so budget one recovery round trip per unit whose verification runs long and treat "waiting for" in a completion notification as the tell. A wall of `Failed to load ApplicationContext` errors was the Colima VM running out of disk, not the branch (see [[reference-occurrent-mongo-fixed-port-contention]]). A rename brief must name the historical surfaces it must NOT touch, because "check the whole repository" made one worker rewrite a 2021 news post. And a recipe test that declares the renamed type under its NEW name proves nothing about a real upgrade, which had to be sent back once and was then generalised correctly by two later workers.
- **rv33 release-day correction, 2026-08-10:** merging docs PR 60 into docs `main` left held PR 49 reporting CONFLICTING on GitHub even though git merged it cleanly against that exact head, an unexplained disagreement between the two. Resolved by merging `main` into `docs/replay-batching` and pushing, after which GitHub reports MERGEABLE and CLEAN. Verified afterwards that PR 49 still carries its own 70 insertions of replay-batching content and that the metadiscourse cleanup survived. The lesson for release day: after ANY merge into docs main, re-check every held PR's mergeability on GitHub rather than trusting a local `git merge` probe, because the merge button is what actually gates the release.
- **rv33 release-day set, 2026-08-10:** seven held docs PRs merge in the issue 57 order, 49, 52, 53, 54, 55, then 58, then 59. PR 58 carries the capability renames, 59 documents the capability system and is branched from 58, 52 carried the `AppliedProjectionPositionStore` rename and is now PULLED from the set by the withdrawal (see the vgpr bullet), 55 carries the fence. Docs PR 60 (prose cleanup) already merged to docs main and is NOT part of the held set. After any merge into docs main, re-check every held PR's mergeability on GitHub, not with a local git merge (see the correction below). Original addendum: docs PR 55's branch now carries `ef786ac7b` (the storages-evaluate correction) on top of the fence content, docs issue 57's merge order is unchanged; changelog and migration guide on main are corrected, so the release notes Johan cuts from are now truthful.
- **apirev (post-0.31.0 API review remediation), COMPLETE 2026-08-07 ~13:25Z.** All 18 units merged and swept (last PR #615). State `.context/epics/apirev.yml`, meta-plan `/Users/johan/.claude/plans/api-review-remediation-0-32-0.md`. Full Wave A gated the 0.32.0 tag, satisfied.
- Standing merge authority (green plus resolved threads, a pending matrix sibling tolerated); visible-disagreement rule: a reasoned-decline Copilot thread stays open and does not block a merge, arc-wide.
- Won't-fix register, do not re-open: DCB invocation dispatch stays unfolded; ADR 76's stale "ships no overriding implementation" consequence stays; the critique's `replaysHistoryToANewSubscription` default claim and its `0b5beb766`-added-the-guard claim were both REJECTED on verification; the `catchup = NONE` dedup difference is a javadoc note at most.
- ADR 107 (A4, #581) settled, do not relitigate: a minor TCK release may add suites and tighten assertions; the fixture SPI never breaks at source level in a minor; no `@since` tags; no skip mechanism (ADR 77 rules b and d stand); the subscription wait budget is uncapped via `SubscriptionModelFixture.deliveryTimeout()`, three fixed budgets stay on purpose.
- Correction found mid-epic: not everything under review was unreleased, check a unit's own types against tag `occurrent-0.31.0` before assuming a change is free.
- **archrev (post-0.31.0 architecture review remediation), COMPLETE 2026-08-08 ~16:45Z (final closeout, 20 units, zero drift).** State `.context/epics/archrev.yml`, meta-plan `/Users/johan/.claude/plans/users-johan-test2-handoff-state-machine-crystalline-llama.md`, milestone 2 "arch review remediation", issues #616 to #625. Wave A (T1 to T6, gated the tag) complete 2026-08-07 ~18:45Z; Wave B (post-release) complete 2026-08-08.
- Won't-fix register, do not re-open: the TCK ratchet strictness stays as decided (ADR 77/107); the saga document-local outbox stays designed-not-built (ADR 63/64/76/81); finding 3's cross-model readiness exploit is unreachable (the per-stack `registeredIds` guard).
- Deliberate, not a bug, do not "fix": `ReactiveHandover` writes the catch-up marker before draining the live buffer where `BlockingHandover` writes it after (`ReactiveHandover.java:52-61`); the reactor projection DSL's per-event `subscribeOn(boundedElastic)` wrapping is load-carrying, not redundant (B6/#639, quantified in `benchmark/ReactorProjectionHandoffBenchmark`, do not re-propose removal).
- B2 (#647) left one open measured reasoned decline (dedup, 14% regression in the common case).
- Post-release obligation: merge held docs PR occurrent-org.github.io#49 and add its `addedTags` entry at the next release.
- **vgpr (version-gated projection reads, #361), SHIPPED 2026-08-08 THEN WITHDRAWN 2026-08-11 BEFORE IT EVER RELEASED.** State `.context/epics/vgpr.yml`, plan `/Users/johan/.claude/plans/eager-exploring-parasol.md`. Unit A shipped as PR #654 (squash `1dc5d70e0`) and ADR 0111 recorded the design, but the cdx33 review found `waitUntilApplied` reading a monotonic maximum as a completed prefix, which requires position-ordered delivery that no Occurrent delivery source provides. Withdrawn whole by PR 739 (squash `0f3980c20`): `AppliedProjectionPositionStore`, both recording wrappers, both Mongo stores and `@Projection`'s `recordAppliedPosition` are gone from 0.33.0, ADR 0122 records why, ADR 0111 carries a withdrawal blockquote, **#361 is REOPENED** and #740 supersedes it with the append-identity design. Nothing of this feature is in the library now, so treat any reference to it below as history.
- ADR 0111's decisions are MOOT, not do-not-relitigate: the types they governed no longer exist. Kept only as input to #740, which must not reuse the completed-prefix premise. The naming, `Backoff` pacing and per-store retry choices were sound and worth reusing if a successor API needs them.
- **Docs PR occurrent-org.github.io#52 must NOT merge at this release.** It documents the withdrawn applied-position feature, so it is retargeted to hold for #740 or closed, and it leaves the issue 57 merge order. Unit UDOCS owns that plus cutting the `AppliedProjectionPositionStore` mention from the 0.33.0 news post on PR 54's branch.
- **What a subscription refusal means (ADR 106, #580, 2026-08-07).** Six conditions under sealed `SubscriptionRefusedException` in `subscription/core` (`DuplicateSubscriptionIdException`, `UnknownSubscriptionException`, `SubscriptionNotRunningException`, `SubscriptionAlreadyRunningException`, `UnsupportedSubscriptionFilterException`, `UnsupportedStartAtException`), root `IllegalArgumentException` so old `isInstanceOf(IllegalArgumentException.class)` assertions still pass.
- Rule to reuse: caller-fixable by passing something else is an argument exception; nothing a caller passes fixes it is a different exception, e.g. `DataFieldReader.refusing()` is `UnsupportedOperationException` (ADR 93).
- Stays, must not be re-litigated: the ISE refusals from ADR 104/105 (failed catch-up, a feed with no projection, a cancelled replay, a competing consumer whose lock another node holds) stay `IllegalStateException`; ADR 90's one-consumer refusal stays plain IAE, it refuses a different id.
- `UnknownSubscriptionException` is a behavior change, not just a type: pausing or resuming an id no model had seen used to claim it was not running or not paused; a wrapper must answer for its own ids plus its delegate's.
- Docs deliberately not touched: `docs.md:5831` stays accurate (the new type extends IAE); naming it there is deferred to a post-tag docs pass to avoid a 14th conflicting branch edit.
- Rebase debt: ADR 106 links ADR 104 and ADR 105, which exist only in PRs #596/#597; both need an amendment blockquote answering the question they deferred.

Last updated: 2026-08-08. 0.32.0 IS released, tag `occurrent-0.32.0` on `601fc6f10` (0.31.0 on `b97856f24`), so everything up to and including 0.32.0 is shipped API and an incompatible change to it needs a recipe plus a migration note. New work goes under a fresh `### Changelog next version` heading.

One external contribution is open: `dcb-events/dcb-events.github.io#75` adds Occurrent to the dcb.events libraries page under Java and Kotlin, awaiting the maintainer's decision; their `main` also carries Sequence Position revisions (issue 27) worth re-reading against `dcbposition` semantics if it lands.

The TCK (#393) is DONE, see the compact record under Planned Work. ADR 77 records its shape.

This file is a durable map: architecture, conventions, load-bearing decisions, invariants, and how to verify. It is NOT a change log. Shipped work lives in git history, `changelog.md`, and the ADRs under `doc/architecture/decisions` (NOT `doc/adr`, which is what the `/adr` skill defaults to).

Never derive the next ADR number by counting files: take max+1 from the first-line numbers. CI enforces uniqueness now (`.github/workflows/adr-numbering.yml`), the cross-branch case the `/adr` skill's own free-number check structurally cannot see, since it only sees one working tree.

ADR 70 collided (the retry ADR and the saga-observation ADR both claimed it): resolved by moving retry to 0074, decided by first-claimant plus held-branch count, not by total churn. ADR 99 collided too (`0099-a-reactive-testing-twin-that-stops-every-model.md` merged to main first): the losing branch renumbered to 0100 on rebase rather than contest a number main had already settled.

Whenever an ADR collision recurs, audit the docs branches with `git grep -l <adr-filename> <branch> -- pages/docs/docs.md` over every branch, a regex sweep for the number undercounts.

Keep this file short: it is re-read every session, so a growing file is the dominant token cost. Prune shipped narration into git rather than accumulating it here.

**Sonatype Central publishing limits, check ~24h after the next release.** Per organization: 80 MB per release, 1 000 files, 7 releases; soft warnings today, enforcement planned from 2026-10-01 for sustained overage (existing artifacts and downloads unaffected).

Occurrent's many modules make file count the concern (every jar, pom, sources/javadoc jar, signature and checksum counts); usage stats update every 24 hours, so check Central's Usage Center for the Occurrent organization about a day after the release, against all three limits.

If one release burns most of the 1 000-file allowance, contact Sonatype support for a higher limit or OSS exemption.

## Architecture Summary

Occurrent is a Maven multi-module JVM event-sourcing library centered on CloudEvents, built as small composable libraries rather than a framework: domain models stay independent of Occurrent.

Root modules (`pom.xml`): `test-support`, `tck`, `testing`, `eventstore`, `subscription`, `cloudevents-extension`, `common`, `application`, `dsl`, `framework`, `deadline`, `library` (currently `hederlig`), `bom`, `example` (active-by-default `examples-module` profile), `rewrite`.

Three homes for test code, and they are not interchangeable (ADR 0077, ADR 0082): `test-support` is unpublished fixtures for this repo's own tests (`ReplicaSetReadyMongoDBContainer`, `MongoTestDatabase`, domain fixtures; its `FlushMongoDBExtension` is gone, every call site now uses the published `OccurrentMongoFlush`), `occurrent-tck-*` is published for someone implementing an Occurrent SPI outside this repo, and `occurrent-testing-*` is published for someone testing an application built on Occurrent. `testing/junit-jupiter` holds the mechanism and depends only on JUnit plus `occurrent-subscription-api-blocking`, `testing/spring-boot` is a thin wrapper (`@EnableOccurrentTesting`) over it. Anything the Spring leaf can do must be expressible against the neutral one.

Layering:

1. `common` + `cloudevents-extension`: reusable primitives (`Condition`, `Filter`, retry/time utilities, Mongo filter/sort conversion, stream metadata `streamid`/`streamversion`). `common/eventstore-capability` is a deliberately tiny dependency-free module holding just `EventStoreCapability` and `EventStoreCloudEventExtensions`; it exists because `common/filter` needs the enum for `Filter.capability(...)` while `eventstore-api-common` (its original home) already depends on `common/filter`, so it had to move somewhere both reach without a cycle.
2. `eventstore`: persistence contracts and implementations. Blocking `EventStore` composes read/conditional-write/existence; wider capabilities are optional interfaces (`EventStoreQueries`, `EventStoreOperations`, `ReadEventStreamWithFilter`).
3. `application`: adapts domain events to CloudEvents via `CloudEventConverter`/`CloudEventTypeMapper`, orchestrates commands via `ApplicationService`/`GenericApplicationService`.
4. `subscription`: reacts to event streams. Blocking + reactor APIs, Mongo/native/Spring/Redis/in-memory adapters, and durable/catchup/competing-consumer/push wrappers.
5. `dsl`: convenience layers over the core APIs for Kotlin/Java (decider, dcb-dsl, view-dsl, projection-dsl, snapshot-dsl, saga-dsl, subscription/query DSLs).
6. `framework/spring-boot-starter-mongodb`(`-reactive`): auto-configures the Mongo stack, gated by classpath presence (mirroring Spring Boot's own split). The annotation machinery it used to own now lives in the store-neutral `framework/spring-boot-autoconfigure/{common,blocking,reactor}` (#409, ADR 0072), so the starters hold only `OccurrentMongoAutoConfiguration`, `EnableOccurrent` and the Mongo implementations of the store-default seams.

### Module boundaries

- `eventstore`: `api/{common,blocking,reactor,dcb}`, `inmemory`, `mongodb/{common,dcb-common,native,spring/blocking,spring/reactor}`.
- `application`: `service/blocking` (+ `dcb`), `command-composition`, `command-dispatch` + `command-dispatch-annotation` + `command-dispatch-dcb`, `cloudevent-converter` (Jackson 2/3, XStream), `cloudevent-type-mapper`.
- `subscription`: `api` (blocking/reactor contracts, plus the `internal` handover engines), `core` (shared checkpoints, filters, start positions, `SubscriptionFilterMatcher`, and `org.occurrent.subscription.internal`: `BoundedIdCache`/`ReplayFilters`/`HandoverOptions`/`HandoverMessages`), `inmemory`, `mongodb`, `redis` (Spring checkpoint storage), `util` (durable, stream-catchup, dcb-catchup, competing-consumer, reactor wrappers), `push/{blocking,reactor}`, `synchronous`.
- `dsl/saga-dsl`: `common` (`Saga` + both builders + flow layer, `SagaStateStore` + `InMemorySagaStateStore`, `SagaEnvelope`, internal `SagaExecutionSupport`), `blocking` (`SagaRunner`, `SagaRunnerConfig`, `CommandDispatcher`). Timers poll the state store, so there is no dependency on the `deadline` module.
- `framework/spring-boot-starter-mongodb` wires `MongoTransactionManager`, `SpringMongoEventStore`, checkpoint storage, lease competing-consumer, `SpringMongoSubscriptionModel -> Durable -> Catchup -> CompetingConsumer`, the DSLs, `GenericApplicationService`, Jackson 3 converter config, plus `@Saga` registration and `SpringMongoSagaStateStore`.

### Primary execution flows

- **Command:** `ApplicationService.execute(streamId, ExecuteOptions, domainFunction)` -> `GenericApplicationService` resolves an optional `ExecuteFilter` via the converter (filtered reads require `ReadEventStreamWithFilter`, else fail fast) -> read stream -> convert to domain events -> run pure domain function -> convert back -> write at expected version -> optional post-write side effects.
- **Mongo blocking write/read:** `write` validates `WriteCondition` (for `anyStreamVersion` it materializes the stream so retries reuse events); inside a transaction it reads the current version, validates, maps to documents with stream metadata, inserts, translates duplicate/write exceptions, returns old/new versions. Reads pin to the current version to avoid skew.
- **Subscription:** `Subscribable.subscribe(subscriptionId, filter, startAt, action)` -> Mongo implementations use change streams + lifecycle-managed listener containers -> `DurableSubscriptionModel` persists the checkpoint after a successful action -> `CatchupSubscriptionModel` replays history for a time/position start before handing over to the live model -> Spring Boot wraps in durable/catchup/competing-consumer layers.

## Conventions And Patterns

- **Occurrent is a public open-source library, so its callers are unknown and unobservable.** Never conclude "every real usage does X" from the repo's own examples and tests; they are not the user population. Design public APIs for arbitrary external callers: prevent a footgun at the type level (or fail loud) rather than assuming discipline or relying on docs. This drove #338, where a single-instance projection was made impossible to construct with a non-String id (private constructor reachable only through String-typed builders) instead of documenting the constraint. When weighing an API-safety tradeoff, treat the unobservable caller as real.
- Java 21 baseline (`maven.compiler.release=21`, Kotlin `jvmTarget=21`, CI matrix `[21, 25]`). Java and Kotlin coexist in most modules (root build-helper adds `src/main/kotlin` + `src/test/kotlin`).
- Public APIs are small capability interfaces, not monoliths. Validate eagerly (`Objects.requireNonNull` / `IllegalArgumentException`); prefer static factories/builders for fluent APIs; Apache 2 headers on source.
- Nullness uses JSpecify (`@NullMarked`, `@Nullable`) in newer APIs, not uniformly across old code.
- **Changelog (`changelog.md`):** unreleased changes go under `### Changelog next version`, never a versioned `### X.Y.Z (date)` section. The maintainer assigns version and date only at release; never invent one. Update it after any change affecting behavior, public API, build/runtime, or user-facing capability; skip it for doc-only edits.
- **Unreleased capabilities get no "change" entries (PR #300).** Users never had the old behavior to change from, so dev-churn on a still-unreleased capability (everything in the unreleased inventory below) stays out of `#### Changes`/`#### Details` and lives only in `#### Highlights`. Feature-announcement bullets and changes to already-released behavior go in `#### Changes`/`#### Details` as normal. **DCB shipped in 0.30.0, so it is no longer exempt:** a change to released DCB behavior is an ordinary `#### Changes` entry. This rule was written while DCB was still unreleased, and the older phrasing that named DCB as the standing example outlived that, so do not reintroduce it.
- Kotlin: avoid extension names colliding with Java members (ADR 0012); typed execute filters namespaced under `ExecuteFilters` (ADR 0013); keep Java contracts honest (`? extends E`) with localized Kotlin bridge casts only where needed. Run at least `test-compile` on affected modules after Kotlin-around-Java-generics changes: Kotlin inference can fail even when Java compiles.
- **New publishable leaf module:** must be registered in `application/pom.xml` (or its family aggregate) plus the BOM, and publishes to Central by default. Companion to the aggregate-POM `excludeArtifacts` rule. See AGENTS.md.
- Testing: JUnit 5 + AssertJ dominant, jqwik for a few property tests, Awaitility for async/subscription/deadline. Unit and integration tests both run under Surefire (no Failsafe split). Docker/Testcontainers (MongoDB, Redis) is common; nothing binds a fixed host port, and every Mongo container owns its own database (ADR 97). Testcontainers gotchas live in `AGENTS.md` (Testing); machine-specific Docker-socket/VPN details in `colima.md`/`nordvpn.md`.
- **A `common` module holds only stack-neutral types (2026-07-25, PR #386).** A blocking-only port must not sit in a `common` module the reactor module also depends on, or the reactor stack imports an interface it can never call and the blocking half gets the unprefixed name by accident of placement. `TransactionExecutor` and `SynchronousEventDispatcher` live in `application/service/blocking`, `SnapshotStore`/`InMemorySnapshotStore` in `dsl/snapshot-dsl/blocking`. `SnapshotSupport` is split on the same line: the store-taking methods in the blocking module's `internal`, the pure `resolveBase`/`isRedelivery`/`requireInt` in `common/internal` because `ReactiveSnapshotSupport` reuses them. **`SnapshotPolicies` is the deliberate exception** and stays duplicated in `blocking` and `reactor`: its one method `whenTerminal(Decider)` is decider-aware, and ADR 0061 keeps it out of common so a `SnapshotView`-only consumer needs no `occurrent-decider` on its classpath. Collapsing it (attempted and reverted) adds a non-optional decider dependency to `snapshot-dsl-common`. Do not regress either rule.

### Restart-test pattern (reusable)

Annotation attributes (`startAt`, `resumeBehavior`, `startupMode`) are compile-time constants, and the dynamic `StartAt`/`DcbStartAt` supplier encoding `resumeBehavior` is evaluated ONCE inside `subscribe(...)` at bean post-processing. Pausing/resuming a running model (`stop()`/`start()`) never re-invokes it, so it cannot demonstrate replay-vs-resume. To test `resumeBehavior`, close the Spring context and boot a fresh one (`SpringApplication.run(...)`) against the same durable MongoDB data (same URI, same database) so the annotation is reprocessed like a real restart. See `DcbSubscriptionResumeBehaviorAnnotationMongoTest` and siblings: a `FirstBootApplication` and `SecondBootApplication` register the same subscriber classes so the second context gets a fresh instance.

## Public API Shape And Load-Bearing Decisions

Terse map of what exists and which choices must not be silently reopened. Detail lives in the named ADR.

### Event store and DCB

- **DCB ships on all four stores** (in-memory, Spring blocking, native, Spring reactive) as an optional capability layered on existing CloudEvent storage (extensions `dcbtags`, `dcbposition`), not a parallel event model. Shared Mongo DCB code in `eventstore-mongodb-dcb-common`; capability set is `EventStoreCapability`. ADRs 0017-0022. **The DCB API surface is frozen for release per ADR 0053**; check that ADR before changing a DCB public type or signature.
- **Capability mode is part of `EventStoreConfig`** (`STREAM`, `DCB`, or both; ADR 0018). It controls index/support-collection creation and runtime API guards, NOT the CloudEvent document format. Occurrent creates only missing indexes/collections for enabled capabilities and never removes indexes. DCB-only writes still assign per-storage-stream Occurrent stream versions (needed if `STREAM` is enabled later). DCB-only Spring Boot must not expose a stream `ApplicationService` or wrap subscriptions in `CatchupSubscriptionModel`, but still exposes `DomainEventQueries`.
- **Capability-boundary contracts, both directions (#280/#282/#283, ADRs 0049-0051).** On a STREAM+DCB store: `DcbCriteria` reads exclude stream-only events via a `dcbTags`-exists guard backed by a sparse index (#280); `StreamCatchupSubscriptionModel`/reactor twin (`@StreamSubscription`) exclude DCB events via `Filter.capability(STREAM)` ANDed into both the replay read and the live handover filter (#282); `@Subscription`/`Subscriptions` are the capability-neutral default, routed through `AgnosticSubscriptionFilter`, delivering both kinds filtered only by type over the unified global position and resuming via `GlobalCheckpoint` (#283). `@DcbSubscription` was untouched (already correct via `isDcbEvent` + `DcbCriteria` post-filter). The write-time invariant making the Mongo capability filter sound: `write(...)` rejects any event carrying the `dcbtags` extension, in all four stores.
- **Composable `DcbReadOptions` (ADR 0056, unreleased for 0.31.0).** A position range, `Direction {FORWARD,BACKWARD}`, non-negative `skip`, and optional positive `limit` compose through `fromBeginning()` or another range factory followed by `forwards()`/`backwards()`, `skip(int)`, and `limit(int)`. Skip is direction-relative and runs before limit. **Invariant: a `DcbEventStream` is always ascending by position regardless of options, and direction, skip, and limit NEVER affect the `DcbConsistencyToken`** (it reflects the whole matching set, so a partial read still guards an append against any later match). Mongo may execute a backward selection as descending sort + skip + limit, then reverse the result. Large Mongo skips can be expensive.
- **`DcbDecider<C,S,E>` couples a decider with its boundary and tags (#289, ADR 0052).** A record wrapping a `Decider` + `Function<C, DcbCriteria>` (null when the decider does not apply) + `TagGenerator<E>`. Composes like `Decider`: composed criteria is `DcbCriteria.anyOf` over children recognizing the command, composed tags are their union. `DcbDecider.criteriaFor(command|commands)` resolves the boundary, rejects an unrecognized command, and requires one shared boundary per execute. **Tagging has two complementary models with strict precedence in `GenericDcbApplicationService.addTags`:** per-execute (`TagGenerator` on `DcbExecuteOptions`, what a `DcbDecider` uses) first, then the optional global `TagGenerator`, else throw. It never appends untagged.

### Deciders, snapshots, projections

- **Java decider facades:** `DeciderApplicationService` (stream, `dsl/decider`) and `DcbDeciderApplicationService` (DCB blocking + reactor, `dsl/dcb-dsl`) wrap an application service and expose `execute(command, decider)` plus `executeAndReturnState`/`Events`/`Decision`. Needed because the Kotlin `execute(command, decider)` extensions are `inline`/`reified` and so uncallable from Java. The decider's event type must equal the service's; widen with `Decider.adapt`/`DcbDecider.adapt`. One uniform `execute*` matrix across all four facades (#342).
- **`DcbCriteriaBuilder` is Kotlin** (`dsl/dcb-dsl/common`, ADR 0066) so `type`/`types` are reified MEMBERS with the base event type inferred from the class type parameter. An extension on the Java class cannot do this: the base would become a function type param and Kotlin's all-or-none explicit-type-arg rule would force `types<A,B,DomainEvent>()`. API: reified `type<A>()`/`types<A,B>()`/`types<A,B,C>()` (the multi-arities carry `@JvmName types2`/`types3` since both erase to `types()`), `KClass` forms, and a `criteria(boundary: DcbCriterion)` overload on `DcbDomainEventQueries`/`DcbSubscriptions` that seeds the builder so `type`/`types`/`tags` refine the boundary while `all`/`anyOf`/`tagsAnyOf` throw when seeded. **The boundary param is `DcbCriterion` (a single alternative), so there is no AND-into-OR** and ADR 0032 is unchanged. `typeOf<T,E>` is deprecated with no OpenRewrite recipe (a recipe cannot drop a type arg). ADR 0012's same-name rule no longer applies (overloaded members, not extensions).
- **Snapshots (ADR 0061, reshaped by ADR 0069).** Opt-in replay optimization: an execute folds only events written after a saved state. Works from a `Decider` (decision state) or a plain `View` (deciders-free), stream and DCB, blocking and reactor. Modules `dsl/snapshot-dsl/{common,blocking,reactor,mongodb-spring-blocking,mongodb-spring-reactor}`. Core seam is additive: `ExecuteOptions.fromStreamVersion(long)` and `DcbExecuteOptions.fromPosition(long)` (a state-agnostic suffix read, so the ONE execute path with its retry/transaction/synchronous-dispatch is reused, not forked), plus `Decider.evolve(S, List<E>)`. Capability `SnapshotStore<S>` and `InMemorySnapshotStore` live in `blocking`, and `SpringMongoSnapshotStore`/`ReactiveSpringMongoSnapshotStore` each have their own `mongodb-spring-*` module (#386); trigger `SnapshotPolicy` (`everyNEvents`/`onEvent`/`whenState`/`always`/`never`/`or`/`whenTerminal(decider)`), never on the read path. **Current API shape: a global facade taking only shared infra, plus per-aggregate `from(...)` specs passed per call** (`new SnapshotDeciderApplicationService<>(applicationService)`, `SnapshotViews.create(eventStore, converter)`; then `snapshots.execute(id, cmd, SnapshotDecider.from(decider, store, options))`, `views.readState(id, SnapshotViewSource.from(view, store))`). The per-command Kotlin `SnapshotExtensions.kt` are gone. `@Snapshot` works on both stacks for stream (keyed per stream) and DCB (per boundary via `DcbSnapshotKeys.canonicalKey`, overridable), maintained through the `@Projection` catch-up/durable infra; the reactor path uses `ReactiveSnapshotStore` since a reactive app has no blocking `MongoOperations`. **Write-path consistency comes from `@Snapshot(mode=SYNCHRONOUS)` or a synchronous subscription folding inside the write transaction, NOT from a `TransactionExecutor` on the executor** (the executors do not enroll the save in the write transaction). **Why DCB snapshots key on the resolved criteria and not the decider (ADR 0061 amendment, #384):** a decider is a per-type constant, so decider-keying collapses all instances into one slot; a criteria change is a boundary change whose altered event set is exactly what should invalidate the snapshot; `schemaVersion` guards state shape, not the folded event set. Closing-the-books is a policy plus a documented pattern (model the closing balance as a real domain event that opens the next period, archive priors with `deleteAllEvents`), not a module. Example `example/snapshot/closing-the-books`.
- **Projection DSL (issue #194, ADR 0058).** Read-side mirror of `DcbDecider`, in `dsl/projection-dsl/{common,blocking,reactor}`. `Projection<S,E,ID>` = `View<S,E>` + `id: Function<E,@Nullable ID>` + `eventTypes` + optional `@Nullable Filter`; `DcbProjection<S,E,ID>` adds a `DcbCriteria` boundary. Built with a per-event-type handler builder producing a reused view-dsl `View`; the fold no-ops on unregistered types and the handler keys drive the subscription filter. Runners (`ProjectionRunner`/`DcbProjectionRunner` + reactive twins, plus Kotlin extensions) create the subscription/query AND materialize the read model across stream+DCB, blocking+reactor, in three delivery modes: async subscription, synchronous read-your-writes, and on-demand pull. **Store binding stays out of the descriptor** (runner overloads take `MaterializedView` | `ViewStateRepository` | `MongoOperations`). Two decisions: **DCB criteria is read verbatim, never AND-ed with handler types**, because intersecting types into an OR-of-alternatives risks `MatchAll`-collapse semantics and the fold already no-ops, so over-reading costs only a few no-op folds; and **reactor has no reactive `ViewStateRepository`** (a view-dsl follow-up), so the reactor push runner offers a reactive `(E)->Mono<Void>` primitive plus a blocking-store convenience bridged on `boundedElastic` (off-thread, NOT read-your-writes, documented). **The on-demand pull fold lives in Java** (`Projections.project`, per stack, three shapes: singleton, keyed-by-instance, DCB) **and the Kotlin `queries.project(...)` extensions are thin delegates to it** (#453), so the selector derivation and the keyed guard exist once. The reactor Kotlin pull returns `Mono<S & Any>` with `S` unbounded: the state may be nullable, and a null fold result becomes an empty completion, but Kotlin will not let a `Mono` be declared over a nullable type, so the non-null element is stated with an unchecked cast (same shape as `SpringMongoViewExtensions`). Example `example/projection/projection-dsl`.
- **`@Projection` (ADR 0059).** Declarative persistent read models: marks a factory method returning a `Projection`/`DcbProjection`; registered by the annotation BPP via `SmartInitializingSingleton` (post-refresh scan, so the factory runs once collaborators are wired). Subscribes through the framework catch-up + durable + competing-consumer composite, so catch-up is a property of the injected model, not the DSL. Dispatch on descriptor type. **Store resolution order: `store()` bean, else unique `MaterializedView`, else `ViewStateRepository`, else Spring Data `CrudRepository`, else zero-config Mongo (blocking only).** `mode=SYNCHRONOUS` gives read-your-writes and is mutually exclusive with catch-up start knobs; duplicate ids are rejected at startup. Reactor v1 deliberately has no zero-config Mongo default (no blocking `MongoOperations` in a reactive app) and no DCB-synchronous mode. Prefactor: `ResumeStartPositions.replayThenResume(+Dcb)` is public and Spring-free in `subscription/api/{blocking,reactor}`, reused by the registrar and usable programmatically without the starter.
- **Annotation types are shared, not per-annotation.** `ResumeBehavior`/`StartupMode` (#330, ADR 0060) and later `StartPosition`/`Capability`/`Mode` (#342, ADR 0060 amendment) live as top-level `org.occurrent.annotation` types. `@Projection`/`@Snapshot` use `startAtGlobalPosition` (renamed from `startAtPosition`). Breaking for released 0.30.0 callers, staged via `MigrateOccurrentRenames_0_31`/`UpgradeToOccurrent_0_31` plus `doc/migration/upgrading-to-0.31.0.md`.

### Subscriptions: synchronous, push, and feeds

- **Synchronous subscriptions (ADR 0057).** A subscription can be declared *synchronous* so its handler runs on the writer thread before `execute` returns, optionally in the write transaction. The decision lives with the subscription (a separate `@SynchronousSubscription`, no per-`execute` flag), mirroring Axon's Subscribing-vs-Tracking split. Register-only `SynchronousSubscriptionModel implements Subscribable` (no lifecycle/startAt/replay). The app service holds an optional `SynchronousEventDispatcher` it calls after a write (defined in `application-service-common`/`-reactor`, so the app service does not depend on the subscription layer above it) plus an optional storage-neutral `TransactionExecutor` defaulting to `noTransaction()`. After a write it re-reads the just-written events enriched with streamVersion+position (stream: tail read; DCB: the appended global-position block via `DcbReadOptions.between`) and dispatches, **guarded so there is zero cost when nothing is registered**. Spring routes `@SynchronousSubscription` through the handler's Spring proxy (resolved lazily) so handler-side `@Transactional` composes; the starters wire a `SpringTransactionExecutor` by default. No-Spring atomicity via `NativeMongoTransactionExecutor` + a thread-bound `ClientSessionHolder` that the native store's write/append/read join. **Fan-out isolation depends on the transaction, and the model is TOLD which regime it is in (ADR 57 amendment 2026-08-04, closes #504).** `TransactionExecutor`/`ReactiveTransactionExecutor` gained `isTransactional()` (reactive returns `Mono<Boolean>`, since the transaction lives in the subscriber context) and the app service passes it to `dispatch(List, boolean)`, **the dispatcher interfaces' ONLY dispatch method: the one-arg form was deleted from both interfaces on purpose, so a third-party dispatcher gets a compile error rather than silently keeping the stranding behaviour it alone can fix.** A `default` delegating overload was tried first and reversed before release, because it needed a warning paragraph in two javadocs, the changelog and the migration guide to paper over a silent wrong answer. The models keep a one-arg `dispatch(List)` as a CLASS method (tests, in-memory write listener), still fail-fast. **`isTransactional()` is answered per call, not fixed per executor:** both Spring executors read the live state (`TransactionSynchronizationManager.isActualTransactionActive()`, and the reactive twin the same via `forCurrentTransaction()`), because a caller-supplied `TransactionTemplate`/`TransactionalOperator` on `PROPAGATION_NOT_SUPPORTED`/`NEVER` opens nothing and a fixed `true` would strand handlers. `NativeMongoTransactionExecutor` keeps a fixed `true`, correctly, since it always opens or joins a session and takes no propagation. Transactional means fail fast (the write rolls back, and running the handlers behind the failure would only do discarded work with possible non-datastore effects). Non-transactional means `routeIsolated`: every handler is offered every event, one failure rethrown as-is, several as the first with the rest in `addSuppressed`. **`isTransactional()` defaults to FALSE on purpose,** same reasoning as `Consumers.ONE`: erring toward wasted work beats erring toward loss, and the three shipped executors override it to true. **A handler that failed is skipped for the REST of that batch** because event 2 would update its read model without event 1, so isolation is between handlers and never within one handler's own event order. ADR 90 deferred this and now points at the ADR 57 amendment. Only `RuntimeException` is caught on the blocking side, so an `Error` still propagates.
- **Push subscription model (#345, ADR 0062).** Lets the Projection DSL be fed from an external source (RabbitMQ, Kafka, Spring events, HTTP) instead of a Mongo change stream. `PushSubscriptionModel` (blocking + reactor) is a register-only `Subscribable` driven by `accept(CloudEvent)`, no lifecycle/startAt/checkpoint/replay; the `Pushable` interface extracts `accept`. The register-and-route machinery is shared via `RegisteringSubscribable` in `subscription/api/*`, so `SynchronousSubscriptionModel` and `PushSubscriptionModel` are thin subclasses differing only in ingestion entry point. Transport-neutral: no broker dependency, and pushed events must carry the occurrent extensions. **Change-stream models are deliberately NOT pushable** (double-delivery + checkpoint corruption); one-bean-both would be a routing composite, not built.
- **A push catch-up replays off the startup path (ADR 91, 2026-08-02).** On the blocking stack the replay runs in a `FutureTask` on a virtual thread named `occurrent-push-catchup-<id>`, and `waitUntilStarted` is the only thing that joins it, so `@Projection(source = PUSH, startupMode = BACKGROUND)` starts the application while the read model is still filling. Copied from `AbstractCatchupSubscriptionModel.startCatchupAsync`/`CatchupSubscription` with **two deliberate differences**: the handle THROWS a replay failure rather than logging and returning `false` (the template used to do the latter, and `ProjectionRunner` discards the return value, so copying it would start an application with a silently empty read model, and `ProjectionPushCatchupTunablesTest` would pass for the wrong reason), **though ADR 105 has since conformed `CatchupSubscription` to throw as well, so only the second is still a difference**; and a stop is signalled by THROWING out of the replay rather than ending the stream, because `BlockingHandover.catchUp` drains, goes live and writes the one-shot marker as soon as the replay completes normally, so a `takeWhile` short-circuit would record a partial replay as a finished one. The registrar keys off `startupMode != BACKGROUND` rather than `shouldWaitUntilStarted`, which maps `DEFAULT` to background-if-replaying and would have moved every existing push projection off the startup path silently. **The 2026-08-02 amendment finished the reactor stack and the domain feed**, so all four push flavours honour it. The reactor model gained the same life cycle, `stopCatchUp()` makes a reactor replay stoppable where it was not, and `subscribeOn(boundedElastic())` moves only the subscribe call rather than each emission. The reactor `CatchupProjectionFeed` is deliberately NOT wrapped in `Mono.defer`, because a re-subscription would subscribe the one-consumer live sink twice and brick the feed. A background replay that fails reaches nobody through `waitUntilStarted`, so both registrars record it on the `PushCatchupStatus` bean in `org.occurrent.springboot.common`, contributed by both starters. **Since #589 that bean answers readiness, not only failure** (`CatchingUp`/`Live`/`NotStarted`/`Failed`/`Unknown`, a sealed family). Three things not to re-derive. Live states are DERIVED via `register(id, catchingUp, running)` so a stop-then-start relaunching a replay reads truthfully. A recorded failure WINS over the model, because a failed replay is forgotten while its refusing registration stays and would otherwise read as `Live`. And `NotStarted` exists because `occurrent.subscription.mode = manual` defers the registration, so asking `isCatchingUp` alone reported a projection nobody had started as `Live`; that is also why `catchup = NONE` registers a supplier rather than recording `Live` up front. `DEFAULT` still waits for push (`SubscriptionAnnotations.pushCatchUpShouldWaitUntilStarted`), which is why the registrar keys off the mode rather than reusing `shouldWaitUntilStarted`. One residual: a reactor live-phase failure reaches no caller, since the catch-up signal has already completed. `isRunning(id)` is TRUE during a replay (matching the event-store model), so #349 added a public `isCatchingUp(String)` to the model rather than overloading it, and the push-saga timer gate is `isRunning(id) && !isCatchingUp(id)` (ADR 96). **#589 (2026-08-07) lifted that method into `ReplayAwareSubscriptionModel`, a one-method capability interface per stack**, implemented by both push models and by the event-store catch-up models (`CatchupSubscriptionModel`, `StreamCatchupSubscriptionModel`, `DcbCatchupSubscriptionModel`, and the three reactor ones, whose `isCatchingUp` was already written and merely package-private). The blocking one carries the `of(Object)` `DelegatingSubscriptionModel` unwrap copied from `IntrospectableSubscriptionModel`; the reactor one does not, because there is no reactor `DelegatingSubscriptionModel`. The saga registrar's concrete-class `instanceof` is gone, which widened the timer gate to event-store sagas behind a catch-up model as well, deliberately and for the same reason.
- **The subscription lifecycle contract (ADR 105, #579, 2026-08-06).** Two sentences bind every model on both stacks. (1) `start()` is a GOAL, not a transition: a second `start()` is accepted and converges (brings up whatever is not running, including a subscription paused on its own). `CompetingConsumerSubscriptionModel`'s `IllegalStateException` is gone, and `ManualStartSubscriptionModel.start` no longer guards on `!delegate.isRunning()`. That guard existed only to dodge the throw, and removing it is what makes the wrapper resume a delegate-paused subscription. (2) A `Subscription` HANDLE answers for the one start it was created for, reporting started once **nothing further is required of the application**. That clause carries the whole contract: a competing consumer waiting for the lock IS started (nothing is asked of the app, and calling it unstarted would block every non-leader node under `startupMode = WAIT_UNTIL_STARTED`), while a manual-mode registration, a stop-interrupted replay, a cancelled catch-up, and a subscription registered on a stopped push/synchronous model are NOT. Blocking answers true/false/throw, reactor completes/never-completes/errors. A deferred registration answers `false` IMMEDIATELY rather than blocking, because `DcbProjectionRunner.project` waits with no timeout and no opt-out and would otherwise hang a manual-mode app. **Four released behaviours changed** (competing-consumer `start`, `CatchupSubscription` failure reporting plus returning its delegate's answer, `CancelledSubscription`, `RegisteringSubscribable` on a stopped model); no OpenRewrite recipe, since these are behaviour changes at unchanged call sites (ADR 90/104 precedent). **Two per-stack differences are deliberate**: a parked reactor replay's handle DOES complete when `start(..)` relaunches it (the sink is re-emittable) where the blocking handle never sees the restart (ADR 104), and a reactor cancel-before-handover ERRORS where blocking answers `false`, since `Mono<Void>` has no third signal. `SubscriptionModelConformance` asserts the `start()` half now. Pause/resume keep REFUSING a redundant call on purpose (model-wide verbs are goals, per-subscription verbs are transitions); #580 owns their exception type.
- **Catch-up then push.** `CatchupThenPushSubscriptionModel` buffers the live feed on first subscribe, replays history position-ordered via `PositionOrderedReader`, drains the buffer, and goes live, de-duping the overlap **by event id, not position** (bounded cache; at-least-once beyond the window). A one-shot catch-up-complete marker reuses the existing `CheckpointStorage`. Buffer is bounded and fails loud. Only stream/agnostic filters replay (DCB rejected). Reactor runs three SEQUENTIAL phases (fold the replay to completion, record the marker, then deliver `liveSink`), each with its own `concatMap`, so only one phase is ever active and the dedup cache needs no locks; blocking uses a phase + lock handover. **Do not collapse the reactor phases back into one `Flux.concat(replay, marker, liveSink)`** (which is what it was until 2026-07-27): `concatMap` prefetches, so the replay `Flux` completes once its last item is EMITTED and `concat` advances to the marker while that item is still folding. With an async fold (the projection DSL's `boundedElastic` bridge) the marker landed mid-replay, and since the marker makes a restart skip the replay, the unfolded events were lost silently. `ReactiveHandoverTest.the_marker_is_recorded_only_after_every_replayed_payload_has_actually_been_folded` pins it by gating the LAST replayed fold (gating the first one does not reproduce). Both engines also reject a null dedup key: it used to reach `BoundedIdCache`, whose `ArrayDeque` eviction order rejects nulls, throwing a bare NPE from inside the cache after the live fold had already run. **A failed catch-up now releases its registration (2026-07-28, ADR 62 amendment).** The handover is registered on the live feed BEFORE the replay (deliberate, that is what captures an event committing mid-replay) so a failure used to strand it: the id could never be reused, and because `route` is an unguarded ordered loop the dead handler rethrew its stored `catchUpFailure` on every later event and starved the healthy subscriptions behind it. `route` keeps its unguarded loop on purpose (a push source needs the error to nack), and since ADR 90 there is at most one consumer for it to loop over on a push model, which is what removed the starvation rather than guarding the loop. **Cancellation is its own capability, `CancellableSubscriptions`, which `SubscriptionModelLifeCycle` extends and `RegisteringSubscribable` implements.** **Superseded (2026-08-01, ADR 85):** this used to say the register-only models must NOT get a full `SubscriptionModelLifeCycle`, on the grounds that `DcbSubscriptionModelAdapter`'s `instanceof` gate must keep failing for them and that a pause drops events rather than deferring them. ADR 85 overturned it: dropped-not-deferred is now the documented contract rather than an argument against having a life cycle, and the same property holds for `InMemorySubscriptionModel`, which implements it anyway. The four `RegisteringSubscribable` subclasses have one, and **ADR 91 (2026-08-02) added one to BOTH `CatchupThenPushSubscriptionModel`s**, which ADR 85 had left out by omission rather than by argument. The reactor one implements `SubscriptionModel` but deliberately NOT `FluxSubscriptionModel`, the bare `Flux`-returning primitive a register-and-wrap model cannot honour (it was `Subscribable, SubscriptionModelLifeCycle` and not the then-Flux-typed `SubscriptionModel` until ADR 98 renamed the primitive; the reason is unchanged, only the name it attaches to). That also settles the `DcbSubscriptionModelAdapter` `instanceof` gate this file used to flag as a risk: its delegate is typed `FluxSubscriptionModel`, so a model that does not implement it never reaches the gate. **`DomainEventFeed` deliberately does NOT get the same recovery:** `catchUpAll` fails terminally and keeps the poisoned feed, because the application declared that projection and running without it is worse than not starting. Since ADR 90 that is terminal for one projection rather than for a queue of them, so the asymmetry with the subscription model is now only about who owns the decision, not about who else it strands. **Two residuals were left deliberately, both reviewed and judged post-tag.** (1) `ReactiveHandover.catchUp` emits `catchupDone` on `doOnSuccess` after the marker phase, so an error in the LIVE phase finds the sink already terminated, `tryEmitError` returns `FAIL_TERMINATED`, and the model's release never runs while `terminalError` still poisons `accept`. That is the same starvation shape the fix closes for the catch-up phase. No reachable trigger was found (`deliver` has `onErrorResume`, sink overflow is handled in `accept`, nothing disposes the pipeline), so it is a hypothesis, and closing it means moving the release onto the whole pipeline rather than keying off `catchupDone`. (2) `DcbSubscriptionModelAdapter.cancelSubscription` gates on the wide `SubscriptionModelLifeCycle` though it only calls `cancelSubscription`, so a `CancellableSubscriptions`-only delegate gets a message telling it to implement `stop`/`start`/`pause`. Narrowing that gate is NOT obviously right, since a push model must keep failing the adapter overall per this ADR, so decide it deliberately rather than treating it as cleanup.
- **Domain-event feeds (no double encode/decode).** For sources already delivering domain events, the CloudEvent push path would double-encode on the hot path (`domain -> toCloudEvent -> toDomainEvent -> fold`). So: `Projections.domainEventFeed(projection, repo)` is a named live-only zero-encode feed; `CatchupProjectionFeed<E>` does domain-space catch-up (live folds directly, replay decodes store CloudEvents ONCE, dedup by a caller `Function<E,String>` in domain space, one-shot `CheckpointStorage` marker); `DomainEventFeed<E>` is the app-owned sink carrying the eventId as a ctor arg, and since **ADR 90 (2026-08-02) it feeds exactly ONE projection**, as does `PushSubscriptionModel`: a broker message has one acknowledgement, so a second consumer would share it and a persistently failing one would strand the rest. Isolation is the default in `RegisteringSubscribable` (`Consumers.ONE` from the no-arg ctor) and the two synchronous models opt into `Consumers.MANY`, since a handler failure there fails the write instead (only inside a transaction, see the ADR 57 amendment entry above for the isolating path without one). Do not reintroduce fan-out on a push sink: it is the hard rule in `AGENTS.md`, not a preference. `subscribe` stays `final`, which was questioned and kept. **`Source.PUSH` is a single enum value, not split into a domain variant:** the registrar resolves the feed bean and branches on its type (`PushSubscriptionModel` -> CloudEvent, `DomainEventFeed` -> domain), so the flavor is inferred rather than a redundant, mismatch-prone enum. `resolveFeedBean` resolves by subscription-model type, name, or the unique push-feed bean across both kinds. `Source` is named `EVENT_STORE`/`PUSH`, deliberately not `CHANGE_STREAM`, which would leak MongoDB into a transport-neutral API. PUSH rejects `mode=SYNCHRONOUS`, `startAt`/`startAtGlobalPosition`/`resumeBehavior`, and `DcbProjection`. It does NOT reject `startupMode`: since ADR 91's 2026-08-02 amendment both flavours honour `BACKGROUND` on both stacks, with `DEFAULT` still waiting (deliberately not `shouldWaitUntilStarted`, which would move every existing push projection off the startup path). The registrar owns the threading, since it is what knows the `startupMode`, and both starters contribute a `PushCatchupStatus` bean because nobody waits for a background replay and a failure would otherwise reach no one. Layering upheld: the CloudEvent base is untouched and the converter is used only on replay. **The reactor catch-up fold is `BiFunction<EventMetadata, E, Mono<Void>>` (#385)** so a projection keyed via `Projection.idWithMetadata()` keys correctly during replay; the replay carries `EventMetadata.from(cloudEvent)` and a live domain event folds with whatever metadata the source supplied. Do not regress the fold back to a bare `Function<E, Mono<Void>>`. **Live metadata is supplied, not derived (2026-07-26, issue #389):** `CatchupProjectionFeed`/`DomainEventFeed` take `accept(EventMetadata, E)` beside `accept(E)` on both stacks, because Occurrent cannot derive a stream id or position for an event that never was a CloudEvent but the application often has them from broker headers. The blocking stack keeps TWO live delivery routes on purpose (`accept(E)` to `MaterializedView.update(E)`, `accept(metadata, E)` to `update(EventMetadata, E)`) because those are separate interface methods a caller's view may implement differently; reactor needs no split since its fold is one `BiFunction`. `Projection.metadataKeyed()` is set only by `id(BiFunction)`, carried across `adapt(...)`, and the materializers fail when a key resolved to null AND the projection is metadata-keyed AND the metadata was empty. That conjunction is deliberate: a registration-time reject would forbid the capability itself and would break the legitimate `id((m, e) -> e.someId())` pattern, and `getPosition()`/`get(key)` return null on empty metadata (only the stream accessors throw), which is what used to drop every live event silently. Amendments in ADR 62 and ADR 68. **Catch-up tunables have TWO tuning points, one per object (#425):** `occurrent.subscription.catchup-then-live.{dedup-cache-size,max-buffered-events}` configures the `CatchupThenPushSubscriptionModel` that the registrars build, while a `DomainEventFeed` takes `CatchupThenLiveOptions` in its constructor because the APPLICATION declares that bean. Do not "fix" the asymmetry by wiring the property into `DomainEventFeed`: one tuning point per object is what avoids a precedence rule, and the property key names the subscription model so it never claimed to cover feeds. The properties are boxed `Integer` (null = unset) because the autoconfigure module holding `OccurrentProperties` (now `occurrent-spring-boot-autoconfigure`, see the annotation-module section) does not depend on `occurrent-subscription-core` and so cannot reference `CatchupThenLiveOptions.DEFAULT_*`.
- **The catch-up handover is ONE engine per stack (#387, ADR 0062 amendment).** `BlockingHandover<T>` (`org.occurrent.subscription.api.blocking.internal`) and `ReactiveHandover<T>` (the reactor twin) live inside the EXISTING `subscription-api-{blocking,reactor}` modules, with `HandoverMessages` in `org.occurrent.subscription.internal` (`subscription-core`) beside `BoundedIdCache`/`ReplayFilters`, and the public `CatchupThenLiveOptions` in `org.occurrent.subscription` (renamed on promotion, because the released `CatchupSubscriptionModelConfig.DEFAULT_HANDOVER_CACHE_SIZE` already owns "handover cache" in public API for the change-stream models). **No new published artifacts:** a first attempt minted `occurrent-subscription-handover-{common,blocking,reactor}` and was reversed, because three artifacts plus BOM entries plus an aggregator pom is too much permanent user-facing surface for reuse across four internal call sites, and ADR 0062 had already set the precedent by putting `RegisteringSubscribable` inside the pre-existing api modules. The `internal` package carries the not-user-API signal instead. The four callers keep their public APIs byte-identical and keep their own replay read, decode, delivery and filter derivation. **One type parameter, not two** (ADR 0062 amendment 2026-07-27): both reasons for keeping `L`/`R` distinct are gone, since a live event can now carry metadata and the blocking feed's two carriers merged into one record with a nullable metadata. The blocking stack still keeps two live delivery routes, because `MaterializedView.update(E)` and `update(EventMetadata, E)` are separate interface methods an implementer may treat differently, but that choice is a null check inside the caller's delivery function, not a type-level one. **Two engines, not one,** because `Stream` vs `Flux` runs through the whole SPI. **Why this matters as a precedent:** the extraction was first assessed and deferred as cross-module churn over tested code, and during that deferral the twins diverged for real (the reactor feed folded replayed events with empty metadata while blocking folded decoded metadata, so an `idWithMetadata()`-keyed projection mis-keyed every replayed event on one stack only, fixed in #385). Deferring a shared-core extraction between deliberately parallel implementations is what allowed a silent one-stack correctness bug. **`BlockingHandover.accept` no longer folds a live payload while holding its own lock (ADR 107, #588, 2026-08-07).** Only the dedup-key reservation (`reserve(T)`) is synchronized now; `deliver.accept(payload)` runs after the block exits, for both a live `accept(..)` and the buffer-to-live drain. Measured with JMH first: the lock was capping throughput at roughly one payload per handler duration regardless of configured concurrency (confirmed against the theoretical fully-serialized ceiling), moving the fold outside it lifted an I/O-shaped 200µs handler from ~4.2k to ~14.4k ops/s at 8 threads. This is a **breaking behavioral change on shipped 0.31.0 API** (`CatchupThenPushSubscriptionModel`, `CatchupProjectionFeed`, blocking stack only): a handler fed by more than one thread once live must now tolerate concurrent invocation, where the lock used to serialize it by accident. `ReactiveHandover` was out of scope, it has no equivalent shared lock (`concatMap` serializes its phases instead).
- **v2 broker-independent live resume is a deliberate NON-GOAL and may never be built.** Ordering and no-loss redelivery for the live feed belong to the transport, not to Occurrent reconstructing them; if ever needed it belongs in the push transport integration resting on the transport's ordering guarantee, not the core. The sound-but-intricate within-core option (checkpoint lagging by `transactionLifetimeLimitSeconds`) is explicitly out of scope. Deployments needing resume the broker cannot provide should rebuild offline. RabbitMQ durable queues already retain the backlog for an offline consumer.
- **Configurable change stream `batchSize`/`maxAwaitTime` (issue #173, ADR 0067).** `NativeMongoSubscriptionModelConfig` gains both (applied to the sync driver's `ChangeStreamIterable` before `.cursor()`); `SpringMongoSubscriptionModelConfig` gains `maxAwaitTime` only. Both `@Nullable`, default unset (driver/server default preserved, non-breaking), validated eagerly. **Deliberately asymmetric because of Spring Data limits:** the Spring blocking `MessageListenerContainer`/`ChangeStreamTask` never applies a batch size, and `ReactiveMongoTemplate.changeStream`/`ChangeStreamOptions` carry neither, so `ReactorMongoSubscriptionModel` exposes nothing yet. **Reactor is deferred with a known hazard:** `shouldRestart` filters on the exception and Spring's `changeStream` runs a persistence-exception translator, so a raw-`execute` bypass yields the untranslated `MongoException` and the history-lost error-code-286 restart detection must be re-verified. The follow-up also moves native's private `createPipeline` into `common/base` for reuse.
- **Reactor `SubscriptionModel` means what blocking `SubscriptionModel` means (ADR 98, 2026-08-05).** Both are exactly `extends Subscribable, SubscriptionModelLifeCycle` with no members. The released reactor Flux-returning primitive was renamed `FluxSubscriptionModel` (recipe `MigrateOccurrentRenames_0_32`, migration section 5). The combining interface sits on the four id-carrying models (`ReactorMongoSubscriptionModel`, `ReactorDurableSubscriptionModel`, `CatchupThenPushSubscriptionModel`, `RegisteringSubscribable`). **As of #556 (2026-08-05) the three reactor catch-up models (`ReactorCatchupSubscriptionModel`, `ReactorStreamCatchupSubscriptionModel`, `ReactorDcbCatchupSubscriptionModel`) were promoted to full subscription models too**, superseding the earlier "reach `FluxSubscriptionModel` through `CheckpointAwareSubscriptionModel` and get nothing else" state. A named subscription now replays history and then hands the live half to the wrapped model's own named `subscribe(..)` via shared `NamedCatchupSupport`, so `Durable(Catchup(Mongo))` inherits the wrapped model's handler retry and filter validation the same way `Durable(Mongo)` already did. **The two interfaces stay separate and must not be merged the way `DcbSubscriptionModel` carries both shapes:** a push-fed model cannot return a cold `Flux` at a `StartAt`, so merging would mint members four shipped models cannot honour. **The rule for any future rename here: an existing selector keeps its meaning and follows the Flux type; nothing moves to the new name.** Two things the audit taught that the #524 lesson did not already say: a `@Bean` method PARAMETER is a bean selector too (`occurrentDcbSubscriptions(SubscriptionModel, ..)` was missed by an annotation-and-`instanceof` sweep and caught by the compiler), and **a user supplying their own asynchronous model still cannot start the context on either stack** (two `Subscribable` beans, no `@Primary`, ambiguous at the subscription DSL's injection point) — pre-existing, symmetric, filed as #541, workaround is to mark the supplied bean `@Primary`.
- **A durable reactor subscription delegates when the model it wraps is named (ADR 101, closes #547 and #550).** `ReactorDurableSubscriptionModel` now inherits retry and filter validation from a wrapped model whenever that model exposes a named `subscribe(filter, startAt)` (every shipped `Durable(Mongo)` and, since #556, `Durable(Catchup(Mongo))` composition). **When it cannot delegate** (the wrapped model offers only the cold `Flux` primitive) **it now refuses loudly with a named `IllegalStateException` instead of silently running unguarded**, and section 6 of the 0.32.0 migration guide covers the remediation. Do not re-derive why two paths exist: a wrapped catch-up model that offers only `Flux` has no named subscription to delegate to.
- **A MongoDB subscription's own tracked change-stream position now wins over resuming at "the present" (#522, ADR 94 amendment "what pause means").** `SpringMongoSubscriptionModel` tracks the position it has read to and rebuilds every request from it, on resume and on error-restart alike, matching what `NativeMongoSubscriptionModel` and `ReactorMongoSubscriptionModel` always did. Both MongoDB models declare `deliversEventsPublishedWhilePaused()` true. Consequences that outlive the fix and must not be re-litigated: delivery across a pause or a restart is now at-least-once, not exactly-once, so a handler must tolerate redelivery (measured as `1,2,3,4,5,4,5` under a competing-consumer pause/resume, `CompetingConsumerSubscriptionModelTest`). A handler that throws gets its event again rather than having it skipped. And restart-after-error is no longer lossy (it used to reconnect at the present for every error but history loss, silently skipping whatever committed during a long outage), though it can now surface change-stream-history-lost (286) where it never could before, which is the `restartSubscriptionsOnChangeStreamHistoryLost` setting keeping the promise it always made. Resuming at the present instead (losing the paused window) was measured and rejected: ADR 57's wasted-work-over-loss rule settles it.

### Sagas and command dispatch

- **Saga DSL (#348, ADR 0063).** An event-driven process manager, the dual of `Decider` (turns events and its own timeouts into commands). Modules `dsl/saga-dsl/{common,blocking}`. Pure-data descriptor `Saga<E,S,C>` with separate `evolve`/`react`; `react` sees the post-evolve state, so replay folds only `evolve` and is effect-free. Two ways to write one `Saga<E,S,C>`: the core DSL (`Saga.builder`, Kotlin `saga(initialState){}`) and the flow DSL (`FlowSaga.builder`, Kotlin `saga{}` with `startsOn`/`step`/`on`/`join`/`timeout`), which is what the documentation calls them. **Timers are executor-polled from the saga's own `SagaStateStore`, deliberately NOT the deadline/JobRunr module**, so the envelope is the single source of truth and timer bookkeeping is exactly-once and CAS-atomic with state. A fired timer whose name has no handler is consumed as a no-op (with a warn, since that means a typo stall); terminal state cancels all timers. **Command dispatch is at-least-once** (dispatch-before-save + CAS + watermark dedup), so receivers must be idempotent, which an `ApplicationService`-backed dispatcher is by construction. A document-local outbox for exactly-once is designed-not-built (the envelope is internal, so it stays additive). Blocking-only v1: `SagaRunner` + per-runner poller, `InMemorySagaStateStore`, `SpringMongoSagaStateStore` (CAS on `_id`+version, indexed due-timer query). Example `example/saga/order-fulfillment`. **The name `Saga` is settled by ADR 0073 and is not to be reopened:** the DSL is a process manager (Hohpe/Woolf), not a saga (Garcia-Molina/Salem) since `SagaEffect` has no compensation member and nothing unwinds in reverse, but `Saga` stays because no short precise alternative exists (`Process`/`Flow`/`Reactor` all collide, `ProcessManager` would be the only two-word `-Manager` type among `Decider`/`Projection`/`View`/`SideEffect`) and Axon/NServiceBus/MassTransit/Rebus have established the term for this exact design. The process-manager caveat in `Saga`'s Javadoc, on `@Saga`, and in the docs is **load-bearing per that ADR: removing it is a regression**, not a cleanup.
- **Saga instance observation (#377, ADR 0070).** `SagaInstance` is a narrow non-generic view (`sagaId`, `SagaStatus`, `isCompleted`, the three timestamps, `nextTimerAt`, `currentStep`) that `SagaEnvelope` implements; `SagaInstances` is the read-only facade, reached from `SagaSubscription.instances()` or, on Spring, the `SagaInstancesRegistry` bean and a per-saga `sagaInstances-<id>` singleton. `SagaEnvelope.Status` became a top-level `SagaStatus`. **This is NOT the #375 `FlowState` narrowing and must not be called encapsulation:** `SagaEnvelope` cannot move to an `internal` package because the SPI both returns and accepts it, so the interface only narrows what an observing caller is handed — which is why the facade and the Spring exposure were non-optional rather than nice-to-have. `state` stays off the view (query-shaped read models belong to the projection DSL); `currentStep` is the one exception, affordable because `FlowState` is already narrow.
  - **`findByStatus` lives on an OPTIONAL `SagaStateStoreQueries` capability, not the core SPI** (AGENTS.md's small-capability rule; mirrors `EventStore`/`EventStoreQueries`, standalone rather than extending). The executor only needs `find`/`compareAndSave`/`findWithDueTimers`. The reason it is genuinely a separate capability: `findWithDueTimers` may return ANY order (in-memory iterates a hash map and breaks at `limit`) while `findByStatus` demands ascending `updatedAt`, so ordering is a new demand on a store. `SagaInstances` takes a plain `SagaStateStore` and checks the capability **at call time, not construction**, so a non-enumerable store still serves `find(sagaId)`. #411's SQL store can ship without observation.
  - **`findByStatus` contract is cross-store law and a prime early TCK candidate for #395:** exclusive `updatedBefore`, ascending `updatedAt` (stalest first; descending inverts the stuck-detector use case), `limit` is a bound NOT a page (ms-precision ties would make a timestamp cursor drop tie groups), a null `updatedAt` never matches. Boundary resolution is store-dependent (Mongo truncates to ms), so a TCK must not assert sub-ms precision.
  - **Observation reads NO saga state.** Both enumerations field-project, and `currentStep` is denormalized to a top-level Mongo field written on save, exactly as `nextTimerFiresAt` already was and for the same reason. So: **every envelope answers every `SagaInstance` member, with no exemption** — that is the real invariant (`streamWatermarks`/`positionWatermark` still differ across stores, so never restate it as "no per-store difference"), and `state()` is null on both enumeration paths because it is not a `SagaInstance` member. Three things fell out: no per-instance decode cost, no poller `currentStep` carve-out, and no undecodable-state failure to tolerate (the earlier degrade-with-WARN path was DELETED, not kept). `find(sagaId)` still throws on an undecodable instance, because the executor folds and saves from it. **Drift on the duplicated `currentStep` is prevented structurally:** `SagaEnvelope`'s compact constructor re-derives it from `state` whenever state is non-null and honours a passed value only when state is null (the projected-read case), so no caller can construct a disagreeing envelope.
  - **`SagaInstancesRegistry` is a READ-ONLY interface; the mutable `SagaInstancesRegistryImpl` with `register` lives in `org.occurrent.dsl.saga.internal`.** `register` has no legitimate application caller (unlike `compareAndSave`, which a store-wiring user needs), so per the unknown-callers rule it is prevented at the type level, not documented — a public mutable type in a public package carries no not-user-API signal, the `internal` package is what carries it. The `@Bean` deliberately declares the impl type, so the registrar's write-side lookup does not depend on instantiation order. Both entry points are empty until the post-refresh `@Saga` scan runs — inherent, not a defect — so inject the registry but never read it from a constructor.
  - Two fail-fast decisions: a user-supplied `SagaInstancesRegistry` Occurrent cannot populate **throws** at startup (it would answer "no sagas running" forever, the worst failure for a stuck-instance tool), while *no* registry at all only warns. A `sagaInstances-<id>` bean-name collision throws with a saga-naming message instead of Spring's bare duplicate-singleton error.
  - Placement: the registry TYPE is in `dsl/saga-dsl/common` (Spring-free, so a programmatic user needs no Boot dependency). Its `@Bean` moved with `SagaAnnotationRegistrar` in #409 and now sits in `OccurrentBlockingAnnotationConfiguration` in the store-neutral blocking module, which is what that move was waiting for. It stays blocking-only, since `@Saga` is.
  - The cost of the denormalized `currentStep` is a second derived copy of state in the persisted form. Drift is prevented structurally, not by discipline: the compact constructor re-derives it whenever `state` is non-null and honours a passed value only on a projected read. Projecting the nested `state.currentStep` instead would be strictly worse (a `FlowStateImpl` with silently empty `received()`), so do not reopen that option.
- **Flow saga history is a bounded window.** `FlowSaga.Builder.historyWindow(int)` (default 100) replaces full retention, removing the O(N^2) re-serialization and the 16 MB ceiling. `FlowState` gained an absolute `windowStart` and keeps `stepEntryIndex` absolute; the initiating event is always pinned at `received.get(0)` and the current step's events are always retained (so a join still matches over its full window). Guards and reactions see only the window except `initiating()`. **`FlowState` is a narrow public interface** (`dsl.saga.flow`); the concrete record with the bookkeeping is `dsl.saga.flow.internal.FlowStateImpl` (public only so the cross-module Mongo store can construct/read it). **The bookkeeping fields (`stepEntryIndex`, `windowStart`, `previousStep`, `lastAction`, `matchedBranchIndex`) are explicitly NON-wire-format** (a G5 carve-out documented on `FlowStateImpl` and in ADR 0063): a store must round-trip them without interpreting them. `SpringMongoSagaStateStore.findWithDueTimers` field-projects (see the observation entry below for the current field set) so the poll never decodes the received log (the executor re-loads authoritatively before firing). Other guards: `startsOn` declares only the start type and its optional start reaction, correlation is always `correlate`/`correlateAll` and `build()` rejects an uncorrelated start type (there is no `correlatedBy` on `startsOn`, which used to force callers to pass an explicit `null`); the Mongo store rejects a `FlowState` store without a `CloudEventConverter`; CAS contention warns and `maxCasAttempts` re-dispatches the whole command list (dispatch amplification, documented); `Saga.create` unions start types into a non-empty `eventTypes`.
- **Command dispatch (#374).** `CommandDispatchers` (turns a `Decider`-backed application service into a `CommandDispatcher`) lives in `occurrent-command-dispatch` (pkg `org.occurrent.command`), not in the saga DSL, so command dispatch is usable outside sagas. Its `occurrent-decider` dependency is optional (plus optional `occurrent-eventstore-api-common` for `WriteResult`), so interface-only consumers are unaffected. Sibling `occurrent-command-dispatch-dcb` adds `DcbCommandDispatchers.decider(...)` in its own module so the heavier DCB stack stays off stream-only users. **Modules live under `application/command-dispatch*`, NOT `application/service/*`.** Non-decider `ApplicationService`-lambda dispatch is the primary path; `CommandDispatchers.decider(...)` is the convenience adapter.
- **Function-shaped commands (ADR 0081).** `Invocation<E>(streamId, Function<List<E>,List<E>>)` + `CommandDispatchers.invocation(applicationService)` let a saga issue the domain function itself, so a codebase following the command philosophy needs no command hierarchy and no handler switch. DCB twin `DcbInvocation`/`DcbCommandDispatchers.invocation(...)` swaps the stream id for a `DcbCriteria` and adds an optional `TagGenerator`. **Nothing in `Saga`/`SagaEffect`/`Step`/the runner changed**, because `C` was never bounded, so this is entirely a dispatch-module feature. **The rejected alternative is a lambda-carrying fifth `SagaEffect`, and the argument is convergence, not a cost list:** anything that makes such an effect routable ends as `Run(streamId, fn)`, which *is* `IssueCommand(Invocation(...))` but with a bespoke interpreter that bypasses `dispatchAll`, `@Saga` wiring and every future dispatcher decorator. Do not re-propose it without addressing that. Three arguments are explicitly **not** usable against it (they hit `Invocation` equally, and ADR 81 says so): broken equality assertions, the old `timerEffects()` negation, and "invites non-idempotent receivers". The stream `dispatchAll` folds *consecutive* same-stream invocations into one `execute` via `ListCommandComposition` (never reorders). The DCB one deliberately does not fold, because equal criteria can carry different tag generators. `occurrent-command-composition` is a **required** (not optional) dep of `occurrent-command-dispatch` precisely because that fold is a rarely-hit branch. Kotlin `issue(streamId) { events -> ... }` extensions live in `saga-dsl/blocking`, NOT `common` (common is the pure descriptor and must stay dispatch-free). Cost, stated in the docs, is no equality assertions on issued commands. Assert `issuedCommands().single().decision().apply(events)` or the resulting events instead. `Saga.adapt` cannot widen `Invocation<Narrow>` to `Invocation<Wide>` (invariance), so type feature sagas on the module-wide event type. **This branch collided with ADR 80 (`0080-concurrent-write-conformance.md`, #467) which merged first, so it moved to 81.**
- **A push-fed saga (#349, ADR 96).** `@Saga` gained `source`, `subscriptionModel`, `subscriptionModelName` and a `catchup` attribute `@Projection` does not have. Only a `PushSubscriptionModel` is accepted, NOT a `DomainEventFeed`, because a domain feed carries no `streamid`/`streamversion` or `position` and the saga executor dedups redeliveries on those. `catchup = NONE` takes live events only and touches no event store, for a feed carrying another application's events; the default replays once and hands over. `startAt`/`startAtGlobalPosition`/`resumeBehavior` are refused on a push saga, `startupMode` is honoured under the default catchup (via `pushCatchUpShouldWaitUntilStarted`, not `shouldWaitUntilStarted`) and refused with `catchup = NONE`. **Two traps a session must not re-derive.** The timer gate cannot key on `isRunning(id)`, which is true throughout a replay, hence the public `isCatchingUp(String)`, since #589 the `ReplayAwareSubscriptionModel` capability rather than a concrete-class method. And a push feed bypasses the SubscriptionModel bean, so `mode=manual` never reaches it: the registration is deferred through `ManualStartPushSources` (renamed from `ManualStartProjections`, free because it was unreleased, and the reactor twin was renamed with it even though `@Saga` is blocking-only). The saga's `SagaInstances` is published at registration and NOT withheld, since it reads the state store and manual mode's caller is exactly who wants to inspect before starting. `Catchup` lives in `framework/annotations`: `@Projection(source = PUSH)` has the same no-local-history gap with no way to say so, which is a gap to close additively, not a decision. **Amended by ADR 109 (#583, A6).** A `PushSubscriptionModel` can still deliver an event carrying none of that metadata, which ADR 96 only warned about. `SagaExecution` now throws `SagaRedeliveryDetectionException` on such an event, and `@Saga(redeliveryDetection = BEST_EFFORT)` (plus `SagaRunnerConfig.withRedeliveryDetection`) is the opt-out that keeps the foreign-broker setup expressible. Fail-only was rejected for exactly that reason. The enum is duplicated across `framework/annotations` and `org.occurrent.dsl.saga.blocking` on purpose, so the saga DSL keeps no dependency on the annotations module.
- **Batched decider dispatch (#480, no ADR).** Both `CommandDispatchers.decider(...)` and `DcbCommandDispatchers.decider(...)` now override `dispatchAll`, folding a run of consecutive commands sharing a target into one `execute` via the existing `DeciderApplicationService.execute(streamId, List, decider)` / `DcbDeciderApplicationService.execute(List, dcbDecider)` overloads. **No ADR on purpose: ADR 76 line 63 already names "a single stream or decider" as the case the seam exists for**, so this applies a recorded decision rather than making one. ADR 76's consequence "Occurrent ships no overriding implementation of its own" is now stale (#479 first, then this), and was deliberately left alone, because an ADR records a decision at a point in time and is not a status page. The grouping lives in `org.occurrent.command.internal.CommandGrouping.forEachRun`, public-in-`internal` per ADR 62 because `command-dispatch-dcb` shares it, and it has three callers (stream invocation, stream decider, DCB decider). **#491 closed the 2n `criteriaFor` derivation the first cut accepted:** `DcbDeciderApplicationService` gained `execute(DcbCriteria, List, decider)` in **both** blocking and reactor, the safe `execute(List, ...)` now delegates to it (so `criteriaFor(List)`'s all-commands-agree check is unmoved), and the DCB dispatcher passes the key `forEachRun` already resolved. **The public-overload objection I filed #491 with was wrong and should not be re-raised:** the stream twin has had `execute(streamId, List, decider)` as ordinary public API all along, so this closed an asymmetry rather than opening a hazard class, and `DcbDecider.from` was already the precedent for documenting an unenforced agreement (same failure mode, an under-scoped append condition) on public DCB API. An `internal` package was rejected on contact with the code: `DcbDeciderApplicationService` is `final` with a `private` field and no accessor, so a subpackage cannot reach the wrapped service, and it would take a same-package helper plus a package-private hook. Reactor got the overload despite having no caller, because that class is user-facing API rather than plumbing and ADRs 52/53 treat blocking-reactor drift as a defect; doing both is also why no ADR was needed. The Kotlin extension was deliberately left alone as a third parity surface with no caller. **`forEachRun` resolves every key up front, exactly once, and that is load-bearing rather than an optimisation:** `StreamIdResolver.streamId` throws on a missing `@TargetStreamId` and `DcbDecider.criteriaFor` throws for a command it does not recognise, so lazy resolution would throw after earlier runs had already appended. It also keeps the uncached user `Function` behind `criteriaFor` to one call per command. **The empty-list case is a real trap, not a formality:** `SagaExecution` calls `dispatchAll` with an empty list whenever a reaction only arms a timer, and `DcbDecider.criteriaFor(List)` throws on empty, so `forEachRun` doing nothing on an empty list is what keeps timer-only reactions working. DCB **invocation** deliberately still does not fold (different `TagGenerator`s per invocation, ADR 81); grouping on a `(criteria, tagGenerator)` pair would be correct but `TagGenerator` is a functional interface with identity equality, so two lambdas written out twice never group and the rule becomes unpredictable. Behaviour that changed for released API: no partial progress when a mid-batch command fails, and on DCB an unrecognised command aborts before any append. Changelog entry, no OpenRewrite recipe and no migration note, matching how the RFC-3339 change was handled (nothing for a user to do).

### Annotation bean-post-processors

**Store-neutral since #409 (ADR 0072).** The machinery lives in `framework/spring-boot-autoconfigure/{common,blocking,reactor}`, published as `occurrent-spring-boot-autoconfigure`, `occurrent-blocking-spring-boot-autoconfigure` and `occurrent-reactor-spring-boot-autoconfigure`, packages `org.occurrent.springboot.{common,blocking,reactor}`. The `common` module is the RENAMED `occurrent-mongodb-spring-boot-autoconfigure`, its second coordinate rename after 0.30, which ADR 0055 warned about and ADR 0072 justifies as riding a migration 0.31.0 already forces. A third would not be defensible.

**Store-neutral is not framework-neutral, do not conflate them.** The registrars are inherently Spring (`BeanPostProcessor`, `ApplicationContext`), so a Quarkus integration reuses `occurrent-annotations` (already Spring-free, only an optional jspecify) and the DSLs, and writes its own registrars. The module names say `spring-boot-autoconfigure` on purpose.

**Annotation naming is decided, do not re-open it (ADR 127, PR 819, decides #725 and answers #721, closing neither).** All seven framework annotations take an `Occurrent` prefix (`@OccurrentProjection`, `@OccurrentSaga`, `@OccurrentSnapshot`, `@OccurrentSubscription`, `@OccurrentStreamSubscription`, `@OccurrentDcbSubscription`, `@OccurrentSynchronousSubscription`). The descriptor nouns stay as they are, per ADR 73. `subscription.api.{blocking,reactor}.Subscription` becomes `SubscriptionHandle`, which frees the word for a new subscription descriptor. Nesting (`@Occurrent.Projection`), a verb prefix (`@RunProjection`) and a single return-type-dispatching `@Occurrent` were each examined and rejected in the ADR, so do not re-propose them. The implementation is EPIC SCALE and NOT STARTED: per-stack descriptors (four, since DCB differs) plus four runners, descriptor paths ADDED to both registrars beside the reflective parameter classification, which the deprecated annotations still need and which is deleted with them a release later rather than in this epic, seven new annotation types with seven deprecations, and an OpenRewrite visitor that keeps the handler method and delegates to it (so external direct callers still compile) with THREE refusal cases (a `@SynchronousSubscription` handler with any statically detectable advice on its class or method, any handler declaring a checked `throws`, and any handler in an application carrying both autoconfigure stacks), plus a migration guide section and a docs branch. The new annotations are read in `afterSingletonsInstantiated` like the other descriptor annotations, not in `postProcessBeforeInitialization`. Register it as its own epic. Related live defect found while writing the ADR and NOT fixed by it: `@Transactional` on a `@Subscription`, `@StreamSubscription` or `@DcbSubscription` handler has never run, because those three registrar paths invoke the raw pre-proxy bean while only `processSynchronousSubscribeAnnotation` looks the bean up by name at dispatch time.

**A store starter owes four things:** import the stack's annotation configuration, contribute whichever `Default*Provider` beans it can, contribute a `StartupWorkaround` if its template needs eager creation (the spring-framework#32904 workaround), and wire its own event store. Seams: `DefaultProjectionStoreProvider`, `DefaultSnapshotStoreProvider`, `DefaultSagaStateStoreProvider` (blocking), `DefaultReactiveSnapshotStoreProvider` (reactor), all optional via `getBeanProvider`. Reactor needs one, not three, because it has no `@Saga` and its projection registrar fails loud instead of defaulting. Storage conventions (`occurrent-snapshot-<id>`, `saga-<id>`) live in the Mongo implementations, not the neutral modules. **Invariant:** `grep -rniE "mongo"` over the blocking and reactor modules' main sources and POMs must return nothing.

**Known gap for #410:** `OccurrentProperties` is neutral in code but NOT in configuration surface. `event-store.collection`, `event-store.time-representation`, `subscription.collection` and `subscription.restart-on-change-stream-history-lost` all describe MongoDB. Splitting means two beans binding the `occurrent.event-store` prefix or renaming released keys, so it was deliberately deferred, NOT overlooked. The `common` module therefore cannot pass the grep above. One public type per stack is exposed, the bean-post-processor, because the store starter needs its `SYNCHRONOUS_SUBSCRIPTION_DSL_BEAN_NAME`; everything else stays package-private. Narrowing the `static @Bean` return type instead was tried and works (Spring resolves `DisposableBean` from the instance), so that concern was unfounded.

Decomposed (#383) into thin coordinators plus package-private per-annotation registrars in the same package (tightest encapsulation, no new public API): blocking `OccurrentBlockingAnnotationBeanPostProcessor` (149 LOC) plus `SubscriptionAnnotationRegistrar`, `ProjectionAnnotationRegistrar`, `SnapshotAnnotationRegistrar`, `SagaAnnotationRegistrar`, `StartPositionSupport`; reactor the same minus saga (reactor has no `@Saga` or `DisposableBean`). The coordinator keeps `SYNCHRONOUS_SUBSCRIPTION_DSL_BEAN_NAME`, owns the single shared `registeredIds` set (passed by reference to id-checking registrars), builds collaborators in `setApplicationContext` (the BPP is fully Aware-initialized before it post-processes others, and constructors only store fields, so bean-init timing is unchanged), and orchestrates the exact lifecycle order: all subscription ids, then projections, then catch-up domain feeds, then snapshots, then blocking-only sagas; `destroy` closes the saga poller.

**Only `shouldWaitUntilStarted(boolean, StartupMode)` is shared** (lifted byte-identical to `common`'s `SubscriptionAnnotations`, alongside `resolveTypeFilter` and `resolveFeedBean`). **Start-position logic is deliberately NOT shared, because the stacks genuinely diverge:** blocking has time-based `StartPositionToUse`/`generateStartAt` that reactor rejects; blocking checks `CompetingConsumerSubscriptionModel` where reactor has no reactive competing-consumer model; blocking uses `CheckpointStorage.exists` vs reactor's `read().blockOptional()`; `MongoOperations` vs `ReactiveMongoOperations`. The per-stack reflection/bean-selection helpers were assessed for dedup and left per-stack: `invokeFactory` carries a **pre-existing** behavioral divergence (blocking null-checks and catches `ReflectiveOperationException`; reactor does neither and catches `Exception`), and the reactor projection path lacks the blocking zero-config Mongo default entirely. Coverage is 20 container-free `ApplicationContextRunner` fail-fast characterization tests over previously-untested error branches.

### OpenRewrite upgrade recipes (`rewrite/`, artifactId `occurrent-rewrite`)

Published, deliberately kept OUT of the release profile's `excludeArtifacts`. Declarative-first: `MigrateOccurrentRenames_0_30` (mechanical type/method/package renames), `MigrateStreamToList_0_30` (best-effort `Stream`->`List` write side; marks sites it cannot safely rewrite with a `/* TODO [Occurrent 0.30 upgrade] */` comment, leaving lambda bodies and Kotlin manual by design), the umbrella `UpgradeToOccurrent_0_30`, and the `_0_31` pair for the annotation-type moves. Scope was limited to symbols that existed in released 0.20.5 (a 0.20.5 user never referenced DCB or reactive-new symbols). `rewrite-recipe-bom` in root `dependencyManagement`; the module depends on `rewrite-kotlin` so renames rewrite Kotlin call sites too (the `Stream`->`List` recipe stays Java-only). **Known limitation, documented as a manual step rather than worked around:** `StartAt.subscriptionPosition(...)` static-factory renames do NOT fire on a Kotlin caller (a rewrite-kotlin limitation). Recipe tests use `rewrite-test` + `rewrite-java-21` + `org.openrewrite.kotlin.Assertions.kotlin(...)` with source type-stubs, since real Occurrent modules are not on the recipe test classpath. Migration guides at `doc/migration/upgrading-to-0.30.0.md` and `-0.31.0.md`. The recipe's correctness rests on the invariant that the default event-store capability is STREAM with DCB an explicit opt-in on all stores and both starters.

Build/verify: `rtk mvn -Drevision=0.30.0-SNAPSHOT -pl rewrite -am test`. **Real-upgrade-trial method (validated twice):** install as `-Drevision=0.30.0-SNAPSHOT`, then run against a real downstream consumer via `mvn org.openrewrite.maven:rewrite-maven-plugin:RELEASE:run -Drewrite.activeRecipes=org.occurrent.UpgradeToOccurrent_0_30 -Drewrite.recipeArtifactCoordinates=org.occurrent:occurrent-rewrite:0.30.0-SNAPSHOT` (no pom edits needed). When trialing against a repo with uncommitted work, stash first, revert only your own trial changes, then pop.

**Remaining known blocker to a fully green downstream trial:** `occurrent-support:2.0.3` (Parkster's internal wrapper) throws `ClassNotFoundException: org.occurrent.dsl.subscription.blocking.EventMetadata` against 0.30.0-SNAPSHOT. That FQN was real, released, public API in 0.20.5 (declared inside `Subscriptions.kt`); #247 moved it to `org.occurrent.dsl.subscription.EventMetadata` and the changelog documented only the new `position` accessor, not the package move, so it never reached the recipe's changelog-derived list. A `ChangeType` entry now covers it. This cannot fix `occurrent-support:2.0.3` itself (a precompiled jar), which needs its own rebuild against a stable 0.30.0.

## Known Fragile Areas And Risks

### Snapshots

- **Suffix-read invariant (do not regress).** Snapshots rely on `read(streamId, V, MAX).version()` returning the TRUE stream head version regardless of `skip` (verified in `InMemoryEventStore` and `SpringMongoEventStore.readEventStream`, which pin to `currentStreamVersion`), and on DCB `read(criteria, afterPosition(P))` returning the whole-boundary consistency token deliberately independent of the position window. That is what keeps a snapshot-resumed optimistic write and DCB append guard correct while reading only the tail. Never "optimize" a store so a skip/position read reports a truncated version or a tail-only token.
- **A snapshot is a discardable, schema-versioned cache.** A `schemaVersion` mismatch or absent snapshot means full replay via `SnapshotSupport.resolveBase`; never deserialize a stale state shape into the new type. Persistence is best-effort by default (a lost snapshot only lengthens the next replay), atomic only when a `TransactionExecutor` is supplied. `SnapshotStore.delete` fails loud by default so archival cannot believe a snapshot is gone when the store cannot delete.
- **Version-drift guard (do not regress).** A snapshot whose version is ahead of the stream's true head means the stream was truncated below it, so it is stale and must not be resumed from. `SnapshotSupport.resolveBase`/`isRedelivery` take an `observedHead` and demote such a snapshot to the initial state (3-arg forms delegate with `Long.MAX_VALUE`, preserving old behavior). The maintained `@Snapshot` stream path probes the true head ONLY when the delivered version is at or below the snapshot version, so the happy path pays no extra read and a reset self-heals instead of freezing the maintainer. The DSL stream executor detects `base.version() > writeResult.oldStreamVersion()` post-write and deletes the stale snapshot. `eventsSinceSnapshot` arithmetic runs INSIDE `maybeSaveBestEffort` (Supplier overload) so a post-reset negative/overflow delta cannot throw a committed command. DCB paths are immune (a global DCB position is monotonic) and keep the 3-arg forms. ADR 0061 amendment.
- **Mongo snapshot stores degrade, never throw.** `findLatest` catches a deserialization failure and returns empty so execution falls back to a full replay instead of failing before `execute` (the read runs outside the best-effort save guard); the reactive store scopes that recovery to the decode step so connectivity errors still propagate. Both scalar and POJO/record state round-trip via the Mongo converter's general value conversion, not the Document-only path.

### Nullness and generics

- **`Decider` state returns must be the plain type variable `S`, never `@Nullable S`, under `@NullMarked`** (fixed twice: `6ed6aed31`, then again in #317 after #287 reintroduced it). `@Nullable S` types as `S?` in Kotlin for a concrete non-null `S`, breaking the common pattern of chaining one decision's state into the next `decide`. Plain `S` makes nullness follow the type argument (parametric nullness) while the `S extends @Nullable Object` bound keeps a null initial state expressible. It was harmless in 0.20.5 only because the package was not null-marked. The interface now carries a guard comment and `DeciderTest` has a Kotlin chaining test that fails to compile if the annotations return.
- The reactor snapshot facades keep `<S extends @Nullable Object, E>` so an idiomatic null-initial-state decider still works with `execute`/`executeAndReturnDecision`; only `executeAndReturnState` requires non-null and fails fast via `requireNonNullState` pointing at `executeAndReturnDecision`, because a `Mono` cannot carry null. An earlier pass made the whole class non-null `S`, which wrongly excluded null-state deciders even from `execute`; nullable-class-plus-fail-fast is the settled shape.

### Positions, catch-up, and DCB storage

- **Positions are reserved OUTSIDE the write transaction** (`MongoEventStore.java:282-284`), so abandoned/rolled-back writes leave PERMANENT gaps, and a lower reservation can commit after a higher one (a temporary hole). Positions are globally-unique and strictly monotonic, never dense and never in commit order (ADRs 0007, 0021, `MongoEventStorePositionTest`). A missing position is therefore ambiguous from a delivery stream, which is why **a feed-derived contiguity/gap-free-frontier watermark is unworkable** (it stalls at the first permanent gap, and any timeout reintroduces loss). A resume point must come from the STORE: a position-ordered catch-up read from a persisted checkpoint (`position > P` skips gaps, exactly how `DurableSubscriptionModel` resumes), with the broker feed as a low-latency live tail and idempotent folds over the overlap, deduped by event id. **This is a MongoDB consequence, not a general law** (see Planned Work decision 1). Reusable pieces: `PositionOrderedReader` (public, `eventstore/api/*`) and `GlobalCheckpoint`/`CheckpointStorage` (public); `PositionCatchupPipeline`/`FixedSizeCache` are package-private in `stream-catchup-subscription` and not reusable without promotion.
- **Catch-up to live handover invariant (ADRs 0014/0008).** `CatchupSubscriptionModel` reconciles events written during the bulk replay via a delta query using `SortBy.natural(DESCENDING)` + `limit` (insertion order), NOT the time-based `catchupPhaseSortBy`. Selecting by time is loss-prone under clock skew (a backdated during-replay event sorts before the boundary and is missed by both the delta and the live resume). **Do NOT "tidy" the delta sort back to `catchupPhaseSortBy`.** The global checkpoint is captured AFTER the bulk replay (fresh token, avoids oplog ageing). #208 closed the count-to-read window by re-reading the recent tail until the matching count stops growing; `runCatchupForStream` dedups cached ids so overlapping re-reads stay at-least-once, and closes its source stream. Residuals needing a global monotonic position: net-count arithmetic loses events if events are deleted mid-replay, and `count(Filter.All)` on Mongo uses `estimatedDocumentCount`, which can undercount after an unclean shutdown. InMemory `SortBy.natural` means GLOBAL insertion order.
- **Subscription resume-marker field (do not regress #272).** `MongoCommons` writes the marker under `checkpoint` (`GENERIC_CHECKPOINT`) and keeps `subscriptionPosition` (`LEGACY_GENERIC_CHECKPOINT`) as a read-only fallback for pre-rename documents (read tries `checkpoint` first, else legacy; `replaceOne` migrates on next save). Removing the legacy read-fallback or changing either key loses not-yet-migrated consumers' resume points.
- **DCB storage.** DCB shares the CloudEvent storage boundary; metadata is CloudEvent extensions so normal subscriptions still see DCB-written events. Position reservation uses a separate collection, but event documents go into the normal event collection with stream metadata. DCB appends are transactional and rely on `MongoTemplate` session synchronization `ALWAYS`. Reads capture a high-watermark and bound matches to `dcbposition > afterSequencePosition && <= highWatermark`, returning that watermark. Append-condition concurrency updates conservative checkpoint keys (`all`, `tag:<tag>`, type-only) to detect racing appends.
- **Two different "is this a DCB event" fields, never conflate them.** `dcbTags` (capital T) is a Mongo-only derived index array, written only by `DcbDocumentMapper` on the DCB `append(...)` path, always present (empty array for zero tags), and sparse-indexed (ADR 0049) so `$exists` on it is cheap. `dcbtags` (lowercase) is the actual CloudEvent extension, present on any event a caller tagged via `DcbCloudEvents.withTags(...)` regardless of write API, and is what `DcbCloudEvents.isDcbEvent` and the in-memory matcher check. The fix for their potential divergence (#282) was NOT to switch the Mongo check to the extension (that would defeat the sparse index) but to make them provably equivalent by rejecting `dcbtags`-carrying events on the stream `write(...)` path in all four stores. New Mongo-side DCB discrimination must use the indexed `dcbTags` array; anything working with a live `CloudEvent` must use the `dcbtags` extension via `isDcbEvent`.
- **A sparse index only accelerates `$exists: true`, never `$exists: false`** (it has no entries for documents missing the field). So the DCB-side guard (#280) gets full index support while the reverse (#282) does not. Accepted as a known low-risk limitation, since the one real caller (`StreamCatchupSubscriptionModel`) always has a position-range predicate to ride on. **Do not "fix" this with a second index;** the correct fix if it ever becomes a hot path is a written marker field on stream-only events, rejected as premature (ADR 0050 has the analysis).
- **DCB partition stream-version collision retryability is MongoDB-version-sensitive (#297).** On a DCB+STREAM store, two concurrent DCB appends to disjoint boundaries can collide on the same stream version when they hash to the same partition. On 4.2.x this surfaced as a transient `WriteConflict` (auto-retried); on newer servers it can be a non-transient `E11000`, which #297 detects via the `streamid_1_streamversion_1` index name and rethrows as retryable rather than as a misleading `DuplicateCloudEventException`. Now validated against MongoDB 8.0 (#302).
- **In-memory `deleteAll()` must reset DCB sequence state**, else an emptied store reports stale DCB high-watermarks.
- **Do not gate the DCB application-service bean with `@ConditionalOnBean(TagGenerator.class)`.** That condition evaluates while bean definitions are still processing and can miss a user's `TagGenerator` in a real Boot context even though slice tests pass. It is a normal generically-typed `@Bean` resolving `TagGenerator` via `ObjectProvider.getIfAvailable()` at instantiation, returning `null` when none exists.

### Spring wiring

- **The starters mark the async `SubscriptionModel`/`Subscriptions` beans `@Primary`** because the synchronous model is also a `Subscribable`, so a bare `Subscribable`/`SubscriptionModel` injection resolves to the async one. Inject the concrete `SynchronousSubscriptionModel` to disambiguate.
- **The native `MongoEventStore` routes `read`/`query`/`count`/`exists`/`readInPositionOrder` through the thread-bound `ClientSessionHolder` when set**, so a synchronous handler sees the uncommitted write consistently. `count(Filter.all())` is the one asymmetry: it uses the fast `estimatedDocumentCount()` when no session is bound (the normal path, and the `CatchupSubscriptionModel` net-count path, both unchanged) but falls back to an exact `countDocuments(session, {})` inside an ambient session, because `estimatedDocumentCount` has no `ClientSession` overload. The slower exact count therefore only fires for a synchronous handler that itself calls `count(Filter.all())` inside the write transaction.

### Tooling and environment

- **Docker/Testcontainers is the biggest verification risk**, though no longer for the data-corruption reason. Async subscription tests are timing-sensitive. Locally the suite needs `DOCKER_HOST=unix:///Users/johan/.colima/default/docker.sock` plus `TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock` (Testcontainers cannot auto-discover Colima). **Running many replica-set suites in one session exhausts Colima and mass-flakes container startup, so rerun failed classes in isolation before believing a failure is real.** The Colima VM was resized 2026-08-06 in `~/.colima/default/colima.yaml` from 2 CPU / 2 GiB (a size at which replica-set init itself could fail with an empty message) to 6 CPU / 12 GiB, taking effect on the next `colima restart`. Until that restart has actually run, the old 2 GiB behavior still applies; after it, roughly 8 to 10 concurrent Mongo replica-set suites fit, the rerun-in-isolation rule still stands, and a mass-flake deserves a fresh look before being blamed on VM exhaustion. **Concurrent Maven runs no longer delete each other's Mongo data (#505, ADR 97):** every container comes from `ReplicaSetReadyMongoDBContainer.withDefaultVersion()` and owns a database named `oc<pid>_<n>`, so a flush only reaches the class that owns it, and nothing pins host port 27017. Historical note on what that symptom used to look like, because it will be mistaken for a store bug if it ever returns: a write returned successfully and the very next read saw nothing, or a stream held 5 of the 6 events just written, reading exactly like an event store losing a committed write; a 55 minute subagent build in the same checkout corrupted the parent session's runs that way. **The remaining reason a local Mongo failure is weak evidence is the shared `~/.m2`:** another session installing `test-support` from its own branch silently replaces the jar this one resolves, which reproduces the old shared-`test`-database corruption exactly. Always run with `-am` so `test-support` is built from source, never `-pl <module>` alone. Known env flake: `SubscriptionProjectionsWithSpringAndMongoDBApplicationTest` intermittently fails context load with `NullPointerException: BsonTimestamp ... is null`; retry.
- **Local incremental Maven leaves a deleted source's stale `.class` in `target/`.** This produced a false duplicate-`@Projection`-id failure until `mvn clean`. CI is unaffected.
- **Maven Central can 403 mid-run, which looks nothing like a container flake and nothing like a code defect** (cdx33 U5, 2026-08-11). PR 731's `example-projection` java-25 shard failed with `status code: 403, reason phrase: Forbidden` on `surefire-junit-platform:3.2.2`, `spring-boot-starter-data-mongodb-reactive:4.1.0` and `jackson-module-kotlin:3.1.4`, cascading into six module FAILUREs and a `BUILD FAILURE`. The repository mirrors test container IMAGES (`ghcr.io/<owner>/occurrent-ci`) but not Maven artifacts, so there is no fallback for this. The tells are a 403 rather than a timeout, several unrelated artifacts at once, and sub-second module failures. Fix is `gh run rerun <run-id> --failed`; the second failure mentions a cached resolution failure, which is the same incident and not new evidence. Do not read it as the PR's fault, and do not chase it as VM exhaustion.
- **A local `-am test` that dies on a container flake in an early module never reaches later modules.** When a shard fails in CI but passed locally, check whether the local run actually reached that module.
- **code-review-graph is structurally weak for architecture boundaries here** (file-based communities, no cross-community edges, test-dominated flows). Use it for inventory and impact, but verify module boundaries from Maven and source.

### Recurring correctness lessons

- **A top-level-declaration sweep misses nested public types and Kotlin free functions.** Sweep both (nested = indented type decls; Kotlin = top-level `fun`). A type-level audit that matched only non-indented declarations missed 4 renamed public nested types reachable via `switch` pattern-matching over sealed types.
- **A single failed `git show <tag>:<guessed-path>` proves a *path* does not exist, not that a *type* never existed.** Verify a type's non-existence with content search (`git grep`), never a guessed path. **The changelog cannot be trusted to list every breaking rename;** the real gaps were found by diffing every top-level type declaration between the `0.20.5` tag and HEAD (526 types audited).
- **Domain event classes fed through `ReflectionCloudEventTypeMapper.simple()` must be top-level, not nested in a sealed interface**, because `Class.forName(pkg + "." + simpleName)` cannot resolve the `Outer$Inner` binary name.
- **A recipe test's stub annotation must use the real enum constants**, not invented ones, or the rewritten before/after snippet references a constant that will not compile against the real target version.
- **A query or read `Stream` in a TCK suite must be closed.** The MongoDB stores back a query stream with a database cursor, so an unclosed stream leaks it and can interfere with the next test. `ConformanceEvents.idsOf(Stream)` closes for you, so prefer it, and use try-with-resources whenever you consume a stream directly (`findFirst`, `map`, `collect`). Copilot caught this in new suite code on two consecutive pull requests (#489 on `readInPositionOrder`, #493 on `query`), and both times review found it rather than any test failing, because a leaked cursor does not fail the test that leaked it.
- **Point subagents at the session worktree path.** A brief naming an absolute repo path makes edit-only subagents write to THAT checkout, not the session worktree; three subagents once edited the main checkout and their work had to be patched across.

## Build And Verification

Use `rtk <command>` wherever `rtk` supports it; raw shell only when unsupported. Managed dependency versions live in the root `pom.xml` `<properties>` and imported BOMs (Spring Boot, Reactor, MongoDB driver, Jackson 3, CloudEvents, JobRunr); do not duplicate them here.

CI: `.github/workflows/maven.yml` builds once per JDK (`install -DskipTests`, Temurin 21 and 25) and then runs the tests as 10 shards per JDK, with Testcontainers reuse via `$HOME/.testcontainers.properties`. Shard count is pinned at 10 because the account's GitHub plan allows 20 concurrent jobs; rebalance by moving paths between shards, never by adding one. Each shard reports its per-subtree timings to the run summary and as `shard-timing` log lines, and warns when it exceeds `SHARD_BUDGET_SECONDS`, so a shard that grew says so instead of quietly stretching the run; read them with `gh run view <run-id> --log | grep shard-timing`. Costs are not additive: with Testcontainers reuse the first subtree to boot MongoDB on a runner pays for the container and its neighbours ride along, so a subtree's time changes when it moves shard. Each shard pre-pulls every image the tests need and caps `pull.timeout` at 60s, so an unreachable Docker Hub fails in under a minute saying so rather than costing ~292s per test class and tripping the 20 minute job timeout (#432). Those images come from a private mirror at `ghcr.io/<owner>/occurrent-ci`, retagged to their Docker Hub names so the tests find them locally, with a `mirror-images` job filling any gap (#436). Both jobs take the list from `.github/scripts/required-test-images.sh`, which derives it instead of hardcoding tags: Mongo from `integration-tests.mongo.version`, Redis from the literal in `SpringRedisCheckpointStorageTest`, ryuk from a bytecode constant in the Testcontainers jar. Every part of that is best effort, so whatever it cannot derive, or the mirror lacks, falls back to Docker Hub with a warning rather than breaking. Do not reach for Testcontainers' `hub.image.name.prefix`: it rewrites every Docker Hub image with no per-image opt-out and no fallback, so a mirror lagging a version bump would turn into a red run. The IDE fallback for the Mongo version comes from `occurrent-test-support.properties`, filtered from the same pom property, so it cannot drift.

Local patterns:

- Full package: `rtk mvn -B package --file pom.xml`
- Focused module: `rtk mvn -pl <module-path> -am test` (or `package`, or `test-compile` for Kotlin/generics changes)
- Focused test class: `rtk mvn -pl <module-path> -am -Dtest=<TestClass> test`
- DCB touched-module set: `rtk mvn -q -pl eventstore/api/dcb,eventstore/inmemory,eventstore/mongodb/spring/blocking,application/service/blocking,subscription/mongodb/spring/blocking -am test`
- **Authoritative gate: read the log for `BUILD SUCCESS`/`BUILD FAILURE`, never trust a wrapper exit code.** Confirm the JDK is 21 (`mvn -version`).
- **`-Dtest=<Class>` combined with `-am` fails the whole reactor** on the first upstream module that has no such test class, so pass `-Dsurefire.failIfNoSpecifiedTests=false` alongside it (cdx33 U7, 2026-08-11). Without the flag the run dies before reaching the module you were targeting, which reads as a failure of your change.

Release: `mvn_local_snapshot.sh` runs a release-profile local install with `-Drevision=...`. `mvn_release.sh` requires Java 21, uses `mvn deploy -Prelease` with GPG signing, source/javadoc jars, Sonatype Central publishing, and tags `occurrent-<version>`.

## Open Items

### Release state and the docs repo

**Nothing is pending release.** 0.31.0 shipped on 2026-07-28 and drained the whole unreleased inventory (saga DSL, projection DSL, snapshots, synchronous subscriptions, the push subscription model, command dispatch, the #409 annotation-module move, and the rest). Every capability in the library is released API now, so the changelog's fold-in rule no longer applies to any of it and an incompatible change anywhere needs an `UpgradeToOccurrent_*` recipe plus a `doc/migration/` note. Work merged after 0.31.0 goes under a `### Changelog next version` heading, created fresh because the release renamed the previous one.

**#488 is closed** (tracker state verified 2026-08-06; whether the `@Disabled` came off was not re-verified here. If the blocking store's conformance run still reports `Skipped: 1`, check that before assuming a new skip).

**Docs live in a separate Jekyll repo (`occurrent-org.github.io`).** Its `_config.yml` reads `occurrentversion: 0.31.0` and the 0.31.0 held branches are merged and gone, so `main` documents shipped API and a branch documenting shipped API is mergeable on approval. The release-gating rule still stands for anything documenting an *unreleased* feature: push the branch, do not merge it until the matching library release ships. **Held branches as of 2026-08-06 (10):** `docs/reactive-testing-twin`, `docs/canonical-time-storage`, `docs/checkpoint-storage-tck`, `docs/deterministic-projection-drain`, `docs/function-shaped-saga-commands`, `docs/mongodb-flush-extension`, `docs/one-push-sink-per-consumer`, `docs/saga-push-source`, `docs/saga-testing-ergonomics` (remote-only), `docs/testing-module-and-introspection`. Most touch the same `docs.md` sections, so at release expect to rebase them in sequence rather than merge independently.

**U10 coverage audit (2026-08-06), three findings that outlive the writing work.** (1) The three testing-module branches are layered supersets of one section: `testing-module-and-introspection`, then `mongodb-flush-extension` adds the flush, then `reactive-testing-twin` adds the reactor twin and multi-stack `@EnableOccurrentTesting`. At release, merge only the superset (or merge in order and expect the earlier two to reduce to no-ops); do not merge them independently. (2) `docs/saga-testing-ergonomics` carries prose that the shipped `Saga.Step.timerEffects()` now contradicts ("the timers get no accessor of their own"); U10e fixes it on that branch. (3) One release-gating library-repo gap: the `CatchupThenPushSubscriptionModel` direct-usage behavior change (failure surfaces via `waitUntilStarted()`, reactor replay on `boundedElastic`) is changelog-flagged breaking but absent from the migration guide; U10f owns it and U12 must verify it landed. The 11 docs gaps are grouped into: U10a event-store-side TCK section (extends `docs/checkpoint-storage-tck`), U10b data-payload filtering (#499/#58), U10c `occurrent.subscription.mode` as a feature (only the property mapping is in the migration guide), U10d subscription life-cycle sweep (sync/push life cycle plus the stale "no lifecycle" text on main, reactor registered-while-stopped guarantee, reactor filter refusal and action retry, `autoStartup(false)`, blocking `CatchupSubscriptionModel.stop()` replay stop, `IntrospectableSubscriptionModel.of`), U10e `timerEffects()`, U10f the migration-guide entry.

A new section needs an `addedTags` entry in `js/_partials/main.js` keyed by the heading's kramdown id (or its explicit `{#anchor}`), per the docs repo's own `AGENTS.md`. Two feature branches both adding an entry conflict on the same line every time, and the resolution is always to keep both.

### The testing modules (ADR 0082, shipped 2026-07-30)

`occurrent-testing-junit-jupiter-blocking` plus `occurrent-testing-spring-boot` provide the deny-by-default subscription pattern: `stop()` in `beforeEach` and `afterEach`, each test opting in with `resumeSubscription(id).waitUntilStarted()`. The pattern came from the Parkster push-notification service, not from this repo, and nothing here used it before.

Decisions a later session must not silently reopen:

- **`SubscriptionModelLifeCycle.stop()` now promises that every running subscription is left paused**, so a per-id `resumeSubscription` works after a model-wide stop. Every implementation already did this, nothing said so. **Pin it in the #395 subscription conformance suite**, because the whole extension rests on it.
- **`startAll()` now exists (ADR 0083, #482, shipped 2026-08-01).** It was cut from ADR 0082 because nothing enumerated subscription ids, so it could only have resumed ids already named and would have silently no-opped. `IntrospectableSubscriptionModel.subscriptionIds()` fixed that. **The obvious source was the wrong one:** the bean post-processors' private `registeredIds` is Spring-only, misses hand-registered subscriptions, and is unreachable from the framework-neutral testing module, so the models report their own ids instead. It is a standalone capability, NOT a method on `SubscriptionModelLifeCycle`, because an abstract method there breaks every external implementation and a `default` returning an empty set is indistinguishable from a model with no subscriptions. **A wrapper implements it only when it knows ids its delegate does not:** the static `of(...)` unwraps a `DelegatingSubscriptionModel` chain, so `Durable`, `Catchup` and the adapters are untouched, but `CompetingConsumerSubscriptionModel` implements it directly because a consumer in the `Waiting` state exists only in the wrapper until `startWaitingConsumer` subscribes on the delegate. Unwrapping past it would under-report in exactly the Spring Boot setup. No reactor twin, no caller for one yet.
- **Every subscription model can now be stopped (ADR 0085, #481 slice 1, shipped 2026-08-01).** The four models built on `RegisteringSubscribable` (synchronous and push, blocking and reactor) had no life cycle, so deny-by-default silently did not cover them and `startAll()` skipped them. **Both classes documented the absence as deliberate** ("a pause would drop them rather than defer them"), and that reasoning was wrong: `InMemorySubscriptionModel.accept(List)` returns early when stopped, so it has the same property and implements the life cycle anyway. Do not re-derive the old argument. The life cycle lives on `RegisteringSubscribable` itself, one change per twin, and the blocking twin also supplies `IntrospectableSubscriptionModel` from the registry it already had. **Dropped, not deferred, is the contract:** an event fed to a stopped model never reaches that handler and resuming does not replay it, which is sharpest for a synchronous projection because the write still succeeds while the projection does not run. Registering on a stopped model yields a paused subscription, which the `mode` property depends on. Cost: `route(...)` pays a volatile read plus a set lookup per registration on every synchronous write. **This collided on ADR 84 with #489, the fourth ADR collision on this line of work.** Mine merged 33 seconds first but moved to 85 anyway, because main's memory already recorded 84 as `0084-what-a-position-guarantees.md` and moving mine touched only my own files.
- **Await conventions stay with `occurrent-tck-subscription-blocking` (ADR 0077 and ADR 94), not duplicated here, and that is now DECIDED rather than open.** ADR 94 demotes the Awaitility factory to a fallback behind a deterministic start position and a start barrier, so what would have moved is smaller than it looked, and the testing module still has no caller for it.
- **#483 is DONE (ADR 95): `occurrent-testing-mongodb` is published, and its flush deletes documents rather than dropping.** ADR 82 deferred it expecting Testcontainers on a published compile path, and **that cost does not exist**: a flush needs a database handle, not a container, so the shipped dependencies are `mongodb-driver-sync`, `junit-jupiter-api` and optional `jspecify`, verified in the flattened consumer POM. **No Occurrent module on its compile path, and that is a constraint:** ~30 modules will test-depend on this leaf once #505 converges the call sites, so a compile edge to an Occurrent module is a reactor cycle. Composition goes through `Runnable` for that reason. **Two reasons to delete rather than drop, and the second is the one nobody wrote down:** a drop invalidates a live change stream (and stopping the model first does not help, it resumes into a collection that no longer exists), AND it destroys the event store's unique indexes, which are created in the store's constructor (`SpringMongoEventStore:145` reaching `:831`, `:862`, `:875`) so a cached Spring context never recreates them, leaving optimistic-concurrency and duplicate-detection assertions passing with no index behind them. That second failure is silent, so it is the worse one. `droppingTheDatabaseIn` still exists because a test asserting a collection or index is **absent** cannot be served by deleting (`MongoEventStoreCapabilityTest:140,147,148`). **The base case names no collections on purpose**, since Occurrent writes to events, `<events>_position`, `<events>_dcb_checkpoints`, `subscriptions` and `competing-consumer-locks`, and a hand-written list silently stops covering one when a feature is switched on. Views and `system.*` are skipped. **It takes `MongoDatabase`, not `ConnectionString`, which is the lesson from `AGENTS.md:127`:** 89 call sites appended a `.collection` suffix believing it selected a database, so minting a published API around that parameter would ship the trap. **No retry loop:** the caller's client is already connected and the driver has its own timeout. **Checkpoint clearing went to the store-neutral leaf, not here** (`OccurrentSubscriptionsExtension.clearingCheckpoints(CheckpointStorage)`), because `CheckpointStorage.delete` already exists and the neutral leaf already depends on it, so putting it in the Mongo leaf would have hardcoded `"subscriptions"` against `OccurrentProperties:235` and excluded a Redis-checkpoint setup. `clearingStateWith(Runnable)` runs after `stop()` and before any resume, so **ordering is no longer the user's `@Order` problem**. **Its tests start their own container** rather than sharing the reused 27017 one, deliberately, because they watch a change stream across a flush and a container another class drops out from under them fails for unrelated reasons. So `testing/mongodb` sits in the `mongodb-native` CI shard and the bare `testing` prefix in `misc` was **split into its two neutral leaves**, since leaving it would run the module twice and `verify-shard-coverage` only fails on zero coverage, never on double. **Left to #505:** converging the 89 in-repo flush sites and deleting `test-support`'s dropping version, whose silent give-up after 10 attempts is a latent bug in this repo's own suite. 77 of those lines carry the connection string #505 is already rewriting, so doing it here would have meant 77 conflicts. **`ReplicaSetReadyMongoDBContainer` deliberately did NOT travel:** it needs Testcontainers on the compile path and reads a build-filtered properties file, so publishing it would ship this repo's Mongo test version as a consumer default.
- **#530 is DONE (ADR 99): `occurrent-testing-junit-jupiter-reactor` is published, and both testing extensions now stop every subscription model they are given, not only the first.** The rename (#529) landed with it: `occurrent-testing-junit-jupiter-blocking` and `-reactor` are a symmetric pair, matching every other paired leaf. **Both extensions took a varargs widening**, `stoppedByDefault(first, more...)` plus a `List` overload, because a Spring context can hold two life-cycle bearing models (the `@Primary` durable one and a `SynchronousSubscriptionModel`), which the single-model form silently left running. `startAll()` now unions ids across every model and `start(id)` tries each in turn, since no DSL wrapper forwards introspection to say up front which model owns an id. **The reactive twin has exactly two differences from blocking, both forced by the types:** it blocks on `Subscription.waitUntilStarted()` and `CheckpointStorage.delete(..)`, both `Mono`-returning on the reactor stack, and introspection is a plain `instanceof` rather than blocking's recursive `IntrospectableSubscriptionModel.of(..)`, since reactor has no `DelegatingSubscriptionModel` to unwrap. **`@EnableOccurrentTesting` now imports an `ImportSelector`** (`OccurrentTestingImportSelector`) rather than one fixed configuration, registering the blocking configuration, the reactor one, or both, by checking which extension class is on the classpath. Both neutral leaves and their subscription API modules became optional dependencies of `occurrent-testing-spring-boot` for this, the same pattern ADR 87 and 95 use. A mixed application gets two extension beans under distinct names (`occurrentSubscriptionsExtension`, `occurrentReactorSubscriptionsExtension`) and distinct types, each stopping only its own stack. **Its tests stay container-free**: there is no reactive in-memory event store, so `SynchronousSubscriptionModel` is the delivery vehicle (no store needed, dispatch is a direct call, life-cycle bearing and introspectable), and two instances stand in for the two-model case. The leaf lodges in the container-free `misc` shard.
- **The neutral leaf must stay usable with no Spring and no container.** Its tests run against the in-memory store and subscription model, which is why `testing` lodges in the `misc` CI shard next to `tck`.
- **`occurrent.subscription.mode` now prevents the boot-time start (ADR 0086, #481 slice 2).** `disabled | manual | auto`, replacing `occurrent.subscription.enabled`, which survives one release as a deprecated alias and fails startup only when it contradicts `mode` (failing when they agree would punish the migration the recipe causes, since a recipe rewrites configuration files but cannot reach an environment variable). **Stopping the model does not work and a session must not re-derive it:** the first implementation stopped the stack on `ContextRefreshedEvent` and passed on a fresh store, then failed across a restart, because a stored checkpoint routes `subscribe` into the catch-up model, which replays out of the event store rather than the change stream. Setting the catch-up `stopped` flag before registration is worse: `shouldKeepReplaying` returns false on entry and `startDelegatedSubscription` hands back a `CancelledSubscription` never registered with the delegate, so the subscription is lost rather than paused. The fix is `ManualStartSubscriptionModel` in `occurrent-subscription-api-blocking`, which defers the whole `subscribe` call and performs it on `resumeSubscription(id)`. It must sit **above** catch-up. **The reactive stack was only half right and that claim must not be repeated:** its durable model declines to subscribe while stopped and sits outermost, so it withholds delivery, but the branch that parks a subscription returned before resolving a start position, so it did not pin. Fixed in #501, then wired to the property in ADR 0089 (#481 slice 3), which needs no wrapper for exactly the layer-order reason above. **The position is pinned at registration, not at start:** `DurableSubscriptionModel` writes `globalCheckpoint()` the first time a start position resolves and `SpringMongoSubscriptionModel.subscribe` forces that resolution, so deferring `subscribe` would make a first run silently skip everything written while it waited. Three paths bypassed the subscription model entirely and are gated now: the saga timer poller, `@Projection(source=PUSH)` and `DomainEventFeed.catchUpAll()`. The OpenRewrite recipe is `org.occurrent.UpgradeToOccurrent_0_32`, and #481 is fully done.

`CatchupSubscriptionModel.stop()` was fixed in the same change: it only stopped the live delegate, so a catch-up replay kept delivering events after `stop()` returned. The children are told via `stopReplay()`/`resumeReplay()` rather than their own `stop()`, because a child's `stop()` reaches the shared live delegate and would fire it once per child. `CatchupSubscriptionModelStopPropagationTest` is the no-Mongo guard, and it fails on 2 of 3 tests without the fix.

### Planned work: TCK (DONE), SQL event store, broker integration (issues filed 2026-07-25)

- **TCK is DONE (#393 umbrella and both children #395/#396 closed 2026-08-05).** Published JUnit conformance suites hold every Occurrent event store, subscription model, `CheckpointStorage` and `CompetingConsumerStrategy` implementation to one contract, so a behavioral divergence between stores or models is a suite failure rather than an undiscovered surprise. Layout: root `tck/` aggregator (excluded from release artifacts) plus published leaves `occurrent-tck-common`, `occurrent-tck-eventstore-{blocking,reactor}`, `occurrent-tck-subscription-{blocking,reactor}`. Suites ship from `src/main/java` (JUnit/AssertJ compile scope) so an out-of-tree implementation can run them, and `tck` lodges in the CI `misc` shard. Nine phases landed across `#457`/`#460`/`#470`/`#476` (event-store dedup), `#521` (skip-detection), `#524`/`#537`/`#538` (subscription phase 6), `tck/subscription-reactor` (phase 7), and `#544`/`#548`/`#555` (phase 8 wrapper models).
- **Load-bearing rules that outlive the arc, do not re-derive or reopen:**
  - A fixture declaration is asserted in BOTH directions and is only ever for a genuinely intended difference, **never to hold a bug**: where stores disagreed for real (#396's four findings), the suite picked a winner and the losing implementation was fixed, not flagged with a knob.
  - `Assumptions`/`Assume`/`@Disabled`/JUnit condition annotations are banned inside suite code. `SkipMechanismScan` enforces it per leaf by scanning compiled bytecode (bytes, not source, since comments do not survive compilation) plus a `SkipsOnPurpose` control class the scan must still catch. The subscription leaf additionally runs its full suite green against `WorkingCheckpointStorage` to prove every guarded line is actually reached.
  - A capability the store was not built with is refused with a named `UnsupportedOperationException`, never answered emptily (`CapabilityGuardConformance`, ADR 93).
  - Position is monotonic with permanent gaps by design (reserved outside the write transaction), never dense, never commit order: never assert a literal position value or that `currentPosition()` equals the last visible event's position.
  - Deliberately NOT shared across stores, so it never gets normalised into a vague cross-store assertion: the cause of `DuplicateCloudEventException`, read skew, `queryOptions` config smoke tests, the Mongo manual-unique-index duplicate case, and anything Mongo-specific (indexes, `TimeRepresentation`, transactions, change-stream error codes, Bson/Json filter specs, the in-memory write `Listener`).
- **Decisions, detail in the named ADR:** ADR 0077 (shape), ADR 0088 (DCB append-condition model), ADR 0093 (capability guards + reactive-only contract), ADR 0094 (subscription TCK: both-direction fixture declarations, `StartAt` restriction mechanism, `RestartConformance` opt-in suite), ADR 0098 (reactor `SubscriptionModel` parity), ADR 0101 (durable reactor subscription delegates when the wrapped model is named).
- **Residual, not shipped:** `CompetingConsumerSubscriptionModel`'s general-conformance wiring waits on **#553** (it accepts a duplicate subscription id and a pause of a subscription that is not running, where the other four models refuse both). The held-back test class is preserved outside the branch, and #553 must not be closed by loosening the suite. **#557** (open) is a routing gap in the reactor catch-up dispatcher for subscriptions it did not create, found during #556's review.
- **SQL event store (#392 umbrella, 16 children).** `eventstore/jdbc/{common,postgresql,mysql}` = `occurrent-eventstore-jdbc-{common,postgresql,mysql}`. Plain JDBC over a `DataSource` (NOT Spring Data JDBC, which maps aggregates), so it works without Spring the way the native Mongo store does, with Spring transaction wiring in the starter. Blocking only, STREAM + DCB both in scope. Supersedes #71 (recommended for closing); #72 stays open as the deferred R2DBC epic and is the named reason the reactive starter has no SQL story. Children: #397 position/live-source ADR, #398 schema + mapping + DDL + position assignment + exception translation, #399 `Filter`/`Condition`/`SortBy` to SQL, #400 PostgreSQL STREAM, #401 DCB, #402 MySQL (the check on whether the dialect seam was drawn right, so **the seam must stay internal and non-public**), #403 SQL `CheckpointStorage`, #404 logical-decoding spike, #405 PostgreSQL CDC subscription model, #406 SQL `CompetingConsumerStrategy`, #407 MySQL binlog model, #408 polling fallback, #409 the annotation-machinery move, #410 the starter, #411 SQL `SnapshotStore` + `SagaStateStore`, #420 docs.
- **Broker integration (#388, the maintainer's pre-existing umbrella, children #412-#419 and #421).** #412 architecture, #413 transport-neutral foundation, #414/#415 RabbitMQ publish/consume, #416/#417 Kafka publish/consume, #418 reactor variants, #419 end-to-end example, #421 docs.

Four load-bearing decisions from the design pass (SQL/broker, not yet built):

1. **A SQL live subscription reads change data capture, not a position poll (#397, #405).** Because positions are reserved outside the write transaction, position order is not commit order, so a `position > checkpoint` tail read can skip an event whose transaction has not committed and then advance past it forever. MongoDB is immune only because its live checkpoint is a change-stream resume token, not a position. A WAL LSN is the direct analogue, so `LsnCheckpoint` goes where `MongoResumeTokenCheckpoint` goes and `globalCheckpoint()` returns the current LSN. The maintainer chose CDC over the simpler lock-until-commit option for consistency with the Mongo architecture and to avoid serializing appends. **Coupled knob:** position strategy `sequence` (default, what CDC wants) vs `commitOrdered` (serializes appends, the only thing that makes #408's polling sound), and wiring polling over `sequence` MUST fail at startup. **Pushback the ADR has to carry:** a stalled replication slot retains WAL until the primary's disk fills (worse than Mongo's benign error 286), logical decoding needs `wal_level=logical` plus privileges that some managed instances and all transaction-pooling poolers deny, there is one slot per store and never per subscription, and CDC does not make positions dense. **ADR 0062's "a feed-derived position watermark is unworkable" line is a MongoDB consequence, not a general law, and the new ADR must say so.**
2. **The broker consume side is a bridge into `Pushable`, not a new `SubscriptionModel` (#412, #415, #417).** The push machinery already ships transport-neutral, and a broker-backed model cannot honestly implement `startAt`/replay. Seams: `EventDestination` (per transport, headers in the record from day one), `DestinationResolver` (derives routing from `CloudEventTypeMapper` so publisher and consumer agree by reading one mapping, and answers the reverse `destinationsFor(SubscriptionFilter)`), and `CloudEventSink`/`DomainEventSink` as the ONE seam a user swaps for their existing publisher wrapper (routing lives behind the sink because their wrapper already decides both). Deliberately not called a "decorator". Both directions work at the CloudEvent AND domain-event level, with domain types in the same artifact as their transport, in a separate package. **Two invariants:** the Occurrent extensions travel as message headers at both levels (otherwise `EventMetadata` breaks and #389 gets worse), and filter-derived bindings only narrow what arrives while `SubscriptionFilterMatcher` still decides.
3. **A SQL Spring Boot starter needed #409 first, and #409 is DONE** (ADR 0072). See the annotation-module section above for what a store starter now owes. The remaining gap for #410 is `OccurrentProperties`, which is store-neutral in code but not in configuration surface.
4. **DDL ownership is an explicit decision, not a port of Mongo's behavior (#398).** Mongo creates missing indexes per enabled capability at startup, which does not transfer to a production relational database, so this needs shipped DDL, opt-in auto-create for dev/test, and a documented Flyway/Liquibase path.

Docs (#420, #421) follow the release-gated held-branch convention, and **#420 must carry the CDC operational prerequisites** (`wal_level`, privileges, managed-instance parameters, slot-lag monitoring), because getting those wrong takes a database down. **Release-time task carried over from the TCK's canonical-time work:** the docs branch `docs/canonical-time-storage` (occurrent-org.github.io#18) is held until it ships.

### Deferred, not blockers

- **Two candidate fixes declined in #383 to keep the move verbatim,** both pre-existing: `StartPositionSupport.toOffsetDateTime` (blocking) has a dead inner catch and lets a malformed ISO8601 escape as `DateTimeParseException` instead of `IllegalArgumentException`; the reactor `SnapshotAnnotationRegistrar`'s "must return a SnapshotView" message should also name `DcbSnapshotView`.
- **P3: `@Projection` catch-up per-event read-modify-write.** Batching writes during replay needs the catch-up model to tell the runner which delivery is catch-up vs live, and that seam does not exist (`ProjectionRunner` subscribes with a single `Consumer` invoked identically in both phases). A clean fix means adding a delivery-phase signal to the SHARED catch-up infra on both stacks, out of proportion for a projection-dsl change. Left as an infra follow-up.
- **Cross-`@Projection` helper dedup (C8).** `SnapshotView.HandlerDispatch` is a byte-copy of `Projection.HandlerDispatch` (extract into `view-dsl`), and the snapshot BPP copies of `filterFor`/`reflectStateType`/`invokeFactory` duplicate the projection ones (share via `ProjectionFilters` + a common reflection helper). Do this as a standalone projection+snapshot BPP cleanup.
- **Reactor `batchSize`/`maxAwaitTime`** (hazard documented above), plus moving native's `createPipeline` into `common/base`.
- **C5 reified widening; C6 reactive naming split** (`Reactive*` prefix vs bare in `dcb-dsl`/`Projections`); **P4/P5/P6 minor perf** (sync-subscription enrichment read paid when no handler type matches; DCB enrichment re-fetch, a contract change; registrar `findLatest`-per-event cache, overlapping P1). Minor, unscheduled.
- `Projection.compose`; a reactive `ViewStateRepository` materialization convenience; a singleton projection form so single-instance read models can skip `id { }` (issue #331).
- **`queryForSequence` closing semantics.** Kotlin `DcbDomainEventQueries` Sequence/List extensions are leak-free (they materialize into a `List`). Open: whether the stream-side `DomainEventQueries.queryForSequence` needs a closing/loan-pattern variant when the store returns a live Mongo-cursor-backed `Stream`.
- Two `simpleName!!` non-null-assertion threads on PR #257 were left as a deliberate open disagreement with a Copilot suggestion. A safe fallback for anonymous event classes in the stream/DCB DSLs is a small standalone follow-up.
- **Counter-contention benchmark deferred** (ADR 0045 non-goal). If an open-source user reports scale pain, benchmark naive vs block-reserved against a production-like Atlas cluster (WiredTiger write-ticket/write-conflict metrics); sharded counters are the escape hatch.

### Consciously not done (would be net-negative, do not re-raise)

- **Splitting `Saga.Step.effects()` into `issuedCommands()` + `timerEffects()`** (considered while fixing #448, rejected). `effects()` is the transition's ordered log and the executor reads it as one pass, where timer order is load-bearing (`cancelTimeout("x")` then `startTimeout("x")` is a re-arm, the reverse is a cancel), so two buckets would make the interleaving unreconstructable. It also breaks a shipped record's constructor, `equals` and deconstruction pattern for no functional gain, and OpenRewrite cannot split one list into two, so the migration would be by hand. `effects()` stays, `issuedCommands()` is a derived reading of it, and the timer effects get no accessor because `SagaEffect.cancelTimeout(...)` is already an assertable value while a command is wrapped in `IssueCommand`. An `Effects<C>` wrapper type was rejected too: the name collides with the `SagaEffects<C>` reaction receiver, and it would have to spread across `react`, `onStart` and `Step` to be coherent.
- **Renaming `ReceivedEvents.initiating()` so the reified Kotlin `initiating<T>()` resolves without an import** (issue #449, rejected as *ineffective*, not merely expensive). A top-level Kotlin extension in another package always needs a named or star import to be in scope, before overload resolution has anything to rank, so no rename removes the import. The member only changes the diagnostic: it makes a missing import report "No type arguments expected for `fun initiating()`" instead of an unresolved reference, and it makes the type argument mandatory (`val e: OrderPlaced = received.initiating()` binds the member and fails on type mismatch). Note the issue's claim that `first`, `all` and `count` shadow too is **wrong**: they take a `Class<T>`, so a no-arg reified call is not even applicable to the member. This is one diagnostic wart on one name. ADR 12's usual remedy (rename the Kotlin side) is not available either, since it would not remove the import and would cost the good name. Fixed by documenting it in `ReceivedEvents.initiating(Class)`'s javadoc, `ReceivedEventsExtensions.kt`'s header, and the docs snippets. This is a deliberate carve-out from the AGENTS.md rule that Kotlin extensions must not collide with Java members, and it is tolerable because the collision is always a compile error and never a silent mis-binding, which is the harm ADR 12 was written against.
- A shared `SnapshotDocuments` Mongo helper: no clean shared module for ~15 lines, so adding a dependency is overengineering.
- Collapsing the start-position converters via `Enum.valueOf(name)`: trades a compile-time-exhaustive switch for runtime name-coupling.
- **"Agnostic `Subscriptions` delivering DCB events makes `EventMetadata.streamId` crash" is FALSE.** DCB events are stored via the shared stream mapper with a generated partition stream id, so `EventMetadata.streamId` returns that partition id.
- **Documented gaps consciously accepted for this release:** no reactive competing-consumer model (ADR 0044), and reactive `@StreamSubscription` cannot honor a specific historical start time (ADR 0045). The latter fails loud, and a correct fix needs time-to-position translation with clock-skew handling.
- **Cross-store sort caveat:** a natural sort step combined with other sort steps is a valid insertion-order tiebreaker in-memory but is rejected by the MongoDB stores (documented on `SortBy.natural`), because MongoDB cannot express a natural sort inside a compound sort. On 7.0+ the server rejects it; 4.x silently dropped the field keys.

## Orchestrator Operating Notes

- Read this file first at every session start. It is ground truth over in-context recollection.
- **Every orchestrator planning round runs /johan-plan** (Johan's standing instruction, 2026-08-08, granted via structured ask; it satisfies the skill's explicit-invocation rule). This covers epic registration plans, meta-plans, and dispatch plans: the full review checklist in the skill's references for complex plans, its short self-review for simple ones, always completed BEFORE presenting the plan for approval. Exploration, write, exit is not a planning process; the plan must survive one adversarial self-review first (see lessons.md, ccpause registration).
- **Concurrent sessions share only GitHub.** Before starting or even proposing work on an issue, check it is unclaimed (`in-progress` label, assignee, recent claim comment) and claim it with the label plus a claim comment before the first edit. Release it when a PR is opened or the work is dropped. Full protocol in `AGENTS.md` ("Claiming a GitHub issue before working on it"). Pass the same rule into every delegated brief that touches an issue — subagents do not read GitHub on their own initiative.
- Route rather than default to doing the work: delegate substantial work (roughly 10+ minutes, 2+ files, or anything in a fragile area), handle trivial one-line/single-file work inline, and ask the user one consolidated question only when they alone can decide.
- Before delegating, re-read the relevant sections here and use code-review-graph where it helps (`get_minimal_context_tool`, impact radius, relationship queries). Briefs must carry goal, owned files, forbidden files, conventions copied in verbatim, exact `rtk` verification commands, out-of-scope, and the blocked-resolution protocol.
- **Every sweep tick delivers its summary through a channel that renders.** Prose written before the re-arming ScheduleWakeup call collapses in Johan's view, so: send a PushNotification headline (under 200 chars: merged/moved/needs-him) before re-arming, and additionally attempt a short final text message AFTER the ScheduleWakeup call (if the harness lets the turn continue, that text renders as the turn's final message; if not, the push already covered it). A tick where nothing changed still pushes "no change". Decisions keep going through AskUserQuestion, which renders on its own.
- **Fleet-sweep staleness is judged on the work item, never on session activity.** A pr-fix watch refreshes its session's timestamp on every no-op pass, so an idle-since-last-tick heuristic misses human-gated holds entirely (it cost two units a multi-hour stall on 2026-08-06). Track each open PR's (rollup, unresolved-thread count) across ticks; unchanged for two ticks with a cycling session means read the tail, extract the decision it is waiting on, and route that decision (resolve it under delegated authority, or AskUserQuestion with a recommendation) the same tick.
- **Always arm the v7 pattern from `references/fleet-monitor.md`, never a fresh monitor, however simple the epic looks.** A hand-rolled one keyed on the open-PR set reports only arrival and departure, which is the least useful pair: arrival already arrives as a task notification, and departure comes after the merge decision was needed. v7 selects `statusCheckRollup` and derives a `DONE`/`running` flag, so it wakes you when a PR becomes merge-ready. This cost U18 about twenty idle minutes on 2026-08-10 with Johan asking for status twice, which is a detection defect by the skill's own rule. Detail in `lessons.md`.
- **A green matrix is not a green PR, so the merge gate checks review threads too.** Query `reviewThreads` filtered on `isResolved == false` alongside the check-run rollup, and treat empty output as part of the merge condition. `mergeStateStatus: CLEAN` does not mean review is finished, since this repository does not require conversation resolution to merge. This is the enforcement half of the pr-fix rule above, and it is here because the rule above was silently omitted from four briefs in a row on 2026-08-11 and nothing caught it until Johan did. A reasoned-decline thread stays open on purpose under the policy, but that has to be a decision someone took rather than one nobody saw. Detail and the query in `lessons.md`.
- **Every implementation brief includes a /pr-fix loop over the unit's PR** (Johan's directive, 2026-08-06): loop until CI is green and every review thread is addressed and resolved, flagging disagreements instead of burying them. Docs-repo units run the loop too (revised 2026-08-06, superseding a same-day significance carve-out): the docs repo has almost no CI so iterations are cheap, and Copilot review catches real defects there (dead anchors, identifiers drifted from shipped API, cross-section contradictions). Markdown-only library PRs and docs PRs run few or no CI checks by design, so their loop is about review comments, never about waiting for checks. Landing/merging stays with Johan unless a brief grants it.
- After delegated work completes, review scope and actual verification (verification you cannot see did not happen), then update this file as a durable map, not a change log.
- After any correction from the user, record the pattern in `.context/lessons.md` (it does not exist yet; create it on first correction).
- **A subagent can inherit a stale plan-mode stance whenever the session recently passed through plan mode at all, not just while it is active.** Tell: a fork returning near-instantly with zero tool calls and a summary that parrots your last message, or one that writes a plan instead of implementing. The fix is to ensure the orchestrator itself has genuinely exited plan mode, then **resume the SAME agent in place via `SendMessage` to its agent ID** with an explicit "you returned 0 tool calls, that was wrong, you are NOT in plan mode, do the work now". A coordinator message alone cannot lift a subagent's permission mode. Never spawn a fresh `Agent` call to correct a subagent: that starts an unrelated agent with no memory and wastes the exploration already done.

## 0.33.0 docs preview, 2026-08-10

- **The release-day held set is now EIGHT PRs, not seven.** Order: 49, 52, 53, 54, 55, 58, 59, then 61. PR 61 (`docs/707-step-conditions`) appeared after the rv33 closeout and its own title says it merges at the 0.33.0 release, but the library feature it documents does NOT exist on main yet (no `StepCondition`, ADR numbering stops at 119) because the stepcond epic is still in flight. **Do not merge 61 unless #707 has actually landed in the library**, or the docs will describe an API nobody can call.
- **Preview branch `docs/0.33.0-preview` exists and must NEVER be merged.** It is all eight branches merged together plus a local re-date of the news post so Jekyll renders it. Delete it after the release. Build it with `LC_ALL=en_US.UTF-8 bundle exec jekyll build`, because without the locale Ruby reads the SCSS as US-ASCII and the build dies on a non-ASCII character.
- **Two defects found by building the merged result, both fixed on their own branches.** PR 58 renamed the capability types but left the method as `of`, so `IntrospectableSubscriptions.of(..)` and `ReplayAwareSubscriptions.of(..)` shipped against a library that only has `findIn` (fixed, `67df554`). PR 52 used the two-tab `docsSnippet.html` macro for two Java-only snippets, and with `kotlin` unset the macro emits a bare `~~~kotlin~~~` that kramdown reads as an unterminated fence, which swallowed the `#the-projection-annotation` and `#projection-annotation-without-starter` anchors (fixed by plain fences, `4d494e2`, measured 2 anchors broken before and 0 after). **The general lesson: a docs rename PR needs the merged site built and its anchors counted, because neither defect produced a git conflict or a broken link that review would see.**
- PR 54's news post was corrected on its own branch (`861f79b`) but now names a WITHDRAWN API and needs that mention cut by UDOCS: `AppliedProjectionPositionStore` (withdrawn, remove), an intro and summary that cover checkpoint fencing rather than the read side alone, a section on the capability renames plus the `UpgradeToOccurrent_0_33` recipe, and two behavior-change bullets. Its `2026-08-24` filename date is still the documented placeholder and still needs setting to the real release date.
- PR 55 still shows the pre-correction news post because it is branched from 54 and behind it. The rebase that issue 57 already prescribes resolves this, so nothing to do beyond not forgetting it.

## rv33 reopened for U18, 2026-08-10T17:06Z

- **U18 adopted: `any`/`none` predicates on `ReceivedEvents`.** Johan read `received.all(Rejected::class.java).isEmpty()` in the step-conditions docs example and asked for a `none` predicate plus reified Kotlin forms. Chip `task_117d1dc6`, Sonnet/high, base `origin/main`. Work-item monitor re-armed as `brlnuhl57` under the lifetime rule, since an adopted unit reopening a closed epic re-arms it and that unit's own sweep retires it.
- **What the investigation changed about his ask.** The reified `all<T>()` and `any<T>()` he asked for ALREADY exist in `ReceivedEventsExtensions.kt`, so the Kotlin half is shipped and the docs example simply was not using it. The real gap is that neither stack has `none`, and the Java interface has no `any` either, so a Java reaction can only write `all(X.class).isEmpty()`. Design is two `default` methods on the interface plus one reified Kotlin `none<T>()`, with the existing `any<T>()` retargeted onto the new Java member so both stacks share one implementation.
- **The docs example is guard-tested twice**, `DocumentedFlowSagaKotlinTest.kt` and `DocumentedFlowSagaTest.java`, so a docs-site snippet change is never standalone in the saga chapter. Worth remembering for any future saga docs edit.
- **Naming asymmetry recorded, deliberately not actioned.** Kotlin collections use `any`/`none`/`all` as a predicate triple, but this API's `all<T>()` returns a `List`. The released name already breaks that symmetry and renaming it is a break for a later release, so U18 leaves `all` alone.
- **Cross-epic touch.** U18's docs-site snippet lands on `docs/707-step-conditions` (PR 61), which the `stepcond` epic owns, and this session also edited that branch earlier for the `join` restructure. That epic's U3 and U5 are still unswept and its recorded release-day trap is that six of seven held branches carry a stale "a step is either a set of `on(...)` branches or a single join" claim which a later merge can reintroduce. Coordinate before release day.

## Tenth epic registered 2026-08-10T19:51:25Z: timerid, a saga timer's identity becomes a value (#716)

- **State** `.context/epics/timerid.yml` (5 units), plan `/Users/johan/.claude/plans/plan-to-fix-this-serialized-storm.md`, approved by Johan after he rejected the first two proposals. Registration timestamp above is the tracker-scan lower bound. Work-item monitor `bn9yxelys`, the **v7 pattern from `references/fleet-monitor.md`**, MONITOR-READY at 0 open PRs so the arming sweep had nothing to act on. U1 chip `task_3b940167`, Opus/high. Issue 716 claimed with the label and one routing comment.
- **The framing Johan corrected, and it is the durable part.** #716 reads like a testing-ergonomics complaint and my first plan answered it with a helper that builds the `step:` string. He rejected that as treating a symptom: the prefix is a modelling defect, the model should distinguish timer kinds itself, and the encoding belongs only in the database. He is right, and `doc/architecture/decisions/0063-saga-dsl.md:117,173` already records the same gap from the other side, deferring `compose` partly on timer-name collisions between child sagas.
- **Do not relitigate** (decided by the approved plan): timer identity becomes a sealed value in the saga core, one case a plain name and one a namespaced name; the flow layer owns the `step` namespace and stops concatenating; the on-disk format is unchanged so no in-flight timer is stranded; encoding happens at one boundary only; **no interim string helper**; ships in 0.33.0, which Johan holds for it.
- **The rejection worth keeping.** Johan asked for a two-string shortcut, `SagaInput.timeout("game-1", "awaiting-players")`. It is not offered, because core cannot tell a step name from a timer name, would parse it into the unqualified case, would never match the flow's namespaced one, and would be consumed as a silent no-op. That is the exact failure this epic deletes, in its most inviting shape. The shortcut that ships takes an identity instead, `SagaInput.timeout("game-1", stepTimer("awaiting-players"))`.
- **A constraint that shaped the design:** there is no `module-info.java` in the repository, so the unnamed module forces a sealed type's permitted subtypes into one package. The flow layer therefore cannot contribute a case to a core sealed type, which is why the flow distinction is carried as a namespace value rather than as a type. That turns out to be what `compose` needs anyway, since composition wants arbitrary child namespaces.
- ADR number **0121**, chosen after auditing every remote branch where 0120 was the highest claimed. U1's brief tells the worker to re-run that audit, because a number claimed on an unmerged branch has already collided here once.
- rv33 stays closed at revision 50 with 18 units DONE. Its file, sessions and branches are off-limits to this epic's sweeps, as are the `stepcond` epic's.

## timerid progress, 2026-08-11

- **U1 merged, ADR 121 as squash `7a9a5cf8b`.** It went through an adversarial review that returned REVISE with six must-fix, all addressed and re-verified, then a final edit so it records Johan's ruling rather than posing it. **Do not relitigate the ADR as merged:** `TimerName` with `Simple` and `Qualified`, `parse(String)` total and `of(String, String)` refusing a colon in the namespace (two names because they differ in whether they throw, which one overloaded `of` would hide behind an argument count), `toString` returning `encode`, the string API kept and NOT deprecated, `SagaTimeout` with one constructor taking the value and no two-string form, the on-disk format unchanged, and `compose` given a representation for namespacing but explicitly not an answer.
- **U2 merged as squash `ba2d1f791`**, matrix 27 of 27 on its head and 26 of 26 on main after. `TimerName`, the value carried through `SagaTimeout` and `SagaEffect`'s three timer records, overloads across `SagaEffect` and `Saga.Builder`, `SagaInput.timeout(String, TimerName)`, the registration maps re-keyed, and the Kotlin overloads. Both round trips are property-tested over a corpus with unicode, 10k-character names and ISO timestamps.
- **U2 merged with an unresolved Copilot thread, and the thread was right.** `SagaInput.java:82` javadoc cited `stepTimer(..)`, which did not exist yet. Two orchestrator failures: the brief omitted the standing pr-fix loop, and the merge gate never looked at threads. Fixing that javadoc is a required deliverable of U3. Both failures and their fix are in `lessons.md`.
- **Johan's ruling, 2026-08-11:** take the source break and take the constructor change, on the grounds that it is what is best long term under the AGENTS.md principles. That reverses the approved plan's claim that the change would be additive.
- **U6 was missed at registration** and added later. The plan assumed an additive change, so no unit owned the OpenRewrite recipe and the migration-guide section that `AGENTS.md:75` makes mandatory once a feature has shipped. Found by the adversarial review, not by the orchestrator.
- Release-day effect: 0.33.0 is held for this epic, and U5 adds a ninth held docs branch to the merge order in `occurrent-org.github.io#57`.
- **U3 merged as squash `1f17d6e0f`**, U6 as `19a5825fa`, U4 as `75fa299e2`, each on a green matrix with zero unresolved threads, the thread gate run before the merge rather than after. U3 exposed `FlowSaga.stepTimer` with a Kotlin twin and made the flow match site call it, so production and tests read the same helper. U6 folded `MigrateSagaTimerName` into `UpgradeToOccurrent_0_33` (it rewrites the two provable shapes and marks the rest) plus section 7 of the upgrade guide. U4 is the persistence proof, `StoredTimerNameTest` seeds a store the way 0.32.0 wrote it and runs the real poll over `step:awaiting-players`, `payment` and `a:b`.
- **U6 depends on U3 as well as U2, corrected during the epic.** Depending on U2 alone would have let the migration guide document `stepTimer` before it existed, which is the same defect PR 719 shipped.
- **timerid EPIC CLOSED 2026-08-11T07:47Z at state revision 13**, six of six units DONE and swept, main 27 of 27 green on `75fa299e2`, issue 716 closed with the summary comment, monitor `bn9yxelys` retired under the lifetime rule, tracker scan clean from the registration timestamp. U5 delivered docs PR `occurrent-org.github.io#64` (held, last in the merge order after 55, no `addedTags` entry needed, verified after merging with `git grep 'step:'`), the news bullet on PR 54's branch, and the timer name added to that post's source-breaking paragraph. The orchestrator added one clause naming `stepTimer` where the chapter first uses it, since the worker left the symbol standing 15 lines ahead of the sentence that introduces it.
- **U5's second deliverable was obsolete, not skipped.** It asked for a changelog PR in the library repo, written before U6 existed. U6 folded that entry in, so a second PR would only have duplicated `changelog.md` line 41.
- **The closeout sweep found a stranded commit, which is the sweep earning its place.** `8851a68e0` landed on `timerid/u4-persistence-proof` at 06:59:40Z, two minutes after the CAS merge pinned `0804b1dbd`, so the worker pushed it to a branch GitHub had already deleted and recreated it in the process. It is the read direction of the Mongo proof, a 0.32.0-shaped document inserted by hand and read back, which the existing write-then-read test cannot cover because it only proves the store agrees with itself. Adopted as U7 and cherry-picked onto main as PR 724. The merge gate is the wrong place to catch this, since the head was right when it merged. What catches it is a sweep that asks whether each unit branch is behind the merged head instead of treating a merged PR as a finished branch.
- **U7 merged as squash `593f34bba`**, 26 of 26 on its head, zero unresolved threads, Copilot reviewing with no findings. Epic re-closed at revision 15, monitor `bl25n5umk` retired, `timerid/u4-persistence-proof` deleted with its content confirmed on main first, every unit worktree removed and all six worker sessions archived (plus `stepcond/U1`, which had been left unarchived since that epic closed).
- **A chip session leaves a session branch behind that archiving does not remove.** The six timerid workers each left one (`johan/sleepy-merkle-0e3f9a`, `funny-agnesi-f443d1`, `unruffled-spence-c6a726`, `practical-vaughan-79671d`, `ecstatic-pike-e1e658`, and `johan/silly-tu-d26a72` in the docs repository), all with zero commits of their own, plus U5's docs worktree and two remote-tracking refs pointing at branches GitHub had already deleted. A sweep that removes only the work branch and the worktree looks finished and is not, so check for the session branch and finish with `git remote prune origin` in both repositories.
- **A verified Copilot request needs the bot id read from a real review, not guessed.** The first request on PR 724 passed an invented `botIds` value, the mutation returned the PR number, and `reviewRequests` stayed empty. `BOT_kgDOCnlnWA` is the id, readable from any earlier review's author, and the request has to be confirmed by reading the request list back. This is the GraphQL-layer twin of the REST failure already recorded, where the POST returns 200 and adds nobody.
- **The preview and the release-day issue were both stale after U5, found by checking rather than by assuming.** `docs/0.33.0-preview` still taught `step:` in five places and its news post had no timer bullet, since a held branch does not reach the preview on its own. Updated to `04ecbd5` by applying U5's two patches with `git apply -3` (never a blind merge, per the criss-cross duplication this repository already produced) against the preview's own news filename, which is date-shifted to `2026-08-10-0_33_0.md` so Jekyll does not skip it, then verified by building the site and grepping the rendered page.
- **Docs PR 63 was missing from the merge order entirely**, found while checking the preview. `occurrent-org.github.io#57`'s body is now the authoritative release-day checklist, rewritten because the order had spread over eight comments with three of them each claiming the last slot. Order: 49, 52, 63, 53, 54, 55, the unopened `docs/707-step-conditions-release-note`, 61, 58, 59, 64, then 62 after the release. 63 sits after 52 because the header rename only reads as a pair once the asynchronous section exists, and no branch links to the old anchor.
- **Two issues surfaced during this epic and belong to a later one:** #718 (parallel competing consumers without a subscription-wide lease) and #721 (the name clash between `@Projection` and `Projection`). #718 is still unclaimed. #721 is ANSWERED by ADR 127 (PR 819, r34 U15), so do not treat it as free work or re-decide it. PR 819 has no closing keyword for #721 or #725 and must not, because both ask for code changes the ADR only designs, so both stay open through the implementation epic and are closed by hand at the end of it. #725 keeps its claim released, since the PR is the stronger claim on the design half only.

## Eighteenth epic registered 2026-08-22T08:0xZ: sdi, the ADR 0127 subscription-descriptor implementation (#725, #721)

- **State** `.context/epics/sdi.yml` (schema v1, 10 units U1-U10, revision 1), plan `/Users/johan/.claude/plans/sdi-subscription-descriptor-725.md`, journal `.context/orchestrator/sdi/` (local-only, matching ayi and rel34; main's `.gitignore` negates `decisions.jsonl` since `ec9fb808c`, but the primary checkout sits on a docs branch whose `.gitignore` predates that, so tracked mode would promise a commit that cannot happen. Converts to tracked at closeout if the primary is back on main). Registration timestamp above is the tracker-scan lower bound. Milestone `0.34.0` is number 3, existing, nothing created. Mode ATTENDED. Session worktree `admiring-bhabha-362d44`, branch `johan/admiring-bhabha-362d44`. PLAN PENDING Johan's approval; four open questions listed in the plan.
- **Definition of done:** #725 and #721 closed by merged PRs on main; ADR 0127 implemented rather than reinterpreted; breaking changes carried by `org.occurrent.UpgradeToOccurrent_0_34` additions plus a section in `doc/migration/upgrading-to-0.34.0.md`; changelog entries under the existing `### Changelog next version` heading; the docs branch HELD for release day, never merged by this epic; memory checkpointed. **rel34's R3 gates on this epic closing.**
- **ADR reservation: NONE TAKEN.** Re-audited 2026-08-22T07:5xZ across `origin/main` and every remote branch: max is 0133 (brk's, merged), rel34 holds 0134 for #818. sdi implements a decision already made, so it needs no new ADR on the current plan. If one becomes necessary, re-audit first, allocate 0135 or higher, record the reservation here BEFORE the brief goes out, and hand it to the unit as a resolved literal.
- **Cross-epic exclusions, both directions.** sdi's sweeps exclude every rel34 unit (#876, #871, #857, #877, #878, #879, #891, #837, #866, #834, #753, #867, #818 and the release mechanics) and every brk unit (#388, #421, #893, #896). rel34's and brk's sweeps exclude #721 and #725. Coordination runs ONLY through this file, fetch-rebase-push at every checkpoint.
- **THE FENCE IS WIDER THAN A DIRECTORY, and sdi found a second gap by reading the issues.** rel34 already recorded one widening (U3 now holds two bean methods in `framework/spring-boot-starter-mongodb/.../OccurrentMongoAutoConfiguration.java`). sdi confirms that widening bites: the `SubscriptionHandle` rename touches three files in `framework/spring-boot-starter-mongodb/src` as well. The second gap is semantic and has no shared file at all. **#753 changes `EventTypeExpansion` in `common/filter`**, whose `deriveFilter` is what `dsl/subscription-dsl/common/.../SubscriptionFilters.kt` calls today and what sdi's new descriptor selector must call. One of #753's own listed remedies is refusing a non-final concrete declared event type, which would change what a descriptor accepts. **sdi's descriptor unit therefore waits on #753 whatever directory that fix lands in.** rel34: this is not a request to change #753, only a note that its landing gates sdi U2 semantically, so record its merge here as you do the others.
- **#871's DSL-bean risk CHECKED AND CLEARED, not assumed.** The issue proposes carrying the fact "on the DSL bean itself", which would have put rel34/U3 inside `dsl/subscription-dsl/**`, sdi's own module. rel34's dec-0005 shows U3 went to `ComposedDefaultStartPosition` plus the starter's auto-configuration instead, so the DSL beans are untouched. No action for rel34; recorded so a later sweep does not re-raise it.
- **#837 routing, settled and not to be re-litigated.** #836 (the proxy-unwrap prerequisite ADR 0127 names) is CLOSED and shipped as r34/U19, so sdi does NOT inherit it. #837 was routed here first and then explicitly pulled back to rel34 as a standalone fix, recorded on the issue. sdi's registrar unit must PRESERVE that fix on the deprecated reflective paths, which outlive 0.34.0, rather than dropping it while rewriting the surface.
- **Sequencing, with the evidence rather than the assertion.** The handle rename goes first among the fenced units because after the descriptor lands, `dsl/subscription-dsl/blocking` would hold a `Subscription<E>` in the same package as `Subscriptions.kt`, which imports `org.occurrent.subscription.api.blocking.Subscription` for its return type. Both languages resolve that in favour of the explicit import, so it compiles and every unqualified `Subscription` in the file silently means the handle. Three units (descriptors, DCB descriptors, annotation types) are unfenced and carry enough work to absorb the wait for three small rel34 units, so the fence is not expected to bind.
- **Measured facts the ADR's own numbers are stale on.** The rename is 71 blocking plus 32 reactor importing files (103), not the ADR's 78, because main gained the broker modules since 2026-08-16. `dsl/subscription-dsl` is Kotlin-only (0 Java main sources against projection-dsl's 29), while every descriptor the ADR cites as precedent is Java. Adding Java there costs no pom change: `dsl/projection-dsl/common` holds both source roots while declaring only the bare `kotlin-maven-plugin`, so joint compilation is inherited from the root pom.
- **PLAN REVISION 2 after a fresh-context adversarial pass** that returned REVISE with fifteen findings, every factual correction re-verified independently before folding and every one holding. Now ELEVEN units, not ten. Three pieces of ADR-required work had no owner: the declarative annotation `ChangeType` recipes (now U5, precedent `annotation-enums.yml:33-46`), the annotation-set normalization across the other five registrars (split out as U7), and the Kotlin migration of eight example handlers the Java visitor cannot reach (U10). Changelog and migration prose DECENTRALIZED to the unit that breaks each thing, because centralizing them would have had five units merge public-API changes with no entry, against `AGENTS.md`. The fixpoint demoted from a unit to a phase.
- **Three measurement corrections worth carrying, for the method rather than the number.** The rename is 78 blocking plus 37 reactor equals 115 importing files, not 103: my grep required a trailing semicolon and so silently excluded every Kotlin import. The fence intersection is 9 files with 2 main, not 7 with 1: the reactor `ProjectionAnnotationRegistrar` writes the type fully qualified inline rather than importing it, which it does because of the very name clash this epic removes. And there are SEVEN annotation registrars totalling 3309 lines, not four: the ADR's "four registrars" counts subscription-annotation registrar PATHS, and carrying that phrase forward as a claim about registrar FILES lost the reactor Projection (776) and reactor Snapshot (356) ones.
- **`buildDcbCriteria` is the epic's quiet structural problem.** ADR 0127 requires `DcbSubscription.tags(..)` to narrow "exactly as `buildDcbCriteria` does today", but that method is `public static` in `framework/spring-boot-autoconfigure/common/.../SubscriptionAnnotations.java:226`, a Spring module, while the descriptor is Spring-free by design. So U3's FIRST deliverable is extracting it with the Spring caller delegating, not writing the descriptor. Rewriting it instead is the epic's highest-risk line by #725's own decomposition, and `AGENTS.md` warns about independently derived duplicates by name.
- **Fence surfaces, final: FOUR, not one.** `framework/spring-boot-autoconfigure/{blocking,reactor}/**` as briefed, plus `.../common/` (where `SubscriptionAnnotations` lives), plus `framework/spring-boot-starter-mongodb{,-reactive}` (rel34's own dec-0005 widening, which sdi's rename confirms bites both ways), plus the semantic edge on `common/filter/EventTypeExpansion`. Every other rel34 issue (#876, #857, #877, #878, #879, #891, #866, #867, #818, #834) was checked against the units' files and none overlap.
- **U2's #753 gate clears on RESOLUTION, not on a merge.** #753's own author recommended leaving it unfixed and reconsidering it with #751. A decline therefore clears sdi U2 exactly as cleanly as a fix does, so rel34 should record either outcome here.
- **Journal exposure window 08:00Z to 08:30Z: sdi was NOT affected, verified rather than assumed.** A `.gitignore` negation briefly made decision journals tracked and two fleets' journals reached public main before Johan's revert `9eecf8b30`. sdi's checkpoint at `9ca27ae38` touched exactly `ORCHESTRATOR.md` and `epics/sdi.yml`, because every checkpoint here uses a path-scoped `git add` and never `-A`. sdi's journal also sits in the primary worktree, which is on a docs branch. Both facts held independently; the path-scoped add is the one to keep.
- **First wave at the gate is ONE unit, U5.** Adding the U1-to-U2 edge (they collide on `Subscriptions.kt`) makes everything except U5 transitively fenced. U2 and U3 are developable on their branches before U1 merges but not mergeable, and U2 additionally waits on #753 resolving. This is the honest schedule and it is stated rather than smoothed over: revision 1 claimed three unfenced units would absorb the wait, and that was false once the edge was drawn.
- **PLAN GATE, Johan's three rulings 2026-08-22T08:3xZ, journal dec-0001 to dec-0003. Do not re-ask dec-0001 or dec-0002.**
- **dec-0001: sdi MOVES TO 0.35.0 and rel34's R3 is UNGATED.** #725 and #721 re-milestoned to milestone 4, verified on both issues; milestone 3 dropped to open=18. Johan took the recommended option after being shown sizing his original 0.34.0 triage ruling was made without: eleven units, a 115-file rename, a 3309-line registrar surface, 138 source files to sweep, and eight of eleven units fenced behind three rel34 fixes of which one was dispatched. rel34 was told directly rather than left to read this file, because a release was waiting on it. The fence itself is UNCHANGED: sdi still holds until #871, #837 and #753 merge, but the fence now costs the release nothing, and rel34 was asked not to reprioritise those three on sdi's behalf.
- **dec-0002: convergence is MAJOR-and-above, cap FIVE rounds.** Johan chose more rounds than the recommended three, which matched rel34 and brk. Reaching the cap ends the loop and is reported as reaching the cap, never folded into convergence; the remainder returns as one structured ask; below major is filed as later-release issues.
- **dec-0003 is NOT decided and is the gate's remaining item.** Johan declined all three offered options on the Kotlin `Subscriptions` DSL and directed: investigate what the best solution is and do what is best long-term on `AGENTS.md` principles. So the recommendation to leave it out of scope is explicitly NOT approved. Two AGENTS.md lines point away from it and were the weak part of that recommendation: pre-1.0 means past mistakes get corrected and a breaking change with a recipe is preferred over carrying a known-wrong design into 1.0, and existing structure is not a constraint to design around. One mechanism is open that the recommendation did not consider: ADR 0127 is UNRELEASED, so `AGENTS.md` permits amending it in place rather than writing a successor. Investigation running; no unit brief may depend on the answer until it concludes.
- **Deprecation text decided by derivation, not asked.** The seven `@Deprecated(forRemoval = true)` annotations carry no `since` and name no removal release, because all twelve existing `forRemoval` deprecations in this repository use exactly that bare form.
- **rel34's fence rule, adopted as binding for sdi briefs:** derive a fence from where a symbol is USED and CALLED, never from where it is DECLARED, and never from a grep anchored on import syntax. rel34 and sdi hit the same shape from opposite sides within an hour, rel34 through U3's cross-module BLOCKED and sdi through the reactor `ProjectionAnnotationRegistrar` that writes the type fully qualified inline.
- **I misread #753 and the correction is worth more than the fact.** I reported that its author recommended leaving it unfixed. That text carries `<!-- orchestrator-routing -->` and is an orchestrator's, and the 2026-08-22T06:51Z comment supersedes it by pulling #753 into 0.34.0. The broader form, which is wider than rel34 stated it: every session in this fleet writes under the johanhaleby account, so authorship proves nothing on ANY surface, and the stale recommendation is in the issue BODY too, marked `⌁[cdx33/U12]`. Checking only comments for markers still hands you the stale reading straight from the body. Expect #753 to MERGE. sdi does not plan around a decline and raised nothing on the issue.
- **dec-0004 CLOSES dec-0003 and is the epic's biggest structural finding. Do not re-open.** The Kotlin `Subscriptions` DSL is NOT a user-facing convenience the epic could take or leave: it is the execution substrate of the annotation path. Six registrar call sites across both stacks run every annotated handler through `applicationContext.getBean(StreamSubscriptions.class).subscribe(..)`, in `SubscriptionAnnotationRegistrar` (the two files ADR 0127 decision 4 rewrites), `ProjectionAnnotationRegistrar` and `SnapshotAnnotationRegistrar`, and the starters create the bean `@ConditionalOnMissingBean`. **ADR 0127 never says what the descriptor path executes through.** ADR 0029 recorded the dependency in 2026-06 ("the annotation processor looks it up with `getBean`, and callers inject it") and ADR 0127 mentions `Subscriptions` only as its declare-and-start example. Johan approved the recommendation: **the runner is the single execution path**, the registrar descriptor path calls it, and the Kotlin classes are reshaped into an idiom layer over descriptor plus runner while keeping their class identity and their bean, because ADR 0029's binary-compatibility constraint still binds (released since 0.20.4, callers inject it). ADR 0119 lines 71-76 already name that Extensions-over-a-class-based-API shape as the convention, so this REMOVES a duplicate execution path rather than adding an abstraction.
- **ADR 0127 is AMENDED IN PLACE, not superseded**, which `AGENTS.md` permits for an ADR that has not shipped. No new ADR number is taken, so the reservation ledger is untouched and 0135 stays free.
- **THIRTEEN units now.** U12 writes the ADR amendment and is UNFENCED, so it dispatches at the gate beside U5 and goes first because every downstream unit reads it. U13 does the DSL reshape and is signature-preserving by constraint (the inline/reified overloads plus the `@JvmName` and `@JvmOverloads` surface are released API). U6 and U7 now depend on U12.
- **The three offered options on the Kotlin DSL were all wrong, and that is the lesson.** Two of them (leave it alone, deprecate it) assumed it was peripheral; the third assumed an adapter was additive. All three were framed from reading the DSL's own file rather than from reading who CALLS it. This is rel34's caller-not-declaration rule again, hit a third time in one day, now on a design question rather than a file fence. A menu of options is worth an investigation only after the callers are read.
- **dec-0004 was WRONG and dec-0005 supersedes it, pending Johan. Read dec-0005, not dec-0004.** A second adversarial pass returned REVISE with fifteen findings, three blockers, and the biggest landed on the correction dec-0004 itself was. The failure repeated the lesson it corrected: dec-0004 was framed from `Subscriptions.kt` plus one grep, exactly the read-the-declaration-not-the-callers mistake recorded two bullets up. Twice in one session, the second time inside the fix for the first.
- **What the caller-side read actually found, and it is cleaner than the review implied.** 21 lookup sites across FIVE registrar files, not six sites across one class. Each registrar has FOUR paths: stream (`StreamSubscriptions`), agnostic (`Subscriptions`), synchronous (a NAME-QUALIFIED `Subscriptions` bean over a `SynchronousSubscriptionModel`, which nothing had noticed), and DCB (`DcbSubscriptions`, in `dsl/dcb-dsl`). The reactor `ProjectionAnnotationRegistrar` is not among them at all: it uses `ReactiveProjectionRunner` directly and touches the subscription DSL nowhere. **All SIXTEEN non-DCB calls are one shape,** `subscribe(id, <Filter>, startAt, [waitUntilStarted], (metadata, event) -> ..)`, and all five DCB calls are `subscribeWithMetadata(id, criteria, startAt, consumer)`. **Not one call site passes an event `Class`**: the registrar derives the selector from the annotation first and hands the DSL a finished filter plus exactly one handler.
- **The descriptor is missing a capability, and it is missing it for users too.** ADR 0127 registers handlers per type and says an event the selector admits which no handler handles is ignored, while also allowing an explicit `Filter` to override the derived selector. Together those leave a hole: a caller with an explicit `Filter` cannot say "handle everything it matches". A descriptor built from a filter plus one consumer has zero registrations and drops every event. That blocks the reshape AND is a genuine gap in the user-facing design, which is what makes closing it right rather than expedient. It changes U2's type design, three edges upstream of U13, which is why U12 cannot be written until it is settled.
- **"Single execution path" is a TWO-RELEASE claim, corrected.** 0.35.0 puts the sixteen stream, agnostic and synchronous sites on the runner (the synchronous ones for free, being the same two classes with different arguments). The five DCB sites stay on `DcbSubscriptions`, which U13 does not own and U3 does. Recorded as out of scope rather than implied by a claim the epic cannot deliver.
- **`waitUntilStarted` is FORWARDED, never moved.** It is computed from the Spring context via `SubscriptionAnnotations.subscriptionsStartOnTheirOwn`, and ADR 0127 requires descriptors to stay Spring-free, so the caller computes it and the runner only honours it. It is also a released parameter on four public overloads. The reactor DSL has none and does not gain one.
- **Citations corrected in the amendment's brief.** ADR 0119 is about where reified extension functions live, not execution paths, and dec-0004 leaned on it for weight it does not carry. ADR 0029 is superseded in part by ADR 0051, which revived `Subscriptions` as the capability-neutral default, so U12 cites 0051 for what the class IS and 0029 only for the constraint that still binds (released 0.20.4, injected, stays a real class).
- **RELEASE-NAMING BLOCKER, now fixed.** dec-0001 moved the epic to 0.35.0 and only three places in the plan absorbed it. U5, the unit about to be dispatched, would have written `renames-annotations-0_34.yml` into `UpgradeToOccurrent_0_34` for a 0.35.0 change. **Neither `upgrade-0_35.yml` nor `doc/migration/upgrading-to-0.35.0.md` exists**, so U5 creates the recipe file, the aggregate and the guide before writing any rename into them. Milestone in the state file corrected to 4.
- **Fence re-verified 2026-08-22T09:35Z**, prompted by the stall detector rather than a timer: #871, #837 and #753 all still OPEN; rel34's PR 902 (`rel34/u3-never-replays-warning-identity`, #871) open with 6 checks green and 20 pending. Recorded as an observed fact on U1. Next recheck 10:35Z.
- **dec-0005 RULED 2026-08-22T10:3xZ, both parts, one AGAINST the recommendation. Do not re-ask.** (1) The descriptor gains a type-free catch-all handler, **PUBLIC on the builder**, and ADR 0127 is amended to carry it. Johan took the public option over the internal one: the gap is real for any user supplying an explicit `Filter`, and the standing rule that "nothing here calls it" is no reason to skip a public API addition bites harder where things demonstrably do call it. (2) **DCB is IN 0.35.0 as U14, sequenced last**, not out of scope as recommended, so the runner genuinely becomes the single execution path across all 21 call sites within the release rather than as a two-release goal. (3) `waitUntilStarted` FORWARDED not moved, decided by derivation.
- **FOURTEEN units.** U14 reshapes `DcbSubscriptions` on both stacks onto the two DCB runners U4 builds, covering the four `subscribeWithMetadata` sites. It depends on U3, U4 and U13, which is the LONGEST dependency chain in the epic, so a slip there slips the release rather than one unit. Two constraints carried into its brief: flag early if U13 shows the pattern does not transfer to DCB, and U3 must land first so the `buildDcbCriteria` extraction is inherited rather than repeated. Johan accepted that cost knowingly, in exchange for an ADR that does not say single-path while meaning sixteen of twenty-one.
- **Both journal queues EMPTY** (dec-0001 to dec-0005 all terminal). Plan gate fully closed.
- **CHANGELOG COLLISION with rel34, found on Johan's prompt to check cross-epic conflicts, and neither fleet had flagged it.** `changelog.md` has exactly ONE landing zone for unreleased work, `### Changelog next version`, and a release cut RENAMES that heading in place: `4c04880e5` turned it into `### Changelog 0.33.0 (2026-08-16)`. Since dec-0001 put sdi in 0.35.0 while rel34 cuts 0.34.0, **any sdi entry under that heading at cut time publishes as a 0.34.0 release note for an API 0.34.0 does not contain.** Live, not theoretical: milestone 3 has 24 open issues so the tag is not imminent, while sdi's two unfenced units are dispatchable now and would land first. **Decided: no sdi unit writes `changelog.md` until the `occurrent-0.34.0` tag exists.** Units still AUTHOR their entries in their DELIVERY_RESULT, so the decentralized ownership decision is unchanged and only the landing defers; U8 lands the collected block once the heading means 0.35.0, and carries a `blocking_on` for the tag. Inventing a `### 0.35.0` heading early is NOT the escape, `AGENTS.md` forbids it by name. rel34 told, with the ask being only that they tell me rather than silently strip if they find an sdi entry at cut time.
- **Disjoint from rel34, verified rather than assumed:** `doc/migration/upgrading-to-0.35.0.md` and `UpgradeToOccurrent_0_35` are NEW files sdi creates, touching neither of rel34's 0.34.0 equivalents. sdi takes NO new ADR number, amending unreleased 0127 in place, so 0135 stays free and rel34's 0134 reservation for #818 is unaffected.
- **rel34 verified all four of my changelog claims independently and then found a GAP IN MY PLAN, which is the value of the channel working in both directions.** My landing rule said U8 lands the collected block "once the heading means 0.35.0", which assumed a next-version heading would be there. It will not be. `4c04880e5` is a ONE-LINE change, `-### Changelog next version` / `+### Changelog 0.33.0 (2026-08-14)`: the cut RENAMES and seeds nothing below it, so after a tag the file has no next-version heading and the first writer after the tag creates it. Verified myself against the commit. **U8's brief must say it CREATES the heading**, because a worker told to add an entry under an existing heading and finding none is exactly the worker who invents `### 0.35.0`, which is the rule the whole gate exists to protect. rel34 owes an answer on whether their cut will seed a fresh heading; if it does, U8 uses it. Either way U8's brief states which.
- **#912 EXTENDS THE FENCE into `dsl/dcb-dsl/common`, a module sdi owns.** Flagged by rel34 and verified on the issue: #912 makes `DcbCriteriaBuilder.type/types` derive through `EventTypeExpansion`, which they do not today. That is the same derivation U3's `tags(..)` must match, in the module U3 and U14 own, and it is a follow-on from #753 which already gates U2. U3 and U14 both carry it as a blocker. #909 is in the reactor `ProjectionAnnotationRegistrar`, already inside fence surface 1 and one of U1's two main-source rename files, so it tightens an existing fence rather than adding one.
- **Stall threshold for fence-blocked units is 120 minutes, not the 60-minute default.** Those units wait on ANOTHER fleet's merge cadence, so the default cries wolf: it fired twice on units doing exactly what they should. Derive them with `--stall-minutes 120`. The correct response to the signal is still to re-verify the fence, which is what produced the 10:26Z and 10:55Z blocker facts rather than a timestamp bump.
- **I WAS WRONG about "no other rel34 issue overlaps", and the claim is corrected here because other fleets read this file.** I checked issue BODIES rather than PR DIFFS. Two collisions were invisible that way and both are verified against `gh pr view --json files`:
  - **rel34/U4, PR 914** (`rel34/u4-applied-append-store-limits`, closing #857, #877, #878, #879) edits `framework/spring-boot-autoconfigure/common/.../OccurrentProperties.java` AND both `OccurrentMongoAutoConfiguration.java` and `OccurrentReactiveMongoAutoConfiguration.java`. Those are fence surfaces 2 and 3. Additive bean wiring rather than registrar logic, so the risk is rebase friction rather than semantic conflict, but it is a real overlap I recorded as absent.
  - **brk/U15, PR 910** (`brk/u15-blocking-lock`) edits both `subscription/push/{blocking,reactor}/.../CatchupThenPushSubscriptionModel.java` plus two tests, and ALL FOUR import the exact type U1 renames. Sequence U1 after PR 910 lands.
- **The structural reason brk/U15 was invisible to BOTH fleets: cross-epic exclusion lists are keyed by ISSUE NUMBER, and brk's U15 carries no issue.** Johan's registration prompt fenced sdi against "#388, #421, #893 and #896", and a unit with no issue number cannot appear in a list of that shape however carefully it is maintained. Any fleet-exclusion mechanism keyed on issue numbers has this hole. The fix that actually works is diffing open PRs by branch prefix, which is what found it.
- **Third instance in one session of the same mistake shape: checking the DECLARATION instead of the ARTIFACT.** rel34 hit it on a file fence drawn from where a symbol is declared rather than called; I hit it on an import-anchored grep that missed fully qualified uses; and now on issue bodies standing in for PR diffs. Same correction each time: read the thing that actually carries the change.
- **FENCE STATE 2026-08-22T11:15Z, measured by CONTEXT COUNT not just conclusions, per rel34's partial-matrix lesson.** A full matrix here is 26 contexts, 27 with an extra job. **#871: PRs 902 AND 908 both fully green at 26.** **#753: PR 913 fully green at 27**, which is U2's semantic gate. **#837: still not dispatched, no branch.** So two of sdi's three fence issues have genuinely green PRs waiting to merge and the fence could clear substantially in one step. rel34's PR 914 shows only 3 contexts and brk's 910 only 7, so neither has spawned its matrix.
- **brk is CLEAR of sdi's surface after PR 910, verified independently rather than accepted.** 910 is brk's only open PR and its four files are exactly the four that collide with U1's rename. Their remaining U16 is test-only in `broker/kafka/blocking/src/test`, outside everything sdi touches. brk confirmed it had TWO unnumbered units (U15 and a closeout carry under U9), which is the concrete evidence behind the issue-keyed-fence hole recorded above.
- **Warned brk that 910's rollup is partial, not near-green.** They said it merges on its rollup within the hour; at 7 of 26 contexts, that rollup would read zero pending and zero failing while nineteen shards never ran. Passed on with rel34's lesson and the comparative numbers. sdi is not waiting on 910, so this was about their merge rather than my schedule.
- **sdi's merge gate reads the WORKFLOW RUN, not the context rollup.** `gh run list --commit <sha>`, requiring "Java CI with Maven" to be `completed`/`success`. brk's mechanism, verified by me on two live heads and strictly better than the context-count check rel34 proposed: counting needs the magic number (26, or 27 with an extra job), needs a sibling PR to derive it from, and fails in the DANGEROUS direction when the matrix legitimately shrinks, since a smaller real matrix makes a partial set look complete. Passed to rel34. Full detail in lessons.md.
- **My warning to brk was aimed at a gate they do not use**, which is its own lesson: ask what a peer's mechanism IS before telling them it is broken. The shared lesson described the mechanism someone else used.
- **rel34's cross-check confirms every CROSS-epic collision is `changelog.md` and nothing else**, so brk's PR 910 collides with sdi's U1 alone and the sequencing cost is mine. rel34 also found two WITHIN-rel34 collisions it had not verified (902 with 914 on `OccurrentMongoAutoConfiguration.java`, 908 with 914 on the reactive twin), resolved by reading hunk headers: different bean methods tens of lines apart, so they merge. That tightens sdi's picture too, since 914 is the PR sitting on two of sdi's fence surfaces and the bean methods are now known.
- **sdi's monitor is NOT YET ARMED, which is now an advantage: it will be built without the v7 DONE defect rather than patched later.** The shared `references/fleet-monitor.md` derives DONE at line 23 as "no context is still pending", which is not "CI finished". Verified live on rel34's PR 914: `contexts=3`, all SUCCESS, zero pending, a perfectly green rollup, while `gh run list` shows "Java CI with Maven" ABSENT entirely. rel34 had called that PR green forty minutes earlier against its previous head and would have merged it. **When sdi arms its monitor, the flag is labelled `nopending` not `DONE`, and readiness is asked separately via the run-conclusion check at the merge gate.** Keeping the cheap rollup derivation as a CHANGE DETECTOR and fixing only the label costs no extra API calls; the gate already makes one call per PR.
- **PR 914 is the one this bites, and it is on two of sdi's fence surfaces.** Not merging until its workflow concludes, adversarial pass still running, bean methods unchanged at `occurrentAppliedAppendStore` line 198 blocking and 182 reactive. rel34 will tell sdi when it actually merges, so U1's rebase lands against a fixed point.
- **A sweep is a snapshot, not a standing fact.** rel34 corrected its own "every cross-epic collision is changelog.md alone" on exactly this ground after PR 916 opened; re-run, the conclusion held (916 touches `common/retry` and changelog only). Worth carrying: sdi's own cross-epic conclusions above were computed at a moment and are re-derived rather than cited.
- **FENCE 2026-08-22T11:35Z, by run conclusion.** brk's PR 910 **MERGED** as `cacdc25b9`, verified on main, so brk holds nothing in any sdi path and U1's sequencing constraint against brk is GONE. rel34's PRs 902, 908 and 913 all read `Java CI with Maven completed/success`, so three fence PRs are genuinely green and awaiting merge only. **PR 914 independently confirms the defect: `Java CI with Maven` is ABSENT from its run list while its rollup reads 3 contexts all SUCCESS.** Fence issues #871, #837 and #753 all still OPEN; #837 has no PR and is not dispatched, so it is the long pole.
- **DISPATCHED 2026-08-22T11:55Z, the fleet is no longer empty.** U12 as chip `task_c9ada93f` (Sonnet/high, amend ADR 0127 in place) and U5 as chip `task_892b1fcb` (Sonnet/medium, seven prefixed annotations plus the 0.35 recipe and guide that do not exist yet). Both briefs inlined in the chip, not referenced by path. Both recorded as `pending_actions` act-001 and act-002, `safe_to_repeat: false`, expected observable being a session carrying the unit marker plus a branch off `02bd5c6c5`. Tier and outcome go beside each unit here when it resolves.
- **Why the fleet sat empty as long as it did, recorded because it is the failure this file exists to catch.** The plan gate closed at 08:3xZ and nothing dispatched until 11:55Z. I treated the missing session title as a hard blocker, then established it was not one, then kept waiting anyway by deferring to Johan on units whose gate he had already closed. Johan asked why nothing had spawned. Two READY units and an empty fleet is a stall wearing a victory face, and the coordination work in between was genuinely valuable, which is exactly what made it easy to mistake for progress.
- **Orchestrator address is DERIVED, not guessed:** `uds:/tmp/cc-socks/67638.sock`. Peer sockets are their Claude process PIDs (rel34 is 20994, brk 70080), this session's parent PID is 67638, and that socket exists. Both briefs carry it as a literal with the documented fallback: if it does not resolve, write a durable PR comment and STOP, and never fall back to another Orchestrator-titled session.
- **Work-item monitor `b69kiey8w` armed 2026-08-22T12:0xZ, replacing `bpuux4bkc` which the post-arming sweep proved defective within one poll.** v7 pattern with the CORRECTED `nopending` label (shared skill fix `ca083e38d`). **The defect in the first arming, worth keeping because it is a whole class:** I excluded `rel34/*` head branches to cut matrix noise, and rel34's branches are where THREE of my four fence PRs live (902, 908 on `rel34/*`; 913 and 914 elsewhere). I had armed a monitor blind to my own gate clearing, which is the one event the fence exists to wait for. A filter written to reduce noise silently removed the signal. Replacement shape: **discovery repo-wide, DELTA and OPENED post-filtered to exclude `rel34/*` and `brk/*`, and CLOSED-OR-MERGED never filtered**, with the four fence PR numbers named so a fence merge emits `FENCE-PR-CLOSED-OR-MERGED` rather than a generic line. Lifetime: runs while any sdi unit has an unmet deliverable a PR event could advance; the sweep completing the last such unit stops it. Restart rule: recorded here but absent from TaskList means re-arm.
- **Monitor `b067u002c`, FOURTH and final arming 2026-08-22T12:4xZ. The foreign set is now COMPUTED at arming, not hardcoded.** The third arming's list was written by hand and was stale within twenty minutes (911 closed, 919 opened), which is the defect a hardcoded snapshot always has. The monitor now runs `gh pr list --json number` once at startup and treats every PR open at that moment as foreign, which is exactly right because sdi has no PR yet, and emits `MONITOR-FOREIGN-SET` so the set is visible rather than assumed. Anything appearing afterwards is either sdi's own or genuinely new, and both deserve an event. It also emits `MONITOR-UNHEALTHY` if the set cannot be computed, since silently suppressing nothing is better than silently suppressing everything.
- **The judgement that produced the fourth arming, since I had just said I would stop tuning.** Stopping was right as a principle and wrong for this case: one re-arm cost two tool calls against roughly six model turns that PR 919 would have generated through its CI cycle. The discipline is to compare the two costs, not to hold a position because it was recently stated.
- **Superseded, kept for the reasoning: monitor `bjw7hepm0` (third arming), and the branch-name filter is abandoned as unworkable.** Two noise deltas in twenty minutes (916 then 911) made the leak real, which was the condition I had recorded for revisiting. The fix I had assumed would work does NOT: rel34 names five of its seven open PRs `rel34/*` or `johan/rel34-*`, but 911 is `johan/determined-euclid-f4836e` and 913 is `johan/refuse-non-final-concrete-event-type`, so **no substring of the branch name separates rel34's work from anyone else's**. Verified by listing all nine open PRs rather than assuming. Replacement filter is by PR NUMBER: deltas suppressed for the nine PRs open at arming, every one of which is foreign since sdi has none yet, while OPENED and CLOSED-OR-MERGED are never suppressed. The property that makes it self-correcting: a PR not in the list is either sdi's own or genuinely new, and both deserve an event. The cost is that the list goes stale as rel34 opens PRs, each new one costing one OPENED plus deltas until the next refresh, so REFRESH IT at each sweep. `FOREIGN=801 872 901 902 908 911 913 914 916`, `FENCE=902 908 913 914`.
- **Checkpoint verification uses the ANCESTOR test, not equality.** `[ HEAD = origin/main ]` reported a failed push at 12:2xZ that had in fact landed: rel34 pushed `e9360c959` in the window between my push and my fetch, so the equality held for nobody. In a repository three fleets push to, equality is the wrong question. Use `git merge-base --is-ancestor HEAD origin/main`, which asks the thing that actually matters, whether my work is on main.
- **ADR 134 MERGED as `e9360c959` (rel34's PR 911, #818).** The ledger maximum is now 0134 on main rather than held on a branch. sdi still takes no number, amending unreleased 0127 in place, so 0135 remains the next free one for anybody.
- **Third arming validated in use:** 911's deltas were suppressed as intended while its MERGE emitted, which is precisely the split the number filter was built for. The `FOREIGN` list now carries one stale entry (911, closed), which is harmless since suppressing deltas for a closed PR suppresses nothing.
- **A filter tuned for noise reduction has now been wrong twice in one hour, in opposite directions.** The first arming suppressed the fence itself, the second suppressed too little. Both times the error was invisible until an event arrived, because a filter's mistakes are silent by construction: it removes things, and removed things do not announce themselves. Any future re-arm gets a post-arming sweep AND an explicit answer to the question of which signals this filter can no longer deliver.
- **First live use of the corrected label, and it behaved as intended.** The delta read `0fail nopending`, which under the old `DONE` wording would have read as finished. It prompted a run-conclusion check instead, and the check confirmed. The rename is doing the work it was meant to do.
- **Post-arming sweep DONE.** Five non-fleet PRs open: 916, 913, 911 (all rel34's, on `johan/*` branches rather than `rel34/*`, which is itself why a prefix filter is a weak fence), 872 dependabot, 801 a community PR. None is sdi's; sdi has no PR yet. Nothing to act on.
- **FENCE GATE ONE CLEARED 2026-08-22T11:22:58Z: #753 is CLOSED, PR 913 merged as `a86909f5d`.** The monitor's fence flagging fired correctly on its first real use, which is the one guarantee the fourth arming was built for. **U2's semantic gate is cleared** and U2 now waits only on U1, which is still fenced. Fence remaining: #871 (PRs 902 and 908, both green, unmerged) and #837 (still no PR, not dispatched, the long pole).
- **THE #753 CONTRACT, read from the merge diff rather than from rel34's promised delivery report, because the diff is the artifact and the report describes it.** `EventTypeExpansion` now REFUSES a concrete class that is neither final nor sealed. Up to 0.33.0 such a type was accepted with only itself in the filter, so a caller declaring `class OrderPlaced` and publishing a subclass never saw that subclass and got no warning. Four escapes are named in the javadoc: make the class final, seal the hierarchy, declare the concrete types, or SET AN EXPLICIT FILTER. `expandWhatCanBeFound` stays lenient and now counts a non-final concrete class as lenient-eligible, which is the direction #912 cares about. U2 is written against these semantics and its tests encode the refusal, never the 0.33.0 behaviour.
- **A convergence that raises confidence in dec-0005.** #753's fourth escape is an explicit filter, and an explicit filter with no typed handler is precisely the catch-all case dec-0005 added to the descriptor. Two independent requirements, reached from opposite directions by different fleets, landed on the same missing feature. That is evidence the catch-all is right rather than expedient, and it belongs in U2's and U12's briefs as a reason rather than as a rule.
- **FENCE NOT ADVANCED by the 2026-08-22T11:41:19Z event, and the distinction is the point. PR 908 was CLOSED WITHOUT MERGING**, no merge commit, `mergedAt` null. #871 stays OPEN with only PR 902 (the blocking half, green) still standing, so the reactor half of that fix went away rather than landing. Had I read `FENCE-PR-CLOSED-OR-MERGED` as a merge I would have recorded my fence as clearing while it had not moved at all. The event is named for both outcomes precisely because they are indistinguishable from the outside, and the reading is `gh pr view --json state,mergedAt,mergeCommit` every time. Fence now: #753 CLEARED, #871 open (PR 902 only), #837 open with no PR and not dispatched.
- **Worker session identity RESOLVED BY MARKER, not by title, 2026-08-22T13:2xZ.** U12 is `local_f56f896e-3466-409a-bc7d-6c58a837eba5`, worktree `modest-keller-66e04f`. U5 is `local_75e86e26-ddb7-434e-866a-b339e804ef4a`, worktree `upbeat-fermat-722819`. Both established by full-text searching the sessions for the resolved `⌁[sdi/U12]` and `⌁[sdi/U5]` strings, which are in each transcript verbatim because the brief carried them. The titles happened to match this time, which is exactly why the check is worth running rather than skipping: a title is a convention any fleet can wear and a marker is evidence. Recorded so a later tick does not re-derive it and a restart does not re-guess it.
- **MY CALL-SITE SPLIT WAS WRONG AND IS CORRECTED EVERYWHERE: 16 non-DCB and 5 DCB, not 17 and 4.** The total of 21 was right, the split was not. Caught by U12's own worker, confirmed by Copilot, and then verified independently by counting per registrar: blocking Projection 4+1, blocking Snapshot 3+1, blocking Subscription 3+1, reactor Snapshot 3+1, reactor Subscription 3+1. **This was not cosmetic: U14 would have been briefed to reshape four DCB sites when there are five, so one DCB execution path would have been left off the runner while the ADR claimed a single execution path across all of them.** Corrected in the plan, the epic state, and this file. dec-0005's journal record still carries the old number in its recommendation prose; the RULING it records is unaffected, so it is not superseded for a wrong supporting count, and this line is the correction of record.
- **Copilot found it in a SUPPRESSED COMMENTS block**, which creates no inline thread and is invisible to any thread count. That is the exact finding class the merge gate reads review bodies for, and it earned its place on the first sdi PR to exist. The review also reported "Review effort level: Balanced" in its own overview, so effort evidence for this PR is VERIFIED by GitHub rather than unverified as the brief assumed.
- **U12 CONVERGING on substance, three heads, each a real fix.** `3bc60a12b` initial, `9fb6033ba` the 16/5 correction, `a19365a84` extending the catch-all to all four builders. Still ADR-only and still purely additive at 89 insertions, 0 deletions. Two Copilot reviews, both COMMENTED, four suppressed findings in the second. Sessions resolve by WORKTREE name in ListAgents, not by chip title: U12 is `modest-keller-66e04f-15`, U5 is `upbeat-fermat-722819-c4`.
- **Copilot found a real defect in ADR 0127 that I had missed and that U12's brief therefore never mentioned.** Section 6 item 7, line 364, still reads `doc/migration/upgrading-to-0.34.0.md`. dec-0001 moved this epic to 0.35.0 after the ADR was written, so the ADR's own epic breakdown points at the wrong release. Verified present on BOTH `origin/main` and U12's branch. Scope extension sent to the worker with explicit authority to touch that line, since its brief scoped it to four decisions and this is a fifth edit.
- **TWO of that review's findings are corrections to MY decision text, not to the worker's execution.** First, "every lookup calls the runner INSTEAD OF the DSL method" contradicts U13: the registrar lookups are not rewritten at all, they keep calling the DSL and the DSL is reshaped to hand off to the runner. A later unit reading my wording literally would rewrite roughly 2300 lines of registrar for nothing. It is now phrased as an execution path. Second, `waitUntilStarted` is NOT uniform: the blocking `Subscriptions` overloads default it to `true` while `DcbSubscriptions.subscribeWithMetadata` does not wait at all and the registrar waits on the returned handle separately, so the amendment distinguishes the two rather than stating one forwarding rule.
- **One Copilot finding correctly IGNORED, with evidence.** It reported `.context/epics/sdi.yml` still carrying 17/4. It does not: the correction landed before that review ran and Copilot read a stale main. Verified zero occurrences on `origin/main`. Worth recording that an automated reviewer's claim about repository state is a claim like any other and gets checked, not obeyed.
- **#922 is a fleet-wide CI tax and sdi must not misattribute it.** "RabbitMQ connection-recovery test times out intermittently in the misc shard", OPEN, milestone 0.34.0, NO fix PR yet. The defective fence is on main, so every branch that merges main inherits it, and rel34 saw four units hit it across two JDKs in hours without any of them touching the module. **U12 is immune**, its diff being prose-only and carrying `[ci skip]`, so no matrix runs at all. **U5 is exposed**, running the full matrix. A red misc shard on a RabbitMQ connection-recovery timeout in an sdi PR is #922 and NOT the unit's defect: do not send the unit back for it, and do not let it burn pr-fix attempts on it. rel34's rule on rerunning applies with the sign it gives: rerunning is legitimate only where a fix exists that the branch has not picked up, and for #922 no fix exists yet, so the honest options are wait or rerun-and-say-so, never treat green-after-rerun as proof the unit was fine.
- **Scope extension to U12 VERIFIED APPLIED, from the diff rather than from the send succeeding.** Head `cdd9147db` fixes ADR 0127 line 364 to `upgrading-to-0.35.0.md`, removes the "instead of `StreamSubscriptions`" phrasing in favour of execution-path wording, and adds the `subscribeWithMetadata` distinction for the DCB `waitUntilStarted` default. Four commits, every one substantive. The PR is no longer purely additive, correctly, because line 364 is a replacement. NOT merging: holding for DELIVERY_RESULT, since the worker may request a third review and its brief makes a reasoned-decline round the final one.
- **FENCE NARROWED TO ONE ISSUE, at or before 2026-08-22T12:24:48Z (reconstructed bound). #871 is CLOSED**, PR 902 merged as `4f12b2ffb` at 12:23:29Z, issue closed one second later. Checked merged-versus-closed rather than assumed, which mattered here because its sibling PR 908 was CLOSED UNMERGED earlier, so 902 carried #871 alone in the end. With #753 already cleared, **only #837 remains**: OPEN, no assignee, no label, no branch, no PR, not dispatched. It is now the sole thing standing between sdi and its whole fenced chain, and its start is not yet visible anywhere. One `u6|837` branch match on the remote was a FALSE POSITIVE, `cdx33b/u6-retained-size-warning` from a dead epic, checked rather than counted.
- **RELEASE GATE, recorded because I committed to it in a message to U5 and an unbacked commitment is worse than none.** Copilot found on PR 924 that `UpgradeToOccurrent_0_35` rewrites applications onto `@Occurrent*` annotations that **neither bean post processor scans**: both still search only the seven legacy classes. So a user who runs the recipe today gets a silently unregistered projection, saga or subscription. It is NOT a U5 defect, since U5 was explicitly forbidden the registrars and told the wiring is a later unit, and it is correct to merge to main because nothing releases from main. **The gate: `UpgradeToOccurrent_0_35` must not reach a RELEASE before U6 and U7 have landed the registrar and bean-post-processor wiring.** U5 declines that thread with this reason rather than fixing it.
- **A decomposition artifact worth naming, since it will recur.** U5 wrote a migration-guide section telling users to convert every subscription handler by hand, which contradicts ADR 0127, where the recipe keeps the handler and generates a delegating factory. U5 was not wrong to write it: that body-rewriting visitor is a LATER unit, so U5 documented a world where it does not exist. **Rule applied: each unit's guide section covers its OWN change and nothing it merely anticipates.** U5 scopes its section to the rename, which its declarative recipe does handle end to end, and the visitor unit writes the conversion section when it exists. Decentralized changelog and guide ownership has this failure mode built in, and the fix is scope discipline per section rather than recentralizing.
- **rel34 PR 928 (`rel34/u16-type-filter-expansion`, #912) is OPEN and lands in a module sdi owns.** It touches `dsl/dcb-dsl/common/{pom.xml, DcbCriteriaBuilder.kt}` plus a new expansion test, which is U3's and U14's territory, exactly the gate recorded earlier. It also edits `upgrade-0_34.yml` and `upgrading-to-0.34.0.md`, which are rel34's own artefacts and NOT sdi's 0_35 pair, so no collision there. The do-not-unify constraint on U3 and U14 now has a concrete referent: after this merges, `DcbCriteriaBuilder` in that module carries a direction-dependent expansion policy while `buildDcbCriteria`, which U3 extracts into the same module, narrows uniformly.
- **U5 stop-and-escalate OVERRULED for one round, and the ground matters more than the ruling.** The worker stopped deliberately after two Copilot rounds, correctly citing that both found prose rather than code and that such loops do not converge, and handed the continue-or-stop call up. That is exactly what this fleet's ten-round ADR lesson prescribes and it applied it well. I ruled one more round **because the unit is blocked on a queued matrix anyway, so the round costs no wall-clock at all**, and because the guide is materially different from anything either review saw. Recording the ground explicitly: had U5 been otherwise ready to merge, the stop would have stood. A free round and a costly round are different decisions and the reason should not be reusable as "always one more".
- **The empty-review-shell trap bit twice today and was caught both times by filtering on login.** PR 924 shows EIGHT review entries of which only TWO are Copilot's; the other six are shells created by the worker's own thread replies. PR 923 showed three where only two were Copilot's. Counting reviews, or filtering by body length, reports a re-review that never happened. `select(.author.login=="copilot-pull-request-reviewer")` is the only reading that holds.
- **U12 IS DONE. First sdi unit merged: PR 923 squashed as `5ba3b9bfa` at 12:38:27Z**, CAS-pinned to `d981dc44d`, post-merge head verified. Gate as run: prose-only `[ci skip]` so the verdict carried by compare from base rather than from a matrix, four Copilot rounds every finding triaged (counted by LOGIN, since the PR showed seven review entries of which three were reply shells), one unresolved thread carrying a concrete reasoned decline which the policy permits, adversarial verify skipped under the skill prose exception, and the design gate closed by the unit handover, which the policy names as sufficient. Tier outcome for the record: Sonnet/high, no retry, four rounds each finding real content. Correct tier.
- **The one open thread on 923 was a decline I endorse.** Copilot asked the worker to edit `.context/ORCHESTRATOR.md` and `.context/epics/sdi.yml` inside its PR. The worker declined with a concrete reply (orchestrator-owned, outside the diff, already corrected on main, explicitly forbidden by its brief) and left it unresolved rather than resolving a disagreement itself. Under `review_threads: allow_reasoned_declines` that stands and does not block, so no human action is owed on it.
- **THE LAST FENCE GATE IS IN FLIGHT: rel34 dispatched U6 for #837**, and it hands sdi a hard constraint that is easy to lose. Its fix makes the handler target whatever the context yields FOR THE BEAN NAME, looked up INSIDE the handler lambda at EVENT-DELIVERY time, never the instance handed to the post processor. The lookup POINT matters as much as the lookup: during `postProcessBeforeInitialization` the bean is unfinished, so hoisting the lookup to registration time is premature or circular. **sdi U6 moving registration to `afterSingletonsInstantiated` changes exactly that timing, which makes it the moment the invariant is most likely to be silently undone.** Recorded on U6 with the requirement that it carry a test which still FAILS if the target reverts to the raw bean. Precedent in both files already: `processSynchronousSubscribeAnnotation` does it correctly and says why in a comment. rel34/U6 owns both `SubscriptionAnnotationRegistrar` files until it merges and is fenced from starting the phase move, which stays sdi.
- **An open design question rel34 deliberately did NOT decide, and it is sdi to own.** `SubscriptionAnnotations.invokeDescriptorFactory(..)` also runs against the raw bean, in all five registrars. It is NOT the same defect as #837: `Projection`, `Saga` and `Snapshot` never invoke a handler per event, they call that factory ONCE at registration and the per-event work goes through the returned descriptor. So whether a descriptor factory should run on the proxy is a design question the descriptor work owns. rel34 fenced itself out and filed nothing, correctly. sdi has not decided it either; it belongs with U2 and U6 and must not fall between the fleets.
- **ADR numbering, reciprocal protocol agreed with rel34 2026-08-22T15:0xZ.** 0135 is FREE and recorded as such on both sides. rel34 holds no number at present; if one of their units comes back BLOCKED needing one they claim it and tell sdi BEFORE anything is written, and sdi does the same in reverse. sdi still expects to take none, having amended unreleased 0127 in place.
- **My 16/5 correction did NOT propagate into rel34, verified by them with a grep over every brief and their whole epic state rather than from memory.** Their only call-site count is U6's own "six call sites in two files", derived independently and unrelated to sdi's 21. Also checked on their side: no rel34 brief cites ADR 0127's CONTENT, only its existence, so the amendment staled nothing there. Pushing the correction out rather than fixing it locally is what made that check happen at all.
- **U5 round 3 CLEAN.** Review 5000193097 on the current head, Approval recommended, zero comments, NO suppressed block, 20/20 files, Balanced. Six threads and zero unresolved. **The merge gate is now blocked on exactly one thing: `Java CI with Maven` is `in_progress` on head `4abb58d11`.** Everything else is met. The worker's own read of the round is worth keeping: the free-because-blocked argument for a third round was right, and the non-convergence lesson survives intact, because two of three rounds found real prose issues and only the round that saw the settled text came back empty.
- **Adversarial verify RUNNING on U5, and this diff earns it where U12 did not.** U12 was prose and took the skill's explicit prose exception. U5 ships an OpenRewrite recipe that rewrites user applications, so a wrong mapping silently breaks code nobody in this repository can see. The pass attacks the recipe's own claim rather than reviewing the diff: nested enum types surviving an outer rename, fully qualified and wildcard and Kotlin uses, the removed `eventTypes` and `tags` attributes leaving dangling compile errors, attribute-set and meta-annotation equivalence across all seven pairs, and whether the tests test the claim or merely the behaviour.
- **ADVERSARIAL PASS ON U5 FALSIFIED THE CLAIM, and the outcome is an issue rather than a change to U5.** The migration guide says a renamed `@Projection`, `@Saga` or `@Snapshot` "compiles and behaves as before". Against main TODAY that is false: both bean post processors look the OLD types up by class literal (`OccurrentBlockingAnnotationBeanPostProcessor.java:115`, reactor twin at `:120`), the seven new types are referenced nowhere outside the annotations module, the recipe and the docs, so after a user runs the recipe `findAnnotation` returns null for every method and **every projection, saga and snapshot silently stops registering. Source compiles, nothing throws, read models just stop.**
- **U5 does NOT change the sentence, and the reasoning is the part to keep.** `AGENTS.md` says user documentation describes the RELEASED behaviour as settled fact, never the transient state of main. U6 and U7 land the wiring in the same release, so at 0.35.0 the sentence is true for the person reading it. Hedging it would make the guide describe a state that never ships, which the same rule forbids. What changed is not the prose but the STATUS of the dependency: the claim is now conditional on U6 and U7 landing, and that condition lived nowhere but this file.
- **So it is now [#929](https://github.com/johanhaleby/occurrent/issues/929), milestone 0.35.0, carrying the `⌁[sdi]` marker.** An orchestrator memory file is not a release gate; whoever cuts 0.35.0 will not read it. The issue is the durable form and it outlives this session. This is the finding-routing protocol applied to a finding of my OWN rather than a worker's, which is the case the protocol does not spell out and should.
- **A second requirement recorded on U6 and U7 from the same pass: the recipe tests cannot catch this and a green suite proves nothing.** They rewrite hand-stubbed annotation classes and never load a Spring context, so they assert the annotation NAME changed rather than that anything still registers. Whichever unit lands the wiring adds a test that registers a bean through a NEW annotation and asserts it receives events.
- **What the pass confirmed sound, recorded so it is not re-litigated:** all seven `ChangeType` mappings name the correct fully qualified types, the nested `StreamSubscription.StartPosition` enum moves with its outer annotation, and attribute sets plus meta-annotations match pairwise across all seven pairs apart from the deliberate `eventTypes` and DCB `tags` removals. The rename itself is sound. Only the runtime has not caught up.
- **U5 IS DONE. PR 924 merged as `6f47516c6` at 12:48:17Z**, CAS-pinned to `4abb58d11`. Gate: `Java CI with Maven completed/success` with 26/26 contexts, the authoritative test rather than the rollup; six threads zero unresolved; three Copilot rounds counted by LOGIN out of nine entries, round 3 Approval recommended with no suppressed block; adversarial verify ran and routed to #929; boundaries re-verified; `mergeable` re-read past a transient UNKNOWN. Tier outcome: Sonnet/medium, no retry. Correct tier. Its changelog text is authored and parked in the PR marker comment for the post-tag collected block.
- **EMPTY-FLEET AUDIT, triggered by the fleet going to zero in flight. 2 DONE, 7 BLOCKED, 5 PLANNED, nothing READY.** Every one of the twelve remaining units traces its dependency chain back to U1, so the epic has exactly one thing standing in front of it.
- **AND THE AUDIT FOUND THAT U1 MAY NOT ACTUALLY BE BLOCKED. Measured, not argued.** U1 touches FOURTEEN files under `framework/`: both `ProjectionAnnotationRegistrar` main sources plus twelve tests. rel34/U6 (#837) owns exactly two files, both `SubscriptionAnnotationRegistrar`, stated in writing by rel34 and fenced hard from the phase move. **Those sets are disjoint.** rel34 PR 914 is the only other open PR on a fenced surface and its fourteen files are ALSO disjoint from U1's, verified by a `comm -12` of the two sorted lists rather than by reading. The other two fence issues, #871 and #753, are merged and closed.
- **So the fence's stated REASON is spent while its LETTER still binds.** Johan fenced `framework/spring-boot-autoconfigure/**` as FILE CONTENTION, when three rel34 units were landing there and their file ownership was not yet known. Two have merged, and the third's ownership is now known precisely and provably does not overlap. Holding U1 for a collision that cannot happen is the instruction outliving its purpose. **Not deciding this alone: it is an explicit user instruction, and setting one aside on my own reading is different from deciding a question the records answer.** Brought to Johan with the file lists attached. U1's brief is prepared so it dispatches the moment he rules either way.
- **dec-0006, Johan 2026-08-22T15:2xZ: FENCE LIFTED FOR U1 ONLY, dispatch now and merge when green.** He took the recommended option on the measured evidence. **The fence REMAINS for U6, U7 and U13**, which genuinely do touch the two `SubscriptionAnnotationRegistrar` files rel34/U6 owns; only U1 was shown disjoint. U1 dispatched as chip `task_421856cb`, Sonnet/medium, branch mandated as `sdi/u1-subscription-handle-rename` so rel34's prefix filter works. Recorded as `act-003`, `safe_to_repeat: false`.
- **U1's brief carries the epic's own scar tissue, which is the point of writing briefs late rather than at registration.** It tells the worker the ADR's file counts are stale and to derive its own, warns that an import-anchored grep misses fully qualified uses and every Kotlin import (the mistake that undercounted this very fence by a third), names #922 as a known-foreign red shard, explains the empty-review-shell trap with the login filter, states the changelog gate with the `4c04880e5` evidence, and tells it to scope its guide section to its own change because a sibling unit had to rescope for exactly that reason. None of that existed when the epic was registered.
- **STANDING PRACTICE from Johan 2026-08-22T15:4xZ, binds every ask from here on:** every question goes through `AskUserQuestion` with an ORCH recommendation attached, and **`AGENTS.md` is read as a second opinion BEFORE asking**, to test whether the question needs asking at all. Investigated rather than just adopted: the skill already required exactly this for questions relayed from WORKERS (the "All questions flow through the orchestrator" rule) while bucket C, which governs the orchestrator's OWN questions, listed code, `ORCHESTRATOR.md` and a graph query and never mentioned the conventions document. So relayed questions faced a higher bar than mine. Patched in the skill itself as `orchestrator` commit `e0c3eea93`, since it generalises to any repository with a conventions document. The tell that exposed it was Johan returning the Kotlin DSL question as "investigate it on AGENTS.md principles", which is the user paying for a lookup I owed.
- **Session title still owed:** `[sdi] Orchestrator`. No longer blocking, since the socket address is in both briefs, but it remains the thing that stops a worker addressing another fleet by mistake. Repeat this ask in every report until the host's session list shows it, because an untitled orchestrator is invisible to every worker's `ListAgents`.

## Seventeenth epic registered 2026-08-22T07:05:33Z: rel34, the 0.34.0 RELEASE epic (milestone 3)

- **State** `.context/epics/rel34.yml` (schema v1, 15 units), plan `/Users/johan/.claude/plans/rel34-0-34-0-release.md`, journal `.context/orchestrator/rel34/` (local only: `.gitignore` negates `ORCHESTRATOR.md`, `lessons.md` and `epics/*.yml` but NOT the journal path). Registration timestamp above is the tracker-scan lower bound. Milestone `0.34.0` is number 3, existing, nothing created. Mode ATTENDED. Session worktree `rel34-epic-registration-8c2066`, branch `johan/rel34-epic-registration-8c2066`.
- **ADR reservation ledger opens at 0134.** Audited 2026-08-22T07:20:10Z: max across `origin/main` and every remote branch is 0133 (brk's, merged). 0134 is held for U11 (#818) if Johan adopts it, and is the highest unused reservation, so it stays on the books until an ADR above it exists.
- **Cross-epic exclusions, both directions.** rel34's sweeps exclude #388, #421, #893 and #896 (brk's). brk's sweeps exclude every rel34 unit. Coordination runs ONLY through this file, fetch-rebase-push at checkpoints. rel34 gates its tag on the brk epic closing.
- **rel34 TAKES PR 69's PushObserver repair out of brk/U8's scope.** The staleness recorded in the brk section (`docs/802-push-observation-hook` documenting the pre-U11 boolean contract) is one of FOUR surfaces describing the same shipped contract too narrowly, so it is enforced as one invariant in one pass by rel34/U1 rather than split across two fleets. brk/U8 keeps #421 and docs PR 74. brk: read this before dispatching U8.
- **The shipped `RoutingOutcome` has SIX values, not three.** `subscription/core/.../RoutingOutcome.java`: DELIVERED, FILTERED, NOT_DELIVERABLE, DEFERRED, UNAVAILABLE, REFUSED. `git log -S` proves it: DEFERRED arrived with `11b1fa91a` (#889, 2026-08-21), UNAVAILABLE and REFUSED with `65041b387` (#896, 2026-08-22). Four prose surfaces are stale against it: `changelog.md:6` (the Highlights line, three values plus "a single match flag"), `changelog.md:27` (factually complete but written as development history, which `AGENTS.md:75` forbids for a still-unreleased feature), the news post on `docs/append-identity` (three values, fixed from boolean at `a17c955e` 07:02:15Z and stale on arrival), and `pages/docs/docs.md` on `docs/802-push-observation-hook` (still the boolean lambda `(cloudEvent, matched) -> ...`, which does not compile against the shipped API). rel34/U1 owns all four.
- **ayi's ship-day acts are absorbed into rel34's definition of done** (merge docs PR 73 taking its news post over PR 72's, close #740 as shipped, close #361 as its duplicate, triage #876 before tagging). rel34 therefore takes ownership of `docs/append-identity` from now on. The ayi orchestrator session was still live and wrote to that branch at 2026-08-22T07:20:10Z, so nothing else may write it without reading this line first.
- **The held docs set is SEVEN PRs, not eight branches.** Eight `docs/*` branches exist in `occurrent-org/occurrent-org.github.io`, but `docs/saga-step-conditions-added-tag` has no PR and its only change (`"saga-step-conditions" : "0.33.0"` in `js/_partials/main.js`) is already on docs `main`. Delete it, never merge it. Held PRs: 68, 69, 70, 71, 72, 73, 74. **PR 74 is stacked on PR 69's branch**, so 69 merges first and GitHub retargets 74. Conflicts: `js/_partials/main.js` (69, 73, 74 each append one `addedTags` entry, keep all three), `pages/docs/docs.md` (six branches, branch-local anchors), `_posts/news/2026-08-25-0_34_0.md` (add/add between 72 and 73, take 73's). PR 72 also carries the `_config.yml` `occurrentversion` bump. News date is a placeholder, set the real one at merge (the PR 54 lesson).
- **`preview/0.34.0` is NOT automatically current.** At registration it was `behind=0` against six held branches but DIVERGED from `docs/421-broker-modules`, one commit behind. Verify preview per branch tip after every held-branch change; never assume a recent rebuild covers everything.
- **Sonatype: the limit to read is files, not size.** 80 MB, 1 000 files, 7 releases per organization, soft warnings today, enforcement planned 2026-10-01. Three broker modules ship in 0.34.0, so the 1 000-file allowance is the one that moved. Check at ~24h against all three.
- **Decided by derivation at registration, not asked.** #867 (RabbitMQ bridge startup retry) is IN 0.34.0: `AGENTS.md:120` requires every component to ship production ready including surviving a transient outage of the store it talks to, and the broker modules ship for the first time in this release. Its earlier decline for PR 861 is one of the deferrals the triage sweep superseded. Johan may reverse.
- **PLAN GATE CLOSED 2026-08-22T07:33:33Z, Johan's four rulings, do not re-ask** (journal `dec-0001` to `dec-0004`, queues EMPTY). **D1 sdi (#721/#725): a SIBLING epic, registered now in its own session, NOT a rel34 unit; rel34's R3 gates the tag on it closing.** **D2 #818: IN 0.34.0 as rel34/U11, plan-first, carrying ADR 0134** (Johan overruled the recommendation to defer; it is the unit whose C1 findings are most likely blocker-shaped). **D3 #884: re-milestoned to 0.35.0**, joining #781, #864 and #870, verified on the issue. **D4 C1 stopping rule: MAJOR-AND-ABOVE reopens rel34's scope after a clean round, cap 3 rounds**, remainder returns as ONE structured ask and reaching the cap is reported as reaching the cap, never folded into convergence; below major is filed as 0.35.0 issues. Johan chose the same line as brk/U9 after being shown that it produced #893 there, still open and gating brk's close.
- **Work-item monitor `bzqoo36k1`** (v7 pattern, `johanhaleby/occurrent`, **`brk/*` head branches excluded**), armed 2026-08-22T07:47:58Z, replacing `bqt406kda` (stopped) which woke this orchestrator three times in five minutes on brk's PR 897 alone. The v7 reference makes the scan repo-wide deliberately, because a fleet-native branch name is not known in advance, but EXCLUDING a known-foreign prefix is safe in a way that including-only-known-prefixes is not: a rel34 PR will never sit on a `brk/` branch, and brk's own orchestrator watches those. Lifetime: runs while any rel34 unit has an unmet deliverable a PR event could advance; the sweep completing the last such unit stops it. Post-re-arm sweep done: three open non-brk PRs, none rel34's (872 dependabot, 801 Shubh2-0's community PR, 898 is brk's and now filtered). **brk PR 898 is `brk/u9-round3-carry`, so brk's U9 is at its round-3 cap**, which matters because rel34's R3 gates on brk closing; read brk's own section rather than this monitor for that.
- **Wave 1 dispatched 2026-08-22T07:41:09Z as four chips**, briefs at `<scratchpad>/briefs/{U1,U2,U3,U8}.md` (session-local, not in the repo): U1 `task_a3355294` Sonnet/high, U2 `task_8def816f` Sonnet/high, U3 `task_72a7f36a` Sonnet/medium, U8 `task_b6a7ee0f` Sonnet/medium. Tier and outcome go beside each unit here when it resolves. Wave 1b (U6 after U3 merges, U9) is prioritized ahead of U4/U5/U7/U10 to clear the registrar surface for sdi.
- **CORRECTION to the dec-0001 safety assessment: sdi and rel34 DO overlap.** The registration ask told Johan sdi had "zero scope overlap with any rel34 unit". That was wrong and he ruled partly on it. `framework/spring-boot-autoconfigure/{blocking,reactor}/**` holds the four annotation registrars plus `ComposedDefaultStartPosition` and `ComposedReplayPhase`, and THREE rel34 units land there: U3 (#871), U6 (#837) and U9 (#753). sdi (#725 subscription-as-descriptor, #721 the `@Projection`/`Projection` name clash) restructures the same surface. The ruling still holds (sibling epic, R3 gates on it) but the fence is now explicit: **sdi plans in parallel from now, and does not land implementation in `framework/spring-boot-autoconfigure/**` until U3, U6 and U9 have merged, then rebases onto them.** rel34 records each of those merges here for sdi to read. **sdi DISPATCHED as chip `task_91335010`, title `[sdi] Orchestrator · Opus/high`, 2026-08-22T07:52:13Z on Johan's word.** The full prompt is inlined in the chip, not referenced by path. Preconditions re-verified live immediately before the spawn rather than carried from the registration ask: #721 and #725 both OPEN, both milestone 0.34.0, zero assignees and no `in-progress` label; ADR max still 0133 across `origin/main` and every remote branch; #871, #837 and #753 all still OPEN, so the registrar fence is live as written. sdi has its own orchestrator from birth and coordinates only through this file.
- **dec-0005, U3 ownership extended, and a brief-writing lesson.** U3 returned BLOCKED before its first edit: the fix shape #871 and the brief both agree on was unreachable from the files the brief gave it. Verified against the code, not taken from the report: `ComposedDefaultStartPosition` is a bare boolean with no `suppliedBy`; the only production caller of `defaultBypassesCatchup()` on the blocking side is `framework/spring-boot-starter-mongodb/.../OccurrentMongoAutoConfiguration.java:260`; and the reactor mirror the issue names as THE pattern, `ComposedCatchupModel.suppliedBy(Object)`, is itself called from a starter module at `OccurrentReactiveMongoAutoConfiguration.java:230`. **The fix is structurally cross-module, so no boundary drawn at `framework/spring-boot-autoconfigure/**` could ever have contained it.** Ownership extended to that one file, scoped to `occurrentComposedDefaultStartPosition()` and the existing `defaultBypassesCatchup()` call inside `occurrentCompetingDurableSubscriptionModel(...)`, plus that module's tests. Constraint carried: mirror the reactor invariant that `suppliedBy` and `defaultBypassesCatchup` are called once and from the SAME bean method, since only the auto-configuration that composed the model knows both facts. **The lesson for the remaining briefs: when a brief tells a unit to mirror an existing pattern, the file ownership must be derived from where that pattern's CALLERS live, not from where the pattern's class lives.** Relay recorded as `act-001` (message-session, `safe_to_repeat: false`), unverified until U3 acknowledges or pushes.
- **U6 (#837) brief must FENCE OUT** `OccurrentMongoAutoConfiguration.occurrentComposedDefaultStartPosition()` and `occurrentCompetingDurableSubscriptionModel(...)`, which U3 now owns. U6 keeps `framework/spring-boot-autoconfigure/**`.
- **sdi fence addendum:** the registrar-surface contention is not only `framework/spring-boot-autoconfigure/**`. rel34/U3 now also holds two bean methods in `framework/spring-boot-starter-mongodb/.../OccurrentMongoAutoConfiguration.java`. sdi waits on U3, U6 and U9 merging before landing implementation in either module.
- **#721/#725 STAY OUT of 0.34.0. Decided by the orchestrator on Johan's explicit delegation** ("investigate and do what's best long-term according to the principles of AGENTS.md", 2026-08-22T09:44:06Z), superseding dec-0001's "sibling epic gating R3". The move to 0.35.0 landed at 08:25:22Z and is verified on both issues; milestone 3 recounted. Derivation, not preference: AGENTS.md's release-status rule makes a shipped-API break carry a recipe plus a migration section, and sdi's own sizing puts this at eleven units, a 115-file rename and 138 source files to sweep, with eight of those units fenced behind three rel34 fixes that are still in flight. Landing that in a release already carrying four breaking changes bundles two large migration arcs into one upgrade, which is worse for a reader than two digestible ones, and the 0.34.0 changelog and its seven held docs branches are already written around the current scope. The countervailing principle, that safety work finishes in the release that starts it, does not reach this: #721 is a name clash and #725 is a data-structure redesign, neither is a correctness hole. **R3 therefore gates on brk alone.**
- **sdi's fourth-registrar-path finding, for U6's brief (#837).** Each of the five registrar files has FOUR paths, not the two or three a grep for `getBean(X.class)` suggests: stream, agnostic, DCB, and a NAME-QUALIFIED synchronous DSL bean (`occurrentSynchronousSubscriptionDsl` blocking, `occurrentSynchronousSubscriptions` reactor), looked up by name at six registrar sites and constructed at `OccurrentMongoAutoConfiguration.java:416` and `OccurrentReactiveMongoAutoConfiguration.java:383`. The by-name path is the one where #837's advice question is genuinely different, because `processSynchronousSubscribeAnnotation` resolves its bean by name at dispatch time and therefore gets the AOP PROXY rather than the raw target. ADR 0127 section 5 records the asymmetry: advice reaches exactly one of the four annotations today, and it is the synchronous one. U6 must enumerate all four rather than grepping for class-typed lookups.
- **Copilot effort evidence, correction to the 2026-08-11 note.** That note says effort is UNVERIFIED for a manually requested review regardless of the repository default. On PRs 900, 902, 908 and 913 Copilot's own review overview reported "Review effort level: Balanced" explicitly, so effort was PR-CONFIRMED from the review text rather than assumed. Read the overview; do not record it as unverified when the review itself states it.
- **Session title owed:** `[rel34] Orchestrator`. Repeat this ask in every report until the host's session list shows it. An untitled orchestrator is invisible to every worker's `ListAgents`, which is how the 2026-08-17 cross-fleet misaddress happened.
- **`ORCHESTRATOR.md` is over its ceiling** (525 lines against roughly 500). Pruning is deferred to R4's checkpoint rather than done mid-release, because dropping a fact the release needs is the more expensive failure.

## Fifteenth epic registered 2026-08-17T12:19:31Z: ayi, append identity for read-your-writes (#740)

- **Registration:** anchor issue #740, milestone `0.34.0`. Tracker lower bound now 2026-08-22T00:5xZ. Orchestrator session `local_3c7455d7-46ac-48bf-9295-f3fe35aa3608` (worktree `api-review-verification-c3b0ea`, branch `johan/append-identity-epic-ayi-088bb1`), ListAgents name `api-review-verification-c3b0ea-e5`.
- **Session id correction:** the host lists the orchestrator as `local_b9707f0d-ef3a-4605-a66d-04d1eaa47e70` (title `[ayi] Orchestrator`), not the `local_3c7455d7...` id recorded at registration. Resolve by title plus worktree, never by id.
- **Unit sessions:** U1 confirmed by marker `⌁[ayi/U1]`, worktree `competent-davinci-330ec8`, branch `ayi/u1-adr-append-identity`. U2 session `local_0adf4d40-dfb7-4321-9d59-26638a9b0939`, worktree `vigilant-wilson-c6862c`, ListAgents send name `vigilant-wilson-c6862c-7e`, marker `⌁[ayi/U2#740]`. U3 dispatched as chip `task_cadd2ad4`, worktree `quirky-archimedes` (later reused by U10). U11's worker resolved the orchestrator by title plus worktree using the ListAgents name above.
- **Mode (current, dec-0018):** UNATTENDED. May decide alone anything AGENTS.md, meta-plan revision 6, merged ADR 0132, the policy file's standing grants, or a recorded ruling answers, including CAS merges and dispatching units as READY. Queued for the return ask: taste and wording calls, unsettled API shape, the session-archiving batch, ship-day acts. Return condition: Johan's next message fires the structured return ask, mode flips only on his explicit word. Decision queue EMPTY.
- **PLAN DONE:** meta-plan revision 6 approved 2026-08-17, gate closed, meta-plan at `/Users/johan/.claude/plans/ayi-append-identity-740.md`, U11 design history at `/Users/johan/.claude/plans/ayi-u11-replay-window-recording.md`, rulings in the journal.
- **U1 DONE:** ADR 132 merged `1a0f0854a` (PR 843).
- **U2 DONE:** stamping merged `494d609f6` (PR 844).
- **U3 DONE:** store and wait merged `3afcc4ed` (PR 851).
- **U4 DONE:** recording projections merged `bb6b5495e` (PR 860).
- **U5 DONE:** falsifier suite merged `8a77ac2a` (PRs 863 and 875).
- **U6 DONE:** docs PR 73 held (occurrent-org.github.io, Johan's to merge).
- **U8 DONE:** reset honesty merged `59854d0f` (PR 869).
- **U7 DONE:** fixpoint round 1 CLEAN, changelog fix merged `4bf7de731` (PR 880).
- **U10 DONE:** flaky filtered-rebuild fix merged `ea02788cc` (PR 888).
- **U11 DONE:** pushed-signal catch-up recording merged `50bd66ec` (PR 892).
- **Do not reopen, ADR 132 and D2:** the merged ADR 132 design (see U1 DONE) is final. D2's `Optional<AppendId>` soft-widening derivation is settled, pointer only in the journal and the plan file, never re-derive as a guess.
- **Do not reopen, 865 and U4:** 865's ruling stands (default start position stays live-only, replay opt-in, honesty over refusal). U4's five plan rulings (named `ReplayPhase` type, reactive WARN-on-unanswerable, failed-clear retry on every tick, autoconfigure-common dependency sharing, push-with-catch-up registers with the poll) are settled, pointer only in ayi.yml and the journal.
- **Do not reopen, U11 final shape:** the pushed-signal design (two signals carrying the per-attempt token under the ownership lock, no sampling, no snapshot generation) is U11's final shape. Sole-carrier check confirmed two facts: no OpenRewrite recipe or migration-guide section needed (nothing shipped in 0.33.0), and the blocking dispatcher's empty-stream reduce is unreachable (every constructor assigns a child).
- **Do not reopen, caps and deferrals:** U11's Copilot loop cap (one structural round, dec-0017) is final. #891 deferred as dec-0015 (Johan may pull forward). #887 declined as dec-0013.
- **Decline register:** full list in the journal (dec-0001 through dec-0022) at `.context/orchestrator/ayi/decisions.jsonl`. Notable standing declines: #842 (reactive capability forwarding, deferred outside ayi), U3/PR 851's retry-overrun bound (declined, cited to the ADR-111 precedent), U3 round-9 remainders (deferred to #857, not declined).
- **Cross-epic (brk):** brk's push-handover work overlapped U11 on the same four files (PRs 889, 894, 895). brk's epic-end addendum #893 is sequenced AFTER PR 892 per dec-0021. ayi's copy of the 0.34.0 news post wins over r34's at the docs merge, a deliberate duplication reconciled by hand.
- **Monitor history:** five re-arms across host restarts and a reboot (`bjwc7nmm0` through `bef8hxqee`, `b4e0d1fth`, `bricleqrn`, `bvb69bbvo`), retired at U11's merge (PR 892), the last PR-advanceable deliverable.
- **Copilot facts (ayi-wide):** automatic review does not fire on some PRs here. Request via GraphQL `requestReviews` with bot id `BOT_kgDOCnlnWA`. `gh pr view --json reviewRequests` hides bot reviewers, verify through GraphQL. A thread reply creates an empty review shell that looks like a re-review, filter by author before trusting it.
- **SHIP-DAY ACTS for 0.34.0, Johan's, recorded so nothing depends on memory:** (1) merge docs PR 73 (occurrent-org.github.io, held branch docs/append-identity), taking AYI's copy of the news post over r34's per the reconciliation note above. (2) close #740 as shipped. (3) close #361 as a duplicate of #740, never resolved separately. (4) triage #876 (pre-existing updateEvent position corruption, milestoned 0.34.0) before tagging. (5) Docs merge hazard found in the 2026-08-22T07:39:14Z sweep: merging docs/802-push-observation-hook then docs/421-broker-modules raises NO conflict in docs.md and concatenates two versions of the PushObserver contract (802's boolean-era block above 421's six-outcome one). Drop 802 from the merge, 421 carries all eight of its commits. (6) Pre-existing docs defect, not 0.34.0's: docs.md calls the @Projection attribute startAtPosition at three places, the annotation declares startAtGlobalPosition.
- **ORCHESTRATOR LOOP FINISHED 2026-08-22T08:46:11Z, session archived on Johan's word. Status projection written as finished, page shows ayi finished. Remaining acts are Johan's (ship-day acts above), journals and epic state kept as the record.**
- **CLOSEOUT after U11 (2026-08-22T06:12:14Z):** session-archiving batch DONE by Johan's word (U1, U2, U3, U10, U11 sessions and the finished r34 orchestrator archived, worktrees cleaned by the archive). Remaining: the ship-day acts above. Journal queues EMPTY (dec-0001 to dec-0022 all terminal). Tracker scan clean from 2026-08-21T05:40Z, next lower bound 2026-08-22T00:33:41Z: only #893 (brk) and #891 (ayi, deferred dec-0015). Routing comments verified on #887, #890, #891. Monitor retired. Page paused. Docs preview branch preview/0.34.0 built from the eight held branches on Johan's request (throwaway, never a merge target, head f4d87d4). All seven 0.34.0 content branches simplified in a johan-writing sweep (one fact per paragraph, cold-reader order, Spring Boot versus core stated per item, RoutingOutcome has SIX values, the earlier three-value highlight was wrong and is corrected on both news copies), pushed to their held PRs, preview at 758c6667 after the Consuming from a broker subsection (#broker-consuming) was added on docs/421-broker-modules at 85ddf49 on Johan's word. Earlier fix a17c955 (2026-08-22T07:02:36Z, PR 73 updated), so ayi's copy is current at the docs merge.

## Sixteenth epic registered 2026-08-18T04:44:26Z: brk, broker integration (#388)
- **brk NEXT (2026-08-22T14:1xZ):** PR 920 MERGED cfee1477b (U16, #884 closed). U9, U15, U16 DONE. U17 verdict: PR 895's channel-generation fence is a production defect (silent consumer stall after recovery, #922, dec-0025), fix in flight on brk/u17-recovery-fence (delete the fence). brk closes when it merges, rel34's tag waits on it. #893 item 34 (Kafka cold-start offset default) is a decision for Johan at closeout.

- **State** `.context/epics/brk.yml` (schema v1, revision 1, 9 units U1-U9). Registration timestamp above is the tracker-scan lower bound. Plan `/Users/johan/.claude/plans/brk-broker-integration-388.md` revision 2, after one fresh-context adversarial review (REVISE, 16 findings, all folded). Units committed at Johan's 2026-08-16 gate ruling: #412-#417, #419, #421; #418 OUT (verified: no milestone, no divergence). Milestone 3 already holds all nine issues, nothing created.
- **Orchestrator session** `local_5be2772d-d436-44c5-b62d-155a712d6410` (derived from the scratchpad path, verify on first worker message), worktree `zealous-williamson-0ad901`, branch `johan/exciting-lovelace-80dd0b` (= main `90e3e4bb3` at registration). Title for this session: `[brk] Orchestrator, broker integration #388`. Mode: ATTENDED since `msw-20260820T0459Z` (ayi originated, global, typed ack sent; decision questions route to Johan as structured asks rather than derivation-based rulings, merges stay under the standing grant with the full gate plus adversarial verify; running subagent workers continue, they were the Unattended vehicle but converting mid-unit would strand work). Earlier: Unattended per `msw-20260818T1815Z` (brk originated) from 2026-08-18T18:15Z, Attended per `msw-20260818T0427Z` before that.
- **ADR 0133 RESERVED for brk/U1** (2026-08-18T04:3xZ scan: max across every `origin/*` branch is 0132, ayi's, merged on main; ledger empty above it). Brief carries 0133 as a resolved literal plus the re-audit command; collision is BLOCKED, never a self-renumber. The reservation is a courtesy for concurrent epics; adr-numbering CI on the merged checkout is the enforcing guard.
- **Load-bearing plan decisions (review-folded, do not silently reopen):** every merge to main releasable as-is (shared-file entries ride the PR creating the artifact they reference; nothing pre-landed for artifacts that do not exist); ONE broker changelog entry, written by U3, edited in place by U4-U7, never appended to (AGENTS.md fold rule); publish-side serialized (U5 waits on U3) plus an ADR cross-transport convention section instead of a conformance suite (extraction path stays open if #418 revives); kafka-clients managed version owned by U5, not U2; testcontainers-bom already manages both broker Testcontainers modules, no new version entries; U2 declares `integration-tests.{rabbitmq,kafka}.version` properties and the required-test-images.sh greps as the container-image contract; U8 bases on the held docs branch already documenting push subscriptions/feeds (identified at dispatch by enumeration, never docs main); U9 carries the mechanical release-readiness checklist (mvn_local_snapshot.sh artifact proof, image-script warning-free, shard-timing, API symmetry, #414 thinness re-check).
- **Registration-ask rulings, Johan, 2026-08-18T04:5xZ, do not re-ask:** RELEASE TRAIN: 0.34.0 waits on brk+ayi (r34's release gate stays queued until both complete; broker docs join the release-day held set). Plan revision 2 APPROVED. U9 stopping rule: MAJOR-AND-ABOVE reopens after the fixpoint, cap 3 rounds, remainders return as one structured ask — Johan chose stricter than the recommended blocker-only. U1 dispatched.
- **U1 dispatched 2026-08-18T04:55Z as chip `task_6afd231e`** (act-001 in brk.yml, VERIFIED). #412 claimed (in-progress label + routing comment 5323773029, read back); #388 claimed the same way (comment 5323772925). On chip start: resolve the session by the `⌁[brk/U1]` marker via transcript search, never by title; arm the quiet/stalled detector once the worker worktree exists (journaled trigger in brk.yml U1 next_action); the three-decision design gate (message binding, auto-config scope, #414 publish-vs-example) arrives as a worker message and goes to Johan as ONE structured ask.
- **Coexistence:** r34 (release gate, monitors retired) and ayi (U2 RUNNING, monitor `bjwc7nmm0` on ayi/*) are off-limits to brk sweeps and vice versa; coordination only through this file, fetch-rebase-push on checkpoints. ayi's monitor will report brk PR traffic as deltas; expected, not ayi's to act on, and mirrored for brk's own monitor (post-filter DELTA to `brk/*`, discovery repo-wide).
- **Monitor `bzxn5j7be` (v8), armed 2026-08-18T16:5xZ: CANCELLED excluded from the fail count** after a cancelled twin run made U12's green head read as five red shards (lesson in lessons.md; fold into references/fleet-monitor.md at next edit). Quiet detector `bk48soy5q` (45-min threshold, U2/U11/U12 worktrees; U2's path is stale-but-harmless post-merge, drops at next re-arm). Lineage: v8 replaced `b0zm33rvj` (11:0x, post-second-restart), which replaced `b3izst32w` (06:2x, post-host-restart), which replaced `b1dmwr7sh` (same v7 pattern, repo-wide on johanhaleby/occurrent, SKIPPED excluded, DELTA post-filtered to `brk/*`, discovery repo-wide; MONITOR-READY at 3 open PRs, re-arming sweep clean). Retirement condition: stop in the sweep that completes the last brk unit holding an unmet deliverable a PR event could advance. Restart rule: recorded-but-absent from TaskList means re-arm.
- **Host restart 2026-08-18T~06:26Z, recovery run per the skill:** PR 847 and its branch fully intact on the remote (push-survives held), U1's fourth-round fold complete on the platform (13 threads, 0 unresolved), U1's session `local_6d2cff8f-dcf0-45fd-abc7-0a70177df2a7` (worktree `hungry-shtern-13eb45`, identity via transcript marker search, never title) was down owing only its delivery bookkeeping; resume message sent with the exact obligation, deadline 07:00Z, salvage-from-platform as the fallback. No redispatch, no worktree reuse, nothing assumed lost.
- **Copilot review-effort facts for briefs:** no repository default on record; review requested manually via pr-create, so effort evidence is UNVERIFIED regardless of default (checked 2026-08-11, cdx33); briefs select Balanced and name the local-review fallback.
- Sweep-loop ceiling: 40 ticks or 400k output tokens.
- **U1 design gate CLOSED for content 2026-08-18T05:1xZ, rulings relayed, do not re-ask:** binding is BINARY MODE ONLY (Johan delegated on AGENTS.md principles; derivation: structured mode forces the four extension values into body and headers to keep the header invariant, two copies that can disagree is a correctness gap per AGENTS.md:134-135, refusing structured is reversible at zero interface cost so nothing wrong is carried forward); Spring Boot auto-config is LATER as #846, in milestone 0.34.0, adopted as brk/U10 (Johan's explicit instruction at the gate ask); #414 SHIPS AS A MODULE, provisionally on the issue's own terms, U9 re-checks thinness; the sink name is CloudEventSink. The design-GATE-closes-the-merge rule still applies: Johan has ruled on the decisions, and the merge additionally waits for his explicit close on the folded ADR text.
- **U1 session resolved:** send address `uds:/tmp/cc-socks/33860.sock`, identity confirmed by the U1 marker in its own gate message. Branch `brk/u1-adr-broker-architecture`, gate head `15737e7ba` verified by ls-remote. **Brief-address correction for every later brk brief:** a session id like `local_5be2772d-...` does NOT resolve as a SendMessage address; briefs must say "resolve the orchestrator via ListAgents (currently `[brk] Orchestrator`)" instead of handing the raw id (U1 worked this out itself and flagged it).
- **Gate-ask rulings 2026-08-18T~10:58Z, do not re-ask:** the ADR 0133 machine review loop STOPPED at ten rounds (each had produced real findings; the worker's deliberate stop-and-escalate was correct); Johan's own read of PR 847 is the closing review and the design gate, no deadline set on it. #848 ADOPTED as U11 at Opus/high (Johan's explicit tier choice), milestoned 0.34.0, blocking U4 and U6. (Corrected 2026-08-18T18:2xZ against the SHIPPED U11 contract, the stale form was a Copilot suppressed finding on PR 850: the surface was UNRELEASED, no recipe and no migration guide, the pending changelog entry absorbed the outcome contract; final shape is RoutingOutcome DELIVERED/FILTERED/NOT_DELIVERABLE with per-surface DELIVERED semantics documented, the feed as matcher with first-use-cached-permanent refusal via UnreadableLiveFilterException, and ADR 133 decision 5 amended in the PR.) Review-round facts worth keeping: the filter-derived-bindings mechanism from the design pass DIED on evidence (no bridge can read a subscription's filter; bindings default to catchAllDestination, narrowing is explicit app config; the bindings-only-narrow invariant survives); the duplicated domain filter was event loss, fixed by making the feed the matcher; Kafka needs acks=all AND enable.auto.commit=false AND per-record offset commits; RabbitMQ publishes mandatory=true with basic.return correlation; PARK is a bridge-performed confirmed publish.
- Visibility-repair list (brk): empty (#852 comment 5329133224 read back 2026-08-18T13:5xZ; #413, #388, #412, #846, #848 verified earlier).
- **brk decline-register:** #853 (durable model's eager first-position save writes any()/notOlderThan, skipping ADR 130's race resolution; the two-node race ADR 89's amendment already records as open) DEFERRED outside brk 2026-08-18T14:15Z: pre-existing recorded residual, narrower than #852, and the fix wants a check that no composing wrapper depends on last-write-wins. Filed by the orchestrator from U12's phase-1 observation. (#852's own deferral was REVERSED by Johan 2026-08-18T~14:00Z, adopted as U12.)
- **U12 ruling REFINED by Johan 2026-08-18T~14:33Z, do not re-litigate:** refusal stays the DEFAULT, plus an opt-in override mirroring the restartSubscriptionsOnChangeStreamHistoryLost precedent at BOTH levels (model config boolean + OccurrentProperties subscription property wired through the starters), default false, because shared-Atlas instances refusing hostInfo are common on test servers and a hard refusal with only a code-change escape stops running applications at startup. The refusal message names the override. Worker flags its chosen name in the DELIVERY_RESULT for Johan's rename window. Reactor-consistency question (should the reactor durable model honour the same property) echoed at the gate, not silently decided.
- **U12 fix-direction RULED by the orchestrator 2026-08-18T14:1xZ, candidate (a), derivation recorded, Johan holds a veto until the merge gate:** eager refusal from subscribe(..) when startAt is default, storage empty, and globalCheckpoint() answers null. Derivation: ADR 89 ruled the direction (unanswerable position source refuses registration, AGENTS.md's a-loss-window-is-a-loss rule, ADR 89:146), the blocking ManualStartSubscriptionModel shipped the same refusal in 0.33.0, ADR 89's amendment names the durable model's first-record as the residual (#852 IS that residual), the worker proved (b) unimplementable in the defect state and (c) contradicts the recorded rule. Never-from-the-supplier constraint (retry-loop trap), ManualStart-mirrored wording, id left free, migration-guide section and changelog entry owed. U12's ownership widened by exactly the two stale globalCheckpoint comments in the blocking Mongo models. Reactor twin verified NOT sharing the defect.
- Visibility-repair list (brk): empty (#853 comment 5329453208 read back 2026-08-18T14:1xZ; all earlier entries verified).
- **Changelog-authorship correction 2026-08-18T~14:1xZ, plan decision amended:** the single broker changelog entry is authored by U2, not U3, because AGENTS.md's changelog gate covers any public-API addition and the foundation is usable alone (bring-your-own-sink needs no transport), which the adversarial verify pass caught against my own brief. The single-entry invariant is unchanged: U3-U7 EDIT the U2-authored entry in place. U3's brief carries "edit the existing entry", not "write it".
- **U12 (#852) adopted and chip `task_5c2382f9` spawned 2026-08-18, time unverified:** investigation-first, fix-direction gate at orchestrator venue (refuse-to-start vs pinned-initial-position vs better), reactor twin parity check required, independent of all broker units, runs parallel. Milestoned 0.34.0, labeled, routing comment 5329133224 updated in place and read back.
- **U1 DONE 2026-08-18T12:05Z: ADR 133 merged as `2948368c0`** (PR 847, CAS pin 9c9b200 matched, tip==pin before branch delete, #412 closed). Gate closed by Johan's explicit "close the gate" after his read. Ten Copilot rounds, 17 threads all resolved, 26 suppressed items triaged, stop-and-escalate at round ten was Johan's ruling. Do not relitigate ADR 133 as merged: bridge-into-Pushable never a SubscriptionModel; binary content mode only with the explicit AMQP 0-9-1 mapping (cloudEvents_ headers) and cloudevents-kafka on the Kafka side; CloudEventSink/DomainEventSink naming; CloudEventForwarder for stored events, DomainEventForwarder only for app-implemented sinks; bindings default to catchAllDestination, narrowing is explicit; feed is the matcher (duplicated filter was event loss); acks=all and enable.auto.commit=false with per-record commits; mandatory=true with basic.return; PARK is a bridge-performed confirmed publish; auto-config later (#846/U10); #414 ships as a module provisionally, U9 re-checks thinness.
- **U10 (#846) registered 2026-08-18T05:18Z** at brk.yml revision 3: Spring Boot broker auto-config, depends on U4+U6, Sonnet/high chip when ready, in-milestone per Johan's gate ruling. U9's fixpoint now depends on U7, U8 and U10, and the definition of done includes #846.
- **U3 DONE 2026-08-19T04:45Z: RabbitMQ publish merged as `344d8738`** (PR 856, CAS pin 7e19522 matched, tip==pin before branch delete, #414 closed). Adversarial verify MERGE-SAFE against the confirm-and-routed invariant across seven attack surfaces; three MINOR notes journaled as U9 fixpoint inputs (shortstr length unvalidated pre-publish, ShutdownSignalException path neither retires the channel nor retries, mapper fixture gaps). Two review threads stand as evidence-backed reasoned declines (pom name redeclaration per repo precedent; async channel close, openChannel fails fast on exhausted channel_max). The verifier confirmed the per-attempt correlationId shape against the broker via Testcontainers, not mocks.
- **U5 DONE 2026-08-20T~11:0xZ: Kafka publish merged as `8f2a4d2d4`** (PR 862, CAS pin 0d52a5bc6 matched, tip==pin before delete, #416 closed). Twenty review rounds. Do not relitigate as merged: the DEFAULT resolver is the SHARED topic keyed by stream id (Johan-delegated ruling, ADR 133 amendment carries the derivation), topic-per-type stays as the documented opt-in with the honest per-stream-and-type guarantee; acks=all set when absent and refused when weaker; transactional.id refused; ordering prerequisites closed as a two-leg predicate (key-respecting partitioner, retry-safe ordering) warned at build; the acknowledgement timeout bounds one attempt as a whole with send time deducted; retry cancellation via the sink's shutdown flag; EventTypeNarrowing in the shared api module with empty narrowing binding the catch-all. U9 carries: the RabbitMQ leaf pom name redeclaration (U3's decline was wrong, AGENTS.md states inheritance), the max.in.flight>5 loud-refusal nuance, and the .context branch-snapshot decline. **U6 dispatched in the same tick** with the discriminator decision at its plan gate.
- **U4 DONE 2026-08-19T~20:0xZ: RabbitMQ consume bridges merged as `bf72e48d0`** (PR 861, CAS pin `558d5e052` matched, tip==pin before delete, #415 closed). Thirteen rounds, four adversarial verify passes. Do not relitigate as merged: a bridge acks only on DELIVERED or FILTERED and never on a throw; readiness is answered by `BlockingHandover.isReadyForLiveDelivery()` rather than a flag the feed maintains, after three separate defects came from keeping a copy; `acceptCloudEvent` reports NOT_DELIVERABLE for in-memory-only buffering and reads readiness BEFORE accepting; the AMQP mapper restores `streamversion` and `position` as numbers on read (without which a numeric filter reports FILTERED and the bridge acks the event away) and keeps present-but-empty data distinguishable from absent, both recorded as ADR 133 amendments; `RabbitMqConfirmPublisher` is the single confirmed-publish path shared by the sink and parking. Carried to U9 unfixed, deliberately: the changelog entry under-describes when NOT_DELIVERABLE is reported, and a decodable park does not carry original AMQP metadata outside the CloudEvents mapping.
- **U7 DONE 2026-08-20T~11:4xZ: broker example merged as `63b9022eb`** (PR 868, CAS pin 1052b2137 matched, tip==pin before delete, #419 closed). Eleven rounds. The dominant defect class across rounds 7, 10 and 11 was count-based waits and assertions satisfiable by the forwarder's legal at-least-once duplicates, closed by keying every wait and assertion on distinct event identity via `distinctEventIdsOnQueue` in `AbstractBrokerExampleTest`. Building the runnable half surfaced a real bug the tests masked (neither bootstrap declared the exchange, 404 on a clean broker) plus the `directConnection=true` requirement, both fixed with empirical proofs. U9 carries from the round-11 suppressed comment: the redelivery counter in `RabbitMqCloudEventLevelBrokerExampleTest` cannot distinguish bridge redelivery from a forwarder legal duplicate after a lost confirm, a theoretical vacuity in an example test (quiesce the forwarder or publish one persisted CloudEvent directly for that assertion).
- **U4 and U5 dispatched 2026-08-19T04:49Z as worktree SUBAGENTS** (Unattended vehicle, acts 006/007, Sonnet, plan gate at first stop). #415 and #416 claimed with routing comments and in-progress labels, read back. U4's brief carries U11's per-surface DELIVERED caveat, stop-not-retry on UnreadableLiveFilterException, reuse of U3's 0-9-1 mapper, and the catchAllDestination default; U5's carries U3-as-shape-template with written-reason divergence, kafka-clients version ownership, and checkpoint-after-acked-send with acks=all. Both carry the changelog edit-in-place rule with keep-both rebase resolution against each other. Monitor `bk5gsm959` (v8) continues, now watching for the two new brk/* PRs.

- **DECISION QUEUE, brk: EMPTY again as of 2026-08-20T~05:2xZ.** The topology item below was RESOLVED by Johan's delegation at the Attended-mode structured ask ("Do what's best long-term according to the principles of AGENTS.md"): the DEFAULT resolver becomes a SHARED topic keyed by stream id, topic-per-type stays as the documented opt-in alternative. Derivation recorded in the ADR 133 amendment riding PR 862: decision 7's stated purpose (a projection reading one stream needs its events in order) fails under topic-per-type for the canonical multi-type stream, a documented narrower guarantee makes the wrong default honest rather than right, and reversibility is asymmetric since shared-now-plus-per-type-later is additive while correcting a shipped per-type default breaks deployments whose topics exist. U6 consumes from the shared topic by default. Historical entry follows. *Kafka default topic strategy versus the ordering guarantee ADR 133 decision 7 states.* The shipped `KafkaTopicPerTypeDestinationResolver` derives a topic per event type and keys each message by stream id, and both its javadoc and the changelog claim that keying buys per-stream ordering. It does not: Kafka orders within one partition of ONE topic, so two event types from the same stream land on different topics and identical keys buy nothing across them. ADR 133 decision 7 states the purpose plainly, "a projection reading one stream needs that stream's events in order, so keying by stream id puts them on one partition", and never mandates topic-per-type; that came from #416 and the epic plan, and the orchestrator confirmed it at the plan gate citing the plan's wording, so the mistaken combination is the orchestrator's as much as the worker's. THE CHOICE: (a) keep topic-per-type as the default and ship the narrower honest guarantee, ordering within a stream AND type pair, relying on ADR 133's own escape hatch that an application wanting different partitioning replaces the resolver; or (b) make the default a shared topic keyed by stream id so cross-type stream ordering actually holds, which is what the ADR's rationale assumes and what event-sourced projections folding multi-type streams need, at the cost of losing per-type topic separation for retention and consumer scaling. Not decided here because it changes what users deploy and what the release promises. The false claims are being corrected NOW under either answer, and PR 862 is HELD from merge pending this. Escalate as a push only if it becomes the last thing blocking the epic, since U4 and U7 can proceed meanwhile. **The module prose is now corrected under either answer** (worker verified the false claim was confined to the resolver javadoc and one changelog sentence, with no test name or assertion in the module implying cross-type ordering), **but ADR 133 decision 7's own text carries the same imprecision** and needs an amendment riding whichever answer comes: under (a) it must state the guarantee as per stream AND type, under (b) its rationale stands and the resolver changes instead. The worker correctly left the ADR alone rather than editing an open design surface.
- **TYPED EXTENSION RULING 2026-08-19T~14:4xZ, amends ADR 133's mapping section, recorded for U6 to mirror.** A bridge that rebuilds a CloudEvent from a broker message gets String-typed extensions, so a numeric Occurrent extension such as `streamversion` comes back as `"3"`. `ConditionMatcher` (checked on main) compares Number against Number by value through BigDecimal but falls to `Objects.equals` for a String against an Integer, and its own trailing comment names that case. So a filter that should match reports FILTERED, and FILTERED is a case a bridge ACKS, which discards an event the projection would have accepted. Silent loss, and it refutes the earlier verifier pass that logged the String-only round trip as a documented deliberate limitation: documented yes, its consequence no. RULING: do NOT preserve types on the AMQP wire even though field tables allow it, because Kafka's binary binding writes headers as strings by the CloudEvents spec and the two transports would then disagree about which events match a filter, which decision 7 exists to prevent. Instead RESTORE the extension types Occurrent itself owns when rebuilding the event, leaving unknown application extensions as documented Strings, so both transports behave alike without fighting the binary binding. Amendment recorded in U4's PR per the #848 precedent. **U6 (Kafka consume) must mirror this**, and U5 does not need it since publishing never rebuilds.
- **brk decline-register addition: #864 filed 2026-08-19T~13:3xZ, a genuinely flaky test, deferred outside brk.** `CompetingConsumerSubscriptionModelTest.consumption_is_resumed_after_prohibited_when_lease_time_is_low` failed on TWO unrelated brk PRs the same day with OPPOSITE symptoms: on PR 861 at `3c702a7a2` it tripped the fail-fast `should never have more than 1 event` (two consumers held the lease at once), and on PR 862 at `bf30ca749` it timed out waiting for the one event (no consumer held it). Neither branch touches `subscription/util/blocking/competing-consumer-subscription`, main was green around both, and both reruns passed on the same commit. Two unrelated PRs failing the same test in opposite directions is what promoted it from "rerun it" to a filed issue; the rerun-until-green habit would hide a real double-delivery defect here. Not brk's to fix.
- **Reboot 2026-08-19T~12:00Z, recovery run per the skill.** Both brk PRs survived intact on the remote (861 at `23e8f5624`, 862 at `246c287e2`, both MERGEABLE), main at `1d08d47fc`, push-survives held again. Both worker agents were gone. U4's worktree `brk-u4-rabbitmq-consume` was CLEAN, so it died before starting its round; U5's worktree `agent-ace718d4d9821cadb` held UNCOMMITTED round-4 work (the `EventTypeNarrowing` extraction plus resolver, pom and changelog edits) that had to be preserved. So both were re-dispatched as NON-isolated subagents pointed at their EXISTING worktree paths, since a fresh `isolation: worktree` agent would have forked a new directory and stranded U5's draft. Monitor re-armed as `b0o6pqs6z` (v9). Worth keeping: after a reboot, check each worker worktree for uncommitted work BEFORE re-dispatching, and route the replacement into that same path rather than a new isolated one.
- **RETRY CANCELLATION RULING 2026-08-19T~10:0xZ, do not relitigate: both broker sinks retry forever with no way to stop.** The Kafka merge-gate verifier raised it as a BLOCKER and it is shared identically by the already-merged RabbitMQ sink, so it is a convention gap rather than one unit's slip. A persistent-but-retriable failure (a cluster degraded under min.insync.replicas throwing NotEnoughReplicasException, which extends RetriableException) retries with backoff for the length of the outage, while both classes' javadoc claimed the acknowledgement timeout bounds the whole of `publish`. RULING: the attempts are NOT capped, because infinite retry on a transient failure is the sanctioned pattern and ADR 133 decision 7 names `NativeMongoCheckpointStorage` as the template deliberately; what both sinks lack is that template's CANCELLATION path, `executeWithRetry(op, __ -> !shutdown, retryStrategy)` with a volatile flag flipped on shutdown (`subscription/mongodb/native/blocking-checkpoint-storage/.../NativeMongoCheckpointStorage.java:54,115,201`). Both sinks adopt that shape in their own units, U5 for Kafka and U4 for RabbitMQ (U4 owns that file this round, so routing it there avoids a collision), keeping the two symmetric per decision 7, and the javadoc says truthfully that the timeout bounds each ATTEMPT while the retrying call is bounded by the sink's lifetime.
- **HELD DOCS PR 69 IS STALE AGAINST SHIPPED MAIN, found 2026-08-19T~08:0xZ, brk owns the repair.** `docs/802-push-observation-hook` documents `PushObserver` with the pre-U11 boolean contract, an example lambda `(cloudEvent, matched) -> { if (!matched) ... }` plus prose about the match check, the reactor twin and the feed guard. U11 (PR 850, merged) replaced that with `void observe(CloudEvent, RoutingOutcome)` carrying DELIVERED, FILTERED and NOT_DELIVERABLE, verified on main by reading the shipped interface, so the documented example does not compile against the API of the release the branch is held for. r34 authored the branch and its epic is complete, and the held PRs are Johan's release-day acts, so nobody owned this; brk caused the break and takes it. Bound into U8's brief as mandatory extra scope in the same branch U8 already bases on. Anyone touching the 0.34.0 held docs set before U8 runs should know PR 69 must not merge as written.

## Eleventh epic registered 2026-08-11T09:05:38Z: cdx33, codex 0.33.0 design-review remediation

- **State** `.context/epics/cdx33.yml` (13 units), meta-plan `/Users/johan/.claude/plans/unified-bubbling-koala.md`, approved 2026-08-11 via ExitPlanMode. The review (21 findings, boundary `occurrent-0.32.0..f4224c7b6`) was independently re-verified by three agents before planning: both blockers CONFIRMED, finding 14 PARTIAL (pre-existing 0.32.0 defect, not the claimed new exposure), finding 12 verified as NOT the #681 edge, finding 18 traced to PR #647.
- **Decisions resolved at approval, do not re-ask:** D1 the ADR 116 CheckpointStorage break stays (Johan: best long-term per AGENTS.md; the compatible shape's silent delegate-any() default is finding 16's runtime failure in disguise); D2 `capability`/`hasCapability` stay (rv33 register holds); D4 finding 3's incremental matcher-state evaluation ships IN 0.33.0 (persisted-shape change is free only pre-tag; U3 grew accordingly and carries the schedule risk); D5 ambiguous `CompetingConsumerStrategy` beans FAIL STARTUP (U6; reverses the documented deliberate fallback). D3 (finding 1 direction) is deliberately open until U1's plan gate, with the docs-truth floor defined so the tag cannot deadlock on it.
- **Deferred/declined (UREG files these at closeout):** finding 5 defer (wrapper contract test issue), finding 20 decline (ADR 0110 documented limitation), finding 21 defer (measurement issue). Finding 13 non-gating with the honest rationale recorded in the plan (grazes the silent-loss criterion; stays non-gating only because U10 ships in Wave A anyway).
- **Copilot review-effort facts for briefs (checked 2026-08-11):** no repository default on record in this file; review is requested manually via pr-create's flow, so effort evidence is UNVERIFIED regardless of default; briefs select Balanced and name the local-review fallback.
- **Visibility-repair list (transient, steady state empty):** EMPTY. #732, #736, #737, #738 each carry a confirmed `orchestrator-routing` comment (verified by re-read 2026-08-11). #734 and #735 are held off deliberately, since PR 739 deletes the code they describe and they close as moot against it once that merges.
- **#740 is the designated 0.34.0 successor epic** (append identity for read-your-writes), routing comment confirmed present, #361 reopened and superseded by it. Register it in a fresh session after the release. Repair queue EMPTY again.
- **#741 is the accepted pre-tag follow-up** for design-review finding 3's other half (persisted counters plus a bound on the active step's retained events), routing comment confirmed, and its issue body carries the two known-broken cache-key designs plus the persisted-shape constraints so the next unit needs no rediscovery. #743 (a saga declaring a supertype event subscribes on a CloudEvent type no event carries, pre-existing, INFERENCE, silently receives nothing) deferred with its comment confirmed. Repair queue EMPTY.
- **UDOCS scope, gathered 2026-08-11 while U6's matrix ran, so the brief needs no rediscovery.** Docs held set is ELEVEN open PRs plus one unopened branch, and issue 57's body is the authoritative order: 49, 52, 63, 53, 54, 55, the unopened `docs/707-step-conditions-release-note`, then 61, 58, 59, 64, and 62 after the release. What cdx33 changes about that list: **#52 (`docs/vgpr-361-version-gated-projection-reads`) must LEAVE the order entirely**, since PR 739 withdrew the feature it documents, and #63 renames that same read-your-writes header so it goes with it or gets rescoped; the news post on #54's branch names `AppliedProjectionPositionStore` and must lose that mention; #55 (checkpoint fencing) needs the U6 additions, meaning startup refusal on ambiguous strategy beans, `evaluatesWriteConditions`, the `fence-checkpoints` opt-out and the two-lease-owners fix; #61 (step conditions) needs the U3 semantics, meaning the fired window, the `allOf` duplicate refusal and the determinism requirement. Issue 57's body must be rewritten to match, and the preview branch rebuilt and grepped for zero hits on the withdrawn API. Docs-repo merges remain Johan's, and a subagent cannot use Edit or Write outside its session worktree, so a docs unit run as a subagent must be told to write through scripted Bash and `git -C`.
- **#748 DECIDED by ADR 128 (r34/U9, PR 816): a missing step is a refusal**, an `IllegalStateException` naming the step and the two ways out, never a recovery guess. The old deferral note told future sessions to decide this; do not reopen it.
- **#750 and #751 deferred with confirmed routing comments.** #750 is the projection DSL carrying the identical supertype defect, and the divergence PR 752 creates is deliberate and named in ADR 0124: from 0.33.0 sagas expand a sealed supertype while projections keep documenting the constraint plus their `filter()` hatch. #751 is `Saga#filter()` parity, declined for 752 as new public API in a held release. Repair queue EMPTY.
- **#753 CLOSED by PR 913 (rel34/U9).** The exemption is gone from `EventTypeExpansion`, so a declared event type is accepted only when every level from it down is sealed or final. Breaking, folded into the existing 0.34.0 breaking-change entry, with section 3 of the 0.34.0 upgrade guide and NO recipe (a recipe cannot tell a refused declaration from a sealed one that now works, since the sealed modifier is not in the type model behind a class literal, and a subclass may live in a module the recipe never sees). ADR 0124's exemption paragraph is superseded by ADR 0126's amendment, and 0124's Status points at 0126. **Do not re-derive #753 as open.** The old bullet here claimed the `EXEMPT_AND_MISSES_SUBCLASSES` outcome asserts it and that ADR 0124 gives it a titled paragraph; both were true and are now false, the outcome is deleted. The worker declined to edit this file from its feature branch and routed the correction here instead, which was right: this file goes straight to `main` with `[ci skip]` and a branch edit would collide.
- **cdx33 register COMPLETE for the deferred and declined set.** Filed and routed with confirmed marker comments: #746 (finding 5, replay awareness as an optional runtime protocol every wrapper must forward; narrower now that PR 739 deleted two of the three wrappers it cited), #747 (finding 21, measure the lease refresh round's margin before touching concurrency; narrowed already by #691, #659 and #662). Earlier in the epic: #741 accepted as the pre-tag follow-up for finding 3's counters plus retention bound, #743 deferred, #732 and #736 deferred as recipe-internal, #737 deferred as competing-consumer and catch-up hardening, #738 deferred as a reactor-stack design question, #734 and #735 CLOSED as moot once PR 739 deleted the code they described.
- **Finding 20 DECLINED for 0.33.0, do not re-raise:** the projection runners' two-persistence-calls-per-event replay cost is a deliberate documented limitation that ADR 0110 records, and the review itself accepted keeping the limitation explicit as a valid outcome. Revisit only if a runner-path replay is measured as a real bottleneck, and note that the coalescing design lives at the catch-up subscription and materialized-view boundary, so giving runners the same lifecycle is a design change rather than a fix.
- **cdx33 decline-register, do not re-ask each sweep:** #732 and #736 deferred as recipe-internal to a later recipe-hardening pass; #737 deferred as competing-consumer and catch-up hardening, a candidate epic of its own after the release, and it carries PR 733's reasoned-decline item on per-attempt lifecycle identity across four sites; #738 deferred as a reactor-stack design question (whether reactor also adopts pin-at-registration) rather than a port of #728's patch. Review findings 5 and 21 and the finding-20 decline still owe their issues at closeout (unit UREG).
- Work-item monitor `bkps4vt2j` (v7 pattern, repo-wide on johanhaleby/occurrent, armed 2026-08-11 ~09:08Z before first dispatch; lifetime rule: runs while any unit has an unmet deliverable a PR event could advance).
- **Companion review-thread watch `bvkvmw2tn`, armed 2026-08-11 ~09:5xZ, closing a real gap in the v7 pattern.** v7's delta key is number, head, mergeable, `reviewDecision`, fail count and DONE/running, and a Copilot review lands as `COMMENTED` which leaves `reviewDecision` an empty string. So a worker that answers and RESOLVES a thread without pushing a commit changes nothing v7 watches, and the PR becomes merge-ready with no event at all. That is the same class of defect as the arrival-and-departure-only monitor already recorded in `lessons.md`, and the fix is the missing signal rather than a shorter heartbeat. The watch polls unresolved-thread counts per open PR every 90s and emits `THREADS-ALL-RESOLVED` (merge-gate candidate), `THREADS-OPEN` (count changed, still blocked), or `THREADS` for a PR with no threads, plus READY/UNHEALTHY/RECOVERED lines so silence is never ambiguous. Both monitors retire together under the lifetime rule.
- Session title for this epic: `[cdx33] Orchestrator — codex 0.33.0 review`. Orchestrator session id `local_faee3625-2363-4f07-857d-edcbd27ceedc` (derived from the scratchpad path, verify on first worker message). Sweep-loop ceiling: 40 ticks or 400k output tokens. Prior epics' files, sessions and branches are off-limits to this epic's sweeps.


## Cross-epic: rel34 (0.34.0 release epic, registered 2026-08-22T07:05:33Z, its ORCHESTRATOR.md copy lives on `origin/johan/rel34-u1-pushobserver-prose`)
- rel34's R3 (cut the 0.34.0 tag) gates on brk closing. brk's closing condition: PR 897 and PR 898 merged, U9 mechanical checklist run, U15 PR 2 (blocking per-id lock) merged, then #388 closeout. Status is the brk NEXT line in the brk section below (the hot-restart block belongs to the latest registrant, rel34).
- rel34/U1 took PR 69's PushObserver repair out of brk/U8. Moot for brk: U8 is DONE by Johan's dec-0005 and nothing in brk dispatches on it again.
- OVERLAP, resolved by ownership: rel34/U1's PR 900 owns `changelog.md` (lines 6 and 27 with the other two stale surfaces as one pass). brk PR 898 had rewritten line 6 and now reverts `changelog.md` to main before merging. Told rel34 and the 898 worker 2026-08-22T08:05:13Z.
- **Journal tracking ruling 2026-08-22 (Johan, via rel34, main 9eecf8b30):** decision journals are local-only for every fleet, the brk negation is gone from .gitignore, no history purge. brk's live journal was deleted from disk by the merge that brought the untrack in and restored from the pre-merge parent (26 records, seq 26). Tracking mode is per repository and Johan's call.
- **sdi (0.35.0, Subscription to SubscriptionHandle rename, ADR 0127) sequences behind PR 910.** sdi found the overlap by diffing open PRs by branch prefix, not from the issue-keyed exclusion lists, because U15 has no issue number. brk's only work after 910 is U16 in broker/kafka/blocking test code, outside sdi's surface. Lesson: issue-keyed exclusions are a filter, the PR file lists are the coverage check. Told sdi 2026-08-22T12:3xZ.
- The shared file has diverged by branch (this copy on `johan/exciting-lovelace-80dd0b`, rel34's on its own branch, main's copy stale). Each epic's own section is authoritative in its own copy. Reconcile by hand at brk closeout, not by rebasing one over the other.

### sdi checkpoint 2026-08-22T13:11Z

U1 identity resolved BY MARKER, not by title, per the practice recorded for U12 and U5. Peer `sad-tesla-568fd8-30`, session `46d7a92c-c139-49ef-81bd-a91147f09067`, worktree `.claude/worktrees/sad-tesla-568fd8`, branch `sdi/u1-subscription-handle-rename` local and unpushed. At 13:11Z it stood at 149 files, 760 insertions, 727 deletions, mid full-module compile. The rename direction is confirmed from the diff rather than from the brief: `org.occurrent.subscription.api.{blocking,reactor}.Subscription` becomes `SubscriptionHandle`.

Fence re-checked live, and it has not advanced:

* #837 (rel34/U6, both `SubscriptionAnnotationRegistrar` files) is OPEN with no PR at all. It is the long pole and it binds U6, U7 and U13.
* #912 (rel34/U16) is OPEN, PR 928 open and unmerged. It gates U3 and U14 through `dsl/dcb-dsl/common`.

Six units therefore derive STALLED rather than PROGRESSING, and the stored labels now say so instead of contradicting the derivation. STALLED here is an honest fleet condition, not a defect: sdi has six units queued behind one rel34 issue that has not been dispatched.

brk's PR 930 (`brk/u17-recovery-fence`) opened and was checked against U1 rather than waved through on its fleet marker. It is disjoint on two independent measurements. The file sets do not intersect, and neither `RabbitMqCloudEventBridge.java` nor `RabbitMqDomainEventBridge.java` references the renamed type on `origin/main`, so U1's rebase will not have to follow it into 930's rewritten code. Its `changelog.md` edit is one line inside the broker section and does not touch the landing heading, which is what sdi cares about there. No action for sdi.

**For rel34, a priority signal rather than a request.** #837 is now sdi's single long pole. U7 is the one sdi unit whose entire dependency chain is satisfied (U5 and U12 are both DONE) and it is held by nothing except the registrar fence, which has narrowed to #837 alone. Five further units sit behind it. #837 is OPEN with no assignee, no label, no branch and no PR, so from sdi's side its start is not visible at all.

This is a signal and not a claim on rel34's ordering, and the reason is worth stating plainly: sdi moved to 0.35.0 by Johan's ruling, so R3 no longer gates on sdi closing, which removes the one lever that would have made #837 urgent for rel34's own release. #837 stays in 0.34.0 as rel34's work regardless. sdi is recording where its cost lands, not asking rel34 to reorder around it.

sdi cannot route around it either, and that was checked rather than assumed. U7 rewrites the same registrars #837 fixes, and U7's own brief requires it to preserve #837's transaction advice fix on the deprecated reflective paths, which outlive 0.34.0. The disjointness measurement that lifted the fence for U1 under dec-0006 cannot repeat here, because the overlap is real. Fence recheck moved to 14:30Z for U3, U6, U7 and U13.

### sdi correction 2026-08-22T13:19:34Z: #837 IS running, and the entry above was wrong

Retract the long pole framing in the entry immediately above. It said #837 was not dispatched and that its start was not visible from sdi's side. The first half is false and the second half was true only because sdi looked in the wrong place.

rel34's own state file has `rel34/U6` at `phase: RUNNING` with a resolved session, `local_53811444-7045-4edd-afdb-ec20a97a4ee1`, worktree `amazing-shannon-db71fe`, on claude-sonnet-5 at effort high, and its `next_action` is to await the worker's claim on #837 and its first push. So the work started, and sdi is queued behind something live rather than behind something forgotten. rel34 needs no priority signal from sdi and none is being made.

The error is worth naming because it is the day's recurring shape rather than a one off. sdi checked GitHub, saw no assignee, no label, no branch matching `rel34/u6` and no PR, and read that as not dispatched. Every one of those observations was accurate. The conclusion did not follow, because a fleet's dispatch state lives in its epic state file, which is the shared memory this coordination protocol already points at, and the issue tracker only catches up once a worker claims. The branch check was doubly blind: this worker runs on `johan/amazing-shannon-db71fe`, a session branch off main, so no naming convention would have surfaced it.

**The rule this settles.** Before asserting anything about a sibling fleet's state, read `.context/epics/<slug>.yml`. The tracker is downstream of it and lags it by the whole interval between dispatch and first claim, which is exactly the window in which one fleet is most tempted to conclude the other has stalled.

One observation handed to rel34 without acting on it, since it is rel34's bookkeeping. `rel34/U6` carries `last_meaningful_progress_at: 2026-08-22T18:10:00+00:00`, which is roughly five hours ahead of the actual time as sdi reads it. `derive` computes health by subtracting that stamp from now, so a future stamp always yields PROGRESSING and the unit can never derive STALLED. If that came from stamping local time with a `+00:00` suffix, other rel34 units likely carry it too, and rel34's stall detection is off for all of them.

### sdi checkpoint 2026-08-22T13:24Z: status projection was stale, and the session title was self closable

rel34 announced five shared skill edits and asked every fleet to check its own status projection. sdi's was stale in exactly the way rel34 described its own: `epic_state_revision` 21 against an epic at 33, generated 11:53Z, with U5 rendered RUNNING after it had finished and U1 rendered BLOCKED while it was running. Refreshed through `decision-journal.py status`, read back rather than assumed (revision 33, journal_seq 12, 14 units, every phase agreeing with `derive`), then regenerated the viewer. `stale_after` is 14:45Z because that is a checkpoint sdi intends, not a round hour.

The projection is now bound to the memory checkpoint, so a stale page means a missed checkpoint rather than a forgotten command.

Separately, the session title ask that sdi had carried as an open pending action since registration is closed, and the reason it stayed open is worth recording. The skill asserted that rename tooling cannot retitle its own session and told every orchestrator to repeat the ask until the user acted. In this host that is false: `set_session_title` accepts the literal `self`, and the title was set in one call. The skill has been corrected (`706d2e13b`) to try the host affordance first. The report to Johan says renamed rather than verified, because both `ListAgents` and `list_sessions` exclude the calling session, so neither obvious check can see the thing being checked.

U1 remains healthy at 13:24Z: 149 files, full-module compile passed, now running tests with the Colima environment exported per shell as the recorded lesson requires. No commit yet.

### sdi checkpoint 2026-08-22T13:33:07Z: sdi's own timestamps were fabricated too, 13 of 14

rel34 found forty-three invented `last_meaningful_progress_at` values in its epic state and asked the other fleets to check. sdi had the same defect at the same rate. Thirteen of fourteen values were written from a sense of elapsed time rather than read from the clock, drifting 39 to 90 minutes ahead of the moment they claimed to record. The one correct value was U1's, written immediately after running `date -u`.

The two failures differ in severity and the difference is instructive. rel34's values crossed the present, so `derive` computed a negative age and STALLED was unreachable all day. sdi's stayed behind the present, so STALLED still worked and six units correctly derived it. sdi's only cost was that every age was understated, delaying any stall flag by 40 to 90 minutes. Nothing about the health column looked wrong, and nothing would have.

Repaired by rel34's reconstruction method rather than clamped to now: each distinct value maps to the first commit whose version of the state file contained it, and that commit's author time is a recorded upper bound on when the observation was actually made. Every value mapped, so nothing had to be invented a second time.

**The first repair was too narrow and that is the correction worth keeping.** It swept only `last_meaningful_progress_at`, on the reasoning that this is the field `derive` reads. A second pass over every timestamp in the file found five more fabricated values in `issued_at` and `since`, one of them a pending action recorded as issued two hours in the future. Fabrication is a property of how a value was produced, not of the field it landed in, so the audit has to be file wide. Deadlines are excluded deliberately: `recheck_after` and `stale_after` are future by design.

sdi is at revision 35, `derive` reports zero drift, zero future observation timestamps remain, and the status projection was refreshed and read back at revision 35. Both findings sent to rel34, including a warning that its own repair may carry the same narrow scope.

### sdi checkpoint 2026-08-22T13:38:19Z: the clean file explains the defect

rel34 applied the was-it-measured test to its own file after sdi's warning and found seventeen more distinct fabricated values, thirty-four occurrences, all sitting in the past and all invisible to the future scan it had already passed. The same test found four more in sdi's, in the `at:` fields inside `observed` records. Repaired at revision 36.

Auditing the rest of sdi's durable state produced the finding worth carrying forward. `decisions.jsonl` is completely clean: twelve envelope timestamps, monotonic, none future, none unmeasurable. That is structural rather than lucky, because `decision-journal.py` stamps `at` itself and refuses a payload that supplies its own `generated_at`. The pattern held everywhere the audit looked. Every value written by a program was correct, whether stamped by the journal, returned by `gh`, or recorded as git author time, and every value typed by a model was wrong.

So the rule both fleets wrote this morning is the weaker form. Read the clock is right but incomplete. The stronger version is to prefer a field a tool stamps over a field you fill in, and to ask the owning system for any fact about an external object. sdi's state claimed PR 923 opened at 13:3x; `gh pr view 923` returns 12:04:52Z and would have returned it at any point today. That was invented while the authoritative answer was one call away, which is the same failure as the timestamps rather than a lesser one.

Two observation keys still embed fabricated times in their names, `fence_2026_08_22T11_35Z` and `fence_2026_08_22T13_0xZ`. Values inside corrected, keys deliberately not, since a key is a label with no consumer and renaming risks a reference for no gain. Recorded rather than fixed so nobody later reads those names as measurements.

U1 unchanged at 149 files and quiet for five minutes inside a testcontainers run, which is expected for Mongo startup rather than a stall. Watch it past twenty minutes.

### sdi checkpoint 2026-08-22T13:40:55Z: every completion claim verified against gh, zero exceptions

rel34 extended the audit to completion claims and sdi ran the same pass. Both DONE units rest on merged-PR deliverables and both verify to the second and to the SHA: PR 924 merged `12:48:17Z` as `6f47516c6`, PR 923 merged `12:38:27Z` as `5ba3b9bfa`. Every other external fact the state file cites verifies too: PR 913 merged as `a86909f5d`, PR 902 merged as `4f12b2ffb`, PR 908 closed with a null `mergedAt` exactly as the file's "CLOSED WITHOUT MERGING" note says, #753 closed, #871 closed at `12:23:30Z`, and both anchor issues #721 and #725 still open as the epic expects.

That is the same total split rel34 found on its own data. Values fetched from a program are right, values typed by a model are wrong, and now on two independent datasets with no counterexample in either direction. The mechanism is worth stating over the symptom: a model filling a field cannot tell recalling from inventing, and a program filling the same field never faces the choice.

One scoping note carried back from rel34. Its repair sweep missed three external times because it matched full ISO strings while those were written bare as `HH:MM:SSZ` in prose. sdi's file carried one of that shape, the `12:23:30Z` issue-close time, which verified. An audit anchored on the schema's format will not see what a sentence carries, and prose is where a fabricated time is least likely to be challenged.

### sdi checkpoint 2026-08-22T13:45:03Z: prose swept, and the redaction convention was carrying fabrications

Having named the prose gap, sdi ran the sweep rather than leaving it as an observation. Every time-like token in the state file that was not a full ISO string, five of them, written in a redacted-minute style like `13:0xZ` and `14:0xZ`. Two were fabricated, one pointing into the future, and one was the file's own `updated_at` five hours stale.

The redaction is the part worth keeping. It reads as a statement about precision, hour known and minute deliberately not claimed, so it looks like a value someone already thought about. Every audit either fleet ran matched full ISO strings, so none of these was ever tested, and the `x` signalled care where none had been taken. Inventing the digits around a precision marker borrows the credibility of having been careful, which is why these survived four passes that caught everything else.

Repairing them separated the two recovery sources, which had been treated as one. `PR 908 was CLOSED WITHOUT MERGING at 13:0xZ` is a claim about a GitHub object and `gh` settles it outright at `11:41:19Z`, eighty minutes off. The adjacent claim about when rel34 flagged #912 is a cross-fleet event no external system owns, so only commit reconstruction can bound it. Owning system first, reconstruction as fallback, because reconstruction yields a bound while the owning system yields the value.

sdi at revision 37, prose swept, zero future observation values, the two observation keys still deliberately unrepaired and recorded as such. brk's PR 930 took a new head and remains disjoint from U1 on the measurement already recorded. U1 still running.

### sdi checkpoint 2026-08-22T13:58:00Z: dec-0006's premise expired, and U1 recreated issue #721 inside the epic that fixes it

Two findings from checking U1's in-flight diff rather than waiting for its delivery.

**dec-0006's zero-intersection claim is no longer true, and its conclusion survives anyway.** The decision Johan approved rested on a `comm -12` showing U1's fourteen framework files disjoint from rel34's open PR 914 and from the two files rel34/U6 owns. U1's framework set has since grown to sixteen, and the two new files are exactly `OccurrentMongoAutoConfiguration` and `OccurrentReactiveMongoAutoConfiguration`, which PR 914 also edits. So the files now intersect. The hunks do not: PR 914 works at lines 182 to 206 in each, U1 at 292 and 329, so git auto merges and the decision holds. Recording it because the premise expired through U1's own growth rather than through anything rel34 did, which is a way a measured decision can go stale that nothing in the fence protocol watches for.

**U1 renamed three references to the wrong Subscription.** `org.occurrent.annotation.Subscription` is an annotation and keeps its name; only the API type `org.occurrent.subscription.api.{blocking,reactor}.Subscription` becomes `SubscriptionHandle`. Three added lines now reference a `@SubscriptionHandle` annotation that does not exist, two javadoc lines in the Mongo auto configurations and one comment. A text level rename cannot distinguish the two meanings, which is precisely issue #721, so the epic that exists to fix that ambiguity had begun introducing fresh instances of it. Sent to the worker with the rule that an `@` prefix means the annotation and stays, plus an instruction to sweep its own diff for the same shape rather than fixing only the three named lines.

Caught by reading the worker's working tree during a wait rather than by any gate. The merge gate would have caught it, but only after a full CI run and a review round.
