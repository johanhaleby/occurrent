# Occurrent Orchestrator Memory

Last updated: 2026-07-15 (`@Projection` annotation follow-up implemented on branch `johan/projection-annotation` (off `main`, 2 commits `65d792eb9`+`adcc5b692`), NOT yet PR'd. Declarative persistent catch-up read models on both stacks via a factory-method annotation; the registrar extends each subscription BPP with a `SmartInitializingSingleton`; store-agnostic (`ViewStateRepository`/`CrudRepository`/Mongo-default); async + synchronous modes; ADR 0059; `ResumeStartPositions` for programmatic no-starter catch-up; course-enrollment migrated. Verified per-module green (blocking 7 @Projection tests, reactor 5, course-enrollment 14/14, all on clean builds). Docs added to the same release-gated PR `occurrent-org.github.io#7`. See the @Projection entry under Current State. Prior: Projection DSL (issue #194) MERGED to `main` as #328 (commit `29e7a479e`), ADR 0058, 4-demo example module, docs in PR #7 (still release-gated). Prior: 2026-07-14 Java decider facades (`DeciderApplicationService`, `DcbDeciderApplicationService`) landed on `main` post-0.30.0 (PR #325, unreleased), and docs PR `occurrent-org.github.io#5` is held for the next release, see the Java-decider-facades entry under Current State. Prior: 2026-07-13 dcb.events coverage pass: verified all seven https://dcb.events/examples/ patterns are expressible on the current DCB API; added `DcbReadOptions` direction+limit (`backwardsLimited(n)`, ADR 0056) across all four DCB stores for efficient last-entry reads, and a Kotlin `dcb-patterns` example module cataloguing the five undemonstrated patterns. In PR #324. See the new Current State entry. Prior: 2026-07-10 Fixed the `Decider` Kotlin state-chaining nullness regression from #287, PR #317 merged to `main` at commit `7f21bf8`. See the fragile-areas entry. Prior: 2026-07-09 Added the `occurrent-rewrite` OpenRewrite upgrade-recipe module for the 0.20.5 to 0.30.0 release, on a feature branch (not yet a PR). See the OpenRewrite recipe item under Current State. Prior: 2026-07-07 DCB architecture-review remediation complete. PRs #291-#300 all merged to `main`, HEAD around commit `278f4b7a4`; ADR 0053 froze the DCB API. Earlier main gained #284 position stream catch-up, #285 virtual-thread subscriptions, #286 pattern-matching modernization, #287 JSpecify nullness defaults, #289 DcbDecider/ADR 0052, #290 capability-agnostic subscription change-stream fix.)

This is durable session context: the architecture map, conventions, hard-won invariants, and how to build and verify. It is NOT a change log. Shipped work lives in git history, `changelog.md`, and the ADRs under `doc/architecture/decisions`. Keep this file short.

## Current State

- **Synchronous subscriptions (branch `feature/synchronous-subscriptions`, PR open, unreleased, 2026-07-15).** Lets a subscription be declared *synchronous* so its handler runs on the writer thread, before `execute` returns, optionally in the same transaction as the write. The decision lives with the subscription (a separate `@SynchronousSubscription` annotation, no per-`execute` flag, no `mode` on `@Subscription`), mirroring Axon's Subscribing-vs-Tracking split. ADR 0057. Shape: a register-only `SynchronousSubscriptionModel implements Subscribable` (blocking `subscription-synchronous-blocking` + reactor `subscription-synchronous-reactor`, no lifecycle/startAt/replay); the app service holds an optional `SynchronousEventDispatcher`/`ReactiveSynchronousEventDispatcher` seam (in `application-service-common` / `application-service-reactor`, so the app service does not depend on the subscription layer above it) plus an optional `TransactionExecutor`/`ReactiveTransactionExecutor` (storage-neutral, defaults to `noTransaction()` = best-effort). After a write it re-reads the just-written events enriched with streamVersion+position (stream: tail read; DCB: the appended global-position block via `DcbReadOptions.between`) and dispatches, **guarded so there is zero cost when nothing is registered** ("no free lunch": one extra read per event-producing write, only while synchronous subscriptions exist). Wired on all four app services via a new `builder(...)`. Spring: `@SynchronousSubscription` (agnostic; `id`+`eventTypes`) is routed by the two bean-post-processors onto a name-qualified synchronous `Subscriptions` DSL, invoking the handler **through its Spring proxy** (resolved lazily from the context) so handler-side `@Transactional` composes; the starters wire a `SpringTransactionExecutor`/`SpringReactiveTransactionExecutor` by default, so every `execute` runs in a transaction the event-store write joins (PROPAGATION_REQUIRED). No-Spring atomicity via `NativeMongoTransactionExecutor` (own module) + a thread-bound `ClientSessionHolder` the native `MongoEventStore` write/append/read join. The extracted `SubscriptionFilterMatcher` (`subscription-core`) is shared by the in-memory and synchronous models. **Fragile/known:** (1) the starters mark the async `SubscriptionModel`/`Subscriptions` beans `@Primary` because the synchronous model is also a `Subscribable`, so a bare `Subscribable`/`SubscriptionModel` injection resolves to the async one (inject the concrete `SynchronousSubscriptionModel` or `SubscriptionModel` to disambiguate). (2) The native `MongoEventStore` routes `read`/`query`/`count`/`exists`/`readInPositionOrder` through the thread-bound `ClientSessionHolder` when set, so a synchronous handler sees the uncommitted write consistently. `count(Filter.all())` is the one asymmetry: it uses the fast `estimatedDocumentCount()` when no session is bound (the normal path, and the `CatchupSubscriptionModel` net-count path, both unchanged), but falls back to an exact `countDocuments(session, {})` inside an ambient session because `estimatedDocumentCount` has no `ClientSession` overload and cannot join a transaction. The slower exact count therefore only fires for a synchronous handler that itself calls `count(Filter.all())` inside the write transaction, a niche path. (3) Docs (`occurrent-org.github.io`) still to be written (fold into held PR #5 or a new stacked PR). (4) Verified locally on `main`-checkout: native suite (163 tests) + a native in-handler-visibility test green, and the full framework starter suites passed (exit 0). The earlier cleanup review's two findings both landed (the `SubscriptionAnnotations.resolveTypeFilter` extraction and the native session-routing above). CI is the authoritative gate for the whole PR.
- **`@Projection` annotation (issue #194 follow-up), 2026-07-15, branch `johan/projection-annotation`, NOT yet PR'd.** Declarative persistent read models. `@Projection` (framework/annotations) marks a factory method returning a `Projection`/`DcbProjection`; the registrar is the existing annotation BPP extended with `SmartInitializingSingleton` (post-refresh scan + register, so the factory is invoked once collaborators are wired). It subscribes through the framework catch-up+durable+competing-consumer composite, so it catches up from history + resumes durably like `@DcbSubscription` (catch-up is a property of the injected model, not the DSL). Dispatch on descriptor type (DcbProjection -> DCB, Projection -> agnostic/stream per `capability`). Store-agnostic resolution: `store()` bean, else unique `MaterializedView`, else `ViewStateRepository`, else Spring Data `CrudRepository`, else zero-config Mongo (blocking only). `mode=SYNCHRONOUS` = read-your-writes via the synchronous subscription model (mutually exclusive with catch-up start knobs). Duplicate ids rejected at startup. **Prefactor:** added `ResumeStartPositions.replayThenResume(+Dcb)` (public, Spring-free, in `subscription/api/{blocking,reactor}`) for the DEFAULT resume gate, reused by the registrar AND usable programmatically without the starter; did NOT rewire the existing BPPs or dedup the stack-specific `SAME_AS_START_AT` (each stack reuses its own in-module). **Reactor v1 limits (deliberate):** no zero-config Mongo default (a reactive app has no blocking `MongoOperations`), no DCB-synchronous mode. ADR 0059. Corrected ADR 0058 + `DcbProjectionRunner` "live-only" wording (conditional on the model). Migrated `course-enrollment`'s dashboard to `@Projection` (in-memory `ViewStateRepository`, BEGINNING+SAME_AS_START_AT). Verified per-module on clean builds: blocking 7 @Projection tests + existing subscription tests green, reactor 5 @Projection tests green (2 restart/agnostic flaked on Colima replica-set-init, pass on retry), course-enrollment 14/14. Docs added to the release-gated PR `occurrent-org.github.io#7`. **Lesson:** local incremental Maven leaves a deleted source's stale `.class` in `target/`; the migrated `@Projection` hit a false duplicate-id from the stale `CourseDashboardSubscriber.class` until `mvn clean` (CI is unaffected).
- **Projection DSL (higher-level read-model API, issue #194), MERGED to `main` as #328 (commit `29e7a479e`).** The read-side mirror of `DcbDecider`. New module family `dsl/projection-dsl/{common,blocking,reactor}`. `common`: `Projection<S,E,ID>` (record: `View<S,E>` + `id: Function<E,@Nullable ID>` + `eventTypes: Set<Class<? extends E>>` + optional `@Nullable Filter`) and `DcbProjection<S,E,ID>` (adds a `DcbCriteria` read boundary). Built with a type-safe per-event-type handler builder producing a reused view-dsl `View` (Java `Projection.builder(init).id(..).on(Class<T>, BiFunction).filter(..).build()`; Kotlin `projection(init){ id{}; on<T>{}; filter() }` / `dcbProjection(init){ id{}; on<T>{}; tags("k:v")|criteria() }`). The `View` fold no-ops on unregistered types; the handler keys drive the subscription filter (declared once). `blocking`/`reactor`: `project(...)` runners (Kotlin extensions on `Subscriptions`/`StreamSubscriptions`/`DcbSubscriptions` and `DomainEventQueries`/`DcbDomainEventQueries`, plus Java facades `ProjectionRunner`/`DcbProjectionRunner` and reactive twins) that create the subscription/query AND materialize/fold the read model, across stream+DCB, blocking+reactor, and three delivery modes: async subscription, synchronous read-your-writes (reuses #327's `SynchronousSubscriptionModel` as a `Subscribable` + the app-service `builder(...).synchronousSubscriptions(...)`, no parallel API), and on-demand pull. Store binding stays OUT of the descriptor (runner overloads take `MaterializedView` | `ViewStateRepository` | `MongoOperations`, reusing view-dsl `materialized(...)`). **Two deliberate decisions:** (1) DCB criteria is read verbatim, NOT AND-ed with handler types, because intersecting types into an OR-of-alternatives `DcbCriteria` risks the `MatchAll`-collapse semantics (see the `DcbDecider` compose caveat) and the fold already no-ops on unhandled types, so over-reading only costs a few no-op folds; (2) reactor materialization has no reactive `ViewStateRepository` (that stays a view-dsl follow-up), so the reactor push runner offers a reactive `(E)->Mono<Void>` primitive (the true read-your-writes path) plus a blocking-store convenience bridged on `boundedElastic` (off-thread, NOT read-your-writes, documented); reactor pull is bound to non-null `S` since Reactor cannot carry null. ADR 0058. Example module `example/domain/projection-dsl` (4 demos: Java+stream, Java+DCB, Kotlin+stream, Kotlin+DCB incl. the issue-#194 `isUsernameClaimed`; in-memory, Docker-free, 10 tests). Verified: common 20, blocking 9, reactor 7 (2 need Docker/Colima via `DOCKER_HOST`), example 10, all green on JDK 21. Changelog `#### Changes` bullet added. **Docs:** release-gated draft PR `occurrent-org.github.io#7` (branch `docs/projection-dsl`, commit e3bdf90) documents it; do NOT merge until the projection DSL ships in a release. **Follow-ups:** `@Projection` annotation and the `course-enrollment` migration are now DONE (see the `@Projection` entry above). Still deferred: `Projection.compose`, and a reactive `ViewStateRepository` materialization convenience (so a reactor `@Projection` into Mongo need not bridge the blocking store on `boundedElastic`). **Release trigger (shared with the facades/PR #5 note below):** once the release including this ships, mark docs PR #7 mergeable and drop its gate.
- **DCB read direction+limit (ADR 0056) and the `dcb-patterns` example catalog (2026-07-13, unreleased, post-0.30.0).** `DcbReadOptions` gained an optional `Direction {FORWARD,BACKWARD}` and `OptionalInt limit`, plus `backwards()`/`forwards()`/`limit(int)` withers and the `backwardsLimited(int)` factory. Additive and backward compatible: a source-compatible 1-arg `DcbReadOptions(PositionRange)` constructor preserves all existing callers, and the only raw `new DcbReadOptions(` sites are inside the class itself (everything else uses the factories). Semantics: a `DcbEventStream` is ALWAYS ascending by position regardless of options; direction/limit only select WHICH matches are returned (FORWARD keeps the lowest-position, BACKWARD the highest), and NEVER affect the `DcbConsistencyToken` (it reflects the whole matching set, so a limited read still guards an append against any later match). MongoDB stores implement BACKWARD+limit as `sort(descending(POSITION)).limit(n)` then reverse-to-ascending. Implemented + tested on all four stores (in-memory 26, native 25, spring-blocking 25, spring-reactor 24 DCB tests green; each has a token-invariant test asserting `backwardsLimited(1)` returns the same token as an unlimited forward read). Motive: gapless business sequences (invoice numbers) read their last entry in one round trip instead of folding the whole history. New example module `example/domain/dcb-patterns` (Kotlin, in-memory, no Spring, no Docker, 16 tests) catalogues the five dcb.events patterns not already shown: `uniqueusername`, `idempotency`, `dynamicproductprice`, `optintoken` (all via the `dcbDecider` DSL), and `invoicenumber` (deliberately bypasses the decider fold, reads via `backwardsLimited(1)` + token-guarded append). **Lesson: domain event classes fed through `ReflectionCloudEventTypeMapper.simple()` must be top-level, not nested in a sealed interface** (`Class.forName(pkg + "." + simpleName)` can't resolve the `Outer$Inner` binary name). Verified idioms documented in the module README: global uniqueness/idempotency is just a scoped append condition, and time-based decisions are timestamp-in-payload + now-in-command (the decide/evolve fold never sees CloudEvent metadata). changelog.md has a fresh `### Changelog next version` section (0.30.0 is now a stamped release, dated 2026-07-13).
- **Java decider facades (2026-07-14, unreleased, post-0.30.0), PR #325.** Running a `Decider`/`DcbDecider` through an application service was Kotlin-only (the `execute(command, decider)` extensions are `inline`/`reified`, so uncallable from Java). Added thin Java facades: `DeciderApplicationService` (stream, `dsl/decider`) and `DcbDeciderApplicationService` (DCB blocking + reactor, `dsl/dcb-dsl`), constructed around an application service, exposing `execute(command, decider)` plus `executeAndReturnState`/`Events`/`Decision`. The decider's event type must equal the service's; widen a narrower one first with `Decider.adapt`/`DcbDecider.adapt`. Also added `DcbDecider.criteriaFor(command|commands)` (resolves the read boundary, rejects an unrecognized command, requires one shared boundary per execute); the Kotlin DCB decider extensions now delegate to it. Lives in the changelog's unreleased `### Changelog next version` section. **Pending docs merge:** docs-site PR `occurrent-org.github.io#5` documents these (Java tab on the landing "Stream or DCB" comparison, facade examples in the decider docs) and is deliberately held until the next release publishes. At session start, if occurrent has cut the release including these facades (any one signal: the `### Changelog next version` heading became a versioned `### X.Y.Z (date)`, a new `occurrent-<version>` git tag exists, or the version resolves from Maven Central), tell the maintainer PR #5 is ready to merge, then drop this note. Same release trigger: the docs `main` Examples table (added 2026-07-14, commit `b92b4e6` in `occurrent-org.github.io`) deliberately omits the `dcb-patterns` example row, because its tag-pinned link would 404 against 0.30.0 (the module landed post-release). Once the release publishes, add a `dcb-patterns` row to that table pointing at `example/domain/dcb-patterns`.
- **OpenRewrite upgrade recipe (`occurrent-rewrite`), for 0.20.5 to 0.30.0.** New top-level Maven module (dir `rewrite/`, artifactId `occurrent-rewrite`, published, deliberately kept OUT of the release profile's `excludeArtifacts`). Ships a declarative-first recipe set: `org.occurrent.MigrateOccurrentRenames_0_30` (all mechanical type/method/package renames: the `SubscriptionPosition` to `Checkpoint` family, `PolicySideEffect` to `SideEffect`, `OccurrentSubscriptionFilter` to `StreamSubscriptionFilter`, `ExecuteFilter`/`OccurrentProperties` package moves), `org.occurrent.MigrateStreamToList_0_30` (best-effort `Stream` to `List` write side: auto-converts `StreamCommandComposition` to `ListCommandComposition` and `Stream.of/empty` in `EventStore.write` args, and marks the sites it can't safely rewrite with a `/* TODO [Occurrent 0.30 upgrade] */` comment; lambda bodies and Kotlin are left manual by design), and the umbrella `org.occurrent.UpgradeToOccurrent_0_30` composing both. Scope was deliberately limited to symbols that existed in released 0.20.5: DCB and reactive-new symbols are excluded (a 0.20.5 user never referenced them). `rewrite-recipe-bom:3.34.0` imported in root `dependencyManagement`; the module also depends on `org.openrewrite:rewrite-kotlin` so the renames rewrite Kotlin call sites too, not just Java (the `Stream`-to-`List` recipe stays Java-only). Verified on Kotlin: `ChangeType` renames and instance-method `ChangeMethodName` fire; the `StartAt.subscriptionPosition(...)` static-factory rename does NOT fire on a Kotlin caller (a known rewrite-kotlin limitation, documented as a manual step in the README and migration guide, not worked around in the YAML). Tests use `org.openrewrite:rewrite-test` plus `rewrite-java-21` and `org.openrewrite.kotlin.Assertions.kotlin(...)` with source type-stubs (real Occurrent modules aren't on the recipe test classpath). Migration guide at `doc/migration/upgrading-to-0.30.0.md` (three buckets: automated renames, best-effort-plus-manual Stream-to-List, and behavioral: Java 21, MongoDB 8, `Decider.compose` at least two, catch-up fail-loud, position backfill). Build/verify: `rtk mvn -Drevision=0.30.0-SNAPSHOT -pl rewrite -am test` (18 RewriteTests green on Java 21, including 2 Kotlin cases). The recipe's correctness rests on the invariant that the default event-store capability is STREAM and DCB is explicit opt-in on all stores and both Spring starters (verified: `DEFAULT_EVENT_STORE_CAPABILITIES = Set.of(STREAM)`, starter `OccurrentProperties.capabilities`/capability `Condition`s all default to `Set.of(STREAM)`).
- **Method/function-level breaking-change audit of the recipe (2026-07-10, 5 parallel Sonnet auditors by module group, cross-checked against the 0.20.5 tag).** Ran because the earlier type-level audit only matched top-level (non-indented) declarations and so was blind to nested types and to Kotlin top-level functions. Outcome:
  - **Recipe gap found and FIXED: renamed public NESTED types.** A dedicated nested-type sweep found 4 public nested types renamed and NOT in the recipe (only `SubscriptionPositionStorageConfig$UseSubscriptionPositionInStorage` had been covered). Added `ChangeType` entries (nested `$` FQNs) for: `StartAt$StartAtSubscriptionPosition` to `StartAt$StartAtCheckpoint`, and `SubscriptionPositionStorageConfig$`{`DontUseSubscriptionPositionInStorage`,`UseOnlySubscriptionPositionInStorage`,`PersistSubscriptionPositionDuringCatchupPhase`} to `CheckpointStorageConfig$`{`DontUseCheckpointInStorage`,`UseOnlyCheckpointInStorage`,`PersistCheckpointDuringCatchupPhase`}. These are reachable via `switch` pattern-matching over the sealed types. Recipe now at 20 RewriteTests, green. **Lesson: a top-level-declaration sweep misses nested public types and Kotlin free functions; sweep both (nested = indented type decls; Kotlin = top-level `fun`).**
  - **No mechanical Java method-rename gaps.** Every `ChangeMethodName` the changelog implied is present and correct. The only method-rename candidates found were in `org.occurrent.subscription.mongodb.internal.MongoCommons` (`generateGenericSubscriptionPositionDocument` becomes `generateGenericCheckpointDocument` etc.), deliberately NOT added: `...internal` package, not a consumer API contract.
  - **Confirmed-covered or already-documented-manual (no action):** the `Stream` to `List` signature changes (`EventStore.write`, `CloudEventConverter.toCloudEvents`, `ApplicationService.execute` domain function, `ExecuteOptions.sideEffect`, `InMemoryEventStore`/`InMemorySubscriptionModel` `Consumer<Stream>` becomes `Consumer<List>`, `View.evolve(Stream/Sequence)`), the removed Kotlin `Sequence` families (`executeSequence`/`executeList`, `sideEffectOnSequence` becomes `sideEffectOnList`, `write(Sequence)`, module-dsl `command(Sequence)`, `CommandConversion`/`toStreamCommand`), and `Decider.compose` >=2 (behavioral, in changelog).
  - **Migration-guide additions (recipe CANNOT mechanically fix these; now FOLDED INTO `doc/migration/upgrading-to-0.30.0.md` on 2026-07-10 via /johan-writing, gate passed):** (1) BEHAVIORAL/operational: default subscription id derivation changed (#295) from domain-class simple name to `cloudEventConverter.getCloudEventType(...)` (FQN under the default reflection mapper), so an in-place upgrade orphans existing `subscribe<E>{}`/`@Subscription`-without-explicit-id checkpoints, they replay from scratch. Documented in changelog line ~236 but NOT flagged as an upgrade hazard in the guide. Highest-severity gap. (2) Kotlin `subscriptions { }` DSL: its `subscribe(filter=...)` param is now `AgnosticSubscriptionFilter`, and the `OccurrentSubscriptionFilter` to `StreamSubscriptionFilter` type rename does NOT make it compile, a filtered `subscriptions {}` must move to `streamSubscriptions {}`. (3) `@Subscription` lost `startAtTimeEpochMillis`/`startAtISO8601` (moved to `@StreamSubscription`) and `StartPosition.BEGINNING_OF_TIME` becomes `BEGINNING`. (4) Kotlin `PolicySideEffectExtensions` top-level functions (`executePolicies`, `andThenExecutePolicy`) removed with no `SideEffect`-named replacement. (5) reactive `ReactorDurableSubscriptionModel` `subscribe(...)`/`findStartAtForSubscription` redesigned (in changelog ADR 44). Low-severity/omitted: several Kotlin `<E>` to `<E : Any>` nullness-bound tightenings (break only nullable type-argument callers, unrealistic for event types).
- **DCB is feature-complete on `main` and in pre-release hardening.** A six-angle architecture review (fable generate + opus verify) plus its remediation shipped across PRs #291-#299, and PR #300 swept the changelog to remove unreleased DCB dev-churn. The API surface was frozen for release per ADR 0053. See Release Readiness under Open Items for what remains before a release can be cut.
- **Java 21 baseline** (`maven.compiler.release=21`, Kotlin `jvmTarget=21`, CI matrix `[21, 25]`). PRs #273-#275.
- **DCB shipped on all four event stores**: in-memory, Spring blocking, native driver, Spring reactive. DCB is an optional capability layered on the existing CloudEvent storage (extensions `dcbtags`, `position`), not a parallel event model. Shared Mongo DCB code is in `eventstore-mongodb-dcb-common`; the capability set is `EventStoreCapability` in `eventstore-api-common`. ADRs 0017-0022.
- **Spring Boot starters**: blocking `spring-boot-starter-mongodb` and reactive `spring-boot-starter-mongodb-reactive`, gated by classpath presence (mirroring Spring Boot's own split). Stack-neutral wiring is shared in `spring-boot-autoconfigure-mongodb-common`. `@Subscription`, `@StreamSubscription`, and `@DcbSubscription` work on both stacks (see the capability-boundary item below for what each delivers).
- **Catch-up is DCB-free and dual-mode routed on both stacks** (PRs #269/#270, naming cleanup #271). A `CatchupSubscriptionModel` dispatcher routes each subscription to the stream, DCB, or (as of #283) capability-agnostic inner model by filter type first (a bare global position is ambiguous between stream and DCB position-ordered replays). Stream catch-up no longer depends on the DCB API.
- **Subscription position types renamed to `Checkpoint`** (PR #272, ADR 0046): `SubscriptionPosition`->`Checkpoint`, `GlobalSubscriptionPosition`->`GlobalCheckpoint`, `SubscriptionPositionStorage`->`CheckpointStorage`, `PositionAwareSubscriptionModel`->`CheckpointAwareSubscriptionModel`, `StartAt.subscriptionPosition(...)`->`StartAt.checkpoint(...)`. The stored resume-marker field moved from `subscriptionPosition` to `checkpoint` with a read-only legacy fallback (see fragile areas).
- **Web-UI examples**: blocking `example/domain/course-enrollment` and reactive `example/domain/hotel-booking`, both DCB, materialized-view dashboard + tag-scoped SSE feed (Thymeleaf + HTMX).
- **Capability-boundary correctness completed for reads and subscriptions, both directions (PRs #280, #282, #283, ADRs 0049-0051).** On a store with both `STREAM` and `DCB` enabled, three consumer surfaces now each have a well-defined, symmetric contract:
  - `DcbCriteria` reads (`DcbCriteria.all()`, type-only criteria) exclude stream-only events (#280/ADR 49): a `dcbTags`-exists guard, backed by a sparse index on the Mongo array field.
  - `StreamCatchupSubscriptionModel`/`ReactorStreamCatchupSubscriptionModel` (`@StreamSubscription`) exclude DCB events (#282/ADR 50): a `Filter.capability(EventStoreCapability.STREAM)` guard ANDed into both the replay read and the live handover filter, via a new `Filter.CapabilityFilter` case.
  - `@Subscription`/`Subscriptions` were revived (previously deprecated stream-only aliases) as the capability-neutral default (#283/ADR 51): the same position-based catch-up as stream, minus the capability guard, routed through a new `AgnosticSubscriptionFilter` marker. Delivers both stream and DCB events, filtered only by type, catching up over the unified global position (ADR 45) and resuming via `GlobalCheckpoint`.
  - The write-time invariant that makes the Mongo capability filter sound: `write(...)` (the stream API) now rejects any event carrying the `dcbtags` CloudEvent extension, in all four stores. This closes a real gap Copilot's review caught: the Mongo `Filter.capability(DCB)` check keys off the *derived* `dcbTags` array field (Mongo-only, written only by DCB `append(...)`, sparse-indexed per ADR 49), not the `dcbtags` CloudEvent extension (present on any tagged event regardless of write path, which is what the in-memory matcher and `DcbCloudEvents.isDcbEvent` check). Without the write-time guard those two could diverge; with it they are provably equivalent, so the Mongo filter can use the indexed field without missing a stream-written-but-tagged event.
  - `@DcbSubscription`/`DcbSubscriptions` were untouched throughout; they already excluded stream events correctly (`isDcbEvent` + `DcbCriteria` post-filter).
- **DcbDecider couples a Decider with its DCB boundary and tags (merged to main as #289, ADR 0052).** `DcbDecider<C,S,E>` (Java record in `dsl/dcb-dsl/common`) wraps a `Decider` plus a `Function<C, DcbCriteria>` (read boundary derived from the command, returns null when the decider does not apply, used by composition) plus a `TagGenerator<E>` (tags for the events it emits). The DSL exposes `execute(command, dcbDecider)` on both stacks, replacing the old `execute(criteria, command, decider)` extensions (the low-level `execute(criteria, function)` on the service stays). `DcbDecider` composes like `Decider` (`adapt`/`compose`): composed criteria is `DcbCriteria.anyOf` over the children that recognize the command, composed tags are their set union. Tagging is now two complementary models: a `DcbDecider` carries its own tags (per-execute, routed via a new optional `TagGenerator` on `DcbExecuteOptions`), while decider-less DCB (raw `execute`, or `@DcbTag`) uses the optional global `TagGenerator`. Precedence in `GenericDcbApplicationService.addTags` is per-execute first, then global, else it throws (never appends untagged). The global tagger is now optional at the constructor (new no-tagger constructor overloads) AND in the Spring autoconfig, which now creates the `DcbApplicationService` even with no global `TagGenerator` bean (`DcbApplicationServiceDiagnostics` removed). Course-enrollment and hotel-booking now own per-decider inline tags and have no global tagger bean; word-guessing-game's plain `dcb` module stays decider-less with a global tagger as the counter-example.

## Architecture Summary

Occurrent is a Maven multi-module JVM event-sourcing library centered on CloudEvents, designed as small composable libraries rather than a framework: domain models stay independent of Occurrent. Occurrent stores CloudEvents and provides application-service, event-store, subscription, DSL, deadline, and Spring Boot wiring helpers.

Root modules (`pom.xml`): `test-support`, `eventstore`, `subscription`, `cloudevents-extension`, `common`, `application`, `dsl`, `framework`, `deadline`, `library` (currently `hederlig`), `bom`, `example` (active-by-default `examples-module` profile).

Layering:
1. `common` + `cloudevents-extension`: reusable primitives (`Condition`, `Filter`, retry/time utilities, Mongo filter/sort conversion, stream metadata `streamid`/`streamversion`). `common/eventstore-capability` is a new, deliberately tiny, dependency-free module holding just the `EventStoreCapability` enum and the DCB tag extension name constant (`EventStoreCloudEventExtensions`); it exists because `common/filter` needs `EventStoreCapability` for `Filter.capability(...)` (added in #282) but `eventstore-api-common` (the enum's original home) already depends on `common/filter`, so the enum had to move somewhere both can reach without a cycle.
2. `eventstore`: persistence contracts and implementations. Blocking `EventStore` composes read/conditional-write/existence; wider capabilities are optional interfaces (`EventStoreQueries`, `EventStoreOperations`, `ReadEventStreamWithFilter`).
3. `application`: adapts domain events to CloudEvents via `CloudEventConverter`/`CloudEventTypeMapper`, orchestrates commands via `ApplicationService`/`GenericApplicationService`.
4. `subscription`: reacts to event streams. Blocking + reactor APIs, Mongo/native/Spring/Redis/in-memory adapters, and durable/catchup/competing-consumer wrappers.
5. `dsl`: convenience layers over the core APIs for Kotlin/Java.
6. `framework/spring-boot-starter-mongodb`(`-reactive`): auto-configures the Mongo stack.

ADRs live in `doc/architecture/decisions`, NOT `doc/adr`.

## Module Boundaries

`eventstore`: `api/common` (shared model: `WriteCondition`, `WriteResult`, `StreamReadFilter`, `SortBy`), `api/blocking`, `api/reactor`, `api/dcb`, `inmemory`, `mongodb/common`, `mongodb/dcb-common`, `mongodb/native`, `mongodb/spring/blocking`, `mongodb/spring/reactor`.

`application`: `service/blocking` (+ `dcb`), `command-composition`, `cloudevent-converter` (Jackson 2/3, XStream), `cloudevent-type-mapper`.

`subscription`: `api` (blocking/reactor contracts), `core` (shared checkpoints, filters, start positions), `inmemory`, `mongodb` (common + native/Spring + checkpoint storage), `redis` (Spring checkpoint storage), `util` (durable, stream-catchup, dcb-catchup, competing-consumer, reactor wrappers).

`framework/spring-boot-starter-mongodb`: wires `MongoTransactionManager`, `SpringMongoEventStore`, checkpoint storage, lease competing-consumer, `SpringMongoSubscriptionModel -> Durable -> Catchup -> CompetingConsumer`, DSLs, `GenericApplicationService`, Jackson 3 converter config.

## Primary Execution Flows

Application command flow: `ApplicationService.execute(streamId, ExecuteOptions, domainFunction)` -> `GenericApplicationService` resolves optional `ExecuteFilter` via the converter (filtered reads require `ReadEventStreamWithFilter`, else fail fast) -> read stream, convert to domain events, run pure domain function, convert back, write at expected version, run optional post-write side effects.

Mongo blocking write/read: `write` validates `WriteCondition` (for `anyStreamVersion`, materializes the stream so retries reuse events); inside a transaction, reads current version, validates, maps to documents with stream metadata, inserts, translates duplicate/write exceptions, returns old/new versions. Reads pin to current version to avoid skew.

Subscription flow: `Subscribable.subscribe(subscriptionId, filter, startAt, action)` -> Mongo implementations use change streams + lifecycle-managed listener containers -> `DurableSubscriptionModel` persists the checkpoint after successful action -> `CatchupSubscriptionModel` replays history for a time/position start before handing over to the live model -> Spring Boot wraps in durable/catchup/competing-consumer layers.

## Conventions And Patterns

- Java 21 baseline; Kotlin JVM target follows Java 21. Java and Kotlin coexist in most modules (root build-helper adds `src/main/kotlin` + `src/test/kotlin`).
- Public APIs are small capability interfaces, not monoliths; validate nulls/invalid args eagerly (`Objects.requireNonNull` / `IllegalArgumentException`); prefer static factories/builders for fluent APIs; Apache 2 headers on source.
- Nullness uses JSpecify (`@NullMarked`, `@Nullable`) in newer APIs, not uniformly across old code.
- Changelog (`changelog.md`): unreleased changes go under `### Changelog next version`, NOT a versioned `### X.Y.Z (date)` section. The maintainer assigns a version/date only at release. Never invent one. Update the changelog after any change affecting behavior, public API, build/runtime, or user-facing capability; skip it for doc-only edits.
- **Changelog convention for unreleased capabilities (PR #300):** a capability that is unreleased gets NO "change" entries (users never had the old behavior to change from). DCB is unreleased, so DCB dev-churn (fixes, tuning, API polish of the still-unreleased DCB) stays OUT of the `#### Changes` and `#### Details` subsections and lives only in `#### Highlights`. DCB feature-announcement bullets and non-DCB shipped changes still go in `#### Changes`/`#### Details` as normal.
- Kotlin: avoid extension names colliding with Java members (ADR 0012 uses `executeSequence`/`executeList`); typed execute filters namespaced under `ExecuteFilters` (ADR 0013); keep Java contracts honest (`? extends E`) with localized Kotlin bridge casts only where needed. Run at least `test-compile` on affected modules after Kotlin-around-Java-generics changes (`.context/lessons.md`: Kotlin inference can fail even when Java compiles).

Testing: JUnit 5 + AssertJ dominant, jqwik for a few property tests, Awaitility for async/subscription/deadline. Unit and integration tests both run under Surefire (no Failsafe split). Docker/Testcontainers (MongoDB, Redis) is common; some integration tests bind Mongo to host port `27017`.

### Restart-test pattern (reusable)

Annotation attributes (`startAt`, `resumeBehavior`, `startupMode`) are compile-time constants, and the dynamic `StartAt`/`DcbStartAt` supplier that encodes `resumeBehavior` is evaluated ONCE, inside `subscribe(...)` at bean post-processing. Pausing/resuming a running model (`stop()`/`start()`) never re-invokes it, so it cannot demonstrate replay-vs-resume. To test `resumeBehavior`, close the Spring context and boot a fresh one (`SpringApplication.run(...)`) against the same durable MongoDB backing data (same `spring.data.mongodb.uri`, same database), so the annotation is reprocessed like a real restart. See `DcbSubscriptionResumeBehaviorAnnotationMongoTest` and siblings: a `FirstBootApplication` and `SecondBootApplication` register the same subscriber classes so the second context's subscriber is a fresh instance.

### Testcontainers gotchas

- `MongoDBContainer.getReplicaSetUrl()` (no-arg) always targets the `"test"` database. Use `getReplicaSetUrl(String databaseName)` for isolation; do NOT string-concat a suffix (Mongo forbids dots in DB names and it silently stays `"test"`, causing cross-test collisions).
- Booting via `SpringApplication.run(...)` (not `@SpringBootTest`) gets no `@ServiceConnection`; pass `--spring.data.mongodb.uri=...` in the `args`.
- This repo pins the host port `27017:27017` with `.withReuse(true)` in the restart-pattern tests (see `OccurrentReactiveMongoAutoConfigurationWiringTest`). Root cause not fully isolated (likely Colima/Testcontainers flakiness), but the fixed-port+reuse pattern is reliable here.
- Mongo Testcontainers on this machine intermittently fail with `MongoSocketOpenException`/`Prematurely reached end of stream` right after container start, a known Colima networking flake (`colima.md`). Retry once before concluding a test is broken.

## Known Fragile Areas And Risks

- **`Decider` state returns must be the plain type variable `S`, never `@Nullable S`, under `@NullMarked` (FIXED 2026-07-10, PR #317, commit `7f21bf8`).** PR #287's JSpecify null-marking made `decideOn*AndReturnState` and the private `fold` return `@Nullable S`, which Kotlin types as `S?` for a concrete non-null `S`, breaking the common pattern of chaining one decision's state into the next `decide` call. The fix returns plain `S` so its nullness follows the type argument (parametric nullness), keeping the `S extends @Nullable Object` bound so a null initial state stays expressible. `@Nullable S` on those returns was already present in 0.20.5 but harmless there because the package was not null-marked (the `state` parameter was a flexible platform type); `@NullMarked` in #287 is what turned it into a hard compile error. This exact regression was fixed once before (`6ed6aed31`) and reintroduced by #287, so the interface now carries a guard comment and `DeciderTest` has a Kotlin chaining test that fails to compile if the annotations return. No changelog entry: the break only ever existed on the unreleased 0.30.0 line.
- **Real-upgrade-trial method validated (run twice, 2026-07-10): install the reactor as `-Drevision=0.30.0-SNAPSHOT`, then run the recipe against a real downstream consumer via `mvn org.openrewrite.maven:rewrite-maven-plugin:RELEASE:run -Drewrite.activeRecipes=org.occurrent.UpgradeToOccurrent_0_30 -Drewrite.recipeArtifactCoordinates=org.occurrent:occurrent-rewrite:0.30.0-SNAPSHOT`.** No pom edits needed to run the recipe itself (ad-hoc invocation). Confirmed correct behavior on `scheduling-service` (100% Kotlin, uses `decider`/`view-dsl`/`occurrent-support`): the recipe was a clean no-op both times, since that project never referenced any renamed 0.20.5 symbol directly. The one real Stream/Sequence-to-List break was the View DSL's `evolveAll(events: Sequence<E>)` overload removal (part of #314), fixed trivially by swapping `queryForSequence` to `queryForList` at the one call site feeding `evolveAll` (documented Kotlin-manual bucket, not a recipe gap). After PR #317 (`Decider` nullness fix) landed, rerunning this trial confirmed the fix: the two `OperationDeciderTest.kt` chained-decide compile errors are gone and `mvn -o test-compile`/`test` proceed past them cleanly. Local trial gotchas unrelated to Occurrent: scheduling-service needs `JAVA_HOME` pointed at JDK 25 (`~/.sdkman/candidates/java/25-tem`) and `DOCKER_HOST=unix://$HOME/.colima/default/docker.sock` set explicitly, or Testcontainers can't find the Colima socket. When trialing against a real repo with pre-existing uncommitted work, stash it first (`git stash push -u`), revert only your own trial changes with `git checkout --`, then `git stash pop` to restore the original state exactly. **Remaining blocker to a fully green test run:** `occurrent-support:2.0.3` (Parkster's internal Occurrent wrapper) throws `ClassNotFoundException: org.occurrent.dsl.subscription.blocking.EventMetadata` when built against 0.30.0-SNAPSHOT. **Correction (2026-07-10): my first pass wrongly concluded this FQN never existed in a released version** by checking one guessed file path (`EventMetadata.kt` as its own file) that happened not to exist, instead of searching file contents. It was wrong: `org.occurrent.dsl.subscription.blocking.EventMetadata` was declared as a `data class` inside `Subscriptions.kt` and was real, released, public API in `0.20.5` (confirmed directly against the `occurrent-0.20.5` tag's file contents). PR #247 (2026-06-30, "Share EventMetadata and the common DCB types across the blocking and reactive stacks", one day after the 0.20.5 release) moved it to `org.occurrent.dsl.subscription.EventMetadata` and the commit message called it "Breaking", but the changelog only documents the new `EventMetadata.position` accessor added alongside the move, not the package move itself, so it never made it into the recipe's changelog-derived FQN list. **Fixed:** `occurrent.yml` now has a `ChangeType` entry for this move (19th `RewriteTest`, still green). It won't fix `occurrent-support:2.0.3` itself, since that's a precompiled jar the recipe can't rewrite, that still needs its own rebuild against a stable 0.30.0, but it correctly protects any 0.20.5 consumer whose own source referenced this type directly (scheduling-service's own source did not, only occurrent-support's compiled bytecode does). **Process lesson:** a single failed `git show <tag>:<guessed-path>` proves a *path* doesn't exist, not that a *type* never existed. Verify a type's non-existence with `git grep -i <name>` (content search) or an extracted-tree `grep -rn`, never a single guessed path. This gap was found by systematically diffing every top-level type declaration between the `0.20.5` tag and current HEAD (526 types audited), not by changelog reading alone; the changelog cannot be fully trusted to list every breaking rename.

- **DCB partition stream-version collision retryability is MongoDB-version-sensitive (#297).** On a DCB+STREAM store, two concurrent DCB appends to disjoint boundaries can collide on the same stream version when they hash to the same partition. On MongoDB 4.2.8 (the pinned integration-test version) this collision surfaces as a transient `WriteConflict`, auto-retried. On newer server versions it can instead be a non-transient `E11000` duplicate-key error, which the #297 fix now detects (via the `streamid_1_streamversion_1` index name on the error) and rethrows as retryable rather than letting it fail as a misleading `DuplicateCloudEventException`. The pending Mongo test-version bump (see Release Readiness) validates this against realistic (non-4.2.8) behavior.
- **The DCB API surface was frozen for release per ADR 0053.** Do not casually change DCB public types/signatures without checking that ADR first.

- **Docker/Testcontainers is the biggest verification risk.** `colima.md` and `nordvpn.md` document Ryuk/Mongo timeout/network issues. Fixed `27017:27017` bindings can clash with local Mongo or concurrent runs. Async subscription tests are timing-sensitive (Awaitility).
- **Catch-up to live handover invariant (ADR 0014/0008).** `CatchupSubscriptionModel` reconciles events written during the bulk replay via a delta query using `SortBy.natural(DESCENDING)` + `limit` (insertion order), NOT the time-based `catchupPhaseSortBy`. Selecting by time is loss-prone under clock skew (a backdated during-replay event sorts before the boundary and is missed by both the delta and the live resume). Do NOT "tidy" the delta sort back to `catchupPhaseSortBy`. The global position/checkpoint is captured AFTER the bulk replay (fresh token, avoids oplog ageing). PR #208 closed the count-to-read window (re-read the recent tail until the matching count stops growing; `runCatchupForStream` dedups cached ids so overlapping re-reads stay at-least-once, and closes its source stream via try-with-resources). Residuals needing a global monotonic position (ADR 0008 for streams, `dcbposition` for DCB): net-count arithmetic loses events if events are deleted mid-replay, and `count(Filter.All)` on Mongo uses `estimatedDocumentCount`, which can undercount after an unclean shutdown. InMemory `SortBy.natural` means GLOBAL insertion order. The InMemory lazy-stream CME residual was fixed (#200).
- **Subscription resume-marker field (do not regress #272).** `MongoCommons` writes the marker under `checkpoint` (`GENERIC_CHECKPOINT`) and keeps `subscriptionPosition` (`LEGACY_GENERIC_CHECKPOINT`) as a read-only fallback for pre-rename documents (read tries `checkpoint` first, else legacy; `replaceOne` migrates on next save). Do not remove the legacy read-fallback or change either key, or not-yet-migrated consumers lose their resume point.
- **DCB storage.** DCB shares the CloudEvent storage boundary; metadata is CloudEvent extensions (`dcbtags`, `dcbposition`) so normal subscriptions still see DCB-written events. Position reservation uses a separate collection, but event documents go into the normal event collection with stream metadata. DCB appends are transactional and rely on `MongoTemplate` session synchronization `ALWAYS`. Reads capture a high-watermark and bound matches to `dcbposition > afterSequencePosition && <= highWatermark`, returning that watermark. Append-condition concurrency updates conservative checkpoint keys (`all`, `tag:<tag>`, type-only) to detect racing appends.
- **Capability mode is part of `EventStoreConfig`** (`STREAM`, `DCB`, or both; ADR 0018). It controls index/support-collection creation and runtime API guards, NOT the CloudEvent document format. Occurrent only creates missing indexes/collections for enabled capabilities and never removes indexes (operators create new indexes out-of-band before enabling a capability on large collections). DCB-only writes still assign per-storage-stream Occurrent stream versions (required if `STREAM` is enabled later). DCB-only Spring Boot must NOT expose stream `ApplicationService` or wrap subscriptions in `CatchupSubscriptionModel`; it still exposes `DomainEventQueries` for opt-in DCB helpers.
- **Do not gate the DCB application-service bean with `@ConditionalOnBean(TagGenerator.class)`.** That condition evaluates while bean definitions are still processing and can miss a user's `TagGenerator` in a real Boot context even though slice tests pass. It is a normal generically-typed `@Bean` that resolves `TagGenerator` via `ObjectProvider.getIfAvailable()` at instantiation and returns `null` when none exists.
- **In-memory `deleteAll()` must reset DCB sequence state**, else an emptied store reports stale DCB high-watermarks.
- **code-review-graph is structurally weak for architecture boundaries here** (file-based communities, no cross-community edges, test-dominated flows). Use it for inventory/impact, but verify module boundaries from Maven/source.
- **Two different "is this a DCB event" fields, do not conflate them.** `dcbTags` (capital T) is a Mongo-only, derived index array field, written only by `DcbDocumentMapper` on the DCB `append(...)` path, always present (empty array for zero tags) on a properly-appended event, and sparse-indexed (ADR 49) so `$exists` on it is cheap. `dcbtags` (lowercase) is the actual CloudEvent extension, present on any event a caller tagged via `DcbCloudEvents.withTags(...)`, regardless of which write API persisted it, and is what `DcbCloudEvents.isDcbEvent` and the in-memory filter matcher check. Before #282 landed, the Mongo `Filter.capability(...)` guard checked the derived array; Copilot correctly flagged that a `dcbtags`-tagged event written through `write(...)` (not `append(...)`) would then diverge, no array, so it would misclassify as stream-only. The fix (#282) was NOT to switch the Mongo check to the extension (that would defeat the sparse index, `$exists` on an unindexed field), it was to make the two fields provably equivalent by rejecting `dcbtags`-carrying events on the stream `write(...)` path in all four stores. Any new code that discriminates DCB-vs-stream in Mongo must use the indexed `dcbTags` array; anything working with a live `CloudEvent` (in-memory, or before persistence) must use the `dcbtags` extension via `DcbCloudEvents.isDcbEvent`.
- **A sparse index only accelerates `$exists: true`, never `$exists: false`.** A sparse index has no entries at all for documents missing the field, so there is no way to use it to find those documents either. This is why the DCB-side guard (#280, exclude stream events, `$exists:true` on `dcbTags`) gets full index support from a sparse index, but the reverse (#282, exclude DCB events, `$exists:false` on `dcbTags`) does not, and was accepted as a known, low-risk limitation (no index-backed acceleration for a bare capability-only query with no other narrowing predicate) since DCB has zero production deployments and the one real caller (`StreamCatchupSubscriptionModel`) always has a position-range predicate to ride on anyway. Do not "fix" this by adding a second index; if it ever becomes a real hot path, the correct fix is a written marker field on stream-only events, which was rejected as premature (see ADR 50 for the full analysis).

## Build And Verification

- Use `rtk <command>` wherever `rtk` supports it; raw shell only when unsupported.
- Managed dependency versions live in the root `pom.xml` `<properties>` and imported BOMs (Spring Boot, Reactor, MongoDB driver, Jackson 3, CloudEvents, JobRunr). Don't duplicate them here. MongoDB is wrapped through native-driver and Spring `MongoTemplate` implementations; CloudEvents are the storage boundary (serialization owned by `CloudEventConverter`).

CI: `.github/workflows/maven.yml` runs `mvn -B package --file pom.xml` on Temurin Java 21 and 25, with Testcontainers reuse via `$HOME/.testcontainers.properties`.

Local Maven patterns:
- Full package: `rtk mvn -B package --file pom.xml`
- Focused module: `rtk mvn -pl <module-path> -am test` (or `package`, or `test-compile` for Kotlin/generic changes)
- Focused test class: `rtk mvn -pl <module-path> -am -Dtest=<TestClass> test`
- DCB touched-module set: `rtk mvn -q -pl eventstore/api/dcb,eventstore/inmemory,eventstore/mongodb/spring/blocking,application/service/blocking,subscription/mongodb/spring/blocking -am test`
- Authoritative gate: read the log for `BUILD SUCCESS`/`BUILD FAILURE`, never trust a wrapper exit code. Confirm the JDK is 21 (`mvn -version`).

Release: `mvn_local_snapshot.sh` runs a release-profile local install with `-Drevision=...`. `mvn_release.sh` requires Java 21, uses `mvn deploy -Prelease`, GPG signing, source/javadoc jars, Sonatype Central publishing, and tags `occurrent-<version>`.

## Open Items

### Release readiness

- **Integration-test MongoDB version bump: DONE (#302).** `integration-tests.mongo.version` is now 8.0 (was the end-of-life 4.2.8). Two MongoDB 7.0+ compat fixes came with it: the index-conflict clash reports as error code 86 (`IndexKeySpecsConflict`) on 7.0+ where 4.x reported 85, so both are treated the same, and a compound sort combining a `$natural` step with field keys is now rejected with an `IllegalArgumentException` on the MongoDB stores (7.0+ rejects it server-side, 4.x silently dropped the field keys). This also validates the A1 partition-collision retry fix (#297) against realistic behavior.
- **DCB performance items: DONE (#303).** Both `explain()`-gated candidates proved necessary: a sparse `(type, position)` index (type-only criteria were over-scanning ~1000x) and a sparse `(dcbTags, position)` index (large boundaries triggered an in-memory `SORT`). Both added DCB-guarded across all three Mongo stores with before/after `explain` evidence.
- **Documented gaps CONSCIOUSLY ACCEPTED for this release (revisit post-release if demand):** no reactive competing-consumer model (ADR 0044), and reactive `@StreamSubscription` cannot honor a specific historical start time (ADR 0045). The latter fails loud rather than silently, and a correct fix needs time-to-position translation with clock-skew handling. These are known limitations shipped intentionally, not blockers.
- **Cross-store sort caveat:** a natural sort step combined with other sort steps is a valid insertion-order tiebreaker on the in-memory store but is rejected by the MongoDB stores (documented on `SortBy.natural`). MongoDB cannot express a natural sort inside a compound sort.
- The maintainer assigns the version number and date in `changelog.md` at release. Never invent one. Run a final full `mvn clean install` on the Java 21+25 matrix at the release commit before `mvn_release.sh`.

- **Counter-contention benchmark deferred** (ADR 0045 non-goal). Unified position uses one shared counter with per-append block reservation outside the write transaction. Parkster load is modest, so no benchmark was run. If an open-source user reports scale pain, benchmark naive vs block-reserved against a production-like Atlas cluster (WiredTiger write-ticket/write-conflict metrics); sharded counters are the escape hatch.
- **`queryForSequence` closing semantics.** Kotlin `DcbDomainEventQueries` Sequence/List extensions are leak-free (materialize into a `List`). Open: whether the stream-side `DomainEventQueries.queryForSequence` needs a closing/loan-pattern variant when the underlying store returns a live Mongo-cursor-backed `Stream`.
- Two `simpleName!!` non-null-assertion threads on PR #257 (reactive stream subscription DSL) were left as a deliberate open disagreement with a Copilot suggestion. A safe fallback for anonymous event classes in the stream/DCB DSLs is a small standalone follow-up, not done.
- **Env flake: `SubscriptionProjectionsWithSpringAndMongoDBApplicationTest` (stream projections example)** intermittently fails context load with `NullPointerException: BsonTimestamp ... is null`, the documented fixed-port-27017 / Colima Mongo-readiness flake. Unrelated to code changes, retry.

### Review findings (2026-07-07, shipped)

The six-angle architecture review (ADRs, DCB/decider API, subscription+query DSL API, catch-up design, DCB-store perf, subscription perf) plus an independent code-verified pass produced findings that were addressed across PRs #291-#299 and ADR 0053; see git history and `changelog.md` for detail.

- **Explicitly REJECTED (do not re-raise):** "agnostic `Subscriptions` delivering DCB events makes `EventMetadata.streamId` crash" is FALSE. DCB events are stored via the shared stream mapper with a generated partition stream id, so `EventMetadata.streamId` returns that partition id, not a crash.

## Orchestrator Operating Notes

- Read this file first at every session start.
- For non-trivial tasks, stay read-only while planning, then route: delegate substantial work (10+ minutes or 2+ files) to subagents; handle trivial one-line/single-file work inline; ask the user one strategic question only when they alone can decide.
- Before delegating, re-read relevant sections here and use code-review-graph first when available (`get_minimal_context_tool`, impact radius, relationship queries). Briefs must include goal, owned files, forbidden files, copied conventions, exact `rtk` verification commands, out-of-scope, blocked protocol, and concurrency warning.
- After delegated work completes, review scope and actual verification, then update this file (keep it a durable map, not a change log).
- After any correction from the user, update `.context/lessons.md` with the pattern to prevent recurrence.
- Keep durable-context small: the maintainer's token-discipline note is explicit that a growing file re-read every session is the real cost. Prune shipped detail into git/ADRs/changelog rather than accumulating it here.
- **If the orchestrator itself is in (or was recently in) plan mode, spawned subagents can inherit a stale plan-mode reminder and refuse to edit/build, even after the orchestrator has genuinely exited plan mode.** This happened repeatedly in the #283 session: two subagents independently explored, wrote a *plan* instead of executing, and reported being blocked by "plan mode" even when told explicitly to implement. The fix each time was to actually call `ExitPlanMode` at the orchestrator level first (not just tell the subagent to ignore it), then resume the same subagent with an explicit "you are not in plan mode, execute now" message. Do not accept a subagent's plan-mode refusal at face value if the orchestrator has already exited; check whether the orchestrator itself is still gated first, since a coordinator message alone cannot lift a subagent's permission mode. **Recurred 2026-07-09 (rewrite-recipe session):** a fork launched well after the harness auto-exited plan mode (auto-exit happened on writing the plan file, no explicit `ExitPlanMode` call was ever made) still returned in ~6s with **zero tool calls**, echoing the orchestrator's own status text as its "result" instead of implementing. Tell: a fork completing near-instantly with 0 tool uses and a summary that parrots your last message. The fix worked cleanly: resume the SAME fork in place via `SendMessage` to its agentId with an explicit "you returned 0 tool calls, that was wrong, you are NOT in plan mode, do the work with your tools now". It then executed normally. So the trigger isn't just an *active* orchestrator plan-mode; a fork can inherit a stale read-only stance whenever the session recently passed through plan mode at all.
- **Resuming a subagent requires the message/resume tool addressed to its existing agent ID, not a fresh `Agent` tool call.** A fresh call with the same description spawns an unrelated new agent with no memory of prior context, wasting the exploration the original agent already did. Always resume in place when correcting or continuing a subagent's work.
